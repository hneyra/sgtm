# Version de Java del proyecto (ADR-0001). Se expone como propiedad para poder
# construir en un entorno que todavia no tiene el JDK 25 instalado; el valor por
# omision es el unico admitido en CI.
sgtm.java.version=25

org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
