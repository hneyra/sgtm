/**
 * Lo que el panel es, y las dos funciones puras con que se redacta (#56).
 *
 * <p>Sin Spring, sin base de datos y sin reloj: las fechas entran como argumento. Es lo que permite
 * probar el formato de una cifra y el calculo de un avance sin levantar nada.
 */
@org.jspecify.annotations.NullMarked
package pe.gob.sgtm.indicadores.dominio;
