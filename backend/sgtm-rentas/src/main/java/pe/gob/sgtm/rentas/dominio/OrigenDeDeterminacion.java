package pe.gob.sgtm.rentas.dominio;

/**
 * De donde sale una determinacion. Los tres valores son los del {@code CHECK} de la tabla {@code
 * determinacion} (V2).
 */
public enum OrigenDeDeterminacion {
    ORDINARIA,
    FISCALIZACION,
    RECTIFICATORIA
}
