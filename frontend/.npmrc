# Sin esto, `engines` es una NOTA y no una guarda: npm avisa y sigue.
# Con `engine-strict`, una version fuera del rango falla AL INSTALAR y el
# mensaje nombra la version que hay y la que se pide (#439).
engine-strict=true
