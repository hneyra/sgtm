# SGTM — Contexto para agentes

Sistema de Gestión Tributaria Municipal. Reimplementación del sistema documentado en el **manual
de usuario del SGTM** de la Municipalidad Provincial de Sullana —231 figuras, 12 módulos, 134
opciones— como producto **multi-municipal**: una instalación atiende a muchas municipalidades.

> El manual **no está en este repositorio**: es la transcripción AsciiDoc que vive fuera, en
> `~/Documents/srtm/manual-SGTM/manual-sgtm-asciidoc/`. Lo que sí está aquí es lo derivado de él:
> el catálogo de sus 134 opciones ([`docs/10-negocio/catalogo-de-opciones.md`](docs/10-negocio/catalogo-de-opciones.md)),
> los requisitos ([`docs/20-requisitos/`](docs/20-requisitos/)) y las citas literales que
> justifican una decisión.

El sistema original es de escritorio: Visual Basic .NET sobre SQL Server 2008, arquitectura
de tres capas, cliente Windows XP. **El manual es la especificación funcional; no la técnica.**
La arquitectura la aporta [`../srtm`](../srtm/CLAUDE.md), del que este proyecto hereda
estrategia multi-tenant, estándares de código y forma de verificar.

**Estado:** documentación de arquitectura y datos escrita; del backend existen el esqueleto de
Gradle, el esquema como migraciones Flyway (V1…V57 hoy), el camino del contexto de tenant
(token → `SET LOCAL` → RLS), las verificaciones bloqueantes **y buena parte del negocio**: los
doce contextos acotados tienen código, y la tabla «Verificar antes de afirmar» —al final de este
archivo— registra issue a issue qué se implementó y cómo se demostró. Las barreras se
construyeron primero, a propósito; el negocio entró después, por encima de ellas.

De la **interfaz web** existen **las 134 pantallas**, con las operaciones que el backend
publica ya conectadas y el resto todavía en la forma común:
[`frontend/`](frontend/README.md) porta el catálogo del prototipo a datos tipados y lo compone con
**un** renderizador, sobre un shell con navegación de dos niveles y paleta de comandos. Los datos
llegan por HTTP desde un **proxy que simula la API** ([`ADR-0010`](docs/30-arquitectura/adr/ADR-0010-catalogo-portado-y-proxy-de-datos.md));
conectar el backend es apagarlo, no reescribir la interfaz. Su diseño de referencia —12 módulos,
134 pantallas, design system Juris PE— está en
[`design/`](design/design_handoff_sgtm_web/README.md). El contrato que backend y frontend comparten
está en [`docs/50-api/openapi/sgtm-v1.yaml`](docs/50-api/openapi/sgtm-v1.yaml), derivado de los
`endpoint` que declara cada pantalla del prototipo. **Ese archivo no se edita a mano**: lo produce
[`docs/50-api/generar-openapi.mjs`](docs/50-api/generar-openapi.mjs), y `--comprobar` exige en CI
que siga siendo lo que el generador produce. Lo que el prototipo no puede saber —un filtro que solo
tiene el backend, la `307` de `/catastro/fichas`, la descripción que solo se conoce al implementar
la operación— se declara en las tablas del generador, no en el YAML (#312).

**Stack:** Spring Boot 4 · Java 25 · Gradle Kotlin DSL · PostgreSQL · Flyway · Spring Modulith
· React 19 · TypeScript · Vite · yarn workspaces

## Lo primero que había que construir

**La prueba de aislamiento multi-tenant**, en [`backend/sgtm-esquema`](backend/sgtm-esquema/README.md).
Es bloqueante: `./gradlew verificarAislamiento`.

> **La prueba se conecta como el rol `sgtm_app`, creado en su arranque. No cambies eso.**
>
> La conexión que Testcontainers entrega por omisión es de **superusuario**, y un superusuario
> **omite Row Level Security incluso con `FORCE ROW LEVEL SECURITY`**. Una prueba escrita sobre
> esa conexión pasa en verde **sin verificar nada**. La prueba lo demuestra en vez de afirmarlo:
> con el mismo contexto fijado, verifica que el superusuario ve las dos municipalidades y
> `sgtm_app` una.

Los **cuatro** hallazgos de RLS están en
[`docs/40-datos/modelo-logico-fisico.md`](docs/40-datos/modelo-logico-fisico.md) §0. Dos se
heredaron verificados del SRTM —el superusuario omite RLS, y el acceso directo a una partición
evade la política del padre—: no se volvieron a descubrir, se trasladaron con su mitigación. Los
otros dos salieron aquí: **bajo RLS un `LIKE 'prefijo%'` no llega nunca al índice**, porque
`textlike` no es *leakproof* y PostgreSQL no lo evalúa antes de la política —toda búsqueda por
prefijo se escribe como rango con `~>=~` / `~<~`—; y **una clave foránea nueva sobre una tabla con
RLS no se puede validar**, porque validar es una consulta y el migrador no tiene contexto de
tenant: va `NOT VALID`, que sigue comprobando cada `INSERT`.

**Al agregar una tabla:** si lleva `municipalidad_id NOT NULL`, la prueba le exige RLS sola. Si
no, hay que clasificarla como catálogo o como exenta en el propio código de la prueba, y eso se
ve en el diff. Al agregar una **partición**, repetir el bloque de RLS explícita de `V6__rls.sql`
y **no concederle ningún privilegio**.

## Reglas que no se negocian

| # | Regla | Motivo |
|---|---|---|
| 1 | **Importes en `BigDecimal`/`NUMERIC`.** Prohibidos `double` y `float` | Precisión monetaria (RNF-055) |
| 2 | **Ningún método de dominio recibe `municipalidadId`.** Sale del token, se fija una vez con `SET LOCAL` | Si el desarrollador no lo maneja, no puede olvidarlo (ARQ-03 §3.1) |
| 3 | **`SET LOCAL`, jamás `SET SESSION`** | `SET SESSION` sobrevive al retorno de la conexión al pool y contamina la petición de otra municipalidad |
| 4 | **Sin `DELETE`** en deuda, pagos, recibos, valores, papeletas, asientos ni auditoría. Se anula, se da de baja o se reversa | RNF-051, y el manual §Auditoría |
| 5 | **Ningún literal numérico tributario en el código.** UIT, tramos, alícuotas, valores unitarios, aranceles y tablas de depreciación viven en datos versionados | Reproducibilidad y cambio sin despliegue (RNF-053) |
| 6 | **Las reglas tributarias son funciones puras.** Sin base de datos, sin reloj, sin configuración global; la fecha entra como argumento | Recalcular 2027 en 2037 debe dar el mismo céntimo |
| 7 | **Nada de Spring ni JPA en la capa `dominio`** | Las reglas deben probarse sin levantar el contexto |
| 8 | **`alicuota`, nunca `tasa`**, para un porcentaje | `tasa` es un tipo de tributo |
| 9 | **No existe «la deuda»:** es `deudaActualizadaA(fecha)`, y toda cifra mostrada indica su fecha | RNF-075 |
| 10 | **Toda modificación de datos exige observación del usuario.** Sin observación no se guarda | Manual §Auditoría; RNF-052 |

Las reglas 1, 2, 6, 7 y las fechas están escritas como pruebas de ArchUnit; `SET SESSION` y
`DELETE` sobre tabla protegida, como escáner del código fuente:
`backend/sgtm-aplicacion/src/test/java/pe/gob/sgtm/verificaciones/`. **Si agregas una regla,
agrega también la clase de muestra que la viola**, en `verificaciones/muestras/`: una regla que
no puede fallar no protege nada.

En el frontend, las que le tocan —1, 2, 8, 9 y el idioma— están como **reglas de ESLint**, con la
misma exigencia: `frontend/verificaciones/muestras/` tiene una muestra por prohibición y
`reglas-de-eslint.test.ts` exige que la regla la detecte. A ellas se suma una propia de la
interfaz: **ninguna petición sale por `fetch` suelto**, todas pasan por `solicitar()` de
`@sgtm/api-client`. Es lo que permite cambiar el proxy de datos por el backend con una bandera.

Lista completa con su justificación:
[`docs/30-arquitectura/estandares-de-codigo-backend.md`](docs/30-arquitectura/estandares-de-codigo-backend.md)
y [`docs/60-frontend/estandares-de-codigo-frontend.md`](docs/60-frontend/estandares-de-codigo-frontend.md).

## Idioma

Español en el dominio, inglés en lo técnico (heredado de `ADR-0004` del SRTM). **Sin tildes en
identificadores**: Checkstyle lo revisa en el backend, ESLint en el frontend.

```java
public final class Papeleta { … }                  // dominio: español
public interface PapeletaRepository { … }          // patrón: inglés
autovaluo.calcularTotal();                         // comportamiento: español
repository.findById(id);                           // infraestructura: inglés
```

Tablas y columnas en español `snake_case`. Campos de la API JSON en español `camelCase`.
Comentarios, pruebas y mensajes de commit en español.

## Estructura

```
backend/      Spring Boot 4, multi-módulo. Monolito modular con Spring Modulith    ← existe
frontend/     React sobre Vite, yarn workspaces. Las 134 pantallas, un renderizador ← existe
docs/         Documentación (fuente de verdad del diseño)                          ← existe
design/       Prototipo navegable del que se derivó la interfaz                    ← referencia
infra/        Pulumi en TypeScript con yarn. Dos stacks, el sistema y su respaldo  ← existe
despliegue/   Entorno local canónico: compose, identidad e inicialización del motor ← existe
scripts/      Guiones del corpus normativo (archivar fuentes, importar aranceles)  ← existe
```

Módulos del backend hoy: `sgtm-dominio-compartido` (objetos de valor en `pe.gob.sgtm.dominio` y
`TenantContext` en `pe.gob.sgtm.compartido`), `sgtm-esquema` (migraciones y prueba de
aislamiento), `sgtm-plataforma` (filtro del token, `SET LOCAL`, guardia del pool, y el recorrido
por municipalidades del portal), los **doce**
contextos acotados —todos con código ya—, `sgtm-indicadores` (el panel de recaudación, que no es
un contexto: ARQ-01 §3.13) y `sgtm-aplicacion` (ensambla y aloja las verificaciones).
Límites de cada contexto: [`docs/30-arquitectura/contextos-acotados.md`](docs/30-arquitectura/contextos-acotados.md).

Workspaces del frontend hoy: `apps/backoffice` (shell, catálogo y renderizador), `apps/portal`
(el portal del contribuyente: **una** pantalla, sin shell ni catálogo) y los paquetes
`@sgtm/dominio`, `@sgtm/api-client`, `@sgtm/design-system`, `@sgtm/api-mock` (el proxy de datos),
`@sgtm/lectura` (los adaptadores del contribuyente que las dos aplicaciones comparten) y
`@sgtm/sesion` (la puerta de sesión).
**El catálogo se regenera con `yarn portar-catalogo` y los tipos de la API con
`yarn generar-operaciones`, desde el contrato; los archivos `.generado.ts` no se editan a mano.**

**Dos aplicaciones, un solo origen, y las 134 siguen siendo 134.** `apps/portal` se separó por la
**tercera** condición de [`ADR-0009`](docs/30-arquitectura/adr/ADR-0009-plataforma-frontend.md)
—el paquete arrastra código que solo usa el back-office—, y desde
[`ADR-0020`](docs/30-arquitectura/adr/ADR-0020-la-sesion-del-ciudadano.md) también por la primera:
**el ciudadano tiene realm propio (`sgtm-ciudadano`), emisor distinto y sesión propia**. Se sirve en
`/portal/` del mismo origen, tras **su** puerta, y la opción `portal` del catálogo sigue donde
estaba —es la vista del funcionario, y no gana backend—. Las dos enseñan las mismas cifras a la
misma fecha porque leen con los mismos adaptadores; lo que cambia es el ancho.

**Y ahí el ciudadano no teclea ningún documento.** Su sujeto sale del claim `numero_documento` del
token, `GET /portal/situacion` no tiene ni un parámetro y el servidor **recorre el registro de
municipalidades** —una transacción y un `SET LOCAL` por rama— para componer y sumar con una sola
fecha de corte. No es una consulta multi-municipalidad: son *N* consultas de una municipalidad cuya
unión se filtra a un documento firmado. Con eso se cerró **D-07**, y **D-15** quedó decidida por el
camino B: el documento lo acredita el enrolamiento en ventanilla.

**Y ese enrolamiento ya existe** (#415, ADR-0020 §6). No es una pantalla 135 —las 134 siguen siendo
134—: es el mecanismo declarativo de ADR-0012 extendido, un archivo por municipalidad en
[`despliegue/identidad/ciudadanos/`](despliegue/identidad/ciudadanos/README.md) **sin una sola
clave**, aplicado por el mismo guion en su segundo modo (`reconciliar-identidades.sh ciudadanos`).
La cuenta **se deriva** del documento —`dni-70123456`, con el tipo delante porque `CE 12345678` y
`DNI 12345678` son dos personas distintas—, y el número declarado **tiene que figurar en el padrón
de esa municipalidad**: se cruza a tres bandas contra la base.

## Antes de escribir código, leer

| Si vas a tocar… | Lee |
|---|---|
| Cualquier cosa | [`docs/30-arquitectura/estrategia-multitenant.md`](docs/30-arquitectura/estrategia-multitenant.md) — es el riesgo número uno |
| Base de datos | [`docs/40-datos/modelo-logico-fisico.md`](docs/40-datos/modelo-logico-fisico.md) §0 primero, y **`../srtm/docs/40-datos/ddl/esquema-verificado.sql`** para tipos y longitudes |
| Cálculo tributario | **`../srtm/docs/10-negocio/reglas-impuesto-predial.md`** (NEG-05) y **`../srtm/docs/30-arquitectura/motor-de-reglas-y-parametrizacion.md`** (ARQ-09) |
| Backend | [`docs/30-arquitectura/estandares-de-codigo-backend.md`](docs/30-arquitectura/estandares-de-codigo-backend.md) |
| Requisitos | [`docs/20-requisitos/requisitos-funcionales.md`](docs/20-requisitos/requisitos-funcionales.md) |
| API | [`docs/50-api/openapi/sgtm-v1.yaml`](docs/50-api/openapi/sgtm-v1.yaml) |
| Interfaz | [`docs/60-frontend/estandares-de-codigo-frontend.md`](docs/60-frontend/estandares-de-codigo-frontend.md), y el diseño en [`design/design_handoff_sgtm_web/README.md`](design/design_handoff_sgtm_web/README.md) |
| Unificar un módulo de la interfaz | [`docs/60-frontend/superficies-unificadas.md`](docs/60-frontend/superficies-unificadas.md) (FRO-05) — el patrón, y las mutaciones que hay que medir |
| Una pantalla que el manual capturó como **hoja** y cuyo endpoint escribe | [`docs/60-frontend/hojas-sin-superficie.md`](docs/60-frontend/hojas-sin-superficie.md) (FRO-06) — las siete, y por qué no se les inventa el formulario |

Índice completo: [`docs/README.md`](docs/README.md). Decisiones: [`docs/30-arquitectura/adr/`](docs/30-arquitectura/adr/).

## El cálculo tributario lo define `../srtm`

**No se rediseña aquí.** La estructura del cálculo —qué reglas existen, en qué orden se aplican,
qué parámetros consume cada una, cómo se identifican (`RT-xxx`) y qué casos borde hay— está
resuelta y verificada contra los manuales del MEF en `../srtm`. Este proyecto la **implementa**,
no la reinventa:

> **Si `../srtm` no está en el disco, se clona: `git clone https://github.com/hneyra/srtm`.**
> No es opcional. El motor de reglas se escribió una vez sin poder leer NEG-05 ni ARQ-09, a partir
> de lo que este archivo resume, y salieron dos defectos estructurales: una cadena lineal donde
> NEG-05 §1 describe un grafo, y la lectura de parámetros por ejercicio que ARQ-09 §3 nombra
> como el modelo que falla en silencio. Los dos estaban en verde y ninguno lo habría encontrado
> una revisión. Leer el documento cuesta menos que corregir lo que se construyó sin leerlo.

| Qué | Dónde vive en `../srtm` |
|---|---|
| Reglas del predial: `RT-001`…`RT-016`, fórmulas, orden, casos borde | `docs/10-negocio/reglas-impuesto-predial.md` (NEG-05) |
| Motor de reglas: pureza, versión por ejercicio, redondeo, parámetros sellados | `docs/30-arquitectura/motor-de-reglas-y-parametrizacion.md` (ARQ-09) |
| Frontera estructura/valor y estrategia por ejercicio | `ADR-0006`, `ADR-0015` |
| Plantilla de una regla | `docs/_plantillas/regla-tributaria.md` |

Lo que de ahí no se negocia: la base del predial es **por contribuyente, no por predio** (los
tramos progresivos se aplican al conjunto de sus predios; calcular predio por predio produce un
error sistemático a la baja en todo el padrón); el `% propiedad` pondera la base de cada predio;
la secuencia de la construcción es *valor unitario → +5 % → − depreciación → × área*; y una
implementación que ya se usó en una emisión **no se modifica nunca** —se crea otra con su rango
de vigencia—.

**Las longitudes y tipos de las columnas también vienen de ahí.** Los dominios (`dinero
numeric(15,2)`, `monto_calc numeric(18,6)`, `alicuota`, `porcentaje`, `area_m2`, `ejercicio`) y
el largo de cada campo se toman de `../srtm/docs/40-datos/ddl/esquema-verificado.sql`. Una
columna que existe en ambos esquemas tiene el mismo tipo en los dos; si hay motivo para
apartarse, se anota en el diff. Nunca `numeric(15,2)` suelto donde hay dominio.

### No implementar todavía

**Ninguna regla de cálculo.** Ya no por los valores ni por el redondeo: D-02a está firmada
(2026-08-25), el mecanismo de carga existe (#188) y el redondeo quedó decidido (ADR-0018). Un
tramo equivocado sigue produciendo deuda mal calculada en todo un padrón, con devoluciones
masivas y nulidad de valores, y lo que sigue impidiendo las reglas es: **D-11** —los cuatro
factores que NEG-05 §0.1 marca sin fuente identificada: deducción de Amazonía, `% actualización`,
incremento del 5 %, factor de oficialización; multiplican importes, y un valor inventado escala
el error, así que las reglas que los llevan no se implementan **ni estructuralmente**—; **un**
cuadro que aún no puede cargarse (GOB-03, H-14: la depreciación salió de esa lista con `V57`); y
que ningún conjunto del ejercicio está sellado con cifras reales.

**Ningún componente del design system antes de la pantalla que lo use.** Los que hay salieron
todos del renderizador; el prototipo ya fija las medidas exactas, y un componente escrito antes de
su pantalla es un componente que nadie pidió.

**Ninguna acción de pantalla que escriba sin su campo de observación.** Toda modificación de datos
lo exige (regla 10, RNF-052). El camino de escritura vive en un solo sitio —`useEscritura`— y pide
la observación antes de habilitar la acción; **`useMutation` fuera de ahí no pasa el lint**, con su
muestra que lo viola. Una acción cuya operación es de lectura sigue deshabilitada: no hay a dónde
escribir.

**Ningún campo del formulario que la opción no haya declarado.** El cuerpo de una escritura lleva la
observación y solo los campos que la opción lista en `pantallas/escrituras.ts`; mientras no esté
ahí, su formulario no se puede escribir. Es lo que impide que una contraseña acabe en el estado de
React —y de ahí en cualquier sitio— cuando el backend no la pide.

## Decisiones abiertas que bloquean

Registro completo en [`docs/00-gobierno/decisiones-abiertas.md`](docs/00-gobierno/decisiones-abiertas.md).

| # | Decisión | Bloquea |
|---|---|---|
| D-01 | Municipalidad piloto (**Catacaos**, ya elegida) y validador funcional, que sigue sin nombrarse | La primera iteración de negocio |
| D-02b | Valores **de ordenanza local** con su ratificación provincial | Arbitrios y sanciones |
| D-11 | **Uno solo ya: el `% actualización`.** Los otros tres tienen fuente leída; el 5 % ademas tiene mecanica desde #436 —es unico, no acumulativo—. La hipotesis del art. 14 se **descarto** contra los manuales del SRTM (#437): ese acto es una redeterminacion, no un factor. Queda un candidato normativo nuevo, en `predial-porcentaje-de-actualizacion.md` | `RT-002`, `RT-005`, `RT-011` |
| D-04 | Migración desde la base SQL Server existente. **Quien migre debe considerar D-03c**: reproducir determinaciones del SRTM exige la campaña de observación de sus puntos de redondeo (ADR-0018) | Implantación |
| D-05 | Régimen de firma digital de valores y resoluciones | La capa de documentos |

## Comandos

```bash
cd backend
./gradlew build                   # todo, incluidas Spotless, Checkstyle y NullAway
./gradlew verificarAislamiento    # aislamiento multi-tenant. Bloqueante. Requiere Docker
./gradlew verificarArquitectura   # ArchUnit, escáner de fuentes y Spring Modulith. Bloqueante
./gradlew spotlessApply           # arregla el formato en vez de solo reprocharlo
```

**Si el build se queja del formato, no lo pelees: `spotlessApply`.** Checkstyle no revisa formato
a propósito, para no discutir con el formateador. Lo que sí revisa, y es fácil de incumplir con
el teclado en español, son los **identificadores con tilde**: `alicuota`, nunca `alícuota`.

Las pruebas de persistencia requieren Docker. Sin motor de base de datos **fallan**, no se
omiten: una prueba bloqueante que se salta a sí misma deja el build en verde.

```bash
cd infra
yarn verificar                    # lint, tipos y pruebas. Sin Pulumi, sin token y sin cluster
yarn manifiestos --ambiente stg   # lo que se desplegaria, en JSON. Tampoco necesita Pulumi
yarn capacidad --ambiente prod    # ¿cabe el stack en el nodo? Sin desplegar (#252)
yarn secretos --ambiente stg      # el inventario de INF-06: nombre, clave, rotacion. Nunca un valor
verificaciones/motor/verificar-el-motor.sh --con-aislamiento   # el motor, levantado de verdad
verificaciones/ambiente/verificar-el-ambiente.sh --ambiente prod  # el ambiente DESPLEGADO, contra si mismo
secretos/bootstrap-secretos.sh --ambiente stg      # genera lo que falte, nunca via Pulumi
secretos/asignar-claves.sh --ambiente stg --comprobar  # ¿sirven de verdad? (el secreto puede existir y el rol seguir NOLOGIN)
secretos/rotar-clave.sh --ambiente stg --rol sgtm-app   # rota contra la base en marcha
respaldo/simulacro-de-restauracion.sh --ambiente stg   # el respaldo, restaurado de verdad
observabilidad/verificar-alertas.sh                    # apaga la base, comprueba que la alerta llega
observabilidad/verificar-tableros.sh                   # cada panel del tablero, contra Prometheus
carga-de-datos/sembrar-demostracion.sh --ambiente stg --municipalidad-id 4  # la municipalidad de ejemplo, entera
```

**La municipalidad de demostracion se siembra en nueve pasos y en un solo orden**
([`infra/carga-de-datos/README.md`](infra/carga-de-datos/README.md)): cada archivo nombra por
codigo algo que otro tuvo que escribir antes, y la fila que nombre algo inexistente **se rechaza
sola** —no revienta la carga—. Los seis pasos que siembran datos inventados exigen
`municipalidad.es_demostracion = true`, comprobado **contra la base** por cada proceso antes de leer
una fila (los seis que siembran datos inventados): un `--municipalidad-id` equivocado en un digito no mete personas que no existen en el
padron de una municipalidad que ya opera, y aqui no se borra nada (RNF-051).

**Por ahi no entra ninguna cifra normativa.** Ni aranceles, ni valores unitarios, ni tramos, ni
valores referenciales de vehiculos: esos se publican desde el corpus verificado a doble firma
(`publicar-parametros.sh`, `publicar-cuadros.sh`) o no entran. El importe de `deuda.csv` no es una
excepcion sino otra cosa: es un **saldo**, entra como dato igual que entraria el de la base anterior
el dia que se cierre D-04, y esas filas no emiten resolucion de determinacion ni escriben en
`determinacion` —la deuda se cobra y se lee, pero el sistema no reclama haber aplicado ninguna
regla para llegar a ella—.

**El aislamiento se verifica contra el motor que levanta ese guion, nunca contra uno en
servicio:** la prueba provisiona, y `ALTER ROLE` sobre `sgtm_owner` y `sgtm_app` vale para
todas las bases del clúster de PostgreSQL, no solo para la suya. Apuntarla a `prod` deja fuera
a la aplicación (INF-01 §4.1).

**Ningún secreto de la aplicación vive en el estado de Pulumi** (`ADR-0011` §3,
[`INF-06`](docs/80-infraestructura/gestion-de-secretos.md)): `bootstrap-secretos.sh` los
genera hablando con el API de Kubernetes por `kubectl`, nunca con `pulumi up`.

**El respaldo lo toma `sgtm_respaldo`, no el superusuario ni `sgtm_owner`**
([`INF-08`](docs/80-infraestructura/respaldo-y-recuperacion.md), issue #155). Sus
privilegios son los tres que wal-g necesita —`pg_read_all_settings` y `EXECUTE` sobre
`pg_backup_start`/`pg_backup_stop`—, y ese conjunto se determinó **ejecutando** hasta dar
con el mínimo que no falla: `REPLICATION` resultó no hacer falta, y `pg_read_all_settings`
sí, aunque no aparezca en ninguna guía. Si un respaldo falla, la salida cómoda es darle
superusuario al rol; entonces el respaldo deja de ser un lector y pasa a ser una credencial
con poder total sobre el padrón, sin que ningún síntoma lo delate.

**La clave de cifrado del respaldo no se rota de rutina.** Cambiarla deja ilegibles todos
los respaldos escritos con la anterior; no hay `ALTER ROLE` que los vuelva a cifrar
(`INF-08` §4).

```bash
cd frontend
yarn verificar                    # contrato, lint, tipos y pruebas. Lo que hay que pasar antes de un PR
yarn comprobar-compilaciones      # el juego de datos no llega a produccion, y el presupuesto de paquete
yarn e2e                          # los seis caminos completos en un navegador (Playwright)
yarn test                         # incluye la prueba de que cada regla de ESLint muerde
yarn generar-operaciones          # regenera los tipos de la API desde sgtm-v1.yaml
yarn format                       # Prettier; mismo trato que spotlessApply
```

## Verificar antes de afirmar

Precedente heredado del SRTM: el DDL se **ejecutó** contra PostgreSQL en lugar de revisarse, y
eso encontró tres defectos que la revisión no habría visto, incluidos los dos hallazgos de RLS
que anulaban el aislamiento entre municipalidades.

Aplica lo mismo aquí: **ejecutar la prueba vale más que razonar sobre ella.** Y no basta con que
la verificación esté escrita: **tiene que demostrarse que puede fallar.**

Lo verificado hasta hoy, ejecutando contra PostgreSQL 16. **Cada fila es el registro histórico
de su issue**: sus cifras (número de pruebas, de reglas, de operaciones) son las del momento en
que se verificó, y el estado vigente lo dicen las pruebas, no esta tabla:

| Verificación | Cómo se demostró que puede fallar | Resultado |
|---|---|---|
| Aislamiento del esquema (19 pruebas) | Quitando `WITH CHECK` de la política de tenant | Rojo en las 63 tablas |
| Privilegios sobre particiones | `GRANT SELECT ON determinacion_2026 TO sgtm_app` | Rojo en dos pruebas |
| Guardia del pool | Prueba gemela **sin** guardia | La fuga ocurre de verdad |
| Patrón de repositorio (11 pruebas) | Conectando como superusuario en vez de `sgtm_app` | Rojo en 7 de las 11 |
| Reglas de ArchUnit (12) | Clase de muestra que viola cada una | Las doce muerden, ya sobre dominio real |
| Observación obligatoria (regla 10) | Quitando la `Observacion` de `RegistrarVia` | Rojo en `verificarArquitectura` |
| Auditoría contra PostgreSQL (8 pruebas) | Observación en blanco por SQL directo | La operación completa se deshace |
| Contrato de la API vs. rutas publicadas | Publicando una ruta que el contrato no tiene | Rojo en las dos direcciones |
| Los errores no filtran esquema | Mensaje real de PostgreSQL con tabla y restricción | Ni tabla, ni restricción, ni SQL en la respuesta |
| Autorización contra PostgreSQL (7 pruebas) | Quitando `@RequiereAcceso` de `ViaController` | Rojo en `verificarArquitectura` |
| Administración de seguridad (9 pruebas) | Sembrando dos municipalidades y consultando cruzado | Desde B, el usuario de A no existe |
| Permisos y precedencia (9 pruebas) | Quitando la guarda del último administrador | Rojo: el sistema se queda sin quien administre |
| El administrador inicial administra toda la municipalidad, y un grupo `Seguridad` delegado (14 pruebas) | Devolviendo el filtro por módulo a `permisosDeSeguridad`; ampliando el alcance del grupo `Seguridad` a todo el catálogo | Rojo: el admin deja de llegar al padrón; rojo: un miembro de `Seguridad` entra donde no debe. Y **la marcha blanca destapó de paso** que `GET /catastro/vias` corría sin transacción —sin `SET LOCAL`, RLS falla— porque nadie con permiso había llegado nunca a él: se arregló con `ConsultaDeVias` |
| La interfaz aprende sus permisos del backend, no del token (ADR-0013): matriz efectiva (5 pruebas), centinela `SESION_PROPIA` en el guardia (1), y `permisos.test.tsx` contra el endpoint | Poniendo la excepción de usuario a competir por columna con el grupo en vez de sustituirlo; dejando pasar `SESION_PROPIA` sin token; haciendo que el endpoint de permisos responda 500 | Rojo: la matriz mezcla lo que la excepción niega; rojo: el guardia deja entrar sin autenticar; rojo el front deja de mostrar el menú vacío —muestra todo— cuando no puede leer los permisos |
| Sesión y auditoría (15 pruebas) | Consultando `auditoria_2026` en vez de la tabla padre | La aplicación no tiene privilegio sobre la partición |
| Sellado de parámetros (9 pruebas) | Quitando el disparador de inmutabilidad de `V9` | Rojo: un conjunto sellado se deja editar |
| Lectura sellada (6 pruebas) | Quitando `AND estado = 'SELLADO'` de la consulta, y resolviendo por ejercicio en vez de por conjunto | Rojo: se deja leer un conjunto abierto; rojo: el recálculo devuelve la v2 donde la determinación usó la v1 |
| Motor de reglas como grafo (15 pruebas, sin base ni reloj) | Volviendo a la cadena lineal: aplicar en orden de registro sin mirar las dependencias | Rojo en 8 de las 15: la convergencia de dos ramas no se puede expresar encadenando |
| El corpus de casos de NEG-05 (13 pruebas, sin base ni reloj) | Cuatro roturas: quitar una arista declarada de un caso; declarar un parametro que la regla no pide; llenar un `esperado` que solo se podria comparar contra parametros ficticios; y registrar en el motor una regla que ningun caso declara | Rojo las cuatro. Y una quinta la encontro la propia prueba: declarar «sin regla» un caso borde de `RT-001` —que si esta registrada— la pone roja, y tiene razon: lo que falta ahi no es la regla, es la decision |
| La transcripcion de valores normativos (9 prohibiciones + una muestra en regla) | Tres roturas: quitar la regla de las dos firmas distintas; neutralizar el escaneo de `INSERT` en migraciones; y anadir un rechazo incondicional | Rojo, rojo, y la tercera la caza solo `en-regla` —las nueve prohibiciones seguian en verde y la comprobacion parecia mas estricta que nunca— |
| Los puntos de redondeo (7 pruebas, D-03c) | Devolviendo la politica unica: que un punto sin parametrizar resuelva a la primera que haya, en vez de fallar | Rojo en 3: sin la guarda, el importe sale **sin redondear** y nadie lo distingue del correcto |
| Nada siembra en el perfil por omision (#202) | Quitandole el `@Profile("batch")` a `ImplantarMunicipalidad`, y poniendoselo `web` | Rojo las dos, nombrando la clase. Sin la regla, las dos compilaban y sus pruebas seguian verdes —la instancian a mano—: el unico sintoma habria sido que el proceso web pide la clave de `sgtm_owner` |
| El redondeo entra como dato, no como codigo (13 pruebas, E-7 §3) | Tres roturas: ignorar media politica —escala sin modo— en vez de rechazarla; quitar la guarda del conjunto sin ningun punto; y devolver la politica al codigo del servicio | 2, 1 y 2 en rojo. La tercera la caza ademas el escaner de fuentes, que es lo que #203 pedia: ninguna politica compilada |
| Escáner del código fuente | Muestras con `SET SESSION`, `DELETE`, `UPDATE` prohibidos, con una política de redondeo escrita a mano y con la UIT, un tramo y una alícuota compilados (regla 5) | Las detecta; neutralizando el patrón de la regla 5, rojo |
| Reglas de ESLint del frontend (10) | Quitando la regla de tildes, y la de `fetch`: sus pruebas se ponen rojas | Las diez muerden |
| Las 134 pantallas se dibujan | Montando cada una contra el proxy, y recorriéndolas en Chromium | 134 en verde, 0 errores |
| Los caminos de FRO-03 §6 | Playwright: caja solo con teclado, portal en 360 px, y la misma hoja A4 en dos módulos | Pasan; el primero encontró que la paleta no se operaba con teclado |
| El presupuesto de paquete muerde | Bajándolo por debajo de lo que mide el arranque | Rojo, con el número y qué hacer |
| El juego de datos simulado no llega a producción | Comparando las dos compilaciones, con y sin la bandera | El chunk desaparece |
| Un cambio del contrato rompe la compilación | Renombrando `codRefCatastral` en `sgtm-v1.yaml` y compilando con `tsc` | Rojo; al devolverlo, verde |
| El módulo de seguridad conectado (11 pruebas) | Quitando la guarda de `leerPaginado`, el ejercicio de la bitácora, el vaciado de la caché, la lista blanca del cuerpo y el bloqueo de los campos de clave | Las cinco lo ponen rojo |
| El panel y el portal (12 pruebas) | Retocando un indicador, deduciendo el avance, y quitando la fecha de corte | Dos, dos y una en rojo |
| La copia se ve como copia (7 pruebas) | Quitando el aviso permanente, poniéndoselo también a omisos, y recomponiendo una cifra de determinación | Cuatro, una y tres en rojo |
| Licencias y el acto inalcanzable (8 pruebas) | Abriendo lo opcional, impidiendo que viaje el filtro de descripción, y cambiando el patrón de lo irreversible | Las tres lo ponen rojo |
| Infracciones administrativas (8 pruebas) | Quitando las firmas del bloque, recomponiendo una cifra, habilitando sin observación, y mandando la página aunque sea la primera | Dos, una, tres y una en rojo |
| Trece reportes, una hoja (14 pruebas) | Quitando las firmas del bloque, quitando la marca de no imprimible, y alterando una celda numérica al dibujarla | Seis, seis y una en rojo |
| El expediente coactivo (26 pruebas) | Habilitando las acciones secundarias, dejando la insignia sin texto, y haciendo que `puedeVer` devuelva cierto siempre | Cuatro, una y una en rojo |
| Lo irreversible se confirma (11 pruebas) | Quitando generar, notificar y coactiva de la lista, volviendo a «¿estás seguro?», y enviando sin confirmar | Cuatro, dos y tres en rojo |
| El camino de escritura (`escritura.test.tsx`) y los actos honestos (`actos-honestos.test.tsx`) | Quitando el `focus()` tras guardar, enfocando en cada render en vez de en el flanco, regenerando la clave de idempotencia en cada envío, y dejando que una opción sin declarar «guarde» mandando solo su observación | Dos, una, una y la batería de actos honestos en rojo |
| El módulo que más escribe (12 pruebas) | Habilitando la acción primaria sin observación, y rellenando la deuda del padrón con lo del prototipo | Nueve y una en rojo |
| Ninguna cifra sin su fecha (17 pruebas) | Quitando el bloque de fecha de cálculo, restando el saldo en la interfaz y añadiendo una función de sumar a `@sgtm/dominio` | Trece, dos y una en rojo |
| Catastro conectado (13 pruebas) | Quitando el bloque de versionado, el `historico=true`, la guarda de la ruta, poniéndole cifra al arancel rural y devolviendo el desplegable a las opciones del prototipo | Las cinco lo ponen rojo |
| Padron de contribuyentes (15 pruebas) | Sustituyendo la aproximación por igualdad exacta, y bajando el umbral de parecido a cero | Rojo: el nombre mal escrito no encuentra a nadie; rojo: devuelve el padrón entero |
| Ficha del contribuyente (12 pruebas) | No cerrando el domicilio anterior al mudar, y resolviendo «la última» en vez de la vigente a la fecha | Rojo: dos domicilios abiertos; rojo: una notificación de marzo usaría la dirección de setiembre |
| Predio, catálogos y titularidad (17 pruebas) | Cambiando el disparador de la titularidad de diferido a inmediato, contra PostgreSQL | Rojo: una transferencia legítima —cerrar una titularidad y abrir otra— se vuelve imposible |
| Ficha catastral versionada (11 pruebas) | Sobrescribiendo el `uso` de la versión anterior al cerrarla, y no copiando sus construcciones al versionar | Rojo: el historial miente; rojo: la versión nueva nace vacía |
| Las otras tres fichas (14 pruebas) | Cinco roturas: `siguienteVersion` sin copiar el detalle; sin la comprobación de que el detalle es del tipo de la ficha; sin el disparador diferido del reparto; `vigenteA` resolviendo «la última»; y `TierraRural` admitiendo metros | Rojo las cinco. La segunda deja construir una ficha económica con grupos de tierra; la última admite 15 000 m² leídos como hectáreas |
| Consulta de fichas e histórico (24 pruebas) | Escribiendo el prefijo con `LIKE` en vez de por rango, y devolviendo el padrón entero cuando el filtro por titular no encuentra a nadie | Rojo: el plan pasa a `Seq Scan` sobre 30 000 predios; rojo: buscar un nombre inexistente devolvía todo |
| La fecha de auditoría sale del reloj inyectado | Devolviéndola al `DEFAULT now()` de la base | Rojo: la fila cae en un día que no es el del ejercicio con que se particionó |
| Documentos en tres formatos (26 pruebas) | Cinco roturas: el PDF con fecha de creación dentro; el RTF sin escapar lo no-ASCII; sin la comprobación de que la reimpresión sale igual; sin el disparador de inmutabilidad; y el duplicado sin marcar | Rojo las cinco. La primera es lo que haría cualquier biblioteca de PDF; la segunda escribe «PE?A GARC?A» en un documento oficial |
| Del token firmado a las filas que RLS deja ver (11 pruebas, con un emisor OIDC propio) | Cuatro roturas: sin `oauth2ResourceServer`; con `/api/v1/**` en `permitAll()`; con solo `jwk-set-uri`, sin `issuer-uri`; y el filtro leyendo `X-Municipalidad-Id` «por comodidad» | 5, 1, 1 y 1 en rojo |
| Padron vehicular (13 pruebas) | Cinco roturas: la unicidad de la placa sobre el texto tal cual; el cambio de placa sincronizando `papeleta.placa`; la auditoria llaveada por la placa; los valores referenciales resueltos por ejercicio; y la consulta sin `@Transactional` | 1, 1, 2, 2 y 4 en rojo |
| Las guardas del generador de operaciones (6) | Un contrato de muestra que viola cada una | Las seis muerden |
| La marca de la instalación de demostración (19 pruebas) | Quitando el bloque de la marca de **cada renderizador por separado**; marcando solo al dibujar en vez de al emitir; y cambiando la caché del régimen por una global de un solo valor | Cada renderizador roto pone en rojo su formato y solo el suyo; 2 en rojo; 2 en rojo —la caché global hace que la primera municipalidad que emita decida por todas, y en el orden malo la marcha blanca emite **sin** marca— |
| Los manifiestos del clúster (49 pruebas, sin Pulumi ni nodo) | Poniendo `RollingUpdate` sobre el volumen de la base, `timeoutSeconds: 1` en una sonda, `sgtm_owner` como usuario del Deployment, un puerto en el perfil `batch`, `start-dev` en Keycloak y `/keycloak/admin` publicado | Las seis lo ponen rojo |
| El motor del manifiesto, levantado de verdad | Quitando el `GRANT CONNECT` que devuelve a los cuatro roles lo que el guion de Keycloak revoca de PUBLIC | Rojo: `sgtm_owner` deja de poder conectarse, y con él la aplicación entera |
| El inventario de secretos y su generación (14 pruebas, sin cluster) | Un generador que repite un valor; `keycloakAdminPassword` reintroducida en la lista de arranque de Pulumi | Rojo: `completarSecreto` lanza en vez de crear dos claves iguales; `verificaciones/secretos.test.ts` detecta la clave compartida entre las dos listas |
| La rotación de `sgtm_app`, contra un motor real | Quitando el `ALTER ROLE` de `verificar-rotacion.sh` | Rojo: una conexión nueva con la clave vieja sigue funcionando después de "rotar" |
| Los secretos, generados y comprobados contra un cluster real (CI) | Corriendo `bootstrap-secretos.sh` dos veces y comparando huellas; y forzando la misma clave para `sgtm_owner` y `sgtm_app` con `kubectl patch` | La huella no cambia en la segunda corrida; `verificar-claves-distintas.sh` detecta las dos claves iguales |
| El respaldo, restaurado de verdad (RNF-079) | Cinco roturas del simulacro: sin `recovery_target_time`; con `archive_mode=off`; con `sgtm_respaldo` como `SUPERUSER`; con el sha256 de wal-g corrompido; y sin el `GRANT pg_read_all_settings` | Las cinco lo ponen rojo. La primera restaura **4 filas donde había 3** —la escritura posterior al instante marcado sobrevive—, que es exactamente el defecto que un PITR mal apuntado produce en silencio |
| El rol del respaldo no puede más de lo que necesita | Dándole `CONNECT` sobre la base del padrón, contra un motor real | Rojo: `pg_backup_start`/`stop` son operaciones del clúster, no de una base, y una credencial de más apuntando al padrón es una credencial de más |
| El escaneo de secretos del repositorio entero | Apuntando gitleaks a la muestra de clave de mentira sin la exclusión del repositorio | La encuentra; sin la muestra, el escaneo del repositorio no demostraría nada |
| Una alerta que le llegue a alguien, contra un clúster real | Apagando PostgreSQL sin receptor configurado, y con receptor configurado | Sin receptor: la regla llega a `firing` y el receptor de prueba recibe 0 peticiones; con receptor: la misma alerta activa se entrega |
| Los tableros muestran datos de verdad | Consultando cada panel de `resumen-operativo.json` contra un Prometheus real, con exportadores reales y uno sintético para la JVM | Ninguno vuelve «No data»; quitar un objetivo del scrape lo pone rojo, nombrando el panel |
| kube-state-metrics no tiene privilegio de más | Pidiendo `secrets`/`configmaps` o un verbo de escritura en su `ClusterRole` | `yarn test` lo rechaza: solo `list`/`watch` sobre lo que las reglas y el tablero usan |
| Carga inicial de vías, sectores y manzanas (20 pruebas) | Anotando `@Transactional` sobre el método que orquesta el archivo entero —o, equivalente, envolviendo el bucle en un solo `TransactionTemplate`— en vez de dejar que cada fila abra la suya al llamar a un caso de uso `@Service` distinto | Rojo: la fila que revienta la unicidad se lleva consigo a la fila válida que la seguía, igual que ya demostraba `ViaRepositoryJdbcTest` para dos escrituras en una transacción |
| El orden de las clases de prioridad (5 pruebas) | Intercambiando `PRIORIDADES.datos` y `PRIORIDADES.lote` —PostgreSQL en la prioridad más baja del clúster, las emisiones masivas en la más alta— | **Antes: verde en las 170.** La auditoría exigía que todo pod DECLARASE su clase, nunca que el orden fuera el correcto, así que la inversión exacta del no-negociable del issue #157 pasaba sin ruido. Ahora, rojo en 5 |
| El init container de wal-g con la raíz sellada (`raiz-sellada`, en cada PR) | Ejecutando la imagen real con `--read-only` y **sin** montar `/tmp` —el propio guion lo hace como caso B— | Rojo: `exit 23`, «curl: (23) client returned ERROR on write». Con `/tmp` montado, exit 0 y el binario arranca —«wal-g version v3.0.5»—, que de paso confirma que el `WALG_SHA256` fijado es el del release |
| La reserva del nodo, contra el VPS de `prod` de verdad (`reservar-recursos-del-nodo.sh`, issue #157) | Corriéndolo como root sobre `vmd120205`, con k3s en marcha y reiniciándolo de verdad | Lo asignable bajó exactamente 2 CPU y 2 Gi —`system-reserved` + `kube-reserved`, 1+1 de cada uno—, la capacidad no se movió, el API server volvió solo y ningún pod quedó fuera de `Running`/`Succeeded` |
| El tope de tiempo de `observabilidad-alertas` | Ya pasó, sin necesidad de provocarlo: sin `timeout-minutes`, el trabajo corrió más de 40 minutos el 2026-08-23 —muy por encima del peor caso de sus `--timeout` internos (~25 min)— hasta que se canceló a mano | `timeout-minutes: 15` puesto: un colgado real deja de poder consumir cuota sin límite |
| Que `prod` quepa en el nodo REAL, no en el que tendra (16 pruebas) | Devolviendo los 250m de `RECURSOS.arranque`, que es lo que pedian los dos Jobs de arranque | Rojo en 2, diciendo «Faltan 260m». **Y el intento anterior fallo por adelantarse**: se declararon los 3 CPU que dara la reserva corregida antes de aplicarla, y `aplicar-prod` se paro en «Lo declarado cabe en el nodo real» —ese paso rechaza declarar de MAS, asi que adelantarse solo cambia el paso en el que falla—. Lo permanente (1 540m) siempre cupo en 1 800m; sobraba el pico |
| La reserva del nodo suma lo dimensionado, no el doble (6 pruebas) | Devolviendo la duplicacion: `cpu=1` en `system-reserved` **y** en `kube-reserved`, que es lo que el guion escribia | Rojo en 3, una diciendo «expected 2000 to be 3000». **Y el defecto llevaba tres dias en verde**: la medicion de `INF-10` §4 ya decia «2 Gi exactos, y 2 CPU» —el doble de lo que `INF-01` §2 dimensiona— y se leyo como el coste esperado de la reserva. `vmd120205` repartia 2 de sus 4 CPU, y por eso `prod` no podia desplegarse |
| Que el stack quepa en su nodo (13 pruebas, sin Pulumi ni clúster) | **También pasó antes de escribirse.** `aplicar-prod` se colgó cuatro veces entre el 25 y el 26 de agosto de 2026, una casi seis horas: la reserva de #157 dejó `vmd120205` en 2 CPU asignables y el stack pide 2 040m solo en `Deployment`. Un pod que no se puede ubicar no falla —se queda `Pending`— y el `ConfigGroup` lo espera sin error ni registro. Se demuestra con el nodo real de prod, con los 4 GB que #158 ya probó insuficientes, y subiendo `webReplicas` contra un nodo holgado | Rojo en los tres, diciendo cuántos milicores faltan. Y un cuarto caso al revés —un nodo de 8 CPU/16 GB— en verde: sin él, la comprobación podría estar diciendo que no a todo |
| Que esa aritmética describa a Kubernetes (`capacidad`, en cada PR) | Aplicando el stack entero contra un `kind` real. Lleva dentro su demostración, con el **mismo detector** que el caso principal: un pod que pide 1 CPU más de la que el nodo tiene tiene que salir rechazado por «Insufficient cpu» | Los dos casos pasan contra un planificador real. **Y la primera versión medía la señal equivocada**: contaba «pods sin ubicar» y daba 3 de 14 en falso —`postgres`, `prometheus` y `grafana` estaban `Pending` esperando su volumen (`WaitForFirstConsumer`), no CPU—. La señal correcta es `PodScheduled=False` con «Insufficient» en el mensaje; lo demás es un rojo por un motivo que no es el que se mide |
| Lo que el stack dice del nodo, contra el nodo real (`aplicar-*`, antes de `pulumi up`) | Declarar más CPU o memoria asignable de la que el nodo tiene | Rojo, nombrando las dos cifras. Declarar de menos se admite —solo aprieta la comprobación— y avisa |
| El alta declarativa de usuarios de Keycloak (ADR-0012): TSV derivado en `componentes.test.ts`, y el peldaño «3b» de `despliegue.yml` con el buzón Mailpit levantado | En el test: dos `administrador: true` en un archivo, un `municipalidadId` que no es entero, y el administrador del archivo distinto del que implanta el stack. En `despliegue.yml`: quitar el `execute-actions-email` del guion, y falsear el `municipalidadId` del archivo | Rojo los tres en el test, nombrando el archivo. En CI: sin el envío, el buzón queda vacío y el peldaño 3b se pone rojo; con el número falseado, el cruce a tres bandas —archivo ↔ `SELECT id FROM municipalidad` ↔ atributo en Keycloak— no cuadra |
| Notificación con acuse, prescripción y pase a coactiva (#39, 68 pruebas, 13 contra PostgreSQL real) | Quitar `PLAZO`/`PRESCRIPCION` de la lista de la regla 5; quitar el índice único parcial de `valor_movimiento`; devolver el `UPDATE` que V7 le daba a `notificacion`; quitar `notificacion_exigibilidad_ck`; y lanzar diez pases a coactiva del mismo valor con hilos de verdad | Rojo el escáner de fuentes, nombrando la muestra; rojo: diez pases producen diez movimientos, no uno; rojo: `sgtm_app` puede corregir una diligencia en el sitio; rojo: una diligencia no hallada admite fecha de exigibilidad, escrita por SQL directo |
| Las dos puertas de la cabecera (ADR-0014: lanzador y menú de la persona, 21 pruebas) | Listando `MODULOS` sin filtrar en el lanzador; devolviendo Enter al índice de las flechas en vez de a la entrada enfocada; quitando el oyente de Esc en `document`; y quitando `--accent-ink` del bloque oscuro | Rojo: Coactiva aparece en el menú del cajero; 3 en rojo de teclado; 3 en rojo de Esc; rojo el contraste, «1.15:1» |
| La caja: cobranza atómica, doble cobro y numeración (#33, 56 pruebas, 15 contra PostgreSQL real) | Cinco roturas: quitar el `@Transactional` de `CobrarDeuda`; quitar el `FOR UPDATE` sobre las filas de `saldo_proyectado`; quitar `recibo`/`recibo_detalle` de `TABLAS_INMUTABLES`; y quitar de V29 el `REVOKE UPDATE` y el índice único de idempotencia | 14 en rojo la primera —sin transacción no hay `SET LOCAL`, así que la cobranza ni siquiera escribe—, y la de atomicidad además deja el recibo insertado. 1 en rojo la segunda, **pero solo a la tercera versión de la prueba**: con una caja y un cajero la serializa el turno, y con una caja y diez cajeros la serializa el contador de la serie; las dos pasaban en verde con el candado quitado. Con diez cajas y diez series salen **2 cobros donde debe haber 1**. 1, 2 y 1 en rojo las tres últimas —y las dos del privilegio pasan de `42501` a `23514`: sin el `REVOKE`, lo único que frena el `UPDATE` es un `CHECK` que no estaba puesto para eso— |
| Duplicado y anulación de recibo (#34, 43 pruebas, 17 contra PostgreSQL real) | Seis roturas: quitar la guarda del mismo día; escribir un `UPDATE recibo_movimiento SET` en `src/main`; saltarse la reversión y solo marcar; resolver la fecha del papel con `LocalDate.now()` en vez de la congelada; quitar el índice único parcial de V30; y darle `UPDATE`/`DELETE` sobre `recibo_movimiento` a `sgtm_app` | 2, 1, 6, 2, 1 y 2 en rojo. La quinta produce **4 anulaciones donde debe haber 1** con diez hilos, y con ellas cuatro reversiones: el contribuyente acabaría debiendo cuatro veces lo que pagó. **Y la cuarta pasó en verde a la primera**: la prueba buscaba la fecha del cobro como subcadena, y el instante de emisión —`2026-03-16T00:00:00Z`— ya la contenía, así que un `aLaFecha` resuelto con el reloj de la reimpresión no se distinguía. Ahora se comparan las dos líneas de fecha enteras |
| La ficha del contribuyente y la del vehículo (#330, 11 pruebas) | Devolviendo el índice a `true` —la barra de pestañas vuelve—, quitando la línea de deuda del resumen y poniéndole un cero, y quitando el aviso de dominio | 4, 1 y 1 en rojo. La primera es la que importa: con `true` el índice indexa **la pestaña activa**, así que las 56 claves siguen repartidas en nueve clics y el cambio no se nota en ninguna otra pantalla con pestañas |
| La baja de deuda elige sus filas (#332, 8 pruebas) | Quitando el límite de una obligación por acto de `exigir`, haciendo que la tabla `plana` viaje como lista, quitando la conexión que le da filas y quitando la selección declarada | 1, 1, 8 y 8 en rojo. **Y la tabla estaba vacía desde siempre**: la operación de la pantalla es un `POST`, y una operación que escribe no se pide al abrirla —la columna de selección que el prototipo dibuja no tenía sobre qué actuar hasta que se leyó `consulta_deuda`, que es quien publica esa deuda— |
| Ningún acto promete lo que no puede (#332, 6 pruebas, transversal) | Devolviendo `undefined` a `impedimentoDelActo`; dando el mismo texto a las dos causas; devolviendo a `useEscritura` la operación de una opción sin declarar; y quitando el `aria-describedby` de la primaria | 17, 2, 12 y 13 en rojo, en cinco módulos. La tercera es el defecto que cerró: una opción no declarada «mandaba solo su observación», y con ella la primaria se habilitaba —lo que cinco pruebas de cinco módulos daban por bueno— |
| Un entero es entero entero (5 pruebas) | Devolviendo `Number.parseInt`, que se queda con el prefijo | 2 en rojo: «1 - 4» viajaba como la cuota 1, y las otras tres se perdían sin que nada lo dijera |
| Grupos por tarea y el centro de reportes de Tránsito (ADR-0014, 5 guardas + 49 pruebas de catálogo y centro) | Cinco tablas que violan cada guarda del portador —incluida la de dos grupos homónimos—; desmarcando `{ centro: true }` y regenerando; moviendo una hoja de grupo; y leyendo `MODULOS` en vez del catálogo visible en el carril | Las cinco guardas muerden; 12 en rojo: la barra vuelve a 23 entradas; 19 en rojo: «cada opción exactamente una vez» delata el cambio; rojo: con 2 hojas permitidas el carril lista 13 |
| El campo que resuelve la unidad del alta de deuda y la memoria del predial, tras la triple revisión (#331, #333; 45 pruebas nuevas) | Dieciocho roturas, y las que más dicen son tres: devolver `MULTA_ADMINISTRATIVA` a «sin unidad» —que es lo que `RegistrarPapeleta:164-170` desmiente, porque asienta el cargo **con** el `predioId` de la papeleta—; quitar la opción vacía del desplegable de concepto; y darle al resolutor el `fijarCampo` de la pantalla entera en vez del acotado a los campos que declara, con una **muestra que lo viola** —un resolutor que además escribe `codContribuyente` y un importe— | 2, 1 y 1 en rojo. Las tres estaban en verde: la primera **rechazaba** resolver el predio de una multa que sí cuelga de uno; la segunda dibujaba «IMPUESTO PREDIAL» sin que nadie lo tocara y mandaba el `POST` **sin `tributo`**; la tercera dejaba a un control propio escribir campos que el operador no tecleó. Y las quince restantes muerden una a una: el `outline-offset` de la lista —sin él, del foco solo queda una raya **sobre la fila anterior**, y Enter asienta sobre otro predio—, el cruce del titular, la rama de la placa —que no la verificaba nada: se podía romper entera y las 798 seguían verdes—, el privilegio del acto sobre el control, el 403 que no es «vuelve a intentarlo», el rótulo recordado —que sobrevive al plegado y **no viaja**—, la limpieza tras guardar, el foco al elegir y al cambiar, el recuento recortado, los motivos que ya no mandan a una lista que no está en pantalla, la entrada de la tabla en el índice de `predial_individual` y el `title` de RNF-052 en los secundarios de una pantalla con impedimento |
| El convenio de fraccionamiento, del preconvenio al quiebre (#35, 56 pruebas, 26 contra PostgreSQL real) | Cinco roturas: devolver la deuda escribiendo la fase en `saldo_proyectado` en vez de con asientos; quitar de V31 el `CHECK` que exige el recibo al formalizar; quitar `convenio_deuda_uq`; compilar el interés y el máximo de cuotas en `Cronograma`; y devolver siempre a `ORDINARIA` en vez de a la fase de origen | 3, 1, 1, 2 y 3 en rojo. **La cuarta enseña lo suyo**: con la lista de la regla 5 anterior a #35 —`INTERES_MORATORIO`, sin `CUOTAS`— el archivo de **producción** con las dos cifras compiladas pasa en VERDE y solo se pone roja la prueba de que la muestra muerde; por eso el patrón se ensancha a `INTERES` y gana `CUOTAS`. Y la quinta la caza la comparación **asiento por asiento**: deja `ARBITRIOS/COACTIVA` en −200 y `ARBITRIOS/ORDINARIA` en +200, o sea la deuda coactiva movida en silencio a cobranza ordinaria, que es justo lo que `convenio_deuda.fase_origen` existe para impedir |
| La consulta unificada, la última de `Consultas` (#25, 23 pruebas, 14 contra PostgreSQL real) | Cuatro roturas: que el puerto de convenios de tesorería escriba su propio criterio y se deje el contribuyente; publicar el total del resumen sin su fecha; sumar el resumen sobre la página devuelta en vez de sobre todas las obligaciones; y quitarle el `@Transactional` a la ficha | 3, 1 y 1 en rojo, y 12 la cuarta. **La primera enseña lo suyo**: la ficha de un contribuyente **sin ningún convenio** pasa a mostrarle los de otras siete personas, y la prueba que lo caza no es la del contribuyente completo sino la del contribuyente vacío. La tercera deja el resumen en **300,00 donde debe decir 1 220,00** —la cuarta parte de la deuda, en la cifra que se lee en ventanilla—. La cuarta falla nombrando `declaracion_jurada`: los cinco puertos ajenos traen su propia transacción y disimulan, así que el único síntoma es la sección que se lee del repositorio del anfitrión |
| El expediente coactivo, de la importación al historial (#40, 74 pruebas, 24 contra PostgreSQL real) | Cinco roturas: admitir el valor notificado sin esperar al plazo ni al pase; degradar `expediente_valor_unico_uq` a índice normal; devolverle a `sgtm_app` el `UPDATE` sobre el historial; escribir un `UPDATE expediente_movimiento SET` en `src/main`; y declarar un `Dinero` sin su fecha en el DTO de la capa web | 7, 1, 2, 1 y 1 en rojo. **La segunda enseña lo suyo**: sin el índice único, reintentar la importación *una vez* sigue en verde —la comprobación previa en Java la caza—, y solo con **diez hilos** salen **2 expedientes donde debe haber 1**, o sea dos procedimientos coactivos por la misma deuda. Y una sexta la encontró la propia prueba: analizar el número suponiendo «el ejercicio es el grupo 1» funciona con la plantilla de omisión y lee `0042-2026-EC` como *el ejercicio 42*; probar la numeración con **dos** plantillas —que es lo que D-09 abierta exige— lo destapó antes de que D-09 se cerrara con el error dentro |
| Actos coactivos, REC-1/REC-2 y notificaciones (#41, 56 pruebas, 23 contra PostgreSQL real) | Cinco roturas: que una diligencia no hallada cuente como notificación —quitando `AND exigible_desde IS NOT NULL`—; quitar la guarda del plazo de la REC-1 **y** la del propio `ActoCoactivo`; quitar la guarda de deuda viva; escribir un `UPDATE acto_coactivo SET` en `src/main`; y quitar de V34 el `REVOKE UPDATE` | 1, 3, 2, 1 y 1 en rojo. **La segunda enseña lo suyo**: con las dos guardas de Java fuera, la prueba web devuelve **201** —una REC-2 dictada con el plazo corriendo entra sin ruido— y la única que la para es `acto_rec2_plazo_ck`, que la rechaza con `23514`. Es el mismo mecanismo que `valor_movimiento_exigible_ck` (V28): la fila **copia** la diligencia que la sustenta y el día desde el que la ley la permite, y un `CHECK` compara las dos fechas. Y el hallazgo del diseño fue que **`notificacion` sirve tal cual**: V3 la nació polimórfica y V28 le puso, para #39, el intento, el acuse, la exigibilidad y `notificacion_intento_uq`; nada de eso era del valor, así que la REC se notifica con `objeto = 'ACTO_COACTIVO'` y V34 no le toca una columna |
| Costas procesales, fraccionamiento coactivo y las dos consultas (#42, 32 pruebas, 21 contra PostgreSQL real) | Cinco roturas: dejar la liquidación sin asentar su cargo —la costa como fila del expediente y no como apunte del libro—; quitar `COSTA` de la lista de nombres de la regla 5; hacer que el quiebre devolviera siempre a `ORDINARIA`; quitar de V35 el `REVOKE UPDATE ON costa_procesal`; y quitar la comprobación de quién es dueño de la obligación de costas | 11, 1, 1, 1 y 1 en rojo. **La primera enseña lo suyo**: con el cargo fuera, `DeudaDelExpediente.costas` vuelve a cero y la REC-2 imprime «TOTAL EXIGIBLE 500,00» donde debe decir 535,00 —el papel notificado se lleva la cifra—. La segunda solo la caza la muestra: `ARANCEL_COSTA_REC1` ya la veía `ARANCEL`, pero `COSTA_DE_LA_REC2` no empieza por ninguna palabra vigilada y pasaba sin ruido, el mismo hueco exacto que #35 destapó con `INTERES_DE_FRACCIONAMIENTO`. Y **la quinta es la que no se veía venir**: el libro no distingue expedientes en la clave de una obligación, así que sin `costa_obligacion` el primer expediente muestra **70,00 donde debe decir 35,00** y el segundo **0,00** —su costa está cargada en el libro y ningún expediente la enseña— |
| Cierre y arqueo de caja, avance y distribución (#36, 54 pruebas, 14 contra PostgreSQL real) | Cuatro roturas: contar la anulación como cobro (`neto = cobrado`); olvidar que el recibo de tasas y el de cuota inicial NO abonan en el libro y meterlos en el cuadre; conceder `UPDATE` sobre `cierre_turno`; y redondear la suma de las partes del arqueo con una política escrita a mano | 3, 2 y 1 en rojo, y la cuarta la caza el escáner de fuentes. **Y una quinta la encontró la propia base**: el `REVOKE UPDATE ON cierre_caja` que V32 iba a hacer —el mismo que V29 le hizo al recibo— **deja la caja sin poder cobrar**, porque `SELECT … FOR UPDATE` exige el privilegio de UPDATE y esa fila es donde se serializa la ventanilla desde #33. El síntoma no se parece a su causa: `BadSqlGrammarException` en la primera cobranza, porque el SQLSTATE `42501` cae en la clase 42. `cierre_caja` es la primera tabla cuya inmutabilidad la sostiene solo el escáner |
| La licencia de funcionamiento, con su recibo, su cancelación y su duplicado (#44, 60 pruebas, 13 contra PostgreSQL real) | Cinco roturas: quitar `licencia_funcionamiento` de `TABLAS_INMUTABLES`; quitar de V37 el `REVOKE UPDATE`; degradar `licencia_duplicado_uq` a índice normal; quitar la comprobación de que el recibo no esté anulado; y quitar la guarda del filtro por titular que no encuentra a nadie | 1, 1, 1, 2 y 1 en rojo. **Y la tercera enseña lo suyo**: con el caso de uso entero en los diez hilos, la carrera pasaba en verde con el índice degradado, porque quien la serializaba era otra cosa —`siguienteCorrelativo` de los documentos es un `count(*) + 1`, y `documento_numero_uq` rechaza a los nueve que calculan el mismo número de resolución antes de que nadie llegue al ordinal—. Midiendo lo que dice medir —el ordinal, con los papeles emitidos por adelantado— salen **10 duplicados con 4 ordinales distintos**, o sea seis papeles que dicen «DUPLICADO N.° 1» y no se pueden distinguir. Y una sexta la encontró la propia migración: el `DROP INDEX licencia_contribuyente_ix` que parecía prudente falla con «index does not exist», porque `DROP COLUMN estado` ya se lo había llevado por delante |
| Descargos, internamiento vehicular y resoluciones de gerencia (#50, 46 pruebas, 23 contra PostgreSQL real) | Seis roturas: quitar `resolucion_gerencia` e `internamiento` de `TABLAS_INMUTABLES`; quitar de V41 `resolucion_gerencia_plazo_ck`; quitar `internamiento_liberacion_ck`; creerle a la petición lo de «custodia cancelada» en vez de preguntárselo a `tesoreria`; no asentar la baja cuando la resolución deja la multa sin efecto; y degradar `resolucion_gerencia_ordinaria_uq` a índice normal | 1, 1, 1, 4, 1 y 1 en rojo. **La sexta enseña lo suyo**: con diez hilos dictando la ordinaria de la misma papeleta salen **5 resoluciones donde debe haber 1**. **Y el hallazgo del diseño fue que `papeleta` no sabía a quién se le cobra**: #46 recibía `contribuyenteObligadoId`, asentaba el cargo contra él y no lo guardaba —así que nada podía encontrar después la obligación que un descargo fundado tiene que dar de baja, y `infractor_id`/`propietario_id`/`contribuyente_id` son tres candidatos y ninguno es la respuesta—. Otro salió ejecutando: `notificacion.numero` es `varchar(20)` desde V3 y la diligencia se numera «número del acto/intento», así que `RG_ORDINARIA-2026-000001/1` no entra; el tipo de documento pasa a `RGO`/`RGS`/`RGA` |
| Liquidación, reliquidación, omisos y subvaluadores (#49, 94 pruebas, 12 contra PostgreSQL real) | Cinco roturas: leer los parámetros con `vigenteEn(ejercicio)` en vez de `porConjunto(...)`; hacer que declarar fuera de plazo cuente como omiso; sacar las tres tablas de la liquidación de `TABLAS_INMUTABLES`; quitarle la `Observacion` a `LiquidarFiscalizacion.liquidar`; y que la reliquidación deje de referenciar a la anterior. Y una sexta contra la base: conceder `UPDATE` sobre `liquidacion_detalle` en V39 | 1, 9, 1 —la muestra—, rojo en `verificarArquitectura` nombrando el método, 10, y 2. **La segunda enseña lo suyo**: el AC 3 no es un caso borde sino el eje —quien declaró tarde declaró—, y al confundirlo se cae hasta `FilaDeOmisos`, que rechaza en su constructor la fila «OMISO y además fuera de plazo». Y el diseño lo decidió la base: `conjunto_sellado_uq` (V9) admite **un solo** conjunto sellado por ejercicio, así que «sellar la versión 2 de 2024» no se puede sembrar; lo que PostgreSQL garantiza del AC 1 es que la columna está **copiada** y `sgtm_app` no la puede mover |
| El FUE de edificación completo: por partes, valorizado, ampliado y revalidado (#48, 68 pruebas, 24 contra PostgreSQL real) | Cinco roturas: quitar las ocho tablas del FUE de `TABLAS_INMUTABLES`; quitar de V43 el `REVOKE UPDATE ON licencia_edificacion`; degradar `edificacion_movimiento_emision_uq` a índice normal; hacer que el tramo de la revalidación empiece el día del acto en vez del siguiente al anterior; y que una celda que falta en el cuadro de valores unitarios **valga cero** en vez de fallar | 1, 1, 1, 1 y 3 en rojo. La tercera deja **8 licencias donde debe haber 1** con diez hilos de verdad: dos papeles que dicen otro número para la misma obra. **Y la quinta es la que este issue existe para impedir**: con el cero, la licencia sale con «valor de obra 0,00» y esa cifra es indistinguible de una correcta cuando llega al papel que se exhibe en la obra —y es la base sobre la que se liquidó el derecho de trámite—; por eso V43 **retira la columna `valor_obra`** y la valorización se calcula contra el cuadro de #17, o dice «—» nombrando la llave que falta. Y un defecto lo encontró la propia prueba: el papel **sí** llevaba la raya, y la aserción la leía como ISO-8859-1 cuando el PDF va en CP-1252 —el byte 0x97 se veía como un carácter de control invisible, y la prueba decía que la raya faltaba— |
| La conciliación catastro↔rentas, dicha sin inventarla (ADR-0015, #322: 7 pruebas y un testigo en la franja) | Cuatro roturas: quitar el aviso de la consulta de fichas; devolver a «—» la columna «Cod. Predial Rentas»; quitar la línea de conciliación de la cabecera de la ficha; y clasificar «Conciliar» como acción de salida | 1, 2, 4 y 3 en rojo. **La segunda enseña lo suyo**: el «—» no era una falta de dato sino un comentario equivocado —decía que el código predial de rentas «lo tiene contribuyentes», y no existe tal código: es el mismo de referencia catastral (ADR-0015), así que las dos columnas coinciden y que coincidan **es** el dato—. Lo que sigue con «—» es «Conciliada», que es un derivado que ninguna lectura publica todavía, y ahí el guion es la verdad |
| Anuncios y propaganda, con la deuda generada al autorizar (#51, 82 pruebas, 27 contra PostgreSQL real) | Once roturas, y las que más dicen son tres: degradar `anuncio_movimiento_cargo_uq` a índice normal; darle a la tasa que falta un valor por omisión; y hacer que `licencias` toque un tipo interno de `cuentacorriente` | 2, 4 y 1 en rojo. **La primera enseña lo suyo**: con diez hilos renovando el mismo anuncio salen **10 cargos donde debe haber 1**, o sea diez veces la tasa del mismo año al mismo administrado, y la versión secuencial de la misma prueba pasa en verde —el `if` no existe: dos renovaciones son peticiones legítimamente distintas, con otra fecha y otra clave de idempotencia—. La segunda deja la autorización emitida con **50,00 inventados** que ninguna ordenanza respalda y el borde responde **201 donde debe responder 422**: por eso D-02b se resuelve fallando y nombrando la llave, `TASA_ANUNCIO:<CLASE>`. La tercera la caza Spring Modulith nombrando el tipo —«depends on non-exposed type `cuentacorriente.dominio.Concepto`»—, que es el AC 2 entero: la deuda se le **pide** al libro por `GeneradorDeCargos`, no se asienta por cuenta propia. Y las ocho restantes muerden una a una: degradar `anuncio_idempotencia_uq` (10 anuncios donde debe haber 1), quitar la RLS de `anuncio_movimiento` (4 en rojo, una diciendo «fuga de filas de la municipalidad B»), quitar el `REVOKE UPDATE ON anuncio`, conceder `UPDATE`/`DELETE` sobre `anuncio_movimiento`, quitar la guarda del cese en `RenovarAnuncio` —**201 donde debe ser 409**: un anuncio cesado vuelve a devengar—, estrechar el patrón de la regla 5 quitándole `TASA` y `TARIFA` —de las 4 constantes de la muestra solo caza 1, el mismo hueco que #35 destapó con `INTERES_DE_FRACCIONAMIENTO` y #42 con `COSTA_DE_LA_REC2`—, sacar `anuncio`/`anuncio_movimiento` de `TABLAS_INMUTABLES` (de 4 sentencias detecta 0) y quitar el `generarCargo` (11 en rojo) |
| La ficha 360° del contribuyente (ADR-0016 §2, #297: 31 pruebas nuevas y un camino de Playwright) | Siete roturas: reescribir un rótulo del catálogo —«Código predial» → «Cod. predial»—; darle banda de fecha a «Pagos Realizados», cuyas filas ya traen cada una la suya; componer las papeletas por el código del padrón en vez de por el documento; dejar que una pestaña sin permiso se dibuje; montar los siete paneles a la vez en vez de solo el activo; que la flecha active la pestaña sin llevarse el foco; y dibujar el enlace de una acción cuyo destino el perfil no puede ver | 1, 1, 1, 3, 1, 2 y 1 en rojo. **La cuarta enseña lo suyo**: sin la guarda de permiso, quien solo tiene `papeletas` deja de ver «falta «Contribuyentes»» y pasa a ver una pestaña de papeletas **vacía**, que es exactamente la lectura que ADR-0016 §2 prohíbe —una pestaña vacía ya dice que ahí hay algo que mirar—. **Y la primera es la que sostiene una decisión de coste**: la ficha declara sus columnas en vez de leerlas del catálogo en tiempo de ejecución, porque leerlas costaría descargar los cuatro módulos de sus fuentes al abrir una ficha; lo que esa decisión podría costar —un rótulo reescrito sin que nadie se entere (RNF-080)— lo cierra la prueba que compara letra a letra contra el catálogo portado. La tercera cambia `documentoDelInfractor=03593174` por el código municipal y el listado vuelve vacío: `GET /transito/papeletas` no tiene filtro por contribuyente, y la papeleta de la persona dejaría de aparecer sin que nada lo dijera |
| La misma ficha, tras la triple revisión y la adversaria final de #297 (25 pruebas nuevas: 23 de la ficha, 1 del catálogo y 1 de contraste) | Dieciocho roturas, y las que más dicen son cuatro: quitar el efecto que lleva el foco al nombre —y, aparte, quitarle su guarda—; leer el `totalElementos` de una sección como `contenido.length`; devolver `opcion.ruta` pelada cuando un filtro declarado se queda sin valor; y devolver la activación automática a las flechas de la barra | 1, 1, 1 y 1 en rojo. **La tercera es la que no se veía**: con el documento vacío, «Estado de cuenta de infracciones» abría el estado de cuenta de **todos** presentado bajo la ficha de esta persona —lo que ya hacía bien el registro vacío tres líneas más arriba—. La cuarta cuesta **cinco lecturas por recorrer la barra**, que es lo que ADR-0016 §2 evita al no consultarlas al abrir, deshecho por un gesto de teclado. **Y la de los permisos hizo falta doble**: la prueba que decía «sin el padrón no sale la petición» no llamaba a `espiar()`, así que `pedidas` quedaba vacío y no podía fallar; con el espía puesto y la guarda quitada sale la petición, y solo quitando **además** la del dibujo aparecen el nombre y el DNI **al lado del aviso de que no se pueden ver** — y la adversaria final exigió la tercera vuelta: una identidad **ya sembrada en la caché** tampoco se pinta, demostrado quitando otra vez las dos guardas del dibujo. Las catorce restantes muerden una a una: el 404 dicho como red caída, la ficha compuesta bajo «ese código no está en el padrón» (2 en rojo), el importe del resumen leído sin exigir su `actualizadoA`, la parada de tabulador del panel, los anuncios de estado de cada panel (4), el `aria-controls` en las cinco pestañas cuyo panel no existe, la nota que falta cuando no se puede dar el total consolidado, el rótulo del menú en vez del título del catálogo —«Papeletas» y «Estado de cuenta de papeleta» juntas en una barra (21 en rojo)—, una pestaña cuya opción es un `POST`, el subrayado del enlace que solo se distinguía por color, la condición especial del contribuyente y la atención que no se anotaba al entrar por enlace directo |
| Padrones, resumen anual y certificados de numeración y zonificación (#54, 57 pruebas, 27 contra PostgreSQL real) | Seis roturas: resolver el padrón con `current_date` en vez de con `:aLaFecha`; resolver el ejercicio del papel con el reloj al reimprimir; conceder `UPDATE`/`DELETE` sobre `certificado` en V51; estrechar el patrón de la regla 5 quitándole `VIGENCIA`; sacar `certificado` de `TABLAS_PROTEGIDAS` y `TABLAS_INMUTABLES`; y devolverle a `ResumenAnualDeLicencias` su `@Transactional` | 2, 2, 1 —y una más en `verificarAislamiento`, «sgtm_app no tiene DELETE en ninguna tabla»—, 1, 1 y 1 en rojo. **La primera es el AC 1 entero**: con `current_date` el padrón con corte en marzo cuenta las canceladas de agosto, así que la misma hoja reimpresa dice otra cosa cada vez. **La cuarta enseña lo de siempre**: de las 4 constantes de la muestra el patrón anterior solo caza 2 —`VIGENCIA_DEL_CERTIFICADO = 36` no empieza por ninguna de las quince palabras, ni por `PLAZO`—, el mismo hueco que #35 destapó con `INTERES_DE_FRACCIONAMIENTO`, #42 con `COSTA_DE_LA_REC2` y #51 con `TASA_PANEL`; y su consecuencia es propia: una vigencia inventada no cobra de más, **autoriza de más**. **Y la sexta la encontró la propia prueba**: envolver los tres colaboradores del resumen en una transacción del anfitrión hace que el `EjercicioSinSellar` de **un** año la marque *rollback-only*, y el reporte entero falla al confirmarla aunque la excepción se capture —los cinco años que sí se podían calcular se pierden por culpa del sexto—. Es el reparto de #25 leído al revés: aquí el anfitrión no debe abrir ninguna |
| La transferencia a rentas: la frontera delicada (#52, 40 pruebas, 8 contra PostgreSQL real) | Ocho roturas: quitar `resolucion_determinacion` de `TABLAS_INMUTABLES`; quitar `MULTA` de la lista de nombres de la regla 5; quitar de la lista de tipos ajenos declarados un puerto que `fiscalizacion` sí usa; quitarle el `@Transactional` a `TransferirARentas`; degradar `resolucion_determinacion_liquidacion_uq` a índice normal; anular la guarda del estado de la liquidación; conceder `UPDATE` sobre la resolución en V49; y darle al puerto del padrón su propia transacción con `REQUIRES_NEW` | 1, 1, 1, **7**, 1, 2, 1 y 1 en rojo. **La cuarta enseña lo de siempre y conviene repetirlo**: sin transacción no hay `SET LOCAL`, así que RLS tumba siete de las ocho pruebas de base —la transferencia ni siquiera llega a escribir—. **La quinta es la que más dice**: con el índice degradado, la prueba de extremo a extremo con diez hilos sigue en **VERDE**, porque quien serializa es `documento_numero_uq` —`siguienteCorrelativo` de los documentos es un `count(*) + 1`, el hueco exacto de #44—; solo la prueba que inserta diez filas que **únicamente comparten `liquidacion_id`** lo mide, y ahí salen **10 transferencias donde debe haber 1**, cada una con su papel y su cargo. **Y la octava es el defecto que este issue existe para impedir**: con `REQUIRES_NEW`, la versión nueva de la ficha **sobrevive** al fallo del último paso —12 fichas donde debe haber 11—, o sea el padrón cambiado sin resolución que lo justifique y sin cargo que cobrar. La segunda solo la caza su muestra: las demás prohibiciones siguen en verde, tercera vez que el mismo hueco se abre por el mismo sitio tras `INTERES_DE_FRACCIONAMIENTO` (#35) y `COSTA_DE_LA_REC2` (#42). Y dos hallazgos salieron ejecutando: `resolucion_determinacion` **no puede ser un `valor` de tipo RD** —`RegistrarValor` exige que la deuda ya esté asentada y rechaza con `ObligacionSinDeuda`, así que con D-02a abierta no se emitiría ninguna y se caería con ella la mitad de la transferencia que no depende de D-02—; y el libro está **particionado por ejercicio** con solo 2026 y 2027 declaradas, de modo que transferir una fiscalización de 2024 falla con «no partition of relation found» hasta que alguien cree esa partición |
| Valores masivos de papeletas, constancias, padrones y resúmenes (#53, 43 pruebas, 25 contra PostgreSQL real) | Cinco roturas: quitar `papeleta_valor_unico_uq` y lanzar diez corridas simultáneas sobre la misma papeleta; devolver el `concepto = 'PAGO'` a la consulta de recaudación; recomponer lo recaudado sumando las papeletas en estado `PAGADA`; resolver «pendiente» con el reloj en vez de con la fecha del parámetro; y quitar de `TABLAS_INMUTABLES` `constancia_libre` y `papeleta_masivo` | 1, 1, 2, 2 y 1 en rojo. **Y la marcha blanca destapó tres defectos que ninguna prueba anterior podía ver, los tres porque hasta ahora todo lo que movía deuda a fase VALOR usaba un doble**: `MovimientoDeFaseCuentaCorriente.moverAValor` **nunca funcionó** —`Asiento.nuevo` valida `exigeMotivo` y `AJUSTE` lo exige, así que emitir un valor sobre una obligación con deuda lanzaba `IllegalArgumentException` desde #37—; el bucle de la corrida **no podía ni leer su propia corrida** —sin `@Transactional` no hay `SET LOCAL` y RLS falla con «unrecognized configuration parameter», el mismo defecto que `ConsultaDeVias` cerró—; y `RegistrarValor.emitir` **emite una resolución de multa de 0,00** por una obligación ya pagada, porque la encuentra entre las disponibles y no mueve una fase que no tiene nada que mover |
| El inicio pregunta a quién atiendes (ADR-0016 §1, #296: 25 pruebas y 2 caminos en Chromium) | Seis roturas: quitar la guarda de permiso de las tres franjas; un patrón propio de placa —«tres letras y tres dígitos»—; persistir las atenciones en `localStorage` como hacen los recientes; que Intro abra el primero de varios; contar el 403 como una respuesta más; y devolver `/` al desvío al panel de recaudación | 2, 1, 1, 1, 1 y 2 en rojo (la primera sube a 5 quitando además los tres `enabled`). **La primera enseña lo suyo, y a la primera pasó en VERDE**: la región viva se contaba sobre lo tecleado y no sobre lo preguntado, así que a media pulsación decía «tu perfil no tiene ninguna de las consultas del padrón» —una frase falsa que aparecía sola durante los 300 ms del rebote— y la prueba del permiso la encontraba y se daba por satisfecha sin que la guarda existiera. Con el anuncio atado al valor aposentado, la misma rotura pone 3 en rojo |
| El conjunto de parámetros, abierto contra un clúster real (#247 §2, 12 pruebas, 9 contra PostgreSQL real) | Tres roturas: anotar con `@Transactional` el bucle que compone el archivo; resolver la llave del parámetro con `clave = :clave` en vez de `IS NOT DISTINCT FROM`; y quitar la guarda que rechaza dos parámetros homónimos | 3, 1 y 1 en rojo. **La primera es peor de lo que #328 documenta**: con la transacción envolvente no se pierde solo la fila válida que seguía a la rechazada —la corrida entera revienta con `UnexpectedRollbackException`, porque la fila que se rechaza marca la transacción como *rollback-only* y el informe que la explicaba no llega a devolverse—. La segunda deja al parámetro **sin clave** —el tipo con un solo valor, que es la forma de la UIT— fuera del conjunto, y el informe lo reporta como «no publicado», que es exactamente lo que no es. La tercera mete `FICTICIO:HOMONIMA` en un conjunto que se va a sellar sin que nadie eligiera cuál de los dos |
| El panel de recaudación, la pantalla de inicio (#56, 53 pruebas, 11 contra PostgreSQL real) | Siete roturas: contar la condonación como cobranza; quitar de «lo cargado» el filtro que descarta el cargo nacido de reversar un abono; conectar el pool como superusuario en vez de `sgtm_app`; declarar un `Dinero` sin `actualizadoA` en el DTO de la capa web; hacer que el panel toque `cuentacorriente.dominio.Fase`; inyectarle `ConsultaDeDeudaPublica`, que devuelve una fila por obligación; y que un avance sin base valga 0 en vez de «—» | 4, 4, **10 de 11**, 1, 2, 2 y 4 en rojo. **La tercera es la que enseña lo suyo**: con las dos municipalidades sembradas idénticas, cada cifra del panel sale exactamente **al doble** —800 donde debe decir 400, 1 400 donde debe decir 700— y ninguna de esas cifras parece mal; solo la prueba que cuenta `DISTINCT municipalidad_id` dice por qué. **Y dos defectos los encontró la propia ejecución**: `Asiento#reversionDe` produce el asiento contrario **con el mismo concepto**, así que anular un recibo de 120 dejaba un `CARGO INSOLUTO` de 120 y «lo cargado» salía 520 donde debe decir 400 —la emisión del ejercicio inflada por cada anulación, y con ella el denominador de todas las barras—; y calcular la barra como «(cargado − pendiente) / cargado» dibujaba **100 %** en un tributo con cargos y sin un solo pago, porque `saldo_proyectado` no tiene fila ni cuando la obligación está cancelada ni cuando nadie la ha proyectado todavía |
| El índice que le faltaba a `construccion` (#313, V53: 2 pruebas de plan contra PostgreSQL real, como `sgtm_app` y con RLS activa) | Quitando V53 y volviendo a medir con `EXPLAIN` las dos consultas que filtran por ficha: la suma del área construida de una página y el detalle de `construccionesDe` | Rojo las dos, y el plan es literalmente el que #313 describe: `Seq Scan on construccion` con `ficha_id` del lado del `Filter` —coste 2 000,05 y **59 960 filas descartadas para devolver 40**—; con V53, `Index Scan using construccion_ficha_ix`, 153,11 y 10,09. **Lo que la prueba exige no es la palabra «Index»** sino que `ficha_id` sea **condición** del índice: un plan que use el índice solo por `municipalidad_id` vuelve a leer la tabla entera y seguiría diciendo «Index». Es el reverso exacto del hallazgo del `LIKE`: `int8eq` **sí** es *leakproof*, así que bajo RLS PostgreSQL lo evalúa antes de la política y lo empuja al índice —las dos condiciones, la del filtro y la de la **política**, salen juntas en el `Index Cond`—. Y el `INCLUDE (area_construida)` se midió antes de descartarlo: da un `Index Only Scan` (89,11) pero dobla el índice (1 464 → 2 400 kB) en la tabla que más crece del catastro —la ficha nunca se sobrescribe, cada versión copia sus construcciones— y su ventaja se encoge sola con la tabla viva: recién insertadas 30 000 filas ya declara `Heap Fetches: 20` de 60 |
| La lectura compuesta de la conciliación catastro↔rentas (#344, ADR-0015: 31 pruebas, 18 contra PostgreSQL real) | Once roturas, y las que más dicen son tres: derivar el predicado de `ficha_catastral_id` en vez de `predio_id`; conectar el pool como superusuario en vez de `sgtm_app`; y quitarle a `conciliadaConRentas=No` la guarda del permiso de fiscalización | 3, 3 y 2 en rojo. **La primera es la que este issue existe para no repetir**: la DJ con `ficha_catastral_id` **nulo** —la que produce ventanilla antes de que el predio tenga ficha, y **toda** fila anterior a V19— pasa a «no conciliada», que es acusar de omiso a quien declaró. **La segunda enseña lo de siempre**: con el superusuario, la grilla de la municipalidad B muestra las fichas de A, y dos predios con el **mismo** código de referencia catastral en municipalidades distintas devuelven dos filas donde debe haber una. Y las ocho restantes muerden una a una: quitar el cruce con la DJ (8 en rojo: todo sale conciliado), cambiar los estados que cuentan —fuera `OBSERVADA`, dentro `SUSTITUIDA`— (3, y una de ellas es la rectificatoria que **cambió de predio**, que sin el estado correcto deja conciliado el predio que se declaró por error), publicar el número de la DJ y su fecha en la fila (1: la frontera de ADR-0015 §2.2), quitar la fila de `ACCESO` de la bitácora (1 en la web y 2 contra la base), sacar la ruta de `IMPLEMENTADAS` (1) y del contrato (2, las dos direcciones), quitar la excepción nombrada de la regla 10 —que demuestra que la regla **sí** ve ese método— y hacer que `ConsultaController` **ignore** el filtro en vez de redirigir (2): eso último devuelve el listado sin filtrar, que es exactamente el resultado plausible y equivocado por el que existía el 422 |
| La publicacion de valores normativos, el eslabon que faltaba (#188, #247 §4: 11 pruebas contra PostgreSQL real, 9 prohibiciones del derivado y su muestra en regla) | Cinco roturas: quitarle el `@Profile("batch")` a `PublicarParametros`; quitar la comprobacion previa de «ya publicado»; volver `IS NOT DISTINCT FROM` a `=` en `publicados`; conectar la publicacion como `sgtm_app` en vez de `rol_carga_parametros`; y abortar la corrida en la primera fila mala en vez de informarla | 1 en `verificarArquitectura` nombrando la clase, 1, 1, **7 de 11** y 2 en rojo. **La tercera es la de #247 §2 otra vez**: con `=`, la fila SIN clave —que es la forma de la UIT— no se encuentra nunca, asi que la segunda corrida del mismo archivo publica 2 donde debe haber 1, y con dos homonimas el conjunto ya no se puede componer: `agregarParametroPublicado` rechaza. **Y una la enseño el propio experimento**: quitar la guarda de Java que exige dos firmas distintas deja las once en VERDE —la fila llega a la base y `parametro_doble_verificacion_ck` la rechaza con `23514`—, asi que la regla la sostiene la restriccion y no el codigo; se anoto en el javadoc en vez de fingir que la guarda protege algo. El derivado no lleva ninguna cifra que no este letra por letra en su archivo `VERIFICADO` del corpus: cambiar `5500.00` por `5600.00` lo pone rojo nombrando `uit.md`, y cambiar la alicuota del primer tramo por la del segundo tambien —aunque `0.6` **si** este en el archivo— porque la cifra se busca en el texto de su propia fila |
| El generador de OpenAPI vuelve a reproducir el contrato (#312, `--comprobar` en CI: 169 operaciones y 158 rutas, byte a byte) | Tres roturas, en las dos direcciones: retocar a mano un `summary` del YAML; borrar del YAML la operación `permisos_de_la_sesion` entera; y quitarle al generador la descripción de un parámetro de ruta del FUE | 1, 257 y 833 líneas en rojo, cada una nombrando la primera divergencia con su número de línea y las dos versiones. **Lo que enseña es lo que llevaba quince issues en verde**: regenerar en limpio borraba **519 líneas y dos operaciones** —`permisos_de_la_sesion`, que está en `IMPLEMENTADAS` de `ContratoDeApiTest` y se habría llevado por delante el build del backend, y `costas_procesales_listado`—, y nada lo medía porque el contrato seguía siendo válido, el backend lo cumplía y el frontend generaba sus tipos de él: lo único que se había perdido era poder reproducirlo. **Y dos de las diferencias no eran de contenido sino de orden**: el orden de las claves de un `paths` no significa nada en OpenAPI, así que reproducirlo obligó a enseñarle al generador `antes` y `tras` en vez de «arreglar» el YAML —la regla es que el contrato comprometido es la verdad y lo que se mueve es el generador—. La tercera diferencia sí era un defecto real: la paginación de `respaldo` iba declarada como parámetro del backend y salía **delante** de los filtros de la pantalla, cuando el contrato la tiene detrás como en toda lectura con tabla |
| La ultima opcion de `Consultas`: el acogimiento a una campana de beneficio, simulado sin inventar su descuento (#72, 35 pruebas backend —12 contra PostgreSQL real— y 6 de interfaz) | Nueve roturas, y las que mas dicen son cuatro: ponerle `@Transactional` al caso de uso que orquesta puertos ajenos; darle a la campana que falta un descuento «razonable» por omision; sumar el resumen sobre la pagina devuelta; y devolver el pie que el prototipo dibuja bajo la tabla | 2, 4 —mas el escaner de fuentes—, 1 y 1 en rojo. **La primera es el reparto de #54 leido al derecho**: el anfitrion no debe abrir transaccion, porque `EjercicioSinSellar` —lo que ocurre HOY en todas las municipalidades— se lanza dentro de la del lector de parametros y marcaria la del anfitrion *rollback-only*; con `@Transactional` la pantalla no se dibuja nunca, revienta con `UnexpectedRollbackException`. **La segunda enseña lo suyo**: el 422 que nombra `BENEFICIO:<CAMPANIA>` es lo unico que separa «no hay ordenanza» de «se le perdono la mitad», y un descuento inventado no cobra de mas, **perdona** de mas. **La cuarta es un hallazgo del prototipo**: su pie dice «Deuda total 1,848.66 · acogida 797.77 · con beneficio 250.15» y se pintaba igual para cualquier contribuyente —tres cifras congeladas de una captura, bajo la tabla de otra persona—; se suprime con `PIES`. Y dos defectos los encontro la propia ejecucion: **un conjunto sellado no admite un parametro mas** (disparador de V9) y **solo hay uno por ejercicio** (`conjunto_sellado_uq`), asi que las campanas de una prueba se siembran todas de una vez o no se siembran; y el escaner de la regla 5 **no veia** `new Alicuota(new BigDecimal("50"))` —solo miraba `Alicuota.de(...)`—, que es justo como se escribe un valor por omision dentro de un `if`: el patron se ensancha al constructor y la muestra lo demuestra |
| El titular del predio, resuelto al clic (#366, ADR-0015 §2.4: 22 pruebas, 12 contra PostgreSQL real) | Diez roturas: quitar el `@RequiereAcceso` del controlador; cambiar su acceso al de la pantalla (`consulta_fichas`); quitar el registro de la fila de `ACCESO`; resolver la titularidad con el reloj en vez de con `vigenteA`; devolver «el» titular en vez de la lista de cuotas; conectar el pool como superusuario; quitar la entrada de `SIN_USUARIO_QUE_OBSERVE`; sacar la ruta de `IMPLEMENTADAS`; borrarla del contrato; y retocar a mano un `summary` del YAML | 8 de 10 en la web, 1, 3 en la web y 3 contra la base, 1, 1, 2, y las tres últimas en rojo por su lado. **La primera enseña lo suyo, y al revés de donde se mira**: sin la anotación, la prueba de «sin permiso → 403» sigue **VERDE** —el guardia niega precisamente cuando la anotación falta— y lo que se cae es el camino feliz; la que la nombra es `verificarArquitectura`, «no declara @RequiereAcceso». **La cuarta es el defecto del precedente**: resolviendo con el reloj, preguntar por marzo devuelve al comprador de julio, que es #24 otra vez con los domicilios. **Y el hallazgo del diseño fue que el endpoint no puede vivir donde el issue lo pedía**: `contribuyentes` es la base del grafo de ARQ-01 §2 —no depende de nadie— y `catastro` ya depende de él para resolver el nombre de sus titulares, así que «el endpoint natural es de contribuyentes» era un ciclo de módulos; lo sirve `rentas`, por lo mismo que la conciliación, y catastro solo pone un puerto |
| La escritura de la declaración jurada, el acto que concilia (#365, ADR-0015 §3: 44 pruebas nuevas —33 contra PostgreSQL real— sobre las 60 del área) | Catorce roturas, y las que más dicen son cuatro: guardar la DJ **sin su `predio_id`**; no marcar `SUSTITUIDA` a la anterior al rectificar; sustituir el UPSERT del correlativo por `SELECT` + `UPDATE`; y degradar `dj_rectifica_uq` a índice normal | 5, 5, 4 y 1 en rojo. **La primera es el circuito entero**: el predio queda fichado, declarado y **fuera** del padrón afecto, que es el falso omiso de #344 producido ahora por el acto en vez de por la lectura. **La tercera y su pareja enseñan las dos capas de la decisión 3**: con el UPSERT roto y el índice único en pie salen cuatro rojos y ninguna DJ duplicada —la red aguanta—; con el UPSERT roto **y** `dj_numero_uq` degradado a índice normal salen **9 números distintos donde hay 17 declaraciones**, o sea papeles firmados que no se pueden distinguir. La cuarta da **10 rectificatorias donde debe haber 1**, y hay que medirla insertando diez filas que solo comparten `dj_rectifica_id`: con el caso de uso entero, quien serializa es el disparador de estado terminal y la carrera pasa en verde con el índice quitado (la lección de #44 y #52, otra vez). **Y un hallazgo salió de la propia migración**: el correlativo **no se puede sembrar en el DDL** —`declaracion_jurada` tiene RLS con `FORCE` y el migrador corre sin contexto de tenant, DAT-01 §0 cuarto hallazgo—, así que el arranque por encima del mayor número histórico lo resuelve la primera petición del ejercicio, que sí lo tiene. Las diez restantes muerden una a una: el plazo con valor por omisión compilado (2, y el 422 deja de nombrar `PLAZO:DECLARACION_JURADA`), quitarle la `Observacion` a `anular` (rojo en `verificarArquitectura`, nombrando el método), quitar de V54 el privilegio de columna (2: el número de un papel firmado se vuelve editable), quitar el disparador de estado terminal (1), quitar del dominio la guarda del estado terminal (4 —y el 409 pasa a error opaco—), quitar la RLS de `dj_correlativo` (4 en `verificarAislamiento`, una diciendo «fuga de filas de la municipalidad B»), sacar `declaracion_jurada` de `TABLAS_PROTEGIDAS` (de los 2 `DELETE` de la muestra detecta 0), quitar las cuatro operaciones del generador (rojo en las **dos** direcciones del contrato) y retocar a mano un `summary` del YAML (`--comprobar` en rojo, con su número de línea) |
| El portal del contribuyente, fuera del shell (ADR-0016 §3, #298: 32 pruebas —21 de la pantalla y 11 del escáner— y 2 caminos en Chromium) | Nueve roturas: bajar el presupuesto del portal por debajo de lo medido; importarle el catálogo del back-office, y una dependencia que no es de sus paquetes; un `useMutation`; declararle como lectura una operación `POST` del contrato; quitar del padrón la comprobación de que la fila es la que se pidió; quitar la banda de fecha del resumen; darle banda a «Pagos Realizados»; quitar los avisos de que aquí no se paga; y borrar la opción `portal` del catálogo generado | 1, 1+1, 1, 1, **11**, 1, **2**, 2 y **10** en rojo. **La séptima enseña lo suyo**: la banda de más en los pagos pone en rojo **una prueba en cada aplicación** desde una sola línea de `@sgtm/lectura`, que es exactamente lo que el paquete existe para que pase —dos copias del adaptador habrían dejado al portal fechando hoy un pago de marzo mientras la ficha seguía verde—. **Y la quinta es el defecto que la separación podía introducir**: el proxy no filtra (ADR-0010), así que sin comprobar la fila el portal le enseña a quien teclea su DNI la deuda de la primera persona del padrón. **Lo que no se veía venir lo destapó una mutación del e2e**: `portal-en-movil.spec.ts` comparaba `scrollWidth` con `window.innerWidth`, y bajo emulación móvil Chromium **aleja la página** cuando el contenido desborda —las dos cifras se igualan—; con `min-width: 900px` en la columna la prueba seguía en VERDE, y la sonda devolvió `{sw: 900, iw: 900}`. Ahora se compara con el ancho del **dispositivo** y muerde. El paquete del ciudadano baja de **147,4 KB medidos a 85,1** —los 152 eran el presupuesto, no la medida— y a **80,9** tras la pasada de abajo, presupuestado en 84 |
| El mismo portal, tras la triple validación de #298 (28 pruebas nuevas: 6 de la pantalla, 4 del escáner, 10 de `@sgtm/lectura` y 8 de la caché) | Trece roturas, y las que más dicen son cuatro: dibujar `{anonima}{children}` en la puerta de sesión; devolver `pedirOperacion` al portal; devolver a `nginx.conf` el prefijo `location /assets/` y la exacta `location = /index.html`; y dibujarle al ciudadano la nota del back-office —la que termina en «se ven en «Consulta de deuda»»— | 1, 1, 2 y 2 en rojo. **La primera es la que este repaso existía para encontrar**: la rama anónima de la puerta —la que el portal pasa porque su `redirect_uri` volvería al back-office— **no la probaba nadie**, y con el aviso y la pantalla dibujados a la vez las quince pruebas del archivo seguían en VERDE; la prueba permanente monta con las tres `VITE_SGTM_OIDC_*` puestas y el canje rechazado, y exige el aviso, ningún h1, ningún campo, ningún botón y cero peticiones a `/api/`. **La segunda se mide en el paquete**: `pedirOperacion` resuelve la ruta leyendo el mapa de las 169 operaciones del contrato —84 de escritura—, así que el portal, que es la aplicación destinada a ser pública, llevaba dentro el inventario completo de la API; con la mutación puesta el arranque vuelve de **80,9 KB a 84,9** y `grep` encuentra `/coactiva/prescripcion` en lo que descarga el ciudadano. Ahora declara sus dos rutas y pide con `solicitar()`, y el contrato lo comprueba una prueba —que no viaja al navegador—. **La tercera enseña por qué la comprobación simula nginx en vez de buscar un texto**: con la configuración anterior el archivo ya contenía «immutable» y «no-cache», y lo que le faltaba era alcanzar `/portal/assets/` y `/portal/index.html` —el prefijo no los cubre y la exacta tampoco—, de modo que el paquete del ciudadano se servía **sin ninguna política de caché**; la prueba resuelve la precedencia (exacta, prefijo, regex en orden) y dice qué regla gana en cada ruta. Y las nueve restantes muerden una a una: quitar el `<main>`, comparar el código del padrón con la cadena vacía en vez de con `SIN_DATO` —que era `?contribuyente=—`, una consulta por alguien que no existe—, una entrada de `LECTURAS` con la ruta cambiada (nombrando la ruta) y otra que es un `POST` del contrato (**nombrando el método**, que es lo que arregla poner la aserción del verbo por delante de la lista), un `solicitar('/coactiva/prescripcion')` escrito a mano, un `import` del catálogo metido por `packages/lectura` —que hasta hoy solo cazaba el presupuesto, y por 11 KB de suerte—, un segundo `texto` exportado fuera de `@sgtm/lectura`, renombrar `rUC` en el contrato generado (4 errores de `tsc`, uno de ellos la guarda de vuelta: «'true' no es asignable a 'never'») y bajar el presupuesto por debajo de lo medido. **Y la revisión adversaria final añadió seis, con sus roturas**: la lista negra del mapa se volvió **lista blanca** de lo que se puede importar de `@sgtm/api-client` —con la negra, una llamada viva a `pedirDatosDePantalla` subía el arranque a 85,3 KB, metía `/coactiva/prescripcion` en el paquete del ciudadano y sacaba una petición a `/portal/deuda` con las once pruebas del escáner en verde; con la blanca, rojo nombrando la función—; el filtro de la unificada va tipado `ParametrosDe<'consulta_unificada'>` —al irse `pedirOperacion` se fue la única atadura, y renombrar `contribuyente` en el generado dejaba al portal compilando; ahora da `TS2353` en `Portal.tsx`—; la simulación de nginx exige **un solo `server`** —un segundo sin política de caché pasaba 8/8—; la región viva distingue el 403 —anunciaba «no se pudo hacer», la frase del error reintentable, al lado de un aviso que decía lo contrario (el patrón de #331)—; la primaria apagada tiene la prueba de su `aria-describedby` —quitarlo dejaba 21/21—; y el selector del e2e pasa de `.sgtm-barra-lateral`, que no casa con nada del producto, a `.sgtm-nav`, la clase real de la barra |
| Las cifras de prescripción y del plazo de la REC-1, publicadas sin reescribir la norma (#192, puntos 1 y 3: 11 pruebas nuevas —3 contra PostgreSQL real— y 3 prohibiciones más del derivado, con su muestra cada una) | Seis roturas: quitar la regla de coherencia de cifra; quitar la de coherencia de unidad; quitar la que exige forma máquina a una fila `PLAZO`; hacer que `FilaPublicable` ignore la columna `valor_maquina`; renombrar en el derivado la clave `PRESCRIPCION-DECLARACION_PRESENTADA`; y publicar `7 DIAS_CALENDARIO` donde la norma dice días hábiles | Las tres muestras dejan de detectarse una a una; 2 en rojo contra PostgreSQL; 2 en rojo en `sgtm-valores`; 2 en rojo en `sgtm-coactiva`. El mecanismo es una columna: `valor_texto` sigue siendo la transcripción verbatim que se compara letra por letra contra el corpus —«dentro del plazo de siete (7) días hábiles de notificado»— y `valor_maquina` es lo que llega a la base —«7 DIAS_HABILES»—, porque `Plazo.de` rechaza a propósito toda lectura tolerante y las dos exigencias no caben en una sola celda. **La quinta rotura es la que enseña lo suyo**: renombrar la clave deja `verificar-publicacion.mjs` en VERDE —la cifra sigue estando en la norma y las firmas son las del corpus— y el parámetro queda cargado bajo un nombre que nadie pide, con el mismo síntoma exacto que «no está cargado»; lo cazan solo las dos pruebas que leen **este** derivado desde el módulo que lo consume. **Y la de la unidad hizo falta doble**: quitando solo la mitad que exige las palabras de la unidad, la muestra la sigue cazando la otra mitad —el texto habla de otra unidad— y hay que quitar las dos para que pase en verde. Las unidades no se copian en el verificador: se leen de `UnidadDePlazo`, y añadirle una sin declarar con qué palabras la escribe la norma lo pone rojo |
| Las cifras que a #192 le faltaban: reclamación de valores, exigibilidad municipal y el inicio del cómputo (punto 2: dos archivos nuevos del corpus, transcritos de los PDF oficiales de la rama `gob-docs` y verificados a dos firmas; 5 filas más del derivado —20 en total— y 5 pruebas nuevas) | Cuatro roturas: renombrar `NOTIFICACION_VALOR-RD` en el derivado; publicar la OP con `0 DIAS_CALENDARIO`; quitar de §2 del corpus la frase «el desfase publicado es de cero (0) días hábiles»; y medir la primera rotura **sin** declarar el CSV como entrada de `test` | El verificador queda en VERDE con la clave renombrada —la cifra sigue en la norma, las firmas son las del corpus— y quien la caza es la prueba nueva: 1 de 10 en rojo nombrando `RESOLUCION_DE_DETERMINACION`, la lección de los puntos 1 y 3 extendida a las cinco llaves nuevas. 2 señales la segunda y 1 la tercera. **Y la cuarta enseñó lo suyo**: la prueba ni corrió —Gradle dio `test` por UP-TO-DATE porque el CSV vive fuera del módulo y no era entrada declarada—, así que la rotura pasaba en **verde rancio** en local (en CI corre fresco y muerde); los tres módulos que leen el derivado lo declaran ahora como entrada. Y dos hallazgos los trajo la propia fuente: **el D. Leg. 1421 no tocó el art. 44** —una versión anterior del archivo lo afirmaba de memoria y el PDF de SUNAT lo desmintió; quedó dicho en el propio corpus—, y las dos cifras que la norma implica y no imprime —el cero (0) de la orden de pago (art. 25.1.d, con el art. 31.2 como suspensión y no como espera) y el año de desfase del art. 44.1— nacen en §2 del corpus con las dos firmas, que es de donde el derivado las cita verbatim |
| Los seis nits del revisor final de #341, cerrados (#342: 21 pruebas nuevas en 4 archivos —`escrituras.test.ts`, `composicion.test.ts`, `IndiceDeSecciones.test.tsx`, `TablaDePantalla.test.tsx`— y 6 archivos existentes extendidos) | Ocho roturas: abrir `unidadDelTributo` con `?? 'ninguna'` en vez de dejar `undefined`; `MINIMO` del resolutor en `0`; quitar el `Object.hasOwn` de `widgetDeFiltro` **y** de `resolutorDeCampo`; quitarle `indiceConLaTabla` a `predial_individual`; anular las dos ramas nuevas de `ano` y `documentoOrigen` en `faltaEnElAlta`; devolver el rótulo del índice a «secciones» sin condición; saltarse `agruparMiles` en las celdas `num` de `TablaDePantalla`; y admitir ceros a la izquierda en el regex de `agruparMiles` | 1, 1, 3, 1, 3, 3, 2 y 1 en rojo (15 pruebas). **La tercera enseña lo que el docblock prometía**: sin `Object.hasOwn`, pedir el resolutor de un campo `constructor` no da `undefined`, da `[Function Object]` — el formulario habría intentado dibujar el constructor de `Object` como si fuera un control declarado. **La cuarta confirma que el nit 2 se equivocaba de premisa**: `composicion.indice !== undefined` en `indexaLaTabla` no está inalcanzable — `predial_individual` ya la ejercita desde antes de #341, y quitarle `indiceConLaTabla` deja **sin la primera entrada** al índice de «Cálculo individual del impuesto predial»; se documentó la alcanzabilidad en vez de inventar una prueba redundante. **La quinta es la que cerraba el nit 3**: sin las dos ramas, `alta_deuda` habilitaba «Dar de alta» con el año en blanco — el mismo 422 de ida y vuelta que ya evitaba el concepto — y hubo que extender año y documento en `escritura.test.tsx`, `rentas.test.tsx`, `resolutor-de-unidad.test.tsx`, `actos-honestos.test.tsx` y el e2e de la caja con teclado, que hasta entonces guardaban sin ninguno de los dos. **Y la octava es el borde que `agruparMiles` existe para no pisar**: sin él, `00001182` — un código catastral, no una cantidad — sale «00 001 182», cambiándole el texto que lo identifica |
| Las tres pestañas de la ficha 360° que faltaban por conectar (#363, 9 pruebas nuevas en `pantallas/transito`, `pantallas/sanciones` y `pantallas/coactiva`, más el recurso real de las tres en `packages/api-mock`) | Dos roturas: comentar las tres conexiones nuevas en el registro de `pantallas/conexiones.ts`, devolviendo `papeletas`, `adm_estado_cuenta` y `coactiva_expedientes` al camino común; y duplicar en el adaptador de `papeletas` la columna «Infractor» con el número de la papeleta en vez de dejarla con `SIN_DATO` | 9 y 1 en rojo. **La primera es exactamente el defecto que #363 describe**: `GET /transito/papeletas`, `GET /infracciones/administrativas/estado-cuenta` y `GET /coactiva/expedientes` ya tenían `Controller` desde antes —el mock ahora habla ese idioma para las tres rutas, no el que comparten las 134—, así que sin conexión el camino común sigue pidiendo por HTTP, sigue recibiendo `PapeletaResource`/`ExpedienteResource` de verdad, y los lee como si fueran `DatosDePantalla`: no encuentra `tabla.filas` en el sobre paginado y la tabla sale **vacía, en silencio**, sin ningún error que lo diga. La segunda la caza la única prueba que compara celda por celda: con el número repetido en dos columnas, `findByText` dejó de encontrar un elemento único. **Y la marcha por el archivo destapó dos ajustes en pruebas ajenas al issue**: `packages/api-mock/src/proxy.test.ts`, `pantallas/estados.test.tsx` y `pantallas/busqueda-en-la-url.test.tsx` usaban `/transito/papeletas` como el ejemplo de «opción sin conectar» —para probar el camino común, no la conexión—; los tres se movieron a `/transito/codigos-transito`, que sigue sin `Controller` |
| Las tres tablas de valuación, nacionales (D-13/ADR-0017, #188: 19 pruebas nuevas, 13 contra PostgreSQL real) | Cuatro roturas: sacar las tres de `TABLAS_DE_CATALOGO`; quitar el `REVOKE INSERT/UPDATE` a `sgtm_app`; quitar el disparador de edición sellada; y leer el cuadro sin el `JOIN` con `conjunto_parametro_detalle` | 1, **0**, 1 y 2 en rojo. **La segunda es la que enseña lo suyo**: devolverle a `sgtm_app` el `INSERT` deja las 9 en VERDE, porque quien lo para no es el privilegio sino que la política de escritura de V55 nombra sólo a `rol_carga_parametros` —RLS y `GRANT` son dos guardas independientes y basta una, y las dos dan `42501`, así que el síntoma no distingue cuál actuó—; sólo quitando **además** el `FOR SELECT` de la política de lectura sale rojo. La cuarta deja que **todo** conjunto vea **toda** edición publicada, que es la reproducibilidad de ARQ-09 §3 perdida sin que ninguna cifra parezca mal. **Y cargar el anexo de verdad encontró dos defectos que ninguna revisión habría visto**: de las 54 129 filas del anexo vehicular 2026, **1 905 se perdían en silencio** —15 porque cinco modelos pasan de los 60 caracteres de la columna (el más largo tiene 67) y **1 890 porque el anexo publica «OTROS MODELOS» dentro de cada categoría**, 472 pares `(marca, modelo)` repetidos con valor distinto, y la unicidad se quedaba con el primero: un camión valorizado con la cifra de una camioneta—. Con `categoria` en la clave y `modelo varchar(80)` entran **54 111**, y los 18 rechazos restantes son seis líneas que el anexo repite idénticas |
| Las dos transferencias de Rentas · Registro, con el campo que ninguna pantalla del manual dibuja (#73, 8 pruebas nuevas) | Cuatro roturas: quitar la guarda de `predioId` en «Transferencia de predio»; quitar la comprobación de formato del valor de la transferencia; dejar editable «Transferente — documento» en «Transferencia de vehículo» (RNF-080); y mandar `predioId` como texto en vez de entero | 1, 1, 1 y 1 en rojo. **El hallazgo fue el campo mismo**: `TransferenciaPredioController` y `TransferenciaVehiculoController` exigen `valorTransferencia`, y ninguna de las dos pantallas del manual tiene un campo para él —el prototipo lo dibuja en «Impuesto de alcabala», que es justo la pantalla que el backend **no** lee para esto—. La salida no fue editar el catálogo generado ni inventar el importe con un cero: `ResolutorDeTransferencia.tsx` declara dos controles que **sustituyen** a un campo existente —«Código predial», «Transferente — documento»— y **añaden** el que falta, con su propia etiqueta, nunca la del campo al que sustituyen. La cuarta mutación es la que más dice: sin `entero: true`, el `predioId` viaja como cadena («1» en vez de 1), que `PeticionDeTransferenciaPredio.predioId` —`Long`— no aceptaría. **Y `alcabala` y `espectaculos` se quedan fuera, con el mismo tipo de hueco y sin la misma salida**: a diferencia de las transferencias, el campo más cercano que sus catálogos dibujan ya está ocupado por otro dato —`autovaluoAjustadoS` e `ingresoDeclarado` son `"ro"`, que `Campo.tsx` bloquea siempre sin importar lo que declare `escrituras.ts`, y el campo de texto libre que sí tienen (`valorDeTransferenciaS`, «Organizador») es el que el backend no lee o resuelve otra cosa—; no hay un campo «vacío de significado» al que añadirle uno nuevo sin volver a mentir sobre lo que muestra, así que se anota en `rentas/index.ts` y no se puentea (ADR-0010 §4) |
| Las cuatro opciones de Valores que quedaban por conectar (#75: `valores_individual`, `valores_masivo`, `prescripcion`, `pase_coactiva`; 6 pruebas nuevas en `escritura.test.tsx` y 8 en `valores.test.tsx`, más la recuenta de `actos-honestos.test.tsx`) | Seis roturas: en `TablaDelCuerpo.columnaUnica` (nuevo, para `contribuyentes: string[]`), devolver el arreglo de objetos en vez del de valores sueltos; en `PaseACoactiva`, quitar el `fijarCampo('tipoDeMovimiento', 'PCO')` con que la pantalla fija sola la única respuesta que el endpoint acepta; en `GeneracionMasivaDeValores`, quitar la guarda «hay que pasar por Revisar antes de Generar»; en `PrescripcionDeLaDeuda`, quitar la guarda que bloquea un acto de interrupción elegido sin su fecha; y en `GeneracionIndividualDeValores`, quitar la guarda que exige tributo y periodo antes de emitir | 2 de 3, 1, 1, 1 y 1 en rojo. **La primera enseña lo suyo**: `IniciarCorridaMasiva` declara `List<String>`, no una lista de objetos con una sola clave, y sin `columnaUnica` la única salida habría sido `cuerpo`, la salida de emergencia que pierde la lista blanca por columna. **La segunda es el motivo por el que `pase_coactiva` vive en su propio componente y no en `escrituras.ts` a secas**: sin la siembra, el cuerpo sale `{observacion}`, sin `tipoDeMovimiento` —el 422 que el backend contestaría—. **La cuarta es la que más importa**: sin la guarda, la prescripción se declara igual, pero `hechos` se sincroniza vacío —la interrupción que quien atiende eligió desaparece en silencio, y el cómputo del plazo sale como si nunca hubiera pasado—. Las cuatro pantallas quedaron en su propio componente (`COMPONENTES_PROPIOS` de `Pantalla.tsx`) porque sus cuerpos llevan arreglos que `escrituras.ts` no declara sueltos —`obligaciones` de una sola fila sincronizada a mano, `hechos` igual, y `contribuyentes` por `columnaUnica`— o porque el catálogo no pone la acción que escribe al final (`pase_coactiva`: la última acción es «Imprimir», y el renderizador genérico trata siempre la última como la primaria). El presupuesto de arranque, que llevaba meses sin margen, quedó justo en el límite: 150,0 de 150 KB comprimidos, tras deduplicar el campo declarado de las cuatro pantallas en un solo componente y recortar la prosa de la pantalla de revisión |
| La caja, con teclado y sin ratón (#74, 21 pruebas —incluidas 7 del camino de escritura de `anulacion_recibo`— y 1 camino nuevo de Playwright) | Backend servido para las diez opciones y solo cinco conectadas de verdad: `consulta_convenios`, `duplicado_recibo`, `avance_recaudacion` y `recaudacion_area` leen su recurso; `caja_tributaria` conecta su grilla de deuda (`consulta_deuda`) sin poder cobrar todavía; y `anulacion_recibo` escribe. `caja_tributaria`/`caja_tasas` no dibujan ningún campo para el medio de pago que `CajaController` exige —«Forma de pago» del prototipo es ahí el tipo de cobranza, no el medio—, y `fraccionamiento` no dibuja ninguna grilla para las obligaciones que `PeticionDeFraccionamiento` exige: las tres se mudan de `sin-declaracion` a `sin-campo` (`ACTOS_SIN_CAMPO`, #332). `cierre_caja` (un mapa por forma de pago) y `anulacion_convenio` (un discriminador que decide qué botón se pulsó) se quedan en `sin-declaracion`: el mecanismo declarativo de `escrituras.ts` no sabe construir ninguna de las dos formas todavía | Tres mutaciones sobre la conexión y la escritura, y una sobre el censo: quitar `caja_tributaria` de `ACTOS_SIN_CAMPO` (pone rojo «el medio de pago» y la `data-causa`, que vuelve a `sin-declaracion`); quitar el `exigir` del motivo de `anulacion_recibo` (la primaria se habilita sin motivo); resolver el número del recibo desde el filtro de la URL en vez de la ruta (pide `/tesoreria/recibos/999/duplicado` donde debía pedir `.../001-0000123/...`); y sustituir `leerObjeto` por un paso sin guarda en `avance_recaudacion` (un arreglo suelto —la forma que `leerPaginado` esperaría— deja de fallar en voz alta y dibuja una tabla vacía en silencio). Las cuatro en rojo, y las cuatro restauradas por reemplazo de texto. El e2e nuevo recorre identificar el recibo por la URL, elegir motivo y autorizante con el teclado, y confirmar la anulación —irreversible, regla 4— sin un solo clic; el de la caja actualiza su aserción al texto que nombra el dato exacto que falta. Batería completa: `yarn verificar` 68/68 archivos y 1067/1067 pruebas, `yarn comprobar-compilaciones` y `yarn e2e` 15/15, sin reintentos —el primer intento de `e2e` cayó dos pruebas contra una vista previa **de otro worktree** que había tomado el puerto 4173 en esta máquina compartida; medido de nuevo contra un servidor propio, en verde— |
| Coactiva, conectado hasta las cuatro lecturas que tienen `Controller` (#76, 6 pruebas nuevas de conexión sobre las 26 ya existentes de `coactiva.test.tsx`) | Dos roturas: en `coactiva_deudas_beneficio`, poner una cifra donde `DeudaCoactivaResource` no desglosa insoluto/interés (debía ser `SIN_DATO`); y declarar `actos_coactivos` en `escrituras.ts` con un campo real (`glosaDelActo → glosa`) | Rojo la primera, nombrando la celda («expected '0.00' to be '—'»). La segunda puso rojo el candado «ninguna de las ocho está declarada» **y**, contra la app montada, demostró el hallazgo central del issue: con la escritura declarada, el botón primario (`.sgtm-boton--primario`, «la última acción es la primaria», FRO-03 §5) pasa a ser «Padrón» —un reporte, no un guardado— porque en seis de las ocho pantallas de escritura del prototipo la última acción no es «Guardar». Las ocho se quedan sin conectar por eso (seis) y porque a las otras dos les falta un campo que el backend exige y el prototipo no dibuja (`cambiar_direccion_ref`/`motivo`, `fraccionamiento_coactivo`/`nroExpedCoact`), documentado opción por opción en `pantallas/coactiva/index.ts`. Y `Pantalla.test.tsx` tenía una prueba escrita contra el `proceso_coactivo` **sin conectar** —esperaba «701.08T1», un campo que `ExpedienteResource` no publica—: se actualizó a un valor que sí viene de la respuesta conectada |
| La pasada de arreglos consolidada del PR #379, sobre el árbol correcto de #74/#75 (9 pruebas nuevas: 5 de `packages/api-mock/src/tesoreria.test.ts`, 2 de `papeletas.test.ts`, 1 de `ACTOS_SIN_CAMPO.campos` y 1 de `ResolutorDeTransferencia`) | Cuatro roturas reales, y tres hallazgos de la triple validación que no se reprodujeron contra el código real: `packages/api-mock/src/recursos.ts` no tenía ninguna ruta `/tesoreria/*` —las cuatro lecturas conectadas por #74 leían por HTTP y no encontraban su forma, exactamente el defecto que #363 ya había documentado para tránsito y coactiva—; `caja_tributaria` no dibujaba ningún bloque de búsqueda —su catálogo no declara `filtros`, y `Filtros` está condicionado solo a `estructura.filtros`—; `ResolutorDeTransferencia.tsx` comparaba `consulta.error !== undefined` en vez de `!== null` —`useQuery` nunca da `undefined`, así que el banner «No se pudo buscar el predio» se dibujaba siempre, incluso con la lista encontrada, y la región viva nunca anunciaba cuántos predios había—; y `papeletasTransito()`/`adminEstadoCuenta()` mandaban `licenciaConducir: null`, un campo que `PapeletaResource` no tiene. Verificado y descartado: `consulta_convenios` no «revienta» sino que queda vacía en silencio (matiz, no error hablado); `alcabala`/`espectaculos` **no** se movieron a `ACTOS_SIN_CAMPO` —su primaria del catálogo es «Imprimir liquidación», que `DE_SALIDA` reconoce antes de llegar a esa lista, tal como el propio javadoc de `ACTOS_SIN_CAMPO` ya documentaba: añadirlas habría roto el censo fijo de `actos-honestos.test.tsx` (13/49/41/27/1/3)—; y la sección «Valores» de la ficha 360° mostró «0 valores» con cuerpo vacío contra `/atencion/00000003541`, consistente y no «8 con cuerpo vacío» | Sin ruta en el mapa, `paginadoDe('GET', '/api/v1/tesoreria/convenios')` vuelve `null` y las cuatro pruebas de forma caen; sin `filtrosPropios`, `caja_tributaria` pierde su `textbox` «Cód. Contribuyente»; con `!== undefined` restaurado, el banner de error reaparece sobre la lista de predios encontrados y `screen.queryByText('No se pudo buscar el predio')` dejó de estar vacío; con `licenciaConducir: null` de vuelta, el guardia de campos de `PapeletaResource` lo detecta nombrando la pantalla. Las cuatro en rojo, las cuatro restauradas por reemplazo de texto. `pantallas/tesoreria/composicion.ts` es la declaración equivalente a `widgetsDeFiltro` que le faltaba a `composicion.ts`: un filtro que la pantalla compone cuando el catálogo no trae ninguno, para una opción que ya lee de otra operación (mismo patrón que `caja_tributaria` leyendo `consulta_deuda`). Los cuatro componentes de `pantallas/valores/` (`GeneracionIndividualDeValores`, `GeneracionMasivaDeValores`, `PrescripcionDeLaDeuda`, `PaseACoactiva`) se pasaron a `lazy()`, mismo patrón que las tres de catastro: el arranque bajó de 150,2 a 148,0 KB comprimidos. Batería completa: `yarn verificar` 75/75 archivos y 1129/1129 pruebas, `yarn comprobar-compilaciones` (148,0/156) y `yarn e2e` 15/15, sin reintentos. **Y el propio arranque de esta pasada enseñó lo suyo**: el worktree había nacido de `main` en vez de la cabeza real de la rama (`b0c8170`), así que media hora de trabajo se hizo contra un árbol sin `tesoreria/index.ts`, sin los cuatro componentes de `valores/` y sin `ResolutorDeTransferencia.tsx` —el mismo síntoma que #363 ya advertía: trabajar contra la forma equivocada no falla ruidosamente, deja piezas fuera en silencio— |
| Tránsito, conectado hasta donde llega el backend: veintiuna de veintitrés (#77, sobre `papeletas` de #363; 35 pruebas en `transito.test.tsx` —incluidas las seis hojas ya existentes—, más el recuento fijo de `actos-honestos.test.tsx` y el ejemplo de «opción sin conectar» movido en `proxy.test.ts`, `estados.test.tsx`, `busqueda-en-la-url.test.tsx` y `conexiones.test.tsx`) | Trece lecturas conectadas —cinco `Controller` de `sgtm-sanciones` sirviendo `PapeletaResource`, `PapeletaDelPadronResource`, `CodigoInfraccionResource`, `InternamientoResource`, `ExpedienteResource` y `ResumenDePapeletasResource`—, dos escrituras propias (`transito_cambio_numero`, `transito_valores`, en `COMPONENTES_PROPIOS` por el mismo motivo que `pase_coactiva`: la última acción del catálogo no es la que escribe) y cuatro en `ACTOS_SIN_CAMPO` (`transito_descargos` —el número de expediente es `"ro"`—, `transito_constancia_libre`, `transito_rg_ordinaria`, `transito_rg_sancionadora` —las tres, hoja de reporte sin una sola sección—). Tres roturas: cambiar el campo del cuerpo que declara `transito_cambio_numero` (`numeroNuevo` → `numeroErrado`); quitar `transito_descargos` de `ACTOS_SIN_CAMPO`; y cruzar `placa`/`papeleta` en el mock de `internamiento` | Rojo la primera, nombrando el cuerpo mandado entero. Rojo la segunda **en dos archivos a la vez**: `transito.test.tsx` pierde su `sin-campo`, y el recuento fijo de `actos-honestos.test.tsx` se mueve —`sin-campo` 7→6, `sin-declaracion` 22→23—, que es la prueba de que el censo cuenta de verdad y no repite la fórmula. Rojo la tercera, nombrando la celda cambiada. **Dos resúmenes se quedan sin conectar, y no por descuido**: `AgrupacionDelResumen` del backend solo agrupa por `ESTADO`/`CODIGO`/`PLACA`/`MES` —no existe una agrupación por año—, así que `transito_resumen_papeletas` (columna «Año») no tiene con qué llenarse honestamente, y `transito_resumen_recaudacion` tiene además el problema de RNF-083: su columna «Total S/» pediría sumar varias líneas por mes, y el recurso solo publica el total general. **El contrato no alcanzaba para los dos records** (`transito_record_conductor`/`_vehicular`): ninguna pantalla del prototipo dibuja un filtro para ellos —ni siquiera en `report.meta`, que llega con el sujeto ya resuelto—, y el generador solo conocía `formato`; se amplió `docs/50-api/generar-openapi.mjs` (`DEL_BACKEND`) con `licencia`/`documento`/`placa`, comprobado con `--comprobar` y con `comprobar-operaciones` del frontend. Batería completa: `yarn verificar` 73/73 archivos y 1135/1135 pruebas (un `rentas/transferencias.test.tsx` y un `rentas/fichas-de-rentas.test.tsx` cayeron una vez cada uno por `waitFor` en una corrida paralela muy cargada de esta máquina compartida —ajenos a este issue—, verdes solos y verdes en la corrida limpia final), `yarn comprobar-compilaciones` con el arranque en 154,6 de 156 KB comprimidos (subió 1,9 KB desde 152,7; el portal no se tocó, sigue en 80,9 de 84) y `yarn e2e` 15/15 |
| Infracciones administrativas, las seis lecturas que caben en el camino declarativo (#78, sobre #363: 18 pruebas en `infracciones.test.tsx`, 8 nuevas) | `codigos_cuis`, `adm_codigos_reporte`, `adm_padron_notificaciones`, `adm_notificaciones_vencidas`, `adm_notificaciones_contribuyente` y `adm_resumen_recaudacion` (un objeto suelto, no un listado: `RecaudacionDeMultasResource`) se suman a `adm_estado_cuenta` (#363). Las otras seis se quedan fuera, documentado opción por opción en `pantallas/sanciones/index.ts`: `infracciones_adm` **sí tiene** `Controller` —a diferencia de lo que decía el comentario heredado de #363— pero su filtro «Estado» (PREVENTIVA/CONSTATADA/SANCIONADA…, el vocabulario del procedimiento) no tiene parámetro en el backend, y la columna que sí se podría dibujar es el `enum EstadoDePapeleta` (IMPUESTA/NOTIFICADA/RESUELTA…, el vocabulario de la deuda) — conectarla dejaría un filtro que no filtra nada junto a una columna en otro idioma. `adm_notificacion` y `adm_valores` escriben de verdad en el contrato, pero la última acción de las dos es «Imprimir», no «Guardar» (FRO-03 §5): esa clasificación **ya estaba en el archivo de pruebas heredado del merge de onda 3**, con su primaria apagada y sin franja — la instrucción del issue pedía conectarlas, y la batería exigía lo contrario; se siguió la batería. `adm_reportes` ofrece diez tipos de reporte y el backend solo implementa tres, ninguno con el literal del desplegable. `adm_resolucion_gerencia`/`adm_notificacion_resolucion` no tienen ni una sección ni un campo en el catálogo —hoja en blanco, no un campo que falte junto a otro (#73)— | Cuatro mutaciones, las cuatro en rojo y restauradas por reemplazo de texto: quitar `exige: [codContribuyente]` de `adm_notificaciones_contribuyente` (la pantalla pide sin contribuyente en vez de mostrar el aviso «Busca un contribuyente»); cambiar `leerObjeto` por `leerPaginado` en `adm_resumen_recaudacion` (el objeto suelto deja de leerse: «no trae un listado paginado»); dejar `familia` en la columna «Materia» de `codigos_cuis` en vez de `SIN_DATO` («ADMINISTRATIVA» donde debía decir «—»); y en el mock, indexar el estado de una papeleta por el código de infracción en vez de por la palabra de cobranza del prototipo (**esta la encontró la propia redacción del mock, antes de que ninguna prueba la mirara**: `ESTADO_ADMINISTRATIVA_DEL_MOCK[infraccion]` nunca podía acertar —las claves son «Preventiva»/«Constatada»…, y `infraccion` es un código como «A-014»—, así que toda fila caía siempre en el mismo valor por omisión sin que nada lo señalara; se sustituyó por un diccionario nuevo, `ESTADO_DE_DEUDA_ADMINISTRATIVA_DEL_MOCK`, indexado por la columna que sí trae la palabra). `RecaudacionDeMultasResource.lineas` es (tributo, ejercicio, mes, fase, abonos, recaudado): el prototipo dibuja un mes con las tres fases ya repartidas en columnas, y el adaptador no recompone esa cifra —publica tres líneas por mes, una por fase, con «Total S/» igual al `recaudado` de esa línea y no a una suma (RNF-083)—. Batería completa: `yarn verificar` 73/73 archivos y 1125/1125 pruebas —el primer intento contra el suite entero cayó dos ajenas a este cambio por contención de CPU (`alta-guiada-de-ficha.test.tsx`, `resolutor-de-unidad.test.tsx`, las dos con `Test timed out`); aisladas, las dos en verde, y una corrida limpia del suite completo, sin nada en segundo plano, confirmó las 1125 en verde—, `yarn comprobar-operaciones` (174 operaciones, sin tocar el generador), `yarn comprobar-compilaciones` (arranque 153,2 de 156 KB) y `yarn e2e` 15/15 —el primer intento chocó de verdad con **otro agente** de esta misma máquina compartida que ya tenía el 4173 tomado; medido de nuevo con los dos servidores en 4193/4194, sin tocar el suyo— |
| Autorizaciones y licencias, conectado hasta las siete lecturas que tienen `Controller` (#79, 10 pruebas nuevas en `pantallas/licencias/licencias-conectado.test.tsx`) | Dos roturas: en `anuncios`, poner el mismo documento en «D.N.I.» y en «R.U.C.» donde `AnuncioResource` solo publica uno (debía ser `SIN_DATO`); y declarar `certificados` contra la operación `certificados` (el `POST` que emite) en vez de `certificados_listado` (el `GET` que lista) | Rojo la primera, nombrando la celda. Rojo la segunda **sin ningún mensaje que lo diga**: la tabla no llega a aparecer, porque `paginadoDe` del proxy solo responde a `GET` y `certificados` es `POST` —el mismo mecanismo que ya usan `emitir_licencia` frente a `licencia_funcionamiento` y los cuatro actos de `anuncio` frente a `anuncios`, aquí puesto a prueba a propósito—. **Las cuatro escrituras del issue se quedan sin conectar, y por dos motivos que no son los ya vistos en #73/#76**: `licencia_resolucion_cancelacion` y `licencia_resolucion_duplicado` son la vista previa de una resolución —un bloque `reporte` sin `secciones` ni `acciones` en el catálogo—, y sin `acciones` `Pantalla.tsx` ni siquiera dibuja `<BarraDeAcciones>` (`{estructura.acciones && <BarraDeAcciones .../>}`), así que no hay observación, ni motivo, ni franja que declarar; conectarlas exigiría inventar una pantalla que el prototipo nunca capturó. `anuncios_reportes`, `licencia_padron` y `certificados` sí tienen conducto de escritura, pero su primaria está mal etiquetada: las dos primeras terminan en «Cancelar» —un cuarteto Exportar/Imprimir/Pantalla/Cancelar que el prototipo repite en varios módulos, y aquí «Cancelar» cierra el diálogo, no guarda—, y `certificados` termina en «Imprimir certificado», que `DE_SALIDA` de `pantallas/actos.ts` reconoce como salida **antes** de llegar a `ACTOS_SIN_CAMPO`: declarar ahí el campo que falta (`nDeRecibo`) no habría servido de nada, y se comprobó con una prueba que monta la pantalla y ve el botón `disabled` sin `aria-disabled` ni franja. Las tres piden lo mismo que #75 hizo con `valores_masivo`: un componente propio que sustituya la etiqueta de la primaria por una que diga lo que hace. Documentado opción por opción en `pantallas/licencias/index.ts`. Batería completa: `yarn verificar` 74/74 archivos y 1127/1127 pruebas, `yarn comprobar-compilaciones` (arranque 153,4 de 156 KB) y `yarn e2e` 15/15 contra un servidor propio en los puertos 4193/4194 de esta máquina compartida, con el binario de Chromium ya instalado |
| Fiscalización, la frontera más delicada: cuatro lecturas de ocho, y las otras cuatro con su motivo (#80, 7 pruebas nuevas en `fiscalizacion-conectado.test.tsx` sobre las 7 ya existentes de `fiscalizacion.test.tsx`) | `fisc_omisos`, `fisc_estado_cuenta`, `fisc_historico` y `resolucion_determinacion_fisc` leen su recurso real, con el mismo patrón de siempre y ninguna cifra compuesta —las de determinación son D-02a (#198) y salen `SIN_DATO`—. `fisc_resultados` se queda sin conectar: `LiquidacionResource` nunca publica `insolutoOmitido` ni `multaTributaria`, y su única columna parecida a un predio o una diferencia vive por línea, no en la cabecera de la liquidación; conectarla hoy dejaría la columna de dinero entera en blanco, un retroceso frente al camino común (#78). Las tres escrituras —`fisc_programa`, `fisc_predial`, `fisc_vehicular`— pasan a `ACTOS_SIN_CAMPO`: a las tres les falta un identificador interno para el que ninguna sección del catálogo dibuja un campo editable, documentado opción por opción en `pantallas/fiscalizacion/index.ts`. Conectar las cuatro lecturas obligó a corregir dos pruebas ajenas que darían por sentado que fiscalización seguía sin backend: `proxy.test.ts` probaba el camino común de una ruta con parámetro usando `/fiscalizacion/resoluciones/{numero}` como su único ejemplo sin conectar —era el último que quedaba de los ocho que el contrato declara—, y se reescribió para comprobar en su lugar que el mismo patrón sigue sin filtrar; y `actos-honestos.test.tsx` traía el recuento fijo de las 134 por causa, que cambia en tres al mudarse de `sin-declaracion` a `sin-campo` | Dos roturas: en `fisc_omisos`, poner una cifra donde `OmisoResource` nunca la publica (debía ser `SIN_DATO`); y quitar `fisc_programa` de `ACTOS_SIN_CAMPO`. Rojo la primera, nombrando la celda («expected […, '478.40'] to deeply equal […, '—']»); rojo la segunda, la causa vuelve a `sin-declaracion` cuando debía decir `sin-campo` —la franja diría «la pantalla aún no manda estos campos» en vez de nombrar el identificador que le falta—. Las dos restauradas por reemplazo de texto, y las 14 pruebas del módulo en verde otra vez |
| La franja que la primaria de impresión silenciaba (#385: `alcabala` y `espectaculos` entran en `ACTOS_SIN_CAMPO`, 2 casos de pantalla + 1 testigo sobre las 36 de `actos-honestos`) | Dos roturas: devolver el orden anterior —`DE_SALIDA` decidiendo antes de consultar `ACTOS_SIN_CAMPO`—, y quitar la entrada de `espectaculos` | 5 y 2 en rojo —el censo, los testigos de `actos-honestos` y las dos pantallas de `rentas.test.tsx`, que muerden por su lado—. **La primera es el defecto que este issue existía para cerrar, y las dos entradas nacieron muertas por él**: con el orden viejo, declarar `alcabala` en `ACTOS_SIN_CAMPO` no cambiaba nada —su primaria del catálogo es «Imprimir liquidación», el filtro de salida devolvía `undefined` antes de mirar la lista, y el motivo real (el backend exige `transferenciaId` y el autovalúo ajustado que la pantalla dibuja de solo lectura, D-11) se quedaba en un botón `disabled` cuyo `title` nadie puede leer (RNF-082)—; el censo lo delató al no moverse: 48 de `salida` donde el diff decía 46. La regla queda al derecho: una entrada de `ACTOS_SIN_CAMPO` gana también a la primaria de salida, porque es deliberada y escasa; el filtro de #337 sigue callando en las ~50 consultas que no tienen nada que advertir |
| La municipalidad de demostracion, sembrada de verdad (16 contribuyentes, 23 predios con sus 45 versiones de ficha, 8 vehiculos, 7 transferencias y 54 obligaciones; 25 pruebas nuevas sin base) | Once mutaciones. Sobre los tres importadores de rentas: exigir `codTransferente` en una fila `VEHICULO`; resolver el predio de un cargo a fin de ejercicio en vez de a su fecha valor; quitar la comprobacion de rango del periodo; abortar el archivo entero en la primera fila mala; resolver el predio sin comparar el codigo; y transferir siempre el total. Sobre el detalle de la ficha: meter todas las filas en un solo grupo; tomar la cabecera de la primera fila sin comprobar las demas; admitir en silencio una seccion que no le toca al tipo; y leer el guion de las categorias como una letra mas. Y sobre los propios archivos: cinco roturas de dato, mas un anio de construccion anterior a 1990 | 1 de 13 en rojo cada una de las seis primeras, y cada una la suya; **7 de 14** la de la agrupacion —perder la unidad «un predio, una version» rompe casi todo—, 1 las otras tres, y 1 de 5 cada rotura de archivo. **La primera enseño lo suyo antes de bitar**: la version inicial pasaba el transferente leido del padron por el registro de la fila y la mutacion salio en VERDE, porque `RegistrarTransferencia.transferirVehiculo` **no admite** ese parametro —lo toma del titular vigente—; el campo era codigo muerto que invitaba justo al error que la firma existe para impedir, y se retiro. **La segunda es #24 otra vez**: con el predio resuelto a diciembre, la cuota de arbitrios de enero se le asienta a quien compro el predio en junio. Y ejecutando la siembra entera contra PostgreSQL salieron cuatro cosas que ningun repaso habria visto: `cuenta_corriente_asiento` solo tiene declaradas las particiones de **2026 y 2027**, asi que hoy no se puede sembrar deuda vencida de ejercicios anteriores —la que alimentaria coactiva—; **nada crea un `area` ni una `caja`** fuera de las fixtures de prueba, asi que en una instalacion recien implantada la ventanilla no se puede abrir; la copropiedad **no se puede declarar** en `fichas.csv` —una segunda fila del mismo predio se rechaza, y con razon—, se produce con una transferencia parcial; y **una casa de 1979 no se puede fichar**, porque `Construccion.anioConstruccion` es un `Ejercicio` y `Ejercicio` va de 1990 a 2100 —es el tipo del ejercicio TRIBUTARIO reutilizado como anio de construccion, y el adobe de los setenta es lo mas corriente del distrito— |
| La capa web de la determinación predial (#395, 62 pruebas nuevas —5 contra PostgreSQL real— y 14 de interfaz) | Once roturas, y las que más dicen son cuatro: no ponderar el aporte de cada predio por su `%` de propiedad; que `simulacion = true` asiente igual; que un tramo cualquiera pueda ir sin tope; y renombrar `TRAMO_PREDIAL_LIMITE` en el derivado publicable | 2, 2, 2 y 1 en rojo. **La cuarta es la lección de #192 repetida**: con la llave renombrada, `verificar-publicacion.mjs` sigue en VERDE —«22 filas publicables, todas respaldadas por un archivo VERIFICADO»— porque la cifra está en la norma y las firmas son las del corpus; el parámetro queda cargado bajo un nombre que nadie pide, con el mismo síntoma exacto que «no está cargado», y lo caza solo la prueba que lee **este** derivado desde el módulo que lo consume. **Y la primera es el hallazgo de NEG-05 §1 medido**: sin ponderar, dos predios de 100 000 al 50 % dan una base de 200 000 donde debe decir 100 000; calculados predio por predio, 540,00 donde debe decir 870,00 —330 soles menos por contribuyente en todo el padrón, y ninguna cifra del recibo lo diría—. Las siete restantes muerden una a una: suponer que una petición sin marca asienta, componer la observación del sistema también al asentar (regla 10), tomar la condición del primer titular del predio en vez de la del contribuyente que pregunta, dejar fuera del cronograma el céntimo que no cabe, resolver «la última determinación» con `ORDER BY id ASC`, quitar de V56 el `CHECK` que impide un exonerado mayor que el autovalúo, y publicar el mínimo imponible como porcentaje en vez de en soles. **Lo que el diseño tuvo que decidir, y por qué así**: el autovalúo de un predio **no se deriva** —faltan el cuadro de valores unitarios y la depreciación (GOB-03 H-14/H-15), los aranceles (D-02b) y el `% actualización` (D-11)—, así que se declara, y arrastrar el del ejercicio anterior sería aplicar en silencio un `% actualización` de cero; el `%` de propiedad, en cambio, **no tiene campo en la petición** y sale de `titularidad`, que es lo único que impide inflar la base desde fuera; simular y asentar se distinguen por una marca **obligatoria** del cuerpo —no hay omisión segura en ninguna de las dos direcciones—; y toda cifra del cuadro sale del conjunto sellado con 422 nombrando la llave, de modo que hoy `DERECHO_EMISION_PREDIAL` y `PREDIAL_VENCIMIENTO:‹n›` —las dos de ordenanza local, D-02b— son las que faltan y la respuesta lo dice |
| Catastro, de doce pantallas a cuatro superficies (#391, cinco PRs: #404, #405, #406, #408 y #409) | Cerca de cuarenta roturas, y las cuatro que más dicen: plegar «Predio» en el menú; devolver el orden del catálogo a la barra de acciones; quitar el `al {fecha}` del bloque de cabecera compartido; y cambiar el `centro` de Tránsito por el `plegado` nuevo | **La primera nombra tres opciones que quedarían sin retorno** —el reporte del contribuyente, la consulta de fichas y **`ficha_rural`**, que no deriva del código catastral—, y es la evidencia de que ese grupo no se puede plegar: lo que lo desbloquea no es interfaz, es que el recurso publique el código del titular. 20 en rojo la segunda, en cuatro archivos. La tercera pone rojo en **dos** archivos desde una sola línea, que es la prueba de que el punto de aplicación de la regla 9 quedó único. 11 la cuarta: las trece hojas de Tránsito dejan de listarse. **Y tres mutaciones no mordieron a la primera, que es donde estaba lo que faltaba**: «Abrir la ficha» se habilitaba con el ubigeo tecleado —6 dígitos de 23—, así que un prefijo abría la ficha de ningún predio; clasificar «Modificar»/«Deshacer» como modo **no se puede observar** con opciones que son todas `GET`, y hubo que probarlo con una lista inventada porque lo que importa es que un modo detrás del verbo que guarda le robaría la primaria; y mutar `habilitada` en `BarraDeAcciones` para probar la observación da 0 rojas, porque `apagadaConMotivo` gana antes —la guarda real vive en `useEscritura`—. **Dos defectos los encontró terminar a mano lo que un agente dejó a medias**: el conmutador ofrecía la modalidad rural desde un código de referencia catastral —de él salen `codRefCatastral` y `codEdificacion`, pero no `codUnidad`, así que era un enlace a un 404—, y se había perdido `useFocoEnLaAccion` al mudar el alta guiada, de modo que Esc no devolvía el foco (FRO-04, RNF-082): eso lo cazó la prueba que ya existía. El patrón, con las mutaciones que hay que medir, quedó escrito en [`FRO-05`](docs/60-frontend/superficies-unificadas.md) |
| El filtro «Estado» de la infracción administrativa, y los dos vocabularios que se cruzaban en él (#397, ADR-0010: 20 pruebas nuevas de backend —13 contra PostgreSQL real— y 3 de interfaz) | Diez roturas, y las que más dicen son cuatro: quitar de la expresión la rama que devuelve `NULL` para un acta anulada; resolver el vencimiento de la notificación preventiva con `current_date` en vez de con `:aLaFecha`; escribir en el `WHERE` una **segunda copia** del `CASE` con la rama de la RIS delante de la del pago; y dibujar `estadoDeLaDeuda` en la columna «Estado» de la grilla | 2, 2, 1 y 2 en rojo. **La tercera pasó en VERDE a la primera, y es el hallazgo**: la prueba que compara el filtro contra la columna dependía del orden de ejecución —la única acta pagada *y* sancionada la sembraba otra prueba—, así que con las dos copias divergiendo no tenía nada que contrastar; sembrando esa acta en la propia prueba salen `["AC-F-PAGA", "AC-F-SANC"]` donde debe salir `["AC-F-SANC"]`, o sea el filtro «Sancionada» devolviendo un procedimiento que la columna dice pagado. La primera deja el acta anulada diciendo «CONSTATADA» —«expected: null but was: CONSTATADA»—, que es exactamente la palabra plausible y equivocada por la que la expresión devuelve `NULL`. **Y la decisión de diseño fue no crear columna**: la fase se DERIVA de tres hechos ya escritos —`papeleta.estado`, si existe una `resolucion_gerencia` de tipo `ADMINISTRATIVA`, y si la notificación previa sigue abierta a la fecha de corte—, el mismo reparto que V41 §2 y §5 hicieron con el descargo y el internamiento, V33 con el expediente y V32 con el turno; guardarla habría dejado **dos verdades** sobre la misma fila, y la que se lee en pantalla sería la que nadie recalculó. No hay migración: **V57 sigue libre**. Las seis restantes muerden una a una: `LEFT JOIN` → `JOIN` sobre la notificación previa (**8 en rojo**: el manual permite el acta sin ella, así que media pantalla desaparece de sí misma), publicar la fase bajo el nombre `estado` (2, y el recurso deja de tener dos nombres para las dos cosas), conectar el pool como superusuario (1: «expected 0L but was 1L», el acta de A vista desde B con cuatro tablas cruzadas), quitar de la interfaz la guarda de «Todos» (1: `estado=Todos` viaja, y es el 422 que el backend contestaría), desconectar la opción (4, y la tabla sale con **ocho celdas vacías en silencio** —el defecto de #363 otra vez: el camino común recibe el sobre paginado y no encuentra `tabla.filas`—) y publicar el porcentaje de UIT como fracción (4 en rojo, **en dos módulos**). **Esta última destapó un defecto vivo desde #78**: el proxy publicaba `0.50` donde `Alicuota` significa 50 % —su javadoc lo dice, «en tanto por ciento», y su rango es 0..100—, de modo que la misma columna leída como alicuota decía medio por ciento; se corrigió con `porcentajeDeUit` y las cuatro celdas que lo dibujaban en Infracciones y en Tránsito |
| Las dos operaciones de Tránsito que el contrato declaraba y ningún controlador servía, y los dos resúmenes que no se podían conectar sin mentir (#396, #398: 22 pruebas nuevas de backend —19 contra PostgreSQL real y 3 en el borde web— y 7 de interfaz) | Diecisiete roturas, y las que más dicen son cuatro: quitarle a `AgrupacionDelResumen.ANO` su expresión de año; quitar del `GROUP BY` de `MES` la columna del año; devolver el `agrupadoPor` por omisión a `ESTADO` —en el `GET` y en el emisor, por separado—; y **sumar las tres columnas de fase en la interfaz** para componer «Total S/» | 3, 3, 1+1 y **0** en rojo. **La cuarta es el hallazgo, y pasó en VERDE por partida doble**: la regla de ESLint de RNF-083 mira **nombres de campo de dinero** —`total`, `importe`, `monto`— y la suma se escribía sobre el `texto` de una `Celda`, así que el lint no la vio; y en el juego de datos del prototipo la columna «Total S/» **coincide** con la suma de las otras tres, así que la prueba de pantalla tampoco. Lo que la caza es una prueba nueva con la forma que el backend sí produce y el prototipo no puede: un mes con fase `VALOR` —lo cobrado de una papeleta con su resolución de multa ya emitida—, donde el total del libro son 1 500,00 y las tres columnas del manual suman 900,00; con ella, 2 en rojo. **La segunda enseña lo suyo por ruidosa**: sin el año en el `GROUP BY`, PostgreSQL rechaza la consulta entera —«must appear in the GROUP BY clause or be used in an aggregate function»—, que es exactamente lo que se quiere de un agrupador mal declarado. Las trece restantes muerden una a una: `porMes` vacío (2 en rojo: «Total S/» se queda sin origen), quitar el rechazo de los criterios de más (2, uno en el borde web con los servicios en `null`), quitar el `catch` de `PapeletaInexistente` de la hoja (2: el 404 pasa a error opaco), fechar la hoja con el reloj en vez de con la fecha de la infracción (1), quitarle el `@RequiereAcceso` (rojo en `verificarArquitectura`, nombrando el endpoint), sacar las dos rutas de `IMPLEMENTADAS` (rojo nombrándolas las dos), quitar del generador el `formato` de la hoja (`--comprobar` rojo, línea 3141), no forzar `agrupadoPor=ANO` en la lectura (1), poner `abonos` en «Papeletas pagadas» (1), sacar `caja` de `filtrosBloqueados` (2, y una es `prosa.test.ts`: un motivo sin su filtro no lo ve nadie), sacar la hoja del registro de conexiones (2) y devolver al mock el `?? '1'` del mes. **Ese último lo encontró el propio mock antes que ninguna prueba**: el prototipo escribe «Agosto (al 13)» para el mes en curso, el diccionario no lo reconoce y el valor por omisión lo mandaba a **enero** —una segunda fila de enero con las cifras de agosto, indistinguible de la buena—; el defecto venía de #78 y estaba también en el resumen administrativo. **Y el diseño tuvo que decidir dos cosas**: dónde componer el total por mes —ni en `cuentacorriente`, que «no conoce a nadie» (ARQ-01 §4) y no tiene por qué llevar dentro el pivote de una pantalla de tránsito, ni en los dos controladores, que serían dos copias de la misma cuenta: en `RecaudacionDeMultasResource`, la proyección HTTP del módulo, sobre las líneas que el libro ya publicó—; y que **`transito_reportes` no se conecta**, y ya no por falta de `Controller`: es un `POST`, y las dos únicas puertas del frontend para uno son `useEscritura` —que exige observación (regla 10), y este emisor no modifica nada— y `useSimulacion` —cuya guarda es que el cuerpo declare `simulacion: true`, una marca que `PeticionDeReporteDeTransito` no puede declarar sin mentir—; una `Conexion` lo dispararía **al abrir la pantalla**, sin tipo de reporte elegido. En la interfaz no se pierde nada: el centro de reportes (ADR-0014 §5) lleva a cada hoja de un clic |
| El cálculo vehicular, donde el contrato y el controlador no decían lo mismo (#399, 28 pruebas nuevas: 14 de la capa web, 6 de la regla de transporte, 8 de interfaz, más la muestra de la regla 5) | Ocho roturas, y las que más dicen son cuatro: devolver el controlador a leer los tres filtros **solo del cuerpo**; quitar del contrato el parámetro `placa` de consulta; darle al mínimo imponible que falta un cero por omisión; y quitarle a la pantalla su `Adaptacion` | 2 y **10 de 14** en rojo la primera —la regla de transporte y la capa web, cada una por su lado—; 2 la segunda, 1 la tercera y 6 la cuarta. **Y la segunda pasó en VERDE la primera vez que se midió**: editar el YAML deja `:sgtm-aplicacion:test` en UP-TO-DATE —el contrato vive fuera del módulo y no era entrada declarada—, así que la mutación dio BUILD SUCCESSFUL **sin haber corrido**; es la lección de #192 punto 2, que resultaba aplicable también a `ContratoDeApiTest` y llevaba ahí desde que existe. Declarado el YAML como entrada, muerde y nombra la llave. **La cuarta enseña el defecto de #363 otra vez**: sin adaptación la pantalla lee el recurso como si fuera `DatosDePantalla` —el sobre trae `fechaCalculo`, así que no falla— y la tabla **desaparece en silencio**, sin un solo mensaje. Las cuatro restantes: la regla 5 sin `MINIMO` (1 en rojo, y solo la muestra: las demás prohibiciones siguen verdes, séptima vez que el hueco se abre por el mismo sitio tras `INTERES_DE_FRACCIONAMIENTO`, `COSTA_DE_LA_REC2`, `TASA_PANEL` y `VIGENCIA_DEL_CERTIFICADO`), `simulacion` con valor por omisión (1, y el síntoma engaña: sigue habiendo 422, pero por falta de observación —la petición sin marca se lee como «asienta»—), el censo de lo abierto mintiendo (1) y un «4» inventado en la columna «Cuotas» (1, nombrando la celda). **Lo que el diseño tuvo que decidir, y por qué así**: se corrigió el **controlador** y no el contrato —los tres son `filtros` del prototipo y el contrato está derivado de él (#312), un filtro en el cuerpo dejaría a esta pantalla siendo la única de las 134 cuya búsqueda no se puede compartir por la URL, y `PredialController` ya eligió esa forma en #395—; `simulacion` se queda en el cuerpo porque no identifica lo que se calcula sino si la operación **escribe**, y pasa a ser obligatorio; y `minimoImponible` **se va del cuerpo**, que es donde la regla 5 lo prohíbe: sale de `VEHICULAR_MINIMO` × `UIT` del conjunto sellado, con 422 nombrando la llave que falte. **Y medir el defecto en vez de suponerlo destapó que no era uno**: la misma separación está en **nueve** operaciones —ocho siguen abiertas, censadas en `EL_MISMO_DESAJUSTE_TODAVIA_ABIERTO`—, y una de ellas, `POST /rentas/deuda/bajas`, **sí está conectada** y funciona porque fue la interfaz la que se adaptó al controlador (#332). La respuesta pasó además de lista pelada a objeto (`CalculoVehicularResource`): una lista vacía —un contribuyente con vehículos activos y ninguno afecto, que es una respuesta legítima— no tenía dónde llevar su fecha, y un array contradecía el `type: object` que el contrato declara |
| La depreciacion cargable y las tres primeras cifras del corpus (#188, H-15: `V57`, 8 pruebas nuevas contra PostgreSQL real, 3 casos del corpus con su importe al centimo) | Nueve roturas, y las que mas dicen son cuatro: quitar `uso` de `depreciacion_uq`; quitar el `NULLS NOT DISTINCT`; leer `antiguedad_hasta` con `getInt`; y devolver `cuadro` a la primera columna del manifiesto | 3, 1, 1 y 2 en rojo. **La primera es H-15 medido en vez de razonado**: cargando el Anexo I entero entran **492 filas** y sin el uso en la clave sobreviven **127** —365 descartadas en silencio—, y de esas 127 combinaciones **120 llevan un porcentaje distinto segun el uso**, que es depreciar una oficina con el de una vivienda; el informe nombra la primera perdida («Ya estaba publicada: tabla 02, Concreto / Muy Bueno / hasta 5») y el tramo abierto cae de 40 filas a 11. La segunda da **2 filas donde debe haber 1**: PostgreSQL considera distintos dos nulos por omision, asi que «mas de 50 anios» se podria cargar dos veces con porcentajes distintos y ninguna consulta diria cual es el bueno. La tercera convierte ese tramo abierto en un «hasta 0» que no cubre nada. **Y la cuarta es un defecto que llevaba desde #380 y salio de leer la cadena entera**: el paso 5 del instructivo compone el mismo manifiesto con `ImportarParametrosDelConjunto`, que lee `tipo,clave,vigencia_desde` **por posicion**, y `cuadro` estaba delante —asi que leia «2026» como fecha, rechazaba las dos filas y sellaba el conjunto **sin la edicion dentro**: el nombre del cuadro congelado sin su contenido—. `cuadro` pasa al final, como `valor_maquina` en `parametros-2026.csv` y por lo mismo. **La mutacion que lo demuestra paso en VERDE la primera vez**: el manifiesto vive fuera del modulo y no era entrada declarada de `test`, la leccion de #192 punto 2 otra vez. Las cinco restantes muerden una a una: quitar `DEPRECIACION` de `FilaDelManifiesto.CUADROS` (7 en rojo, y el manifiesto rechazado tambien por `verificar-cuadros.mjs`), cambiar una celda del derivado (`--comprobar` rojo con la linea y las dos versiones, y el sha256 rojo por su lado), cambiar una celda de la transcripcion firmada (rojo en la direccion contraria: el derivado deja de reproducir el corpus), retocar el `esperado` de `RT-013-c01` (rojo nombrando el caso y su fuente) y declarar un parametro de menos en `parametros_requeridos` (rojo con `ParametroDelPredialAusente` nombrando `TRAMO_PREDIAL_LIMITE:2`, en vez de comparar contra un valor ficticio). **Y dos cosas mas las encontro el propio recorrido**: `verificar-cuadros.mjs` existia desde #380 y **no lo corria nadie** —una verificacion escrita que nunca se ejecuta no protege nada; entra al flujo de documentacion—; y el contrato ya declaraba el filtro **«Uso»** de esa pantalla desde #312, derivado del prototipo, esperando este dato. Las tres cifras del corpus —los dos casos del cuadro de tramos y el del minimo— se comparan contra `parametros-2026.csv`, no contra `VALOR_FICTICIO`, y `RT-013-c02` mide por fin AC1 de #30 con cifras reales: tres predios que juntos entran al segundo tramo y uno a uno no salen del primero |
| Las hojas sin superficie, decididas de una vez para los tres módulos (#427 parte B, FRO-06: 4 pruebas de la guarda nueva y 3 de pantalla) | Cuatro roturas: quitar el aviso de una hoja cubierta; sacarla de `ACTOS_SIN_CAMPO`; dejar en «pendientes» una que ya está cubierta; y declararle su escritura en `escrituras.ts` | 2, 2, 1 y 1 en rojo. **Lo que el cotejo destapó es que el problema estaba mal contado**: no son cuatro opciones sino **siete** —`licencia_resolucion_cancelacion`/`_duplicado`, `adm_resolucion_gerencia`, `adm_notificacion_resolucion`, `transito_rg_ordinaria`/`_sancionadora` y `transito_constancia_libre`—, y el docblock de `actos-inalcanzables.test.ts` decía «cuatro» desde que la lista existe. **Y las siete eran mudas**: `Pantalla.tsx` dibuja la barra solo `{estructura.acciones && …}`, así que la causa que `impedimentoDelActo` calcula para ellas entra en el censo y **no la lee nadie** —RNF-082 un escalón más arriba de donde lo cerró #385, donde al menos había un botón apagado—; cuatro de las siete la tenían además equivocada (`sin-declaracion`, «la pantalla aún no manda estos campos», dicho de una pantalla sin ni una sección). La decisión —la hoja se queda hoja, el acto va donde el manual ya dibujó su formulario, y mientras no haya dónde la hoja lo dice— se tomó **cotejando campo a campo contra el controlador**, y ese cotejo es lo que descarta la salida 1 de #427: `licencia_funcionamiento` conoce el `{id}` y hasta trae una acción «Duplicar», pero **no dibuja ningún motivo** y su «Nº de recibo» es el del derecho de la licencia, no el del duplicado; encima el permiso del acto es `@RequiereAcceso(acceso = "licencia_resolucion_cancelacion")`, así que dictarlo desde allí preguntaría por el permiso equivocado. La guarda computa las siete **del catálogo generado**, no de una lista escrita a mano, con la lista de pendientes que no admite entradas rancias |
| Las dos preguntas de `alcabala`, contestadas por escrito antes de tocar codigo (#432: 1 guarda nueva de 29 casos y 4 de pantalla) | Tres roturas: pintar el nombre del campo del backend en la franja; devolver el motivo de `alcabala` anterior a #432; y quitarle el punto final al motivo | 3, 1 y 1 en rojo. **La premisa del issue resulto ser falsa, y eso es la respuesta a su primera pregunta**: «rentas tiene `GET` de transferencias» no es cierto —el contrato declara los dos `POST` que las registran y `/fiscalizacion/transferencias`, que es otra cosa—, asi que `alcabala` **no se puede convertir en una pantalla con grilla**: la grilla exigiria la lectura que no hay. Lo que si existe es `TransferenciaResource`, que el `POST` ya devuelve con su `id` y su `afectaAlcabala` a una pantalla que la interfaz **ya llama** (`transferencia_predio` declara su escritura desde #73): la alcabala de una transferencia recien registrada no necesita lectura nueva, necesita ser la hoja del mismo acto. La diferida si la necesita, y es backend: `TransferenciaRepository` tiene `findById` e `historicoDePredio` y ningun controlador los publica. **La segunda pregunta la contesta el catalogo solo**: dibuja tres campos seguidos y los tres `"ro"` —«Autovalúo del predio (S/)», «IPM aplicado», «Autovalúo ajustado (S/)»—, o sea la cuenta entera; un campo de solo lectura cuyo vecino de arriba es su operando no es una entrada mal marcada, es un resultado. Y abrirlo seria **peor** que dejarlo cerrado: el art. 24 manda tomar el **mayor** entre el valor de transferencia y el autovaluo ajustado, asi que teclear un autovaluo bajo elige la base y baja el impuesto —el defecto de #51 con la tasa por omision, inventado ahora por quien liquida—. `"ro"` se queda (RNF-080) y `alcabala` espera a #188: al autovaluo del predio, que #395 dejo **declarado** porque el sistema no sabe valorizar, y al indice del art. 24, que tiene norma y no tiene ni archivo del corpus —fila 15 del mapa normativo, que no lo nombraba—. Y un hallazgo que no es de interfaz: `AlcabalaController` pide el autovaluo **ya multiplicado** en el cuerpo, o sea traslada el factor a quien liquida, que es el mismo desajuste que #399 corrigio en `minimoImponible` |
| Las dos ultimas hojas sin superficie, y el desplegable que prometia siete reportes inexistentes (#428, FRO-06: 4 pruebas nuevas de pantalla y el emisor conectado) | Cuatro roturas: devolver los diez literales del prototipo al desplegable; quitarle al reporte agrupado el agrupador que la pantalla fija; quitar del proxy la ruta del emisor; y sacar `adm_resolucion_gerencia` de `ACTOS_SIN_CAMPO` | 2, 1, 1 y 3 en rojo. **La segunda no mordio a la primera vez, y ahi estaba el hueco**: el proxy devolvia siempre el resumen agrupado por codigo, asi que quitarle a la pantalla el `agrupadoPor: CODIGO` que fija no cambiaba nada —la hoja salia igual y la prueba pasaba—. Con el proxy respetando el agrupador **y su omision, que es la del backend (`ESTADO`)**, la misma rotura deja la columna «Código» con «Sancionada» dentro: la cabecera correcta sobre las fases del procedimiento. **Y el desplegable era el bloqueo real, no la puerta**: #424 dejo `adm_reportes` fuera a proposito porque ofrecia diez tipos de reporte y `TipoDeReporteAdministrativo` implementa tres; ahora ofrece los tres y de los otros siete dice **donde estan** —cinco son otra opcion del catalogo, y **dos no las sirve nadie**: la relacion de notificaciones por mes, porque el padron no agrupa, y el padron de papeletas administrativas, que no existe como reporte—. El unico rotulo que hubo que interpretar es `RESUMEN_PAPELETAS`: se elige «PAPELETAS POR INFRACCIÓN» y no «PADRÓN DE PAPELETAS» porque el segundo es un listado que el backend no publica y el primero nombra una agrupacion, que es lo que `AgrupacionDelResumen.CODIGO` da. Con las dos hojas administrativas clasificadas, **las siete que `hojas-sin-superficie.test.ts` computa del catalogo estan todas en `ACTOS_SIN_CAMPO`** |

| Las tres hojas sin superficie de Transito, y con ellas las siete (#429, FRO-06: 5 pruebas nuevas de pantalla, 3 del mes del prototipo y la lista de pendientes vacia) | Cuatro roturas: quitar el aviso de la constancia; declarar pendiente una hoja que ya esta aplicada; recortar el aviso de la sancionadora hasta dejarlo sin el dato; y devolver al mock el `?? '1'` del mes anterior a #398 | 4, 2, 1 y 1 en rojo. **La cuarta es un arreglo que llevaba desde #398 sin nadie que lo sujetara**: el prototipo escribe «Agosto (al 13)» para el mes en curso, el diccionario no reconoce esa cadena entera y el valor por omision la mandaba a **enero** —una segunda fila de enero con las cifras de agosto, indistinguible de la buena—. El arreglo vivia en un docblock; ahora la rotura dice «expected [ 1, 2, 3, 4, 5, 6, 7 ] to include 8», que es el defecto entero en una linea. **Las tres estaban bien clasificadas desde #77 y aun asi eran mudas**, que es la mitad del defecto que FRO-06 nombra: sin `acciones`, `Pantalla.tsx` no dibuja `<BarraDeAcciones>`, asi que la causa que `impedimentoDelActo` calcula para ellas entra en el censo y **no la lee nadie** —RNF-082 un escalon mas arriba de donde lo cerro #385, donde al menos habia un boton apagado con un `title`—. Por eso el censo **no se mueve** con este issue y aun asi cambia lo que la pantalla dice: la casilla ya era la correcta. **Y la constancia enseña lo suyo**: las dos resoluciones pueden decir donde SI esta dibujado su formulario —«Descargos y reclamos»—, y ella no, porque no lo hay; lo que su aviso nombra es el filtro que **se le parece y no sirve**, la placa de «Búsqueda de papeletas», que busca lo que hay y no dice que vehiculo se acredita. Con las tres, la lista de pendientes de la guarda queda **vacia**, y se queda declarada a proposito: lo que permite es una excepcion temporal y nombrada, y con la lista vacia una hoja nueva —o una que pierda su aviso— no tiene donde esconderse |
| La sesion del ciudadano: D-07 cerrada y el portal recorriendo el registro (#57, ADR-0020; 8 pruebas de las dos cadenas con dos emisores OIDC, 14 contra PostgreSQL real, 2 del guardia, 7 de infraestructura, 26 de interfaz y 2 caminos en Chromium) | Nueve roturas: apuntar las dos cadenas al **mismo** emisor; no limpiar `TenantContext` entre ramas; conectar el pool como superusuario; que el centinela `CIUDADANO` pase venga de donde venga; devolver el documento como parámetro de la consulta; comparar la guarda de vuelta por prefijo en vez de por igualdad; hacer `numero_documento` editable por el usuario en el realm; totalizar aunque falte una municipalidad; y auditar el sondeo del padrón | 4, 1, **6**, 1, 2, 1, 1, 1 y 1 en rojo. **La tercera enseña lo suyo**: con el superusuario —que omite RLS incluso con `FORCE ROW LEVEL SECURITY`— la persona «aparece» en las tres municipalidades, y entre los seis rojos está el que más dice: *la municipalidad donde NO figura recibe una fila de `ACCESO`*, «expected 2 but was 3». Es exactamente lo que ADR-0020 §5 existe para impedir —la bitácora de cada municipio convertida en una forma de saber que alguien existe en otro—, y sale de una fuga de aislamiento, no de un descuido al auditar. **Y la octava pasó en VERDE con las trece pruebas puestas**: la que miraba «una rama que revienta no tumba las demás» comprueba `Resultado.completo()` del recorrido, no la situación compuesta —que es lo que sale por HTTP—, así que quitarle a `totalConsolidado()` la guarda de `noLeidas` no ponía nada en rojo; con el caso nuevo, un total al que le falta una municipalidad vuelve a ser imposible. El paquete del ciudadano **baja** de 80,9 a 79,1 KB al ganar la sesión: se fueron la caja de documento, sus tres tipos, el adaptador de las seis rejillas y la segunda lectura del padrón |
| Los ocho desajustes contrato↔controlador que quedaban, y el noveno (#425: 31 pruebas nuevas de capa web en 8 archivos, dos de ellos nuevos; el censo queda **vacío**) | Doce roturas, y las que más dicen son tres: que cada controlador **lea el filtro de la consulta y lo ignore** (las nueve a la vez); quitarle el `@RequestParam` a uno, que es el desajuste original; y hacer que **gane la consulta** en vez del cuerpo en el único sitio donde vive la regla | 14 en rojo en 6 módulos la primera, 2 la segunda —las dos del censo, nombrando la operación y el parámetro— y **10 en rojo en 7 módulos** la tercera, desde una sola línea. **La tercera es la que sostiene la decisión sobre `POST /rentas/deuda/bajas`**, la única de las nueve que ya estaba conectada: se corrigió **sin tocar la pantalla**, porque gana el cuerpo y `escrituras.ts` los sigue mandando ahí dentro de la tabla `cuotas` (#332); invertir la precedencia rompe esa pantalla y las de otros seis módulos a la vez. **Y la primera enseña lo suyo en el acta vehicular**: `hallazgo` es opcional, así que ahí el desajuste no daba 422 sino **201 con el acta guardada sin hallazgo** —«expected: SUBVALUADOR but was: null»—, una inspección sin conclusión indistinguible de la que de verdad no encontró nada. En la REC se mide sobre el **modelo del papel emitido**: sin leer la consulta, «proyectar interés al 31/07» imprime la deuda al 15/06. El contrato **no se toca**: `--comprobar` sigue diciendo «175 operaciones en 164 rutas». Y una rotura enseñó algo del propio método: revertir por reemplazo de texto pisó la **cobranza tributaria** en vez de la de tasas —las dos líneas eran idénticas—, y solo lo delató que `:sgtm-tesoreria:compileJava` dejara de compilar; desde entonces cada mutación se revierte contra una copia de los nueve archivos y se compara byte a byte |
| El enrolamiento del ciudadano en ventanilla: D-15 construida por el camino B (#415, ADR-0020 §6; 9 pruebas nuevas de `componentes.test.ts` y el peldaño 3c de `despliegue.yml`, contra el compose levantado) | Tres roturas: declarar en `ciudadanos/200101.json` un DNI que no figura en ningún padrón; meterle `credentials`; y sacar `identidades.ciudadanos` de la huella del Job | Rojo el cruce a tres bandas —«declara DNI 44999999, y esa persona NO figura en el padrón de 200101»—, y rojo el rechazo de la clave **en las dos implementaciones**, la del guion y la de `infra/`, las dos nombrando el archivo. **Y la tercera pasó en VERDE la primera vez**: la prueba de la huella medía el ambiente de `AMBIENTE`, cuyo ubigeo no tiene a nadie enrolado, así que `ciudadanos.tsv` era la cadena vacía y `update("")` no cambia un sha256 —quitar esa parte daba exactamente el mismo nombre de Job—. Medida sobre los manifiestos de 200101, que sí versiona un enrolado, y con la exigencia previa de que el TSV **no** esté vacío, la misma rotura da «expected 'sgtm-prod-realm-70b310364d' to be 'sgtm-prod-realm-fef7b91775'». El peldaño 3c recorre además el acto entero: la cuenta **derivada** del documento con el tipo delante —`dni-70123456`, porque `CE 12345678` y `DNI 12345678` son dos personas—, que nace sin clave y sin poder pedir token, que el enlace llega al buzón, que reconciliar dos veces no cambia nada (mismo `createdTimestamp`, mismo buzón, `UPDATE_PASSWORD` intacto), que **entra al portal y ve su situación** sin teclear ningún documento, que su lectura deja la fila `ACCESO`, que su token no vale fuera del portal ni el del funcionario dentro (401 los dos), y que un documento que el dominio no puede leer recibe **403 `SIN_DOCUMENTO`**, no una situación vacía. **Y la rotura literal de ADR-0020 §4 —quitarle el atributo— se cerró por el lado más fuerte de los dos**: Keycloak **no deja crear** un ciudadano sin `numero_documento`, porque el perfil declarativo lo exige al administrador; la cuenta no llega a existir, así que el 403 del borde es la segunda barrera y no la primera |
| El resolutor de campos, generalizado: el campo que el manual no dibuja, declarado (#422; 21 pruebas nuevas —10 del mecanismo, 11 del censo— y 5 de la pantalla que lo demuestra) | Siete roturas: nombrar una seccion que el catalogo no dibuja; darle al control anadido la etiqueta del campo del catalogo; quitar de `escrituras.ts` la clave que el control llena; declararle un control a `alcabala`; hacer que `Formulario` ignore los controles declarados; quitar la entrada de `LA_QUE_ESCRIBE`; y quitar la traduccion del tipo de recurso | 3, 3, 3, **4**, 12, 4 y 1 en rojo. **La cuarta es el AC que no se ve en ninguna pantalla nueva**: teniendo el mecanismo delante, la salida comoda para las once que quedan en `ACTOS_SIN_CAMPO` es declararles un control y darlas por cerradas, y para las de la tercera forma —el autovaluo ajustado de `alcabala`, D-11/D-02a— eso es darle a quien atiende una caja donde teclear una cifra que ninguna norma respalda; el censo lo rechaza por cuatro sitios a la vez, uno de ellos que la etiqueta inventada **repite** la del campo `"ro"` que ya la dibuja. **La sexta enseña que los dos mecanismos de esta onda son complementarios y ninguno sobra**: sin `LA_QUE_ESCRIBE` (#421) la primaria de `transito_descargos` vuelve a ser «Notificar al administrado» y pulsarla no manda nada; sin el control anadido (#422) no hay donde teclear el numero de expediente de mesa de partes que `DescargosController` exige. **Y una octava se midio y no mordio, asi que no se envio**: envolver el `onCampo` del control declarado en `soloSusCampos` —lo que si hace falta para el resolutor, que es codigo ajeno— da **0 en rojo**, porque un control declarativo solo puede llamar con la clave que declara; una regla que no puede fallar no protege nada, y lo que si muerde es el censo |
| La lectura del programa de fiscalización, la que faltaba (#431: 16 pruebas nuevas de backend —8 contra PostgreSQL real y 8 en el borde web— y 3 de interfaz) | Diez roturas: quitarle el `@Transactional` a `ConsultaDeProgramas`; resolver el «Ejercicio» por el año de `fecha_inicio` en vez de por vigencia; leer el código sin normalizar; quitarle el `@RequestParam` a los dos filtros, y —al no morder— cambiarle el nombre a uno; quitar el `@RequiereAcceso` del `GET`; sacar la ruta de `IMPLEMENTADAS`; quitar la operación del generador; y en la interfaz quitar la conexión, pintar los programas en la grilla y rellenar un campo que `ProgramaResource` no publica | 1, 2, 1, **0** y 1, **0**, 1, 1, y 4, 1 y 1 en rojo. **Dos pasaron en VERDE, y las dos enseñan lo suyo.** Quitarle el `@RequestParam` a `nDePrograma` y `ejercicio` deja las diez pruebas de la capa web en verde: Spring MVC enlaza un `String` suelto por su nombre igual, así que la anotación documenta y no es lo que hace viajar el filtro —lo que separa consulta de cuerpo es que el dato **no** esté en el `record` del `@RequestBody`—; lo que sí muerde es cambiarle el nombre («expected: "PF-300"»). Y quitarle al `GET` su `@RequiereAcceso` **no pone `verificarArquitectura` en rojo**, porque la regla exige la anotación «en la clase o en cada endpoint» y la clase la declara para programar: sin la del método el `GET` hereda `REGISTRO`, de modo que quien solo tiene lectura sobre `fisc_programa` no podría abrir su pantalla y nada lo diría hasta integrar. Se cierra con una prueba que lee la anotación del método, y entonces la misma rotura da 1 en rojo («Expecting actual not to be null»). **Y la primera de la interfaz destapó lo que la pantalla enseñaba antes**: su operación del catálogo es un `POST` y una operación que escribe no se pide al abrirla, así que no pedía **nada** y los desplegables dibujaban su primera opción —«expected 'PREDIAL MASIVO' to be 'PREDIAL'», «expected '01' to be '—'»—, una elección que nadie hizo e indistinguible de un dato del backend. **La grilla se queda vacía a propósito**: las seis columnas de «Predios seleccionados» describen un predio y `ProgramaResource` describe un programa; poner ahí las filas del listado es el «—» de #322 al revés (RNF-080), y la muestra tampoco está en el backend —`programa_fiscalizacion` no tiene tabla de detalle y un acta nace el día de la visita con su predio ya resuelto— |
| Cómo nace un `area` y una `caja` en una instalación nueva (#430: 11 pruebas nuevas, todas contra PostgreSQL real) | Cinco roturas: envolver el bucle del archivo en **una** transacción; inventarle el nombre al área que la fila no nombra; no reutilizar el área ya registrada; quitarle el `@Profile("batch")` a `CargarCajas`; y repetir una serie en `ejemplos/cajas.csv` | **0** y luego 3, 1, 1, 1 y 1 en rojo. **La primera pasó en VERDE**, y el motivo es el que `TransferenciaJdbcTest` lleva escrito: `ImportarCajas` se instanciaba con `new` en la prueba, así que la anotación **no se aplicaba** y la rotura no cambiaba nada. Envuelto en su proxy transaccional de verdad da **3 en rojo**, las tres con `UnexpectedRollbackException` —la fila rechazada marca la transacción como *rollback-only* y se lleva por delante la corrida entera, que es peor de lo que #328 documenta: no se pierde la fila que seguía, se pierde el informe—. **Y lo que este issue existía para cerrar lo demuestra una sola prueba**: en una municipalidad recién implantada `AbrirCaja` falla con `CajaInexistente`, y tras cargar el archivo abre. Hasta aquí **nada creaba una `caja` ni un `area` fuera de las fixtures** —`area` y `caja` existen desde V3, `sgtm_app` las escribe desde V7, `CajaRepository` no tenía `insertar` y `AreaRepository` no existía—, así que una instalación con padrón, predios y deuda **no podía cobrar**. Se resuelve por donde entra la configuración de la municipalidad y no con una pantalla: ninguna de las diez opciones de Tesorería del manual da de alta una caja, y publicar un endpoint que ninguna pantalla llama sería inventar contrato. `cargar-cajas.sh` es el paso 4 de la siembra —que pasa de nueve a **diez**— y **no exige `es_demostracion`**: una ventanilla no es un dato inventado |
| Coactiva, de cuatro pantallas conectadas a doce: **las ocho escrituras** (#426; 9 pruebas de backend contra PostgreSQL real, 2 en el borde web y 12 de interfaz sobre las 46 del módulo) | Catorce roturas, y las que más dicen son cuatro: mandar `rec: 'CARATULA'`; atar «Pago inicial (S/)» a `cuotaInicial`; quitarle a la grilla del fraccionamiento su columna propia de casilla; y declararle a «Historial del expediente» el campo `activo` | 1, **3**, 1 y **0** en rojo. **La primera es el hallazgo del issue, y es de la clase que #421 nombra**: `ActoCoactivoController.recDe` acepta la palabra «CARATULA» y la mapea a `REC1` —en `TipoDeActoCoactivo` no existe ninguna constante para ella—, así que un botón rotulado «Carátula» que la mandara **dictaría la REC-1**: `RegistrarActoCoactivo.dictar` asienta el acto, emite su documento y mueve el expediente a `REC1_EMITIDA`, y si ya había una responde 409. Un papel que se notifica al obligado, bajo un rótulo que promete otra cosa; la acción se queda secundaria y `rec` va como constante. **La segunda es la mentira silenciosa que este issue evitó**: el prototipo rotula «Pago inicial (S/)» —soles— y `PeticionDeConvenioCoactivo.cuotaInicial` es un **porcentaje** (`Alicuota.de`, 0..100), así que teclear «20» soles daría un convenio con 20 % de cuota inicial, una cifra plausible y equivocada que sale impresa en el cronograma que el contribuyente firma; se pregunta con un control propio (#422) y el campo del prototipo no viaja. **La tercera es la que no se veía venir**: «Deudas acogidas» es la única tabla que elige y **no dibuja columna para la casilla** —sus trece empiezan en «Año»—, de modo que la casilla se comía el ejercicio, que es uno de los cuatro datos con los que el convenio identifica lo que se acoge; `SeleccionDeFilas.columnaPropia` la añade. **Y la cuarta pasó en VERDE con 94 pruebas puestas**: declarar `activo2` manda `activo`, que es justo lo que el `record` excluye a propósito —«el movimiento que rige es el último y eso se deriva»—, y ninguna prueba miraba el cuerpo *completo*; con la que compara clave por clave las ocho, 1 en rojo. **Dos defectos vivos los destapó medir en vez de suponer.** `verificaciones/actos-inalcanzables.test.ts` decía leer «la misma lista que `esIrreversible`» y leía otra: su `leerPatron` hacía `.replace(/\s+/g,'')` sobre el literal, así que las seis alternativas de varias palabras —`dar de baja`, `cerrar caja`, `importar valor`…— se convertían en `dardebaja`, `cerrarcaja`… y no casaban con nada; llevaba así desde que existe. Y `etiquetaDeFila` decía «Elegir **la**» siempre: `SeleccionDeFilas.genero` es obligatorio desde #332 y solo lo miraba la banda, así que la primera tabla masculina —«Elegir la expediente EC-2026-00412»— lo destapó. Con el patrón arreglado, la lista de actos inalcanzables sube de 14 a 18 y **ninguna entrada nueva es una pantalla mal conectada**: entran `alcabala`, `fisc_programa`, `pase_coactiva` y `rec_impresion` porque `generar` pasa a ser irreversible —lo es: genera la orden de pago, la muestra, el pase y la REC—, y **sale** `fisc_vehicular`, que era un falso positivo. Las diez restantes muerden una a una: quitar la conexión que da filas al fraccionamiento (3 en rojo, y la tabla desaparece con su aviso), poner el ejecutor en la columna «Nombre» que `ExpedienteResource` no publica («expected 'R. MENDOZA CRUZ' to be '—'»), traducir «DIRECCIÓN NO EXISTE» a `RECHAZADO` en vez de `NO_UBICADO`, quitar el `delFiltro` del expediente de costas —lo único que apaga la primaria, porque `Conexion.exige` apaga **la lectura** y no el botón—, devolver «la» al artículo, dar a un control añadido la clave de un campo del catálogo (2), quitar el filtro bloqueado del «Estado» de las costas —de sus cuatro valores el sistema deriva dos, y los otros dos son actos que nadie registra—, devolver el pie congelado del prototipo («Deuda total 1,848.66 · acogida 1,848.66»: tres cifras de la captura, y la segunda **no puede existir** antes de marcar nada), y estrechar el patrón de lo irreversible. **Y el coste midió lo que #433 acababa de prometer**: conectar el módulo entero cuesta **1,4 KB** del arranque (142,6 → 144,0) y no los 2,5 que habría costado el día antes, porque las composiciones y las conexiones se van con el trozo de su módulo; lo único que sigue pagando todo el mundo son los ocho cuerpos de `escrituras.ts`, que están ahí por diseño. El presupuesto **no se toca** |

| El ambiente desplegado, comprobado contra si mismo (#434: `verificar-el-ambiente.sh`, corrido contra `prod` y contra `stg` de verdad) | Midiendo el aislamiento con el **superusuario** en vez de con `sgtm_app` | 2 en rojo, y el numero lo dice: «con el ajeno: 145» donde debe decir 0, y «el superusuario ve lo mismo que sgtm_app». **Pero la demostracion de que muerde no hizo falta provocarla**: el guion nacio ROJO contra los dos ambientes, y por lo que existia para encontrar — `prod` corria `0eee58e4` con **27 migraciones aplicadas de las 48** que declara `main`, y `stg` **25**, asi que ni las tres tablas de valuacion de `V55` ni la columna de uso de `V57` existian en ninguno de los dos y `publicar-cuadros.sh` no tenia donde escribir. Nueve `pulumi up` de `prod` en verde entre el 27 y el 28 de agosto (mediana 73 s) no habian movido `applicationBootstrapVersion`, y ningun `Deployment` `Ready` lo distingue. **Y subir esa linea no es inerte**, al reves de lo que decia su propio comentario: los dos Jobs de arranque llevan la version en el nombre (`sufijoDeVersion()`), de modo que un valor nuevo no modifica un Job, **crea otro** — comprobado sin desplegar con `yarn manifiestos --ambiente prod \| grep migracion`, que pasa de `migracion-0eee58e43e04` a `migracion-5fc02f3a4493` |
| Las dos guardas de la carga de valores normativos, medidas por separado (#435: 11 pruebas contra PostgreSQL real, mas el `CONNECT` simetrico del motor y la rotacion del rol, ahora en CI) | Dos roturas, y la gracia esta en que dan resultados DISTINTOS: (A) quitar de `V55` el `REVOKE INSERT, UPDATE ... FROM sgtm_app`, y (B) quitar ademas de la politica de escritura el `TO rol_carga_parametros` para que nombre tambien a `sgtm_app` | **1 en rojo la primera, 4 la segunda.** Con solo el `GRANT` devuelto, las cuatro pruebas de sintoma siguen en VERDE —la politica de RLS lo para igual, y da el MISMO `42501`— y la unica que lo caza es la que mira `has_table_privilege` en el catalogo: es el matiz que #380 midio, conservado por fin en una prueba en vez de en un comentario. Con las dos fuera, `sgtm_app` publica una fila de `depreciacion`. **Y ejecutar la rotura encontro un defecto en el propio verificador de rotacion**: sin el `ALTER ROLE`, `verificar-rotacion.sh` salia con codigo 1 y **sin una sola linea que dijera por que** —`exec {fd}>&- 2>/dev/null` aplica sus redirecciones al shell entero y de forma permanente, asi que llevaba desde #154 silenciando el `stderr` de sus dos ultimas comprobaciones, las que de verdad miden la rotacion—. Ese guion, ademas, **no lo corria nadie**: INF-06 lo decia con todas las letras, y ahora corre en CI para los dos roles que rotan |
| La credencial de carga, llevada a un motor que YA existia, y la publicacion corrida de punta a punta en `stg` (#435) | No hizo falta provocar ninguna: **la primera corrida real salio mal, y con el diagnostico equivocado** | `publicar-parametros.sh` termino con `PUBLICADAS=0 RECHAZADAS=22`, **codigo de salida 0**, y un aviso por fila que decia «revise que las dos firmas sean distintas». Las firmas estaban bien. La causa era «role "rol_carga_parametros" is not permitted to log in», y **no aparecia en ninguna linea de la salida**: `CannotGetJdbcConnectionException` es una `DataAccessException`, asi que el `catch` generico la contaba como un rechazo de fila — veintidos reintentos de pool, veintidos diagnosticos equivocados. Y la causa de la causa es estructural: `20-asignar-claves.sh` da `LOGIN` **solo al inicializar el motor**, con el volumen vacio, asi que el `LOGIN` que #387 le dio al rol nunca llego a un cluster creado antes. Tres cosas se arreglaron: `asignar-claves.sh` (nuevo) lleva al motor en marcha lo que el inventario declara y es idempotente; los dos guiones de publicacion **comprueban que la credencial sirva**, no solo que el secreto exista, y nombran el remedio; y el proceso **corta la corrida** en vez de contar filas rechazadas. Con eso: `PUBLICADAS=22 RECHAZADAS=0` de parametros y `PUBLICADAS=492 RECHAZADAS=0` de la depreciacion contra `stg` real, y la segunda corrida informa lo ya publicado sin duplicar (23 filas antes, 23 despues). **Un cuarto defecto lo destapo el propio sondeo**: comprobar todos los roles contra `sgtm` daba rojo en `sgtm_respaldo` y `keycloak`, que **a proposito** no llegan al padron — la base de cada rol pasa a ser dato del inventario (`baseDeDatos`), porque ese rojo falso era indistinguible del de un rol sin `LOGIN` |

| El `% actualizacion` compilado, que es el unico factor cuyo valor «obvio» es NINGUNO (#437, D-11) | Quitando `ACTUALIZACION` y `FACTOR` de la lista de nombres de la regla 5 | **«Expected size: 2 but was: 0»**: sin las dos palabras la muestra pasa entera inadvertida, y ninguna otra prohibicion se pone roja — octava vez que el hueco se abre por el mismo sitio, tras `INTERES_DE_FRACCIONAMIENTO` (#35), `COSTA_DE_LA_REC2` (#42), `TASA_PANEL` (#51), `MULTA` (#52), `VIGENCIA_DEL_CERTIFICADO` (#54), `BENEFICIO` (#72) y `MINIMO_IMPONIBLE_VEHICULAR` (#399). **Y este es peor que los siete anteriores**: los otros obligan a inventar una cifra y quien la escribe lo sabe; aqui el valor plausible es `1`, o sea *ninguno*, y escribirlo no se siente como inventar un dato — pero afirma que el factor vale 1 en todo ejercicio y toda municipalidad, multiplicando el autovaluo de todo el padron. **Ensanchar el patron a «la palabra en cualquier parte del nombre» se midio antes de descartarlo**: da 8 falsos positivos en `src/main`, todos de `MINIMO` en constantes que no son normativas (`LARGO_MINIMO = 5`, `ANIO_MINIMO = 1990`, `DECIMALES_MINIMOS = 2`), y un escaner que grita ocho veces en verde deja de leerse; asi que la regla sigue anclada al inicio del identificador y **una prueba fija ese limite** —exige que `PORCENTAJE_DE_ACTUALIZACION` siga escapandose— para que ensancharla sea una decision y no un descuido |

| La cadena de publicacion de valores normativos, corrida entera contra `stg` real (#438: 22 parametros y 492 filas de depreciacion) | **La rotura no hubo que provocarla: la primera corrida salio mal** (ver la fila de #435). Lo que si se midio a proposito fue la idempotencia: correr `publicar-parametros.sh` dos veces | 23 filas de `parametro_tributario` antes y 23 despues; la segunda corrida informa las 22 como «ya estaba publicado» **nombrando cada llave** y no duplica ninguna. **Y el sello NO se hizo, a proposito**: `conjunto_sellado_uq` admite un solo conjunto sellado por ejercicio y el disparador de `V9` no deja anadirle una cifra mas, asi que sellar 2026 hoy —sin los valores unitarios (H-14), sin el `% actualizacion` (D-11) y sin lo de ordenanza local (D-02b)— dejaria el ejercicio con la mitad de sus cifras y **sin poder recibir la otra mitad**. La lista que hay que poder contestar antes de ese acto quedo escrita en `publicacion/README.md`; el paso que le faltaba a la secuencia —`asignar-claves.sh`, porque la credencial no llega sola a un cluster que ya existia— quedo en `carga-de-datos/README.md` |
| Las tres de licencias que faltaban: dos padrones por la tercera puerta y el certificado que ya emite (#427 parte A, 10 pruebas nuevas de pantalla y el censo recompuesto) | Seis roturas: quitar `anuncios_reportes` de `LECTURAS_POR_POST`; **traducir «ACTIVA» a VIGENTE** en el vocabulario del padrón de licencias; quitar el control declarado del `nDeRecibo`; quitar el resolutor del `solicitante`; quitar la guarda de los dos tipos de certificado que consignan parámetros urbanísticos; y quitar del proxy la ruta del padrón de licencias | 5, 1, 2, 2, 1 y 1 en rojo. **La segunda es el defecto que una auditoría adversaria encontró y este PR se negó a introducir**: los ocho criterios de «Filtrado por» mapean 1:1 **por nombre** contra `PeticionDeReporteDeLicencias` y **no por valor** —el desplegable ofrece ACTIVA / CANCELADA / DUPLICADA / VENCIDA / TODAS y `EstadoDeLicencia` (V37) sólo declara VIGENTE, VENCIDA y CANCELADA; «Tipo Lic.» ofrece (TODOS) / INDETERMINADA / TEMPORAL / CESIONARIO / MERCADO y `TipoDeLicencia` sólo DEFINITIVA, TEMPORAL y CESIONARIA—, o sea **cinco de diez elecciones que devuelven 422 después de rellenar el formulario**, el mismo cruce que dejó `infracciones_adm` sin conectar en #78 hasta que #397 lo resolvió en el backend. La decisión es **no traducir ninguno**: «ACTIVA» se parece a VIGENTE e «INDETERMINADA» a DEFINITIVA, y parecerse no es serlo —una licencia «activa» podría querer decir «no cancelada», que incluye a las vencidas—; el desplegable ofrece sólo los valores que el enumerado tiene letra por letra, la lista **se computa del catálogo** y la ayuda nombra los cinco que quedan fuera. **Y la cuarta es la trampa del «Solicitante»**: `CertificadoResource.solicitante` es el NOMBRE —lo que la grilla ya pinta— y `PeticionDeCertificado.solicitante` es el CÓDIGO, que `EmitirCertificado` resuelve con `contribuyentes.porCodigo(...)`; el prototipo teclea ahí un nombre, así que declararlo tal cual compila, pasa la lista blanca y pasa el lint, y lo que llega a ventanilla es un **404 sobre una persona que sí está en el padrón**. **Lo que se decidió no hacer**: los cinco parámetros urbanísticos son `"ro"` en el catálogo y el backend espera que los teclee quien atiende; abrirlos sería volver editable lo que el manual dibuja de solo lectura (RNF-080) y emitir igual produciría un papel que dice «Este certificado no consigna parámetros urbanísticos» con su correlativo gastado y su SHA-256 sellado —V51 no admite `UPDATE`—, así que la pantalla emite **numeración y jurisdicción** y de los otros dos tipos dice por qué no. El presupuesto de arranque sube de 157 a 158 KB: lo medido es 156,9 y CI mide ~0,3 más (#442), así que con 157 este cambio decidiría en verde o rojo según dónde se compile |
| Las dos escrituras de infracciones administrativas, y el número que el manual parte en tres (#428, 4 pruebas nuevas de pantalla y el censo recompuesto) | Cuatro roturas: quitar el resolutor del número; hacer que el resolutor **no lea la serie**; quitarle a `adm_valores` la mitad de su rango (`fecFin`); y quitar `adm_valores` de `ESCRITURAS` | 2, 2, 1 y 1 en rojo. **La segunda es la que este issue no traía en su enunciado**: el issue decía «con #421 son declaración pura», y `adm_notificacion` no lo es —el manual teclea el número en **tres** campos (Serie, Año, Número) y `NotificacionAdministrativa.numero` es **uno**, con `notif_adm_numero_uq UNIQUE (municipalidad_id, numero)` encima (V4)—. Declarar sólo «Número» deja la serie tecleada y sin viajar, que es el defecto de #331, y aquí además **choca**: `001-004183` y `002-004183` se guardarían los dos como `004183` y la segunda serie no se podría registrar. Lo compone `ResolutorDelNumeroDeNotificacion` (#422) y **el separador no se inventa**: es el que el propio manual imprime en su columna «Serie-Nº»; el año no entra en él, y el control lo dice antes de mandarlo («Se guardará como «001-004183»»). `adm_valores` sí es declaración pura: es la gemela exacta de `transito_valores` —el mismo caso de uso con otra `Familia` en el mismo `GeneracionMasivaDeValoresController`— y declara lo mismo, sólo «por rango»; **y a diferencia de ella no necesita componente propio**, porque `LA_QUE_ESCRIBE` (#421) ya pone «Procesar» de primaria donde el catálogo pone «Imprimir». El censo se mueve en las dos direcciones —`declarada` 20→22, `sin-declaracion` 13→11— y con él la prueba que las contaba apagadas: las dos pasan de «registra el acto por el procedimiento actual» a pedir la observación y decir qué falta rellenar, sin `data-causa` |
| El presupuesto de arranque: qué sale de él antes de las últimas conexiones (#433: 16 pruebas nuevas en `aportes-de-modulo.test.ts`, y las 1 587 del suite recompuestas) | Siete roturas: bajar el presupuesto por debajo de lo medido; devolver a `conexiones.ts` la importación estática de tres registros de módulo; quitar un cargador de los doce; apuntar el cargador de Coactiva al registro de Licencias; quitarle a la ficha 360° el `await cargarConexionesDeLaOpcion`; no registrar las composiciones al cargar un módulo; y quitarle a `Pantalla` la espera del aporte | Rojo con el número —«141,4 y el presupuesto son 141»—; rojo con «149,8 de 145»; **23 archivos** en rojo nombrando el módulo —«El modulo «transito» no declara su aporte […]: sus pantallas conectadas se dibujarian por el camino comun, con la tabla vacia»—; rojo con «‹anuncios› no es de ‹coactiva›»; 3 en rojo en la ficha, que son justo las que comparan celdas; 167 en rojo; y **403 en 44 archivos**. **La última enseña lo suyo, y hubo que ganársela**: los doce archivos que censan el catálogo lo hacían cargando **y registrando** los doce módulos, así que se tapaban a sí mismos —con la espera quitada, la misma mutación daba 236 en 28 archivos y `transito.test.tsx` seguía en VERDE los 44, más los 35 de Coactiva: su propio censo había registrado el módulo antes de montar la primera pantalla—. Con un censo que carga **sin registrar** (`censoDeAportes`), la mutación muerde en los 79. Lo medido: el arranque baja de **155,7 a 141,4 KB** comprimidos y el umbral de 157 a 145; lo que sale son los doce registros de módulo y las cinco composiciones, que llegan con el trozo de su módulo en la **misma** espera que ya bloqueaba el dibujo —ni un viaje más, ni un `Suspense` más—. Y el margen deja de medirse igual: una conexión nueva ya no pesa en el arranque de todos, pesa en el trozo de su módulo (11 KB, el mayor va por 8,9) |
| La última pregunta de `espectaculos`: quién compone la recaudación declarada, y los cuatro filtros que no filtraban (#432, 5 pruebas nuevas) | Tres roturas: dejar bloqueado **un** filtro de los cuatro; quitar `espectaculos` de `ACTOS_SIN_CAMPO`; y declarar un filtro bloqueado **sin su texto** en `prosa-textos.ts` | 4, 5 y 1 en rojo. **La respuesta es que `espectaculos` sigue sin poder conectarse, y ahora se dice de quién es**: la recaudación declarada no la compone la interfaz —una cifra de dinero no se compone en la pantalla (RNF-083, con su regla de ESLint)— ni podría, porque esa multiplicación **es la base imponible del art. 56**, con su mínimo del 50 % cuando la entrada incluye otros servicios, y una regla tributaria no vive en un componente de React (regla 6). Le toca al backend con la forma que #399 dejó probada para `minimoImponible`: la petición recibe **los dos operandos** y el dominio compone la base. Hoy `PeticionDeEspectaculo` **no tiene ni campo para las entradas vendidas**. Resolver sólo `organizadorId` —que sí es resoluble: `ContribuyenteResource` publica `id`— no desbloquearía nada, así que el resolutor no se construye. **Y salió una tercera cosa que el issue no traía**: la alícuota del espectáculo tiene **tres vocabularios para lo mismo** —el desplegable del prototipo (CONCIERTO DE MÚSICA POPULAR, DISCOTECA, CINE…), las claves del corpus VERIFICADO (`ESPECTACULO_ALICUOTA-TAURINO-SUPERIOR-0.5-UIT`, `-CINEMATOGRAFICO`…) y la llave que el código pide (`ALICUOTA_ESPECTACULO:<el literal del desplegable en mayúsculas>`)—, que es el hueco de #192 multiplicado por tres; y dos de las diferencias **no se arreglan traduciendo**: «DISCOTECA» no es ninguna de las siete categorías del art. 57 —sería «otros espectáculos», y eso es una calificación— y el taurino tiene **dos** alícuotas según el precio de la entrada, condición que `RegistrarEspectaculo` no puede expresar porque lee **una** llave por tipo. Lo único que sí se pudo cerrar: los cuatro filtros de la pantalla —«Nº de expediente», «Organizador», «Desde», «Hasta»— estaban **vivos** y no filtran nada (el controlador no lee ninguno y ninguna lectura del contrato lista los espectáculos declarados), así que se bloquean con su motivo, como los de #322 y #398. `alcabala` no necesita lo mismo: su catálogo no dibuja ninguno |
| Los cinco filtros de Fiscalización que no filtraban, y la pantalla que se leía como consulta sin serlo (#431 parte B: 11 pruebas nuevas y el censo recompuesto) | Tres roturas: dejar bloqueado **uno** de los tres filtros de resultados; quitar `fisc_resultados` de `ACTOS_SIN_CAMPO`; y que el módulo cargue sin registrar su composición | 3, 3 y 5 en rojo. **La segunda es el hallazgo, y es de la frontera más delicada del sistema**: `fisc_resultados` se clasificaba `sin-backend` —«aquí todavía no se puede guardar nada: lo que hay es de consulta»— y las **dos mitades** de esa frase son falsas: su primaria, «Emitir resoluciones de determinación», tiene backend desde #52 —`POST /fiscalizacion/transferencias`, que `ResolucionController` declara con `@RequiereAcceso(acceso = "fisc_resultados")` y su javadoc llama literalmente «la accion de `fisc_resultados`»—, y es el único camino por el que un dato de fiscalización pasa a ser el dato oficial del padrón. Lo que le falta son los cuatro datos que esa transferencia exige —`nLiquidacion`, `documentoSustento`, `sustento`, `baseLegal`— y que su catálogo no dibuja: **no declara ni una sección**. Censo: `sin-backend` 37→36, `sin-campo` 15→16. **Y los cinco filtros estaban vivos, en dos formas distintas**: los dos del programa (`tipo`, `estado`) **no llegan** —la lectura de #431 acota por número y por ejercicio, y `parametrosDeBusqueda` descarta lo que el contrato no declara, así que se tecleaban y se caían en silencio—; los tres de resultados **llegan y se rechazan** —«Programa» ofrece códigos («PF-2026-014») donde el controlador pide el id interno y ni siquiera tiene «Todos»; dos de las cuatro opciones de «Hallazgo» no son `CondicionFiscalizada`; y las **cuatro** de «Estado» tampoco—. **Las tres actas siguen sin conectar, y sus listas de `campos` estaban cortas**: las tres omitían el `fiscalizador`, que los dos controladores pasan por `exigir`. La respuesta a la pregunta del issue —qué sale de la fila del programa— es **uno de los tres**: `ProgramaResource` publica `id`, `codigo`, `descripcion`, `tipo`, las dos fechas y el estado; `predioId` y `contribuyenteId` salen de otros módulos, que es legítimo. Lo que de verdad para al acta predial es otra cosa: **`Hallazgo` declara cuatro valores y el desplegable del manual ofrece seis, y ninguna de las seis es ninguno de los cuatro letra por letra**; el campo es opcional, así que declararlo tal cual deja el acta entrando con 201 y sin hallazgo —en la vehicular `LiquidarFiscalizacion.condicionVehicular` lee `null` como CONFORME; en la predial se pierde `NO_UBICADO` y un «PREDIO INEXISTENTE» se compara por área como si el predio se hubiera hallado—. No se traduce ninguno, por lo mismo que #427 no tradujo «ACTIVA» a VIGENTE |
| La ventanilla cobra: la única pantalla por la que entra dinero (#430, 6 pruebas nuevas, 1 camino de Playwright y el censo recompuesto) | Cuatro roturas: quitar el mapa de valores de la fila; **regenerar la clave de idempotencia en cada envío**; quitar la selección de filas; y declarar el medio de pago con la clave del campo del catálogo | 1, 3, 5 y 5 en rojo. **La primera es la que este issue existía para no dejar suelta, y su síntoma es invisible**: sin el mapa, el cuerpo sale con `tributo` y `ejercicio` —los que la celda dibuja— y **sin `predioId`**; `ClaveDeSaldo` compara la obligación por (contribuyente, tributo, ejercicio, periodo, predioId, vehiculoId) con igualdad exacta, así que el cobro abonaría sobre la obligación **sin unidad** de ese contribuyente en vez de sobre la que se marcó: dinero recibido y aplicado a otra deuda, sin que ninguna cifra parezca mal. **La segunda es el AC 4**: dos envíos del mismo intento son **uno** para el servidor —`recibo_idempotencia_uq` (V29) lo garantiza del otro lado—, y regenerar la clave convertiría un reintento en un segundo cobro; muerde en tres pruebas, dos de ellas del camino común desde #331. **Y la cuarta es RNF-080 medido**: reusar la clave `formaDePago` del catálogo deja el control añadido sin declarar —«no declara el campo «medioDePago» en escrituras.ts»— y, con él, un desplegable que se ve y no viaja. Lo que hacía falta eran **cuatro** cosas y la entrada de `ACTOS_SIN_CAMPO` nombraba **una**: el medio de pago —«Forma de pago» del prototipo es el *tipo de cobranza*, y de sus nueve opciones sólo dos llegarían a cobrar; «A CUENTA» ni siquiera existe con ese nombre en el enumerado—, la caja, el cajero y las deudas, que salen de la grilla que la pantalla ya leía con el patrón de #332. Censo: `declarada` 22→23, `sin-campo` 16→15. **`caja_tasas` se queda, y ya no por el mismo motivo que su gemela**: exige al menos un concepto del TUPA y **ninguna consulta publica ese catálogo** —`TasaRepository` tiene un solo método y no hay `GET /tesoreria/tasas`—, así que su lista pasa de nombrar uno a nombrar los cuatro. Y **la primaria de `fraccionamiento` no se declara a propósito**: lo único que el código atribuye a un botón es la simulación, y nada dice cuál de «Fraccionar» y «Aceptar» registra —declarar el rótulo equivocado es el defecto que `LA_QUE_ESCRIBE` existe para impedir— |

| La categoria constructiva llegaba hasta la I y la norma llega hasta la J (#436, `V58`: 1 prueba nueva y la comprobacion contra PostgreSQL) | Devolviendo `ULTIMA` a `'I'` en `CategoriasConstructivas` | 1 en rojo, nombrando el caso. **Y el defecto no lo encontro una revision sino leer los cuatro anexos**: el cuadro de la Costa —el unico transcrito— tiene nueve categorias, y de ahi salio el rango; el Anexo I.4, para la Selva, tiene **diez** —la J es «CAÑA GUAYAQUIL PONA O PINTOC»—. Estaba en **ocho** `CHECK` del esquema, en el dominio y en la caja de la pantalla, y **no esperaba a que se cargara ningun cuadro**: con `A..I`, una municipalidad de la Selva no podia fichar una construccion de ese material. Medido contra PostgreSQL: antes de `V58` el `INSERT` de la `J` da «violates check constraint», despues entra, y una `Z` sigue rechazada. La migracion **solo amplia**, asi que ninguna fila existente puede violarla y no hay reescritura de tabla |
| El ambiente desplegado se comprueba **en CI**, no a mano (#434, #435) | Declarando en `Pulumi.stg.yaml` un `sha` que no esta en el clon | El guion **no concluye** en vez de contar las migraciones del arbol de trabajo, que seria otra version: «esta comprobacion NO se hace (no pasa: no se hace)». Por eso los dos trabajos de despliegue llevan `fetch-depth: 0` — sin el, la comprobacion daria un rojo por un motivo que no es el que mide en cuanto alguien anada una migracion despues del ultimo despliegue. **Y el guion entra a `infra.yml` porque si no, no lo corre nadie**: es el mismo defecto que #435 encontro en `verificar-rotacion.sh`, escrito en #154 y sin ejecutar desde entonces, y no tenia sentido repetirlo el mismo dia. De paso, correr la rotura encontro una errata propia: los acentos graves de un mensaje de aviso se ejecutaban como orden, y el guion intentaba un `git fetch` de un sha inventado |
| Un catalogo nacional no mide aislamiento, y meterlo en CI lo destapo el primer dia | Quitando del selector de la tabla de medida el `municipalidad_id NOT NULL` | 4 en rojo: «sgtm_app no filtra por municipalidad: propias=492, ajenas=492». **Y no hubo que provocarla: paso sola en la primera corrida en CI.** Tras publicar las 492 filas de `depreciacion` en `stg` (#438), esa pasó a ser la tabla con mas filas y el guion la eligio — pero `depreciacion` es un **catalogo nacional** (D-13, ADR-0017): su `municipalidad_id` es nulo y su politica dice `municipalidad_id IS NULL OR ...`, de modo que **todo contexto ve sus 492 filas, que es lo que tiene que hacer**. El guion estaba midiendo el aislamiento sobre la unica clase de tabla que por diseño no aisla, y lo reportaba como una fuga. El criterio correcto es el mismo con que la prueba del esquema separa tenant de catalogo — `municipalidad_id NOT NULL` —, y con el vuelve a elegir `acceso`: 134 propias, 0 ajenas, 402 para el superusuario |
| El catálogo del TUPA que `caja_tasas` necesita: lo que falta es el cuadro, no la lectura (#430, 1 prueba nueva —el censo de la deuda— y el hallazgo escrito) | Dos roturas: **publicar en el contrato un `GET /tesoreria/tasas`**, y quitar `conceptos` de la lista de datos que la franja nombra | 1 y 1 en rojo, y la primera **también pone rojo `comprobar-operaciones`**: el contrato no se edita a mano. **Lo que este issue existía para construir no se construyó, y ése es el resultado**: se fue a publicar la lectura del catálogo y lo que falta no es la lectura. (1) **La tarifa nunca la manda el cliente** —`PeticionDeConcepto` lleva el código del concepto y su cantidad, y el importe lo resuelve el servidor con `TasaRepository.vigenteA(codigo, fecha)`, con `TasaSinTarifaVigente` y `TarifaEnCero` como sus dos negativas—, así que sin catálogo no hay de dónde elegir el código. (2) **Nada en producción escribe la tabla `tasa`**: los únicos `INSERT INTO tasa` del repositorio están en fixtures, exactamente la situación que `area` y `caja` tenían antes de #460, así que una lectura publicada hoy devolvería una lista vacía en toda instalación real y conectar la pantalla cambiaría una franja que **nombra lo que falta** por una grilla vacía que se lee como «esta municipalidad no cobra tasas» —el retroceso que #78 y #80 se negaron a hacer—. (3) **Y sus cifras son D-02b, fila 29**, «derecho de trámite del TUPA» con su acuerdo de ratificación provincial, cuyos issues la propia decisión nombra (#197 entre ellos): el corpus tiene **una** transcripción VERIFICADA a doble firma —la del piloto, `derecho-tramite-licencia-edificacion-catacaos-2023.md`— y ese archivo dice **«No se carga con este archivo»**, la encamina a `parametro_tributario` con el derivado de `publicacion/` **cuando #197 resuelva cómo modelar una tarifa por modalidad**, deja la **ratificación provincial sin confirmar** como pregunta legal abierta, y cubre sólo la licencia de edificación: el resto del TUPA de Catacaos —incluida su sección de tributación— está en páginas sin transcribir. (4) Y la tabla pide dos datos que ninguna ordenanza da: `partida_presupuestal` y `area_id`, los dos `NOT NULL` (V3). Así que se paró y se escribió, y el censo de la deuda queda **medido**: una prueba comprueba que la única operación del contrato con `tasas` en la ruta es el `POST` que cobra, y se pondrá roja el día que la lectura exista — que es justo el día en que hay que releer lo que aquí quedó anotado |

| Diez cifras firmadas que nadie habia pasado al derivado publicable (#438: el conjunto de 2026 pasa de 22 filas a 32, medido contra `stg`) | **No hizo falta provocar ninguna: el hueco lo delataba el propio recuento del verificador** — «VERIFICADO y sin publicar (11)» | `alcabala.md` y `espectaculos.md` estaban `VERIFICADO`, eran de norma nacional, tenian sus dos firmas, y su §2 decia de si mismos —los dos, con la misma frase— «**No se carga con este archivo. Se carga con el derivado de `publicacion/`**». No habia ninguna fila suya ahi, y **no habia un motivo escrito en ninguna parte**: nadie las habia pasado. Es la clase de hueco que `verificar-publicacion.mjs` no puede ver, porque comprueba que **lo que esta en el derivado** este en el corpus, no que **lo que esta en el corpus** este en el derivado. Con las nueve filas nuevas entro la decima, `FACTOR_OFICIALIZACION`, que si esperaba a algo —la segunda firma que #436 le dio—, y con ella **uno de los cuatro factores de D-11 deja de estar solo transcrito y pasa a estar publicado**. Publicadas de verdad contra `stg`: 9 y 1, `RECHAZADAS=0` en las dos corridas, y el conjunto queda en 33 filas de `parametro_tributario`. El verificador **hizo su trabajo cuatro veces mientras se anadian**: rechazo la fuente compuesta a mano en vez de copiada del corpus, el articulo que no era el de la cabecera, un `valor_texto` recompuesto de dos lineas del archivo, y una fila con una coma sin entrecomillar |
| Las partidas del cuadro son TRES y las de la ficha SIETE, y no eran la misma cosa (#436, `V59`: 6 pruebas nuevas, 3 contra PostgreSQL real) | Tres roturas: devolver `PISOS` al `CHECK` del cuadro; devolverlo al enum de catastro; y devolverlo al de licencias | 1, 2 y 1 en rojo. **Y la primera vez que se midieron, las tres pasaron en VERDE**: nada sostenia la decision, asi que el «arreglo» obvio ante una discrepancia futura —volver a igualar los dos vocabularios— la habria deshecho sin ruido. Peor: el propio esquema lo invitaba, porque `V1` afirmaba que eran «las **dos mitades de la misma matriz**» y `V43` lo repetia. **Lo decidio de donde viene cada uno, no una preferencia**: las siete no salen de ninguna resolucion —NEG-05 §RT-002 las clasifica bajo «Confirmado por los **manuales**», y el prototipo del manual las dibuja en dos pantallas de ficha—, mientras las tres son las de la R.M. 277-2025-VIVIENDA, cuya nota al pie dice «SUMANDO LOS VALORES SELECCIONADOS DE CADA UNA DE LAS **3 COLUMNAS**». Se pudo estrechar sin migrar nada: `valor_unitario_edificacion` y `edificacion_estructura` tienen **cero filas** en `stg` y en `prod`. La cuarta rotura —recortar la ficha a tres «para igualarlas»— la caza el **compilador** antes que ninguna prueba. Y `V59` remata lo que `V58` se dejo: el FUE seguia en `A..I`, de modo que una construccion de la Selva se podia fichar y publicar pero **no declarar** |
| Encender una ruta deja de ser una linea sin dueno: la guarda de `YA_SERVIDAS` y el censo de lo que falta para apagar el proxy (#400, 28 pruebas nuevas) | Nueve roturas: una errata de una letra en la ruta; una ruta que ninguna pantalla consume; una escritura cuya respuesta se lee y el proxy todavia no le da la forma del backend; la misma entrada dos veces; **encender una que si esta lista** (contraste); quitar el `encadenada` que expone la segunda lectura; quitar del proxy la forma real del panel de recaudacion; que una peticion directa declarada deje de pedirse; y que una escritura deje de leer su respuesta | 4, 3, 3, 4, **0** —pasa, y lo unico que se mueve es el censo, que es justo lo que tiene que pasar—, 2, 2, 1 y 1 en rojo. **Y la octava no mordio a la primera, que es donde estaba el defecto de la propia guarda**: comprobaba las tres listas declaradas con las tres evidencias en un `||`, y `alGuardar` —que solo dice algo de una— aparecia de paso en los archivos de las otras, asi que cambiar el `pedirOperacion('sectores')` del asistente de alta por otra operacion dejaba las 28 en VERDE. Con cada lista contra su evidencia, muerde; y su limite quedo medido y escrito: la mencion sobrevive en el `queryKey`, y lo que se caza es que el archivo deje de nombrarla. **El censo dice hoy 0 encendidas, 127 listas y 52 pendientes de 179**, y lo que descubrio de paso es que `GET /portal/deuda` —la vista del funcionario— es la **unica** operacion del contrato que ningun controlador sirve **y ninguno va a servir** (ADR-0016 §3): mientras eso no se decida, el proxy no se puede apagar del todo, porque esa pantalla se quedaria sin nadie que le conteste |
| Lo que el proxy publica es lo que el backend publica, **derivado en vez de copiado a mano** (#400: `docs/50-api/formas-de-la-api.json` con las 178 operaciones, 3 pruebas de backend y 95 de frontend) | Seis roturas: retocar a mano un campo del archivo de formas; que la variable de tipo del sobre paginado deje de resolverse; devolver el defecto del dinero; publicar en el proxy un campo que el `Resource` no tiene; dejar de publicar uno que si tiene; y sacar una operacion de la lista de «sin forma comparable» | 1, 3, 2, 1, 1 y 1 en rojo. **Y encontro siete defectos vivos antes de que ninguna mutacion la probara**: el proxy inventaba un `id` en los cinco padrones de transito y de infracciones —`PapeletaDelPadronResource` no lo tiene—, una `unidad` en las participaciones de bienes comunes y un `usuarioId` en los permisos de un grupo, que es justo la clase de defecto que #379 encontro a mano para **una** ruta y aqui sale de las 178 sin lista que mantener. **El del dinero es el que mas dice**: `Dinero` es un `record`, asi que el volcado lo describia como `{valor: numero}` cuando `ConfiguracionDeJson` lo serializa con `writeString` (RNF-055) — y eso no ponia nada en rojo, porque el comparador ve una cadena donde la forma dice objeto, no puede comparar y **se calla**: todos los campos de importe estaban fuera de la comprobacion sin que nadie lo supiera. **Y la del sobre paginado hizo falta doble y despues ninguna**: la sustitucion de la variable de tipo estaba escrita en dos sitios, de modo que quitando cualquiera de los dos el otro la resolvia igual y las tres pruebas seguian en VERDE; dos caminos para lo mismo no son una red, son una regla que no se puede comprobar, asi que se dejo uno y entonces la rotura pone las tres en rojo |
| Que la forma descrita sea la que Jackson emite, hecho emitir en vez de razonado (#400, 2 pruebas mas) | Devolviendo el defecto del dinero: el resolutor mira «es record» antes que «se serializa como cadena» | 1 en rojo, y **es la unica prueba del repositorio que lo caza**. Las demas no pueden: el comparador del frontend ve una cadena donde la forma dice objeto, no compara y se calla. Aqui se serializa una respuesta con el mismo modulo que registra la aplicacion —el sobre paginado, los cuatro objetos de valor, un record anidado, una lista, una fecha, un instante, un enumerado y un nulo— y se comparan los dos arboles de claves. **Y no son las 178 a proposito**: instanciar cada recurso exigiria inventar un valor valido para cada componente y varios objetos de valor validan en su constructor, asi que la mitad se caeria construyendo y una prueba que salta la mitad de sus casos no dice lo que parece decir |

| El valor neutro del `% actualizacion` es CERO, no cien (#437: la primera determinacion real del SRTM con cifras) | La misma de siempre —quitar `ACTUALIZACION` y `FACTOR` de la regla 5— sigue dando «Expected size: 2 but was: 0» | Lo que cambio no es la guarda sino **cual es el valor peligroso**. La captura de la pestana «Datos» del manual `M02-1-020`, leida **como imagen**, trae una determinacion entera: autovaluo `171 179,42`, `% actualizacion 0,00 %`, `% propiedad 80,00`, base imponible `136 943,54`. Y `171 179,42 × 0,80` da **exactamente** esa base — de modo que el porcentaje **no multiplica como factor**: si lo hiciera, con `0,00` la base seria cero. Es un **incremento**, y su neutro es `0`. Eso corrige la secuencia que NEG-05 §0.1 describe («autovaluo → × % actualizacion → × % propiedad») y, sobre todo, la muestra: decia que el valor «obvio» era `1`, y es `0` — que es peor, porque un `BigDecimal.ZERO` en un campo llamado «porcentaje de actualizacion» se lee como «no aplica» todavia mas que un uno. **Y el manual solo lo daba en una captura**: su capa de texto nombra la columna y no la define, asi que este hallazgo no existia para ningun `grep` |
| Las tres primeras rutas encendidas, vistas leer del backend (#400: `YA_SERVIDAS` pasa de 0 a 3) | **No hizo falta provocar ninguna rotura: la marcha blanca las encontro sola.** Se levanto la pila entera sin Docker —PostgreSQL 16 nativo, las 51 migraciones como `sgtm_owner`, la municipalidad implantada, 15 vias, 4 sectores, 15 manzanas, 16 contribuyentes y 23 predios sembrados, la aplicacion en perfil `web` y un emisor OIDC minimo en lugar de Keycloak— y se abrieron las pantallas en Chromium | Las tres dibujan **el dato de la base y no el del prototipo**: «Cayetano Heredia» donde el juego de datos dice «JOSÉ DE LAMA», con `Authorization` en las cuatro peticiones. **Y de las 71 lecturas sin parametros del contrato, 24 contestan 500**: catorce por el hueco de `SET LOCAL` —el controlador o el repositorio resuelven fuera de transaccion, asi que la politica RLS los rechaza con «invalid input syntax for type bigint: ""»— y diez porque un parametro obligatorio ausente sale como 500 en vez de 400. Entre las catorce estan `GET /rentas/contribuyentes` y **las cuatro fichas individuales**: `FichaController.predioDe` resuelve el predio desde el controlador, que es exactamente lo que el javadoc de `InscribirFicha` advierte y lo que `ConsultaDeVias` ya cerro una vez. Ninguna prueba podia verlo: las de modulo llaman al caso de uso con su propio proxy transaccional. **Y encender las tres destapo de paso lo que la lista significa**: `YA_SERVIDAS` dice lo que sirve el backend **en el entorno donde corre la aplicacion**, y bajo el corredor de pruebas no corre ninguno — dejar pasar la peticion puso **69 pruebas rojas en 12 archivos**, ninguna por un defecto de la interfaz. La lista por omision es vacia bajo pruebas; devolverla a la declarada vuelve a poner 46 en rojo |

| El CRUD de predios en catastro, y por donde entra un catastro real (#400, ADR-0021: 324 pruebas del modulo contra PostgreSQL con PostGIS, y la autoprueba del guion en CI) | Diecisiete roturas, y las que mas dicen son cinco: quitarle el `@Transactional` al acto que corrige el predio y versiona su ficha; tomar el bloque de correccion entero como el estado nuevo; `JOIN` interno sobre sector en el listado; quitar `CREATE EXTENSION postgis` del aprovisionamiento; y envolver el bucle de la carga del plano en UNA transaccion | 2, 1, 4, 4 y 3 en rojo. **Lo que este issue existia para arreglar no era una pantalla sino tres huecos**: los datos PROPIOS del predio —direccion, via, numero municipal, sector, manzana, lote, ubigeo y tipo— solo se podian escribir al inscribirlo, asi que una direccion mal tecleada al fichar era para siempre; `RegistrarPredio.darDeBaja` existia desde #290 sin que ningun endpoint la llamara; y **una municipalidad real no podia poblar su catastro**, porque `ImportarFichas` solo era alcanzable desde `CargarFichasDeDemostracion`, que exige `es_demostracion = true` —el mismo hueco exacto que #430 encontro para `area` y `caja`—. **La mitad de ADR-0021 es lo que la geometria NO hace**: `area_terreno` sigue siendo la que midio el tecnico, porque derivarla del poligono cambiaria el autovaluo de todo el padron sin que nadie lo decidiera y el error seria invisible —un area es indistinguible de otra al leerla—. Y **tres roturas pasaron en VERDE la primera vez, cada una por su propio motivo**: la del prefijo del codigo era un fallo de NullAway que el arnes leia como verde porque solo miraba la linea de pruebas fallidas; la del `@Transactional` del bucle no se aplicaba porque el importador se instanciaba con `new` en la prueba (la leccion de #430 con `ImportarCajas`) y, ya envuelto, **seguia sin morder por auto-invocacion** —la anotacion iba en la sobrecarga de tres argumentos y la prueba llama a la de dos—; y quitar la guarda de «el lote necesita su geometria» dejaba la fila rechazada igual, porque el motor rechaza la cadena vacia, asi que la prueba tuvo que pasar a comprobar el MOTIVO. Dos defectos los encontro escribir el guion: un WKT lleva comas y sin comillar parte el CSV en columnas —no falla ruidosamente, llega al backend con otro numero de columnas—, y un plano en UTM aceptado en silencio pondria los lotes del distrito en mitad del Atlantico, que ninguna consulta delata. Y dos referencias a la imagen de PostgreSQL habrian roto sin ruido: el simulacro de restauracion arranca el directorio restaurado, que trae los objetos de PostGIS en el catalogo |

**Sin Docker en la máquina, la prueba no se salta**: se apunta a un PostgreSQL existente con
`-Dsgtm.pruebas.postgres.url` ([`backend/README.md`](backend/README.md)).
