# GOB-02 — Decisiones abiertas

Lo que falta decidir, quién decide y qué queda bloqueado mientras tanto. Una decisión cerrada
sale de esta tabla y entra como ADR o como cambio en el documento que corresponda.

| # | Decisión | Decide | Bloquea | Estado |
|---|---|---|---|---|
| **D-01** | Municipalidad piloto y validador funcional de reglas tributarias. **Municipalidad piloto: Municipalidad Distrital de Catacaos** (decidido 2026-08-24 por Dirección del proyecto, en reemplazo de la Municipalidad Distrital de Chala que se había decidido el 2026-08-23). El validador funcional de reglas tributarias sigue sin nombrarse | Dirección del proyecto | La primera iteración de negocio completa | Abierta — **la municipalidad ya no bloquea** D-02b/E-6 (#202); el validador funcional, pendiente |
| **D-02b** | **Lo que fija una ordenanza de la municipalidad en materia tributaria**, con su **acuerdo de ratificación provincial** cuando la emite una distrital: filas 11-14, 18, 19, 25, 28 y 29 del mapa —arbitrios y sus criterios de distribución, descuento por pago adelantado, inafectaciones, anuncios, TIM, CUIS, interés y máximo de cuotas del fraccionamiento, y derecho de trámite del TUPA—. Sin ratificación no habilita emisión (ARQ-09 §2.4, riesgo R-04) | Rentas del piloto | #189, #191, #196, #197, #199 | Abierta — **la municipalidad ya está elegida (Catacaos, D-01)**; falta su ordenanza y la ratificación provincial (E-6, #202 sigue abierto por eso). **#33 la deja acotada en caja**: la cobranza registra qué campaña se declaró en `recibo.campania_beneficio` y cobra el importe **íntegro**; el día que la ordenanza se firme, el descuento se aplica sin cambiar la selección de deudas ni el recibo. **#35 la deja acotada en el fraccionamiento por el mismo camino**: el interés y el máximo de cuotas entran como **parámetro** desde el conjunto sellado (`INTERES_FRACCIONAMIENTO`, `CUOTAS_MAXIMAS_FRACCIONAMIENTO`), el convenio guarda de qué conjunto salieron, y **falta el parámetro ⇒ falla la operación** nombrando la llave; el gasto administrativo de la cuota queda en cero con su columna esperando. El mecanismo entero —preconvenio, formalización, acogimiento, quiebre— está construido y probado sin ninguna cifra dentro. **#72 la deja acotada en la simulación del acogimiento a una campaña de beneficio** (`consulta_deudas_beneficio`): la campaña, el porcentaje que condona, **sobre qué parte de la deuda se aplica** y con qué escala y modo se redondea el descuento entran como parámetro del conjunto sellado —`BENEFICIO:‹CAMPAÑA›` (valor numérico = alícuota, valor texto = base) y `BENEFICIO_REDONDEO:‹CAMPAÑA›` (valor numérico = escala, valor texto = modo)—; **el catálogo de campañas es el propio dato**, así que sin ninguna publicada la lista sale vacía y simular contra una responde **422 nombrando la llave que falta** |
| **D-02c** | **Lo que fija un acto propio de la municipalidad que no es ordenanza tributaria ratificada**: filas 23 y 26 del mapa —arancel de costas coactivas ‹confirmar quién lo aprueba› y descuentos por pronto pago de papeletas—. Ni se busca en El Peruano ni se ratifica: hay que producirlo | Rentas + asesoría legal | #193, #195, #196 | Abierta — la municipalidad ya está elegida (Catacaos, D-01); falta producir el acto propio. **#72 la deja acotada por el mismo camino que D-02b**: un descuento por pronto pago se publica como una campaña más del conjunto sellado (`BENEFICIO:‹CAMPAÑA›`), y mientras el acto no exista la pantalla lo dice en vez de suponer un porcentaje. **Y #634 hace lo mismo con el arancel de costas**: liquidar un expediente sin ninguna llave `ARANCEL_COSTA:‹TIPO›` publicada contesta **422 nombrando la llave** —antes decía «este expediente no tiene ningun acto pendiente de liquidar», que se lee como «no hay nada que cobrar»—, y que la ordenanza, existiendo, **no tarife** un acto sigue siendo una decisión suya que se respeta |
| **D-03d** | **Redondeo del importe a pagar en el cierre de caja**, que puede no ser el del cálculo | Tesorería + contabilidad | El cierre de caja | Abierta — [ADR-0018](../30-arquitectura/adr/ADR-0018-el-redondeo-decidido.md) cerró D-03a/b/c y **la dejó explícitamente fuera** |
| **D-04** | Estrategia de migración desde la base SQL Server del sistema actual: alcance, corte, conciliación de saldos. **Incluye la campaña de observación de los puntos de redondeo del SRTM del MEF** ([ADR-0018](../30-arquitectura/adr/ADR-0018-el-redondeo-decidido.md)): el piloto arranca con padrón nuevo, pero una municipalidad que migre saldos o determinaciones del SRTM exige reproducirlos céntimo a céntimo, y para eso hay que inventariar dónde redondea observándolo —el procedimiento vive en [`observaciones-srtm-mef/`](../10-negocio/observaciones-srtm-mef/README.md)— | Dirección + TI municipal | Implantación | Abierta |
| **D-05** | Régimen de firma digital de valores, resoluciones y constancias | Asesoría legal | La capa de generación de documentos | Abierta — **no bloquea la emisión**. `PuntoDeFirma` (sgtm-plataforma) es el enganche, entre generar los bytes y entregarlos, y `GeneradorDeDocumentos.generarFirmado` pasa por él en cada emisión; su implementación por omisión devuelve el documento sin tocar. #41 lo estrena con la REC-1 y la REC-2: salen **sin firma digital** y son imprimibles igual, con el bloque de firmas manuscritas del ejecutor y del auxiliar en el pie. Cerrar D-05 será dar una implementación de esa interfaz, no repasar los sitios que emiten |
| **D-06** | Nombre del claim que lleva la lista de municipalidades autorizadas de un usuario con acceso a varias | Arquitectura + operación de identidad | La verificación de pertenencia en `TenantContextFilter` | Abierta |
| **D-08** | Retención de la auditoría y del histórico de fichas: años en línea, política de archivado | Rentas + TI | El plan de particionado a largo plazo | Abierta |
| **D-09** | Numeración de valores y expedientes: correlativo por municipalidad y ejercicio, con qué formato y qué reinicio | Rentas del piloto | La emisión de valores | Abierta — **no bloquea**, porque el formato está afuera. `valor_correlativo` (V26), `convenio_correlativo` (V31) y `expediente_correlativo` (V33) garantizan que el correlativo no se repita ni salte; la **composición** del número del expediente coactivo es un parámetro (`PlantillaDeNumeroDeExpediente`, #40), con `EXP-{ejercicio}-{correlativo:6}` por omisión y el correlativo desnudo guardado aparte, de modo que cerrar D-09 sea cambiar la plantilla y no migrar la columna. Mismo precedente que D-10 con `ComposicionCatastral` |
| **D-10** | **Longitud exacta del código de referencia catastral.** La plantilla del manual (`DDPPddSSMMMLLLEEeeppUUU`) da 23 posiciones; los ejemplos del prototipo de interfaz traen 21 | Catastro del piloto | La validación del código y la carga de fichas | Abierta |
| **D-11** | **Origen y valor de los cuatro factores sin fuente** que reveló el manual M02 del MEF (`../srtm` NEG-05 §0.1): deducción de Amazonía, `% actualización`, incremento del 5 % sobre el valor unitario **antes** de depreciar, y factor de oficialización de obras complementarias. Los cuatro multiplican o restan sobre importes | Rentas del piloto + asesoría legal | `RT-002`, `RT-005` y `RT-011`; se suma a D-02a | Abierta — **tres de los cuatro ya tienen fuente leída en el PDF oficial y transcrita en el corpus**: el **factor de oficialización** es `Fo = 0,68`, del Anexo II de la R.M. 277-2025-VIVIENDA (`obras-complementarias-y-oficializacion-2026.md`, fila 10 del mapa); el **incremento del 5 %** es una nota al pie del propio Cuadro de Valores Unitarios, «EN EDIFICIOS AUMENTAR EL VALOR POR M2 EN 5 % A PARTIR DEL 5° PISO» (`valores-unitarios-2026.md`, fila 7) —falta saber si es único o acumulativo—; y la **deducción de Amazonía** tiene norma (Ley 27037 art. 18 y D.S. 031-99-EF) y no tiene cifra nacional, porque el porcentaje lo fija anualmente cada municipalidad de la Amazonía (`predial-deduccion-amazonia.md`, fila 32, que nace en D-02b). El de los valores unitarios está `VERIFICADO` desde el 2026-08-29 y el de las obras complementarias desde el 2026-08-30; el de Amazonía sigue en `TRANSCRITO`. **Y del incremento del 5 % ya no falta la mecánica**: se resolvió el 2026-08-30 (#436) contra los manuales del SRTM — es **único**, no acumulativo, se aplica antes de depreciar, y su umbral y su porcentaje son **parámetros**, no constantes. **Queda uno solo: el `% actualización`, y sigue sin fuente.** La hipótesis del art. 14 del TUO LTM —la actualización de valores de la emisión mecanizada— **se descartó el 2026-08-30 (#437)** contra los manuales del SRTM del MEF, que es donde se confirma un campo del proceso: ese acto está documentado ahí de punta a punta (Proceso Masivo v3.1.0, Proceso Individual v4.0.0, migración de DJ v6.1.0, Guía del MEF §4.4) y en todos es una **redeterminación** con las tablas del ejercicio nuevo, nunca un porcentaje sobre el autovalúo; y en las mismas pantallas donde los otros dos factores sí aparecen con nombre —el 5 % y el `0.68`—, este no está, así que la ausencia no es un punto ciego. Lo que la búsqueda dejó en su lugar es un **candidato nuevo, normativo**: el párrafo del TUO LTM que actualiza por Decreto Supremo la base imponible del año anterior cuando en un ejercicio **no se publican** los aranceles ni los valores unitarios, con tope en la variación de la UIT — el único porcentaje de actualización de la base imponible del predial que la ley tiene, y encaja con la posición de la columna en M02. Todo eso vive en `valores-normativos/predial-porcentaje-de-actualizacion.md` (fila **33** del mapa, que hasta hoy no existía: un dato sin fila es un dato que nadie va a buscar). **Y el 2026-08-30 se descartó también el camino que parecía obvio**: conseguir `M02-1-020`, el manual del SRTM donde la columna se vio. Ya está leído entero —es el único de los 74 con cobertura «Completa»— y lo único que aporta es **el nombre de la columna en el encabezado de una grilla**; no la define, igual que no define los otros cuatro conceptos nuevos de esa misma sección. **Y el 2026-08-30 se leyeron los dos**: los manuales de «Parámetros» del SRTM —el de la CF2 y el de la CF4— tampoco lo definen (el de la CF2 configura la emisión masiva; el de la CF4, catálogos del buzón electrónico), pero **la captura de una determinación real** del propio M02, leída como imagen, sí enseñó algo: con `% actualización = 0,00 %`, la base imponible es exactamente `autovalúo × % propiedad`. Es decir que **no multiplica como factor** —si lo hiciera, con 0,00 la base sería cero— sino que es un **incremento cuyo valor neutro es 0, no 100**. Eso corrige la descripción de NEG-05 §0.1 y, sobre todo, cuál es el valor peligroso: un `BigDecimal.ZERO` en un campo llamado «porcentaje de actualización» se lee como «no aplica» todavía más que un 1. Lo que sigue sin saberse es **qué pasa cuando no es cero y quién lo fija**. **Mientras tanto no hay valor por omisión**, y la regla 5 lo sostiene: `ACTUALIZACION` y `FACTOR` entraron a los nombres vigilados con su muestra (#437) |
| **D-16** | **Con qué vocabulario se anota el hallazgo de una fiscalización, y qué significa un acta sin él.** El dominio tiene cuatro palabras —`CONFORME`, `OMISO`, `SUBVALUADOR`, `NO_UBICADO` (`Hallazgo.java`)— y el prototipo dibuja **seis** en el acta predial y cinco en la vehicular; de todas ellas **sólo `CONFORME` coincide letra por letra**, y es justo la que esconde a omisos y subvaluadores, que son el objeto del módulo. No es una cifra tributaria: es el vocabulario con que un fiscalizador cierra un acta, así que **no hay norma nacional que lo conteste** y no cae bajo D-02. Tres caminos: **A** ampliar el enumerado del dominio al vocabulario del manual; **B** derivarlo en el backend de hechos ya escritos, como #397 hizo con la fase de la infracción administrativa; **C** acotar el desplegable a lo que coincide letra por letra, como #427 hizo con `licencia_padron`. **Y una cuarta pieza que no es alternativa sino mitad de todas**: qué significa un acta sin hallazgo | Fiscalización o Rentas del piloto (Catacaos, D-01) | #431 —las tres actas de fiscalización, que siguen en `ACTOS_SIN_CAMPO` | Abierta — **una de las tres mitades ya no depende de ella y es la que hace daño hoy**: el `CHECK` de `acta_fiscalizacion.hallazgo` es nulable y `LiquidarFiscalizacion` lee el nulo como `CONFORME`, así que `POST /fiscalizacion/vehicular` sin hallazgo responde **201** y esa acta se liquida conforme — un vehículo que nadie inspeccionó, declarado en regla. Cerrar eso es el patrón de siempre (fallar nombrando lo que falta, como #51, #72 y #399) y **se puede hacer sin decidir el vocabulario**. Lo que la zanja: el manual, cap. 3 §Fiscalización, figuras 100-180 —si los seis rótulos están en la captura o son del autor del prototipo; si la pantalla vehicular es un panel de resultados de un cruce, y entonces «Hallazgo» es un **filtro** que el `POST` está leyendo como una conclusión; y qué acto produce «BAJA INDEBIDA»—. **Esa última ya tiene respuesta parcial y no es de vocabulario**: `EstadoVehiculo.BAJA` está declarado y **nada en `src/main` lo escribe**, así que el sistema no sabe dar de baja un vehículo y no puede tener una baja indebida; es un hueco de dominio, no un rótulo que traducir. La evidencia más directa que nadie ha abierto todavía es `generaDeterminacion`, el checkbox que el manual dibuja **tres líneas debajo** del desplegable en disputa: dice si el hallazgo es un juicio del fiscalizador o un derivado del sistema. **Y el 2026-09-02 (#599) se cerró el sexto rótulo, que era el único que no se podía cerrar por vocabulario**: «USO DISTINTO AL DECLARADO» nombraba algo que el enumerado no sabía decir, y lo que le faltaba no era una palabra sino **dónde sustentarlo** — `acta_fiscalizacion` guardaba el área y ninguna columna de uso. Con `uso_hallado` (`V76`), `Hallazgo` gana `USO_DISTINTO` y los dos enumerados pasan a tener los mismos cinco valores; siguen siendo dos conceptos —uno se **anota** y el otro se **deriva**—, y la guarda que lo impedía unificar se reexpresó en vez de borrarse. Los otros cinco rótulos siguen sin coincidir letra por letra y **siguen sin traducirse** (camino C, el de #427): son `CONFORME`, `SUBVALUADOR` ×2, `OMISO` y `NO_UBICADO` con otras palabras, y elegir cuál es cuál es lo que esta decisión existe para no hacer en silencio |
| **D-14** | **La regla de imputación de un pago parcial**: qué parte de la deuda extingue —insoluto, reajuste, interés o gasto, y en qué orden entre obligaciones—. Es normativa (TUO del Código Tributario, art. 31), no una decisión de diseño: hay que transcribirla y firmarla, no elegirla. **#33 la declinó** para `A_CUENTA` y **#35 la vuelve a declinar** para `CUOTA_CONVENIO`, con la consecuencia que hace segura la omisión: mientras la caja rechace los dos tipos, ningún pago parcial entra, y por eso el quiebre de un convenio nunca tiene que repartir nada —devuelve lo pendiente entero, que es exactamente lo acogido— | Rentas + asesoría legal | `TipoDePago.A_CUENTA` y `TipoDePago.CUOTA_CONVENIO`; con ellos, el cobro de cuotas del convenio y su seguimiento real | Abierta — no bloquea el ciclo de vida del convenio, que está completo y probado sin ella |
| **D-15** | **Quién acredita el `numero_documento` del token del ciudadano.** Todo el portal se sostiene sobre que ese número sea de quien lo presenta: un realm donde el ciudadano se registra solo y **declara** su DNI convierte la enumeración por parámetro en una enumeración firmada, y para siempre. Tres caminos: **A** federación con la identidad nacional (ID Perú / RENIEC), **B** enrolamiento en ventanilla —el funcionario, con el documento delante, crea o vincula la cuenta—, **C** autorregistro con un factor de verificación (código impreso en un recibo, envío al domicilio fiscal) | Dirección del proyecto | La identidad del ciudadano, y con ella todo el camino del portal (#57) | **Decidida el 2026-08-29: camino B**, enrolamiento en ventanilla, por el mecanismo declarativo de [ADR-0012](../30-arquitectura/adr/ADR-0012-usuarios-y-grupos-declarativos.md) extendido y con su rastro de auditoría. **A es el destino y no se puede hacer hoy** —exige un convenio y unas credenciales de federación que no existen; implementarlo sería escribir contra un emisor que no está—, y **C es más débil que B y más caro**, porque acredita menos y además exige un canal de entrega y su operación. Sea cual sea el camino queda fijado, y ya está aplicado: `numero_documento` **no es editable por el usuario** en el realm del ciudadano, `registrationAllowed` sigue en `false`, y el mapeador del claim lee **ese atributo** y no un campo del formulario. **Construida el 2026-08-29** (#415): el acto es «declarar y reconciliar» —[`despliegue/identidad/ciudadanos/<ubigeo>.json`](../../despliegue/identidad/ciudadanos/README.md), sin una sola clave, aplicado por el mismo guion de ADR-0012 en su segundo modo—, la cuenta se **deriva** del documento (`dni-70123456`, con el tipo delante porque `CE 12345678` y `DNI 12345678` son dos personas), y el número declarado tiene que figurar en el padrón de esa municipalidad: se cruza a tres bandas contra la base. **Las 134 siguen siendo 134.** Ver [ADR-0020](../30-arquitectura/adr/ADR-0020-la-sesion-del-ciudadano.md) §4 y §6 |

| **D-17** | **El padrón de administrados cuando la caja cobre lo que no es tributo.** Un mercado, un nicho o un estacionamiento se le cobran a alguien que puede no estar en `contribuyente`. Dos caminos: **A** un registro de personas compartido por los cuatro sistemas —hoy `contribuyentes` es la base del grafo y no depende de nadie ([ARQ-01 §3.1](../30-arquitectura/contextos-acotados.md)), sacarlo multiplica por cuatro las llamadas de cada pantalla—; **B** `caja` guarda su propio pagador y sólo enlaza al contribuyente cuando lo hay | Dirección del proyecto | La fase 4 de [ADR-0029](../30-arquitectura/adr/ADR-0029-cuatro-sistemas-separados.md): abrir la caja a conceptos no tributarios | Abierta — **condicionada al bloque ADR-0024…0032** (`Propuesto`). Hoy no bloquea nada: mientras ADR-0003 siga vigente el sistema es un monolito modular |
| **D-18** | **La clave foránea que se pierde al separar catastro.** `declaracion_jurada.predio_id`, `determinacion.predio_id` y `cuenta_corriente_asiento.predio_id` dejan de poder referenciar `predio`. Dos caminos: **A** referencia validada contra una proyección local alimentada por evento —más limpio y más frágil, porque cambia una garantía del motor por una invariante que hay que mantener viva—; **B** replicar `predio` entero en `rentas`, que es copiar el modelo ajeno con todos sus invariantes y acabar con dos definiciones de «versión vigente a una fecha» envejeciendo aparte, lo que el javadoc de `FichasDelPadron` ya descarta por escrito | Arquitectura | La fase 2 de ADR-0029 | Abierta — **condicionada al bloque ADR-0024…0032** (`Propuesto`). Hoy no bloquea nada: mientras ADR-0003 siga vigente el sistema es un monolito modular |
| **D-19** | **Cómo llega el catálogo de accesos a cuatro sistemas.** [ADR-0013](../30-arquitectura/adr/ADR-0013-permisos-de-la-sesion.md) decidió que los permisos vienen del servidor y no del token, y sigue valiendo; lo que falta es cómo lo consultan los otros tres. Caché con TTL corto y evento de revocación, replicación del catálogo, o un servicio de autorización con presupuesto de latencia declarado | Arquitectura + operación de identidad | La fase 1 de ADR-0029 | Abierta — **condicionada al bloque ADR-0024…0032** (`Propuesto`). Hoy no bloquea nada: mientras ADR-0003 siga vigente el sistema es un monolito modular |
| **D-20** | **Qué dice el recibo cuando la imputación es asíncrona.** Con [ADR-0026](../30-arquitectura/adr/ADR-0026-el-camino-del-dinero.md), `caja` cobra contra una orden y `rentas` imputa después; hoy el recibo y el detalle de imputación salen juntos. Si eso no es aceptable para el contribuyente, la salida es una imputación previsualizada por `rentas` al emitir la orden, y cuesta una llamada síncrona en ventanilla | Rentas + tesorería del piloto (Catacaos, D-01) | La fase 3 de ADR-0029 | Abierta — **condicionada al bloque ADR-0024…0032** (`Propuesto`). Hoy no bloquea nada: mientras ADR-0003 siga vigente el sistema es un monolito modular |
| **D-21** | **Dónde se aplica el `% de propiedad`.** [ADR-0024](../30-arquitectura/adr/ADR-0024-la-frontera-del-calculo.md) propone que lo aplique `rentas` —catastro publica cuánto vale el predio y quiénes lo tienen; decir cuánto le toca a cada uno ya es una base imponible—, pero hoy el javadoc de `ReglaDeAgregacion` dice que se aplica antes, en el grafo por partida. Cambia el ámbito de varias `RT-xxx` y hay que revisar la lista contra `../srtm` NEG-05 §1 fila a fila | Arquitectura + validador funcional (D-01) | La fase 2 de ADR-0029; ninguna regla se implementa con el ámbito sin revisar | Abierta — **condicionada al bloque ADR-0024…0032** (`Propuesto`). Hoy no bloquea nada: mientras ADR-0003 siga vigente el sistema es un monolito modular |
| **D-22** | **Quién opera cuatro despliegues.** [ADR-0003](../30-arquitectura/adr/ADR-0003-monolito-modular.md) tenía razón: una municipalidad no opera cuatro. Si el producto es multi-municipal y lo opera un equipo central, la objeción se disuelve; si cada municipio se autogestiona, no — y entonces la separación no debe hacerse | Dirección del proyecto | Todo ADR-0029. **Es la decisión que lo habilita o lo cancela** | Abierta — **condicionada al bloque ADR-0024…0032** (`Propuesto`). Hoy no bloquea nada: mientras ADR-0003 siga vigente el sistema es un monolito modular |
| **D-23** | **Quién publica las librerías comunes.** `comun-dominio`, `comun-plataforma`, `comun-integracion`, `comun-verificaciones` y los paquetes `@sgtm/*`. Un repositorio `plataforma` con su propio ciclo añade un quinto repositorio; que cada dueño publique lo suyo deja `comun-plataforma` huérfano | Arquitectura | La fase 0 de ADR-0029. **Va antes del segundo repositorio**: un repo creado sin ellas copia y pega, y lo copiado no vuelve | Abierta — **condicionada al bloque ADR-0024…0032** (`Propuesto`). Hoy no bloquea nada: mientras ADR-0003 siga vigente el sistema es un monolito modular |
| **D-24** | **Cuántos saltos entre sistemas tolera una ventanilla.** Con un frontend por sistema ([ADR-0030](../30-arquitectura/adr/ADR-0030-cuatro-interfaces-una-sesion.md)), el funcionario que consulta una deuda y la cobra toca dos aplicaciones. El criterio es medible y hay que instrumentarlo desde el primer día: si una tarea corriente cruza de sistema más de una vez, la frontera está mal puesta, no la interfaz | Rentas del piloto + arquitectura | La fase 3 de ADR-0029. Si sale mal, la salida es embeber la consulta de deuda como componente, sin dejar de ser dos aplicaciones | Abierta — **condicionada al bloque ADR-0024…0032** (`Propuesto`). Hoy no bloquea nada: mientras ADR-0003 siga vigente el sistema es un monolito modular |

| **D-25** | **Si la separación llega o no al hierro.** Cuatro sistemas sobre un k3s de un solo nodo comparten disponibilidad: lo que [ADR-0011](../30-arquitectura/adr/ADR-0011-infraestructura-como-codigo.md) aceptó como «perder el nodo es una caída del servicio» pasa a ser una caída de los cuatro a la vez, y cualquier mantenimiento del VPS sigue siendo una ventana de indisponibilidad para todos. Aislarlos de verdad cuesta más nodos y un motor por sistema. **No es una decisión de arquitectura sino de presupuesto**, y hasta que se tome conviene no decir que la separación mejora la disponibilidad, porque no lo hace | Dirección + TI municipal | Nada hoy. Decide cuánto vale realmente la separación de [ADR-0031](../30-arquitectura/adr/ADR-0031-infraestructura-comun-y-propia.md) | Abierta — **condicionada al bloque ADR-0024…0032** (`Propuesto`) |

## Por qué D-02 bloquea de verdad

No es cautela burocrática. El manual documenta el sistema **como se usa**, no las cifras con que
calcula: las capturas muestran importes de un contribuyente concreto de 2010, no la tabla de
tramos. Implementar el predial con un tramo inventado produce deuda mal determinada en **todo el
padrón**, y su corrección exige anular los valores emitidos y devolver lo cobrado de más.

Mientras D-02 esté abierta —cualquiera de sus tres partes—:

- Se puede construir el **modelo de datos** de los parámetros (existe: `parametro_tributario`,
  `tabla_valor_unitario`, `arancel`, `depreciacion`, `valor_referencial_vehiculo`).
- Se puede construir el **motor** que los lee, los sella por ejercicio y los aplica (existe).
- **No** se puede escribir ninguna función que devuelva un importe determinado.

### Por qué está partida en tres

Era una sola fila que cubría ~27 datos de siete tributos, y por eso bloqueaba en bloque. Pero los
tres grupos tienen responsables y bloqueos distintos:

- **D-02a** son normas nacionales publicadas. **Cerrada el 2026-08-25** ([#200](https://github.com/hneyra/sgtm/issues/200)):
  los 14 archivos de [`valores-normativos/`](../10-negocio/valores-normativos/) que la cerraban
  estaban `VERIFICADO`, con segunda firma distinta cada uno. **Hoy son 19**, y **dos** siguen en
  `TRANSCRITO` a falta de la segunda firma: las obras complementarias y la deducción de Amazonía,
  las dos de D-11. Los valores unitarios volvieron atrás el 2026-08-28 —el cotejo contra el Anexo
  I.2 real devolvió 3 partidas donde se habían transcrito 7 (GOB-03, H-14)— y **volvieron a
  `VERIFICADO` el 2026-08-29**, cuando una segunda persona releyó el PDF y firmó la matriz
  sustituida; la firma anterior, del 25, no respalda ninguna de las 27 cifras de hoy. Cerrar la
  firma no habilita todavía escribir una regla con cifra real. **La carga a la base ya no espera a
  nada**: D-13 se cerró el 2026-08-28
  ([ADR-0017](../30-arquitectura/adr/ADR-0017-tablas-de-valuacion-nacionales.md)) y las tres tablas
  de valuación son catálogos nacionales; el mecanismo completo existe —`PublicarParametros` (#188)
  publica los once valores sueltos, `PublicarCuadros` publica un cuadro entero desde el manifiesto
  del corpus, y `AbrirConjuntoDeParametros` (#247 §2) abre, compone y sella—. Lo que sí sigue
  esperando es **usar** esas cifras: casi toda regla del predial depende además de D-11
  (H-12, [GOB-03](plan-de-desbloqueo-D-02.md) §0.6) —el redondeo ya no: D-03a/b/c se cerraron el
  2026-08-28 ([ADR-0018](../30-arquitectura/adr/ADR-0018-el-redondeo-decidido.md))—, y **uno** de los tres cuadros sigue sin
  poderse cargar —el de valores unitarios, y desde el 2026-08-29 ya no por su firma: le faltan el
  derivado publicable, decidir si sus partidas son tres o siete, y tres regiones (GOB-03, H-14)—.
  La de depreciación salió de esa lista: `V57` le dio su dimensión de uso el 2026-08-29 y sus 492
  filas se publican (H-15).
- **D-02b** exige la ordenanza del piloto *y* su ratificación provincial. No se puede empezar.
- **D-02c** está en medio.

Mientras fueran la misma decisión, lo que se podía hacer esperaba por lo que no. La partición no
cierra nada: hace visible qué está esperando a qué.

**La partición ya es una función**, y no lo era: el criterio no es el tributo sino **quién produce
el valor**, y está escrito en [NEG-02 §2.1](../10-negocio/marco-normativo.md) con las 29 filas
asignadas una por una. Al construir el mapa, cuatro datos cambiaron de parte —espectáculos,
prescripción y plazos, y la tabla de infracciones de tránsito pasaron de `D-02c` a `D-02a`; los
anuncios y la TIM, a `D-02b`— y dos que no tenían fila la ganaron. El saldo práctico: **cuatro
datos más se pueden buscar hoy, sin piloto**, y `#192` deja de esperar a D-01.

Las etiquetas del tablero salen de ahí, y `docs/10-negocio/verificar-mapa-normativo.mjs` comprueba
en las dos direcciones que sigan diciendo lo mismo.

**De D-02, lo que no se resolvía buscando era D-11**, los cuatro factores sin fuente. Buscarlos de
todos modos resultó valer la pena: **tres de los cuatro sí estaban en una norma publicada** —el
factor de oficialización y el incremento del 5 %, en la resolución anual del sector Vivienda; la
deducción de Amazonía, en la Ley 27037 y su reglamento— y lo que la búsqueda devolvió no fue solo la
cifra sino **quién la fija**: la de Amazonía no la fija ninguna norma nacional, sino cada
municipalidad de la Amazonía todos los años, así que nace en D-02b (fila 32 del mapa).

**Queda el `% actualización`, y buscarlo también valió la pena, aunque no aparezca.** El 2026-08-30
(#437) se descartó contra los manuales del SRTM del MEF la hipótesis que llevaba meses en pie —el
art. 14, la actualización de valores de la emisión mecanizada—: ahí ese acto es una
**redeterminación** con las tablas del ejercicio nuevo, no un factor, y en las mismas pantallas
donde los otros dos factores sí aparecen con nombre, este no está. **Descartar una hipótesis no es
avanzar poco**: es lo que impide que la próxima búsqueda repita esta, y por eso queda escrito en
`predial-porcentaje-de-actualizacion.md` con su evidencia. En su lugar apareció un candidato
normativo que los ocho ángulos anteriores no habían mirado —la actualización por Decreto Supremo
cuando no se publican los valores del ejercicio—, que además explicaría por qué su valor habitual
sería neutro.

La decisión sigue en pie, con la pregunta afinada: si la fuente no aparece, ¿se implementa igual, se
omite, o se bloquea la emisión? Multiplica sobre importes, así que omitirlo **no es neutro** — y
aquí el «valor obvio» es 1, que es justamente el que no se siente como inventado. Por eso la regla 5
lo vigila desde #437.

## Cómo se cierra D-03c

**Se cerró el 2026-08-28, y no como esta sección preveía**
([ADR-0018](../30-arquitectura/adr/ADR-0018-el-redondeo-decidido.md)): el piloto arranca con
padrón nuevo, así que no hay determinaciones del SRTM del MEF que reproducir y la ingeniería
inversa no aplica — los puntos los define este sistema (cierre de cada regla, a céntimo,
`HALF_UP`; intermedios en 18,6 sin redondear salvo que la norma diga otra cosa), publicados como
datos `REDONDEO:‹punto›`.

**El procedimiento de abajo no se tira: revive con la primera municipalidad que migre** (anotado
en la fila D-04). Ahí sí hay cifras ajenas que reproducir, y la circularidad que esta sección
describía sigue siendo real: ARQ-09 §7 dice que la decisión bloquea `CAL-02`, y `CAL-02` es
precisamente el contraste contra el SRTM del MEF:

> Toda diferencia en el contraste contra el SRTM será indistinguible de un error de regla.

No se puede usar el mismo contraste para **descubrir** los puntos de redondeo y para **validar**
las reglas. Se rompe en dos pasos:

1. Obtener determinaciones del SRTM del MEF **con su desarrollo intermedio visible** —M02 muestra
   que la pantalla de determinación lo trae— y ajustar los puntos contra ese desarrollo.
2. Correr `CAL-02` como validación, **sobre un juego de predios distinto**.

## Dos casillas que M02 ya cerró

`../srtm` NEG-05 §6 las sigue listando como pendientes de confirmar, y ya no lo están:

- **La base del predial es por contribuyente y no por predio.** NEG-05 §0.1: «Deja de ser
  hipótesis». La grilla «detalle de los predios» del SRTM vive dentro de una única determinación.
- **La fecha de referencia** son las características del predio vigentes al 1 de enero.

Las dos están implementadas: `ReglaDeAgregacion` y `RangoDeEjercicios` en `sgtm-parametros`.

## Cómo se cierra una decisión

1. Se documenta la decisión con su fuente (norma, ordenanza, acta) en el documento que
   corresponda; si cambia la arquitectura, como ADR.
2. Se quita la fila de esta tabla, en el mismo PR.
3. Si desbloquea trabajo, se dice en el PR qué queda desbloqueado.
