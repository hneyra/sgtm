# Registro de archivos de filas migrados a S3, por huella (issue #388).
#
# Un cuadro normativo cuyo archivo de filas no cabe en un ConfigMap (mas de 1 MiB) se archiva con
# scripts/valores-normativos/archivar_derivado.sh, y la URI que ese guion imprime se agrega aqui,
# en una fila nueva, con el MISMO sha256 que ya declara la fila del manifiesto
# (docs/10-negocio/valores-normativos/publicacion/cuadros-2026.csv, columna `sha256`) — es la
# clave que los une, nunca el nombre del archivo ni su ruta.
#
# El manifiesto en git NO cambia cuando se agrega una fila aqui: su columna `archivo_de_filas`
# sigue siendo la ruta relativa de siempre (../fuentes/<edicion>/<archivo>), y PublicarCuadros la
# resuelve exactamente igual. Este archivo es solo transporte —infra/carga-de-datos/
# publicar-cuadros.sh lo consulta cuando el archivo local pesa mas de lo que un ConfigMap admite,
# para saber de donde descargarlo antes de montarlo en el mismo lugar relativo que el manifiesto
# declara— y por eso vive junto al registro de fuentes (README.md de esta carpeta), no en
# docs/10-negocio/valores-normativos/publicacion/, que es donde viven los derivados firmados.
#
# Un sha256 que no aparece aqui, y cuyo archivo local pesa mas de 1 MiB, hace que
# publicar-cuadros.sh se detenga nombrando los dos pasos que faltan.
sha256,uri
