# ARCHIVO GENERADO — no editar a mano.
# Origen: los `endpoint` de design/sgtm-data-{1..5}.js.
# Regenerar con: node docs/50-api/generar-openapi.mjs
#
# Es el contrato PROPUESTO: define verbo, ruta y parametros de las 134
# operaciones que la interfaz espera. Los esquemas de cuerpo y respuesta se
# escriben cuando se implementa cada operacion.
openapi: 3.1.0
info:
  title: SGTM — Sistema de Gestion Tributaria Municipal
  version: 1.0.0-borrador
  description: |
    Contrato de la API del SGTM, derivado de las pantallas del prototipo de
    interfaz. Una operacion por opcion del menu.

    El identificador de municipalidad NO viaja en ninguna ruta, parametro ni
    cuerpo: sale del claim `municipalidad_id` del token validado (ADR-0005).
    Una peticion que lo mande por otro camino no lo consigue.

    Los importes son cadenas decimales, nunca numeros de coma flotante
    (RNF-055), y toda cifra de deuda indica a que fecha esta actualizada
    (RNF-075).
servers:
  - url: /api/v1
    description: Camino base
security:
  - tokenDeAcceso: []
tags:
  - name: "Inicio"
    description: "2 opciones del manual"
  - name: "Catastro"
    description: "12 opciones del manual"
  - name: "Rentas · Registro"
    description: "15 opciones del manual"
  - name: "Fiscalización"
    description: "8 opciones del manual"
  - name: "Tránsito"
    description: "23 opciones del manual"
  - name: "Infracciones administrativas"
    description: "13 opciones del manual"
  - name: "Tesorería"
    description: "10 opciones del manual"
  - name: "Consultas"
    description: "11 opciones del manual"
  - name: "Valores"
    description: "6 opciones del manual"
  - name: "Coactiva"
    description: "12 opciones del manual"
  - name: "Autorizaciones y licencias"
    description: "11 opciones del manual"
  - name: "Seguridad"
    description: "11 opciones del manual"
paths:
  "/indicadores/recaudacion":
    get:
      operationId: inicio
      summary: "Panel de recaudación"
      description: "Avance de la recaudación del ejercicio 2026 al 13 de agosto, con la cartera pendiente por tributo y las tareas abiertas de cada unidad."
      tags: ["Inicio"]
      parameters:
        - name: ejercicio
          in: query
          required: false
          schema: { type: string }
          example: "2026"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/portal/deuda":
    get:
      operationId: portal
      summary: "Consulta y pago en línea"
      description: "Flujo público de autoconsulta: el contribuyente identifica su deuda, elige qué pagar y descarga su constancia sin acudir a la municipalidad."
      tags: ["Inicio"]
      parameters:
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/portal/situacion":
    get:
      operationId: portal_mi_situacion
      summary: "Mi situación en todas las municipalidades"
      description: |
        Lo que el ciudadano autenticado debe y tiene **en todas las municipalidades del
        sistema donde figure**, a **una sola** fecha de corte.

        **Sin ningún parámetro, y eso es la decisión.** El sujeto sale del claim
        `numero_documento` del token del realm del ciudadano —emisor distinto del de
        funcionarios—, validado criptográficamente por la cadena que sirve
        `/api/v1/portal/**`. Sustituye a `GET /portal/deuda?doc=…`, que contestaba
        «quién es esta persona y cuánto debe» a quien tecleara ocho dígitos.

        El servidor **recorre** el registro de municipalidades activas —una transacción y
        un `SET LOCAL` por rama, RLS en cada una—, compone y suma (RNF-083). No es una
        consulta multi-municipalidad: son *N* consultas de una municipalidad cuya unión se
        filtra a un documento firmado.

        `totalConsolidado` es `null` cuando alguna municipalidad no se pudo leer, y
        entonces `notaDelTotal` dice cuáles faltan: un total al que le falta una
        municipalidad es un importe plausible y equivocado.

        Un token de **funcionario** no autentica aquí (401); uno de ciudadano no autentica
        en ninguna otra ruta de la API.
      tags: ["Inicio"]
      responses:
        200:
          description: "La situación del ciudadano, municipalidad por municipalidad"
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinDocumentoAcreditado"
  "/catastro/fichas/urbana/{codRefCatastral}":
    get:
      operationId: ficha_urbana
      summary: "Ficha catastral urbana individual"
      description: "Ficha del predio urbano. El código de referencia catastral se compone de sector, manzana, lote, edificación, entrada, piso y unidad; su cambio obliga a recalcular el autovalúo."
      tags: ["Catastro"]
      parameters:
        - name: codRefCatastral
          in: path
          required: true
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Ficha vigente a esta fecha. Sin ella, la que rige hoy"
          schema: { type: string }
          example: "2026-08-20"
        - name: historico
          in: query
          required: false
          description: "Trae todas las versiones de la ficha, no solo la vigente"
          schema: { type: string }
          example: "true"
        - name: codigoDeRefCatastral
          in: query
          required: false
          description: "Filtro «Código de Ref. Catastral» de la pantalla"
          schema: { type: string }
        - name: codContribuyenteRentas
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente Rentas» de la pantalla"
          schema: { type: string }
        - name: nroFicha
          in: query
          required: false
          description: "Filtro «Nro. Ficha» de la pantalla"
          schema: { type: string }
        - name: uso
          in: query
          required: false
          description: "Filtro «Uso» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/urbana":
    post:
      operationId: registrar_ficha_urbana
      summary: "Alta de ficha urbana individual"
      description: "Inscribe la primera versión de la ficha urbana individual (RF-001) de un predio, y da de alta el predio en el mismo acto si todavía no existe. El cuerpo lleva el código de referencia catastral, la ubicación del predio, los datos de la ficha —áreas y categorías, ningún importe—, su titularidad inicial si ya se conoce, y la observación del usuario, obligatoria (RNF-052). Si el predio ya tiene ficha de ese tipo, es 409: lo que toca entonces es actualizarla."
      tags: ["Catastro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/economica/{codRefCatastral}":
    get:
      operationId: ficha_economica
      summary: "Ficha catastral económica"
      description: "Actividad económica que se desarrolla en la unidad catastral, usada para verificar licencias y determinar el uso real del predio."
      tags: ["Catastro"]
      parameters:
        - name: codRefCatastral
          in: path
          required: true
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Ficha vigente a esta fecha. Sin ella, la que rige hoy"
          schema: { type: string }
          example: "2026-08-20"
        - name: historico
          in: query
          required: false
          description: "Trae todas las versiones de la ficha, no solo la vigente"
          schema: { type: string }
          example: "true"
        - name: codigoDeRefCatastral
          in: query
          required: false
          description: "Filtro «Código de Ref. Catastral» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: ciiu
          in: query
          required: false
          description: "Filtro «CIIU» de la pantalla"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/economica":
    post:
      operationId: registrar_ficha_economica
      summary: "Alta de ficha económica"
      description: "Inscribe la primera versión de la ficha económica (RF-002) de un predio, y da de alta el predio en el mismo acto si todavía no existe. El cuerpo lleva el código de referencia catastral, la ubicación del predio, los datos de la ficha —áreas y categorías, ningún importe—, su titularidad inicial si ya se conoce, y la observación del usuario, obligatoria (RNF-052). Si el predio ya tiene ficha de ese tipo, es 409: lo que toca entonces es actualizarla."
      tags: ["Catastro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/bienes-comunes/{codEdificacion}":
    get:
      operationId: ficha_bienes
      summary: "Ficha de bienes comunes"
      description: "Áreas comunes de una edificación en régimen de propiedad exclusiva y común, cuyo valor se distribuye entre las unidades según su porcentaje de participación."
      tags: ["Catastro"]
      parameters:
        - name: codEdificacion
          in: path
          required: true
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Ficha vigente a esta fecha. Sin ella, la que rige hoy"
          schema: { type: string }
          example: "2026-08-20"
        - name: historico
          in: query
          required: false
          description: "Trae todas las versiones de la ficha, no solo la vigente"
          schema: { type: string }
          example: "true"
        - name: denominacion
          in: query
          required: false
          description: "Filtro «Denominación» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/bienes-comunes":
    post:
      operationId: registrar_ficha_bienes
      summary: "Alta de ficha de bienes comunes"
      description: "Inscribe la primera versión de la ficha de bienes comunes (RF-003) de un predio, y da de alta el predio en el mismo acto si todavía no existe. El cuerpo lleva el código de referencia catastral, la ubicación del predio, los datos de la ficha —áreas y categorías, ningún importe—, su titularidad inicial si ya se conoce, y la observación del usuario, obligatoria (RNF-052). Si el predio ya tiene ficha de ese tipo, es 409: lo que toca entonces es actualizarla."
      tags: ["Catastro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/rural/{codUnidad}":
    get:
      operationId: ficha_rural
      summary: "Ficha catastral rural"
      description: "Predio rústico valorizado por hectárea según el arancel rural, el tipo de tierra y la disponibilidad de riego."
      tags: ["Catastro"]
      parameters:
        - name: codUnidad
          in: path
          required: true
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Ficha vigente a esta fecha. Sin ella, la que rige hoy"
          schema: { type: string }
          example: "2026-08-20"
        - name: historico
          in: query
          required: false
          description: "Trae todas las versiones de la ficha, no solo la vigente"
          schema: { type: string }
          example: "true"
        - name: codUnidadCatastralUc
          in: query
          required: false
          description: "Filtro «Cod. Unidad Catastral (UC)» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: valleSector
          in: query
          required: false
          description: "Filtro «Valle / sector» de la pantalla"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/rural":
    post:
      operationId: registrar_ficha_rural
      summary: "Alta de ficha rural"
      description: "Inscribe la primera versión de la ficha rural (RF-004) de un predio, y da de alta el predio en el mismo acto si todavía no existe. El cuerpo lleva el código de referencia catastral, la ubicación del predio, los datos de la ficha —áreas y categorías, ningún importe—, su titularidad inicial si ya se conoce, y la observación del usuario, obligatoria (RNF-052). Si el predio ya tiene ficha de ese tipo, es 409: lo que toca entonces es actualizarla."
      tags: ["Catastro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas":
    get:
      operationId: consulta_fichas
      summary: "Consulta de fichas catastrales"
      description: "Búsqueda transversal de fichas por código, titular o ubicación, con el estado de conciliación entre catastro y el padrón de rentas."
      tags: ["Catastro"]
      parameters:
        - name: codRefCatastral
          in: query
          required: false
          description: "Filtro «Cod. Ref. Catastral» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: manzana
          in: query
          required: false
          description: "Filtro «Manzana» de la pantalla"
          schema: { type: string }
        - name: lote
          in: query
          required: false
          description: "Filtro «Lote» de la pantalla"
          schema: { type: string }
        - name: conciliadaConRentas
          in: query
          required: false
          description: "Filtro «Conciliada con rentas» de la pantalla. Esta ruta no lo resuelve —el estado de conciliación se deriva de `declaracion_jurada`, que es de rentas, y catastro no puede depender de rentas (ADR-0015 §2)—: la petición que lo trae se redirige con 307 a `/catastro/fichas/conciliacion`, que es la misma grilla servida por rentas."
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "307":
          description: "La petición trae `conciliadaConRentas`: se redirige a `/catastro/fichas/conciliacion` conservando la consulta entera. No se ignora el filtro y no se responde sin él (ADR-0015 §2)."
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/conciliacion":
    get:
      operationId: consulta_fichas_conciliacion
      summary: "Consulta de fichas con su conciliación con rentas"
      description: "La misma grilla de `consulta_fichas` con la columna «Conciliada» y el filtro `conciliadaConRentas`. Un predio está conciliado a un ejercicio cuando existe una declaración jurada de ese ejercicio, con su mismo `predio_id`, en estado PRESENTADA u OBSERVADA (ADR-0015 §1). La sirve `rentas` y no `catastro` —el derivado sale de la declaración jurada y catastro no puede depender de rentas—, pero el acceso que exige es el de la pantalla, `consulta_fichas`. De la declaración jurada no viaja nada: ni su número, ni su tipo, ni sus importes, ni quién la presentó; solo el derivado y el ejercicio al que responde (regla 9). `conciliadaConRentas=No` —la lista de los predios que no generan deuda predial— exige además privilegio de lectura sobre `fisc_omisos` y deja fila en la bitácora con operación ACCESO."
      tags: ["Catastro"]
      parameters:
        - name: codRefCatastral
          in: query
          required: false
          description: "Filtro «Cod. Ref. Catastral» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: manzana
          in: query
          required: false
          description: "Filtro «Manzana» de la pantalla"
          schema: { type: string }
        - name: lote
          in: query
          required: false
          description: "Filtro «Lote» de la pantalla"
          schema: { type: string }
        - name: tipo
          in: query
          required: false
          description: "UNICA | ECONOMICA | BIENES_COMUNES | RURAL"
          schema: { type: string }
        - name: conciliadaConRentas
          in: query
          required: false
          description: "Todas | Sí | No. «No» exige privilegio de lectura sobre `fisc_omisos` y queda registrado en la bitácora (ADR-0015 §2.3)."
          schema: { type: string }
          example: "Todas"
        - name: ejercicio
          in: query
          required: false
          description: "A qué ejercicio responde la conciliación; si falta, el de la fecha de corte. La respuesta lo dice siempre (regla 9, RNF-075)"
          schema: { type: string }
          example: "2026"
        - name: fecha
          in: query
          required: false
          description: "Fecha de corte a la que se resuelven la versión de ficha y el titular vigentes; si falta, hoy"
          schema: { type: string }
          example: "2026-08-28"
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/conciliacion/resumen":
    get:
      operationId: conciliacion_resumen
      summary: "Recuento de la conciliación con rentas"
      description: "Cuántos predios del padrón hay a esa fecha, cuántos declararon ese ejercicio y cuántos no, resuelto en **una** consulta agregada y sin recorrer el padrón (#564). Existe porque la grilla no sirve para contar: su filtro se aplica sobre la página ya devuelta y su `totalElementos` es el del padrón sin filtrar. La población es la misma que lista la grilla —las fichas vigentes a la fecha—, y que lo siga siendo lo comprueba una prueba que compara las dos cifras. A diferencia de `conciliadaConRentas=No`, **no** exige privilegio sobre `fisc_omisos` y **no** deja fila en la bitácora: aquella nombra —es la lista de a quién no le va a llegar recibo— y ésta cuenta."
      tags: ["Catastro"]
      parameters:
        - name: ejercicio
          in: query
          required: false
          description: "A qué ejercicio responde el recuento; si falta, el de la fecha de corte. La respuesta lo dice siempre: no existe «sin conciliar», existe «sin conciliar a 2026» (regla 9, RNF-075)"
          schema: { type: string }
          example: "2026"
        - name: fecha
          in: query
          required: false
          description: "Fecha de corte a la que se resuelve qué versión de ficha rige, igual que en la grilla; si falta, hoy"
          schema: { type: string }
          example: "2026-08-28"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/predios/plano":
    get:
      operationId: plano_catastral
      summary: "Plano catastral: los lotes de un marco"
      description: |
        Los lotes que caen dentro de un marco, **con su polígono**, para dibujar el plano
        catastral (ADR-0022). La geometría sale de `predio.geometria` —`geography(MultiPolygon,
        4326)`, ADR-0021— serializada a GeoJSON tal cual: **ni reproyectada ni simplificada**.
        Un vértice movido es un lindero movido, y un lindero movido no se ve.

        **Se acota por marco y se niega antes que recortarse.** `bbox` es obligatorio. Si
        dentro caben más lotes que `limite`, la respuesta es **422** diciendo cuántos hay:
        una página con los primeros dibujaría un plano al que le faltan lotes, y eso no se
        lee como «faltan», se lee como «ahí no hay nada». Por lo mismo no pagina.

        **`sinGeometria` cuenta los predios del padrón que pasan los mismos filtros de
        sector y de manzana y no tienen polígono, sin acotar por el marco**, y la interfaz lo
        dice siempre, incluso cuando es cero. Que no lo acote el marco no es una omisión: un predio
        sin polígono **no tiene sitio en el marco**, así que acotarlo por `bbox` daría cero
        siempre, y daría cero justo cuando la cifra más hace falta —hoy, sin un solo lote
        digitalizado—. El único dato que podría situarlo, el perímetro de su manzana, no existe
        en el esquema, y derivarlo de la unión de los lotes ya levantados es lo que ADR-0022 §5
        prohíbe. Sin esta cifra el visor afirma algo que no sabe: hoy no hay una sola
        municipalidad con geometría cargada, así que lo honesto es que el plano vacío diga por
        qué lo está —la carga cartográfica de ADR-0021— y no que parezca un distrito sin
        predios.

        Ni un importe y ni un titular, por lo mismo que `GET /catastro/predios`: quién es
        el propietario se resuelve al clic, de un predio cada vez, en
        `/catastro/predios/{predioId}/titulares` (ADR-0015 §2.4). Y **ninguna área**: la del
        polígono no es la imponible, y publicarlas juntas invita a compararlas donde no se
        decide nada.
      tags: ["Catastro"]
      parameters:
        - name: bbox
          in: query
          required: false
          description: "Marco en grados WGS84, `oeste,sur,este,norte`. Obligatorio: sin él la consulta sería el padrón entero, que es lo que esta operación existe para no hacer"
          schema: { type: string }
          example: "-80.71,-4.92,-80.66,-4.87"
        - name: codigoDeSector
          in: query
          required: false
          description: "Filtro «Sector» de la pantalla, por código"
          schema: { type: string }
        - name: codigoDeManzana
          in: query
          required: false
          description: "Filtro «Manzana» de la pantalla, por código"
          schema: { type: string }
        - name: limite
          in: query
          required: false
          description: "Cuántos lotes se sirven como máximo. Si el marco contiene más, la respuesta es 422 con la cuenta; nunca los primeros `limite`"
          schema: { type: string }
          example: "2000"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/{codigo}/actualizacion":
    put:
      operationId: actualizacion_catastro
      summary: "Actualización del catastro"
      description: "Actualiza construcciones y otras instalaciones de una ficha ya registrada. El sistema conserva cada versión declarada y verificada por piso, con su MEP, ECS, ECC y estado de conservación."
      tags: ["Catastro"]
      parameters:
        - name: codigo
          in: path
          required: true
          schema: { type: string }
        - name: codRefCatastral
          in: query
          required: false
          description: "Filtro «Cod. Ref. Catastral» de la pantalla"
          schema: { type: string }
        - name: nDeFicha
          in: query
          required: false
          description: "Filtro «Nº de ficha» de la pantalla"
          schema: { type: string }
        - name: sector
          in: query
          required: false
          description: "Filtro «Sector» de la pantalla"
          schema: { type: string }
        - name: tipoDeActualizacion
          in: query
          required: false
          description: "Filtro «Tipo de actualización» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/economica/{codRefCatastral}/actualizacion":
    put:
      operationId: actualizar_ficha_economica
      summary: "Actualización de la ficha económica"
      description: "Crea la versión siguiente de la ficha económica y cierra la anterior, que queda entera. Lo que el cuerpo no manda, no cambia: una lista ausente copia la de la versión vigente y una lista presente aunque vacía la reemplaza."
      tags: ["Catastro"]
      parameters:
        - name: codRefCatastral
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/bienes-comunes/{codEdificacion}/actualizacion":
    put:
      operationId: actualizar_ficha_bienes
      summary: "Actualización de la ficha de bienes comunes"
      description: "Crea la versión siguiente de la ficha de bienes comunes y cierra la anterior. Las áreas comunes y su reparto se copian si el cuerpo no los declara; declararlos los reemplaza."
      tags: ["Catastro"]
      parameters:
        - name: codEdificacion
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/predios/{predioId}/titulares":
    post:
      operationId: registrar_titular_del_predio
      summary: "Alta de una cuota de titularidad"
      description: >-
        Declara **de quién es** el predio (#490, RF-005): el primer titular, o uno más de una
        copropiedad. Hasta aquí la titularidad se podía leer y transferir, pero **el primer titular
        no se podía registrar por HTTP** — sólo se transfiere lo que ya tiene dueño, y lo único que
        daba el primero era la siembra o el bloque de titular del alta de ficha.

        `condicion` decide si hace falta `porcentaje`: sólo `PROPIETARIO_UNICO` lo es por el
        total. Declarar una **copropiedad** es registrar dos o más cuotas.

        **Pasarse del 100 % es 409, y lo dice la base.** La suma de cuotas vigentes la vigila un
        disparador *diferido*, que habla al confirmar; tiene que ser diferido para que una
        transferencia —cerrar una cuota y abrir otra en la misma transacción— sea posible, porque
        entre las dos operaciones el total pasa de 100 a propósito.

        No reabre D-12 ([ADR-0019]): una titularidad que no llega al 100 % se registra igual, y
        determina sólo la porción con titular identificado.

        Exige `REGISTRO` sobre `actualizacion_catastro` —quien declara de quién es un predio
        está actualizando el catastro, no consultando el padrón— y la observación del usuario
        (RNF-052).
      tags: ["Catastro"]
      parameters:
        - name: predioId
          in: path
          required: true
          description: "El predio, por el `predioId` que publica cada fila de la consulta de fichas"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    get:
      operationId: titulares_del_predio
      summary: "Titulares del predio, con su código de contribuyente"
      description: "Quién es titular de un predio a una fecha, con el código con el que se entra a su ficha de contribuyente. Es la resolución que la fila de `consulta_fichas` necesita para poder enlazar con la persona: la grilla publica el nombre del titular y no su identificador, y añadirlo allí convertiría «quien puede listar fichas» en «quien puede cosechar la correlación predio→persona de toda la municipalidad» (ADR-0015 §2.4). Por eso se resuelve **al clic, de un predio cada vez**: exige privilegio de lectura sobre `contribuyentes` —el permiso del padrón, no el de la pantalla desde la que se hace clic— y cada resolución deja fila en la bitácora con operación ACCESO, la devuelva o no algún titular. Devuelve la **lista** de cuotas vigentes, no «el» titular: un predio puede tener varios —dos cónyuges, una sucesión, un condominio—, cada uno con su porcentaje. La respuesta dice siempre a qué fecha contesta (regla 9, RNF-075). Del padrón no viaja nada más: ni el identificador interno del contribuyente, ni su documento; y de la titularidad, ni sus fechas ni el documento que la sustenta."
      tags: ["Rentas · Registro"]
      parameters:
        - name: predioId
          in: path
          required: true
          description: "El predio, por el `predioId` que publica cada fila de la consulta de fichas"
          schema: { type: string }
        - name: vigenteA
          in: query
          required: false
          description: "Fecha a la que se resuelve la titularidad; si falta, hoy. La titularidad de marzo no es la de setiembre, y la respuesta dice siempre a cuál contesta (regla 9)"
          schema: { type: string }
          example: "2026-08-28"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/predios/{predioId}/inquilinos":
    get:
      operationId: inquilinos_del_predio
      summary: "Quién ocupa el predio, a una fecha"
      description: >-
        Los ocupantes del predio vigentes a la fecha (#490, #31). El manual los registra para la
        cobranza de arbitrios.

        Existe porque terminar una ocupación exige decir **cuál**, y ninguna lectura publicaba ese
        identificador.
      tags: ["Catastro"]
      parameters:
        - name: predioId
          in: path
          required: true
          description: "El predio, por el `predioId` que publica cada fila de la consulta de fichas"
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Fecha a la que se resuelve la ocupacion; si falta, hoy. Quien ocupaba el predio en marzo no es necesariamente quien lo ocupa hoy, y una determinacion de arbitrios de marzo se explica con el de marzo (regla 9)"
          schema: { type: string }
          example: "2026-08-30"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: registrar_inquilino
      summary: "Alta de inquilino"
      description: >-
        Registra a quien ocupa el predio sin ser su dueño (#490, #31). El inquilino entra por su
        **código de contribuyente** —para cobrarle hay que poder notificarle, y el domicilio cuelga
        del padrón—, con el documento que sustenta el registro y la observación del usuario.
      tags: ["Catastro"]
      parameters:
        - name: predioId
          in: path
          required: true
          description: "El predio, por el `predioId` que publica cada fila de la consulta de fichas"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/predios/{predioId}/inquilinos/{inquilinoId}":
    put:
      operationId: finalizar_inquilino
      summary: "Fin de la ocupación"
      description: >-
        Termina la ocupación en una fecha (#490). **No borra** (regla 4, RNF-051): una
        determinación de arbitrios anterior pudo apoyarse en ella, y explicarla exige que la fila
        siga ahí.

        Una ocupación ya cerrada es 404 — no hay tal ocupación abierta que terminar. Exige
        `ELIMINACION`, el privilegio que el manual reserva para las bajas lógicas.
      tags: ["Catastro"]
      parameters:
        - name: predioId
          in: path
          required: true
          description: "El predio, por el `predioId` que publica cada fila de la consulta de fichas"
          schema: { type: string }
        - name: inquilinoId
          in: path
          required: true
          description: "La ocupación abierta que se termina, por el id que publica su listado"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/predios":
    post:
      operationId: inscribir_predio
      summary: "Alta de predio, sin ficha"
      description: >-
        Da de alta un predio **sin levantarle ficha** (#489, RF-001), que es el orden natural de
        ventanilla: primero se identifica el predio —su código de referencia catastral, su
        ubicación, su tipo— y después se le levanta la ficha.

        Hasta aquí un predio sólo podía nacer como **efecto secundario** de
        `POST /catastro/fichas/…` —que lo crea si no existe— o por la carga cartográfica del
        perfil `batch`, así que registrar un lote recién numerado obligaba a inventarle una
        ficha. El predio entra con `fichado=false` en `GET /catastro/predios`, que es
        justamente la cola de saneamiento.

        Vía, sector y manzana entran por **código** —lo mismo que recibe la corrección del
        predio—, y se resuelven **dentro de la transacción**: una referencia que no existe es 404
        nombrándola, no un predio guardado a medias. Un código ya inscrito es **409**; la unicidad
        la sostiene `predio_codigo_uq`.

        Exige `REGISTRO` sobre `actualizacion_catastro`, y la observación del usuario es
        obligatoria (RNF-052). Ni un importe y ni un titular: el valor sale del cuadro de valores
        unitarios (D-02a) y la titularidad es otro acto.
      tags: ["Catastro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    get:
      operationId: listado_de_predios
      summary: "Padrón de predios del catastro"
      description: "Los predios del catastro con su ubicación resuelta a **códigos** —los mismos que la corrección del predio recibe, para que la interfaz no tenga que traducir entre lo que lee y lo que manda—, incluidos **los que nadie ha fichado y los que están dados de baja**. No es la consulta de fichas con otro nombre: aquella lista fichas vigentes a una fecha, así que un predio sale en ella solo si alguien levantó su ficha. Con `fichado=false` da la cola de saneamiento —lo que entra por una carga cartográfica y todavía no tiene ficha—, que ninguna consulta del sistema sabía responder. `fichado` dice si se levantó la ficha alguna vez, no si sigue vigente hoy: eso llevaría fecha (regla 9). Ni un importe y ni un titular: quién es el propietario se resuelve al clic en `/catastro/predios/{predioId}/titulares` (ADR-0015 §2.4). Exige privilegio de LECTURA sobre `actualizacion_catastro`: encontrar el predio es el paso previo de los dos actos de esa pantalla, y pedir el de escribir dejaría sin mirar a quien solo mira."
      tags: ["Catastro"]
      parameters:
        - name: codRefCatastral
          in: query
          required: false
          description: "Prefijo del código de referencia catastral, no una igualdad: el código se compone de sector, manzana, lote y unidad, así que preguntar por un sector entero es lo que se hace al sanear una zona"
          schema: { type: string }
          example: "2501010010"
        - name: codigoDeSector
          in: query
          required: false
          description: "El sector, por su código; el mismo que la corrección del predio recibe"
          schema: { type: string }
          example: "SC-1"
        - name: estado
          in: query
          required: false
          description: "ACTIVO o DADO_DE_BAJA. Si falta, salen los dos: este listado es el del catastro y no el de la emisión, y esconder los retirados sería esconder lo que hay que revisar"
          schema: { type: string }
          example: "ACTIVO"
        - name: fichado
          in: query
          required: false
          description: "`true` o `false`, y nada más —cualquier otro valor es 422, no un false silencioso—. Si falta, salen los dos"
          schema: { type: string }
          example: "false"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/predios/{predioId}/baja":
    post:
      operationId: dar_de_baja_predio
      summary: "Baja del predio en el padrón"
      description: "Retira el predio del padrón. **No borra nada** (regla 4, RNF-051): sus fichas, su titularidad y las determinaciones que se apoyaron en él quedan como estaban, y el predio deja de admitir fichas nuevas. Exige privilegio de ELIMINACIÓN sobre `actualizacion_catastro` —no el de modificación— porque saca la unidad de toda emisión futura. El cuerpo lleva solo la observación del usuario, obligatoria (RNF-052): el predio y el estado al que se va los dice la ruta. Un predio que ya está dado de baja es 409, no un segundo acto sin efecto."
      tags: ["Catastro"]
      parameters:
        - name: predioId
          in: path
          required: true
          description: "El predio, por el `predioId` que publica cada fila de la consulta de fichas"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/predios/{predioId}/reactivacion":
    post:
      operationId: reactivar_predio
      summary: "Reactivación del predio"
      description: "Devuelve al padrón un predio retirado. Existe porque sin ella la baja sería una puerta de un solo sentido: el alta de ficha rechaza a propósito inscribir sobre un predio dado de baja —«reactivarlo es otro acto, con su propia observación»— y ese otro acto no existía. Exige MODIFICACIÓN y no ELIMINACIÓN: restituir no es retirar. El cuerpo lleva solo la observación, obligatoria. Un predio que ya está activo es 409."
      tags: ["Catastro"]
      parameters:
        - name: predioId
          in: path
          required: true
          description: "El predio, por el `predioId` que publica cada fila de la consulta de fichas"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/fichas/rural/{codUnidad}/actualizacion":
    put:
      operationId: actualizar_ficha_rural
      summary: "Actualización de la ficha rural"
      description: "Crea la versión siguiente de la ficha rural y cierra la anterior. Los grupos de tierra van en hectáreas —el arancel rural se publica por hectárea— y se copian si el cuerpo no los declara."
      tags: ["Catastro"]
      parameters:
        - name: codUnidad
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/contribuyentes/{codigo}/ficha.pdf":
    get:
      operationId: ficha_contribuyente_reporte
      summary: "Reporte de ficha del contribuyente"
      description: "Ficha impresa del contribuyente: identificación, domicilio fiscal, documentos, contactos y unidades afectas."
      tags: ["Catastro"]
      parameters:
        - name: codigo
          in: path
          required: true
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Ficha vigente a esta fecha. Sin ella, la que rige hoy"
          schema: { type: string }
          example: "2026-08-20"
        - name: formato
          in: query
          required: false
          description: "PDF | XLS | RTF. Sin el, responde JSON: lo que la pantalla dibuja"
          schema: { type: string }
          example: "PDF"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/vias":
    get:
      operationId: calles
      summary: "Mantenimiento de vías y calles"
      description: "Nomenclatura vial que alimenta el domicilio fiscal y la ubicación del predio. Cada vía guarda su tipo, sector y arancel unitario por tramo."
      tags: ["Catastro"]
      parameters:
        - name: codigoDeVia
          in: query
          required: false
          description: "Filtro «Código de vía» de la pantalla. Por PREFIJO del codigo (#565)."
          schema: { type: string }
        - name: nombreDeCalle
          in: query
          required: false
          description: "Filtro «Nombre de calle» de la pantalla. Por PREFIJO del nombre, sin distinguir mayusculas ni tildes: el catalogo real guarda «Cayetano Heredia» y en ventanilla se teclea «cayetano» (#565)."
          schema: { type: string }
        - name: tipoDeVia
          in: query
          required: false
          description: "Filtro «Tipo de vía» de la pantalla. Por igualdad contra el enumerado TipoVia; un tipo que no existe es 422 nombrandolo, no una pagina vacia (#565)."
          schema: { type: string }
        - name: sector
          in: query
          required: false
          description: "Filtro «Sector» de la pantalla. NO SE SIRVE: se rechaza con 422 con cualquier valor. La tabla `via` (V1) no guarda el sector y `ViaResource` no lo publica —Track 2 de #290—, asi que no hay contra que comparar; ignorarlo devolveria el catalogo entero bajo un filtro tecleado (#565)."
          schema: { type: string }
        - name: activa
          in: query
          required: false
          description: >-
            Solo las vigentes («true»), solo las dadas de baja («false») o las dos (ausente).
            Cualquier otra palabra es 422: un «si» tecleado que se leyera como «no filtres»
            devolveria a la lista las vias que se dieron de baja, que es lo que este filtro existe
            para impedir.
          schema: { type: string, enum: [true, false] }
          example: "true"
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: registrar_via
      summary: "Alta de vía"
      description: "Da de alta una vía del catálogo vial (RF-008). El cuerpo lleva tipo, código, nombre y la observación del usuario, obligatoria (RNF-052)."
      tags: ["Catastro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/vias/{codigo}":
    put:
      operationId: editar_via
      summary: "Edición de vía"
      description: "Modifica una vía existente o la da de baja (activa=false). No se borra: la baja es la misma fila con otro estado (RNF-051). El código de la ruta identifica la vía y no cambia."
      tags: ["Catastro"]
      parameters:
        - name: codigo
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/sectores":
    get:
      operationId: sectores
      summary: "Sectores, manzanas y lotes"
      description: "Estructura territorial sobre la que se arma el código de referencia catastral y se agrupan los padrones por zona."
      tags: ["Catastro"]
      parameters:
        - name: sector
          in: query
          required: false
          description: "Filtro «Sector» de la pantalla"
          schema: { type: string }
        - name: manzana
          in: query
          required: false
          description: "Filtro «Manzana» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: registrar_sector
      summary: "Alta de sector"
      description: "Da de alta un sector del catastro. El cuerpo lleva código, nombre, la zona —opcional— y la observación del usuario, obligatoria (RNF-052). Un sector nace activo: darlo de baja es el PUT."
      tags: ["Catastro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/sectores/{codigo}":
    put:
      operationId: editar_sector
      summary: "Edición de sector"
      description: "Modifica un sector existente o lo da de baja (activo=false). No se borra: la baja es la misma fila con otro estado (RNF-051), y tiene que serlo porque su código es un tramo del código catastral de sus predios. El código de la ruta identifica el sector y no cambia."
      tags: ["Catastro"]
      parameters:
        - name: codigo
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/sectores/{codigo}/manzanas":
    get:
      operationId: listado_de_manzanas
      summary: "Manzanas del sector"
      description: "Las manzanas del sector que identifica el código de la ruta, con lo que cuelga de cada una: `predios` —los **activos** de ese sector que la declaran— y `lotes` —cuántos valores de lote distintos hay entre ellos—. Que `lotes` sea menor que `predios` es lo normal: tres departamentos de un mismo lote son tres predios y **un** lote. Hasta #537 el backend sólo publicaba **cuántas** manzanas tiene un sector (`SectorResource.manzanas`) y el alta de una, así que el árbol territorial no podía enumerarlas. **Pagina como el resto de listados**, y hace falta: un sector de una municipalidad grande pasa de mil manzanas. Un código de sector que no existe es **404**, no una página vacía: cero filas significa «ese sector todavía no tiene manzanas», que es lo contrario. Exige LECTURA sobre `sectores`, el mismo acceso que el listado de sectores —las manzanas no tienen pantalla propia en el manual—. **No publica ningún `activa`**: `manzana` no tiene columna de estado, porque una manzana no se edita ni se da de baja (su código es un tramo del código catastral de sus predios), y un `true` constante sería un filtro que no filtra nada."
      tags: ["Catastro"]
      parameters:
        - name: codigo
          in: path
          required: true
          description: "El sector, por su código; el mismo que identifica el sector en `PUT` y en el alta"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: registrar_manzana
      summary: "Alta de manzana"
      description: "Da de alta una manzana dentro del sector que identifica el código de la ruta. No hay verbo para editarla: el código de una manzana es un tramo del código catastral de sus predios, así que cambiarlo desalinearía el de todos ellos."
      tags: ["Catastro"]
      parameters:
        - name: codigo
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/tablas/aranceles":
    get:
      operationId: aranceles
      summary: "Aranceles de terreno"
      description: "Valor oficial del metro cuadrado de terreno por vía y tramo, publicado anualmente. Es el multiplicador del área de terreno en el autovalúo."
      tags: ["Catastro"]
      parameters:
        - name: anio
          in: query
          required: false
          schema: { type: string }
          example: "2026"
        - name: ejercicio
          in: query
          required: false
          description: "Filtro «Ejercicio» de la pantalla"
          schema: { type: string }
        - name: via
          in: query
          required: false
          description: "Filtro «Vía» de la pantalla"
          schema: { type: string }
        - name: zona
          in: query
          required: false
          description: "Filtro «Zona» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/tablas/valores-unitarios":
    get:
      operationId: valores_unitarios
      summary: "Valores unitarios de edificación"
      description: "Tabla oficial por categoría constructiva. El sistema suma las siete partidas declaradas en la ficha y les aplica la depreciación correspondiente."
      tags: ["Catastro"]
      parameters:
        - name: anio
          in: query
          required: false
          schema: { type: string }
          example: "2026"
        - name: ejercicio
          in: query
          required: false
          description: "Filtro «Ejercicio» de la pantalla"
          schema: { type: string }
        - name: region
          in: query
          required: false
          description: "Filtro «Región» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/catastro/tablas/depreciacion":
    get:
      operationId: depreciacion
      summary: "Tabla de depreciación"
      description: "Porcentaje que se descuenta del valor de edificación según antigüedad, material predominante (MEP) y estado de conservación (ECS)."
      tags: ["Catastro"]
      parameters:
        - name: anio
          in: query
          required: false
          schema: { type: string }
          example: "2026"
        - name: materialMep
          in: query
          required: false
          description: "Filtro «Material (MEP)» de la pantalla"
          schema: { type: string }
        - name: uso
          in: query
          required: false
          description: "Filtro «Uso» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/contribuyentes":
    get:
      operationId: contribuyentes
      summary: "Contribuyentes"
      description: "Padrón único del contribuyente. Su código enlaza predios, vehículos, licencias, papeletas y la cuenta corriente."
      tags: ["Rentas · Registro"]
      parameters:
        - name: codigo
          in: query
          required: false
          description: "Filtro «Código» de la pantalla"
          schema: { type: string }
        - name: nombreRazonSocial
          in: query
          required: false
          description: "Filtro «Nombre / razón social» de la pantalla"
          schema: { type: string }
        - name: dNI
          in: query
          required: false
          description: "Filtro «D.N.I.» de la pantalla"
          schema: { type: string }
        - name: rUC
          in: query
          required: false
          description: "Filtro «R.U.C.» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: registrar_contribuyente
      summary: "Alta de contribuyente"
      description: >-
        Da de alta a un contribuyente en el padrón (#488, RF-013). El cuerpo lleva el código,
        el tipo y número de documento, el tipo de persona, el nombre o razón social, la
        condición especial si la hay, y la observación del usuario, obligatoria (RNF-052).

        El código o el documento repetidos salen como **409**. La unicidad la exige la base
        —es la única que puede—, pero el caso de uso comprueba antes para poder decir *cuál de
        los dos* se repitió; el mensaje del documento repetido **no dice con quién**, que sería
        revelar que una persona está en el padrón a quien sólo teclea documentos.

        No entran la fecha de nacimiento, el estado civil ni el cónyuge, que la tabla sí
        guarda: la lectura del padrón no los publica —lo que no se publica no se filtra—, y un
        campo que se puede escribir y nunca leer de vuelta es una trampa.
      tags: ["Rentas · Registro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/contribuyentes/{id}":
    put:
      operationId: modificar_contribuyente
      summary: "Corrección o baja del contribuyente"
      description: >-
        Corrige el nombre o la condición especial de un contribuyente ya registrado, o lo da de
        baja (#488). **Lo que no viene, no cambia**; para *quitar* la condición especial se manda
        la cadena vacía, que es una instrucción y no una omisión.

        **El código y el documento no se corrigen por aquí.** Son la identidad: el código enlaza
        sus predios, sus recibos y sus asientos, y el documento es con lo que se le acredita.
        Cambiar cualquiera de los dos no es corregir una ficha sino decidir que dos filas eran
        la misma persona, y eso es otro acto.

        `activo = false` es la baja y **no borra** (RNF-051): el código aparece en recibos ya
        emitidos y en asientos del libro. Exige además el privilegio `ELIMINACION`.
      tags: ["Rentas · Registro"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/contribuyentes/{id}/ficha":
    get:
      operationId: ficha_del_contribuyente
      summary: "Ficha del contribuyente a una fecha"
      description: >-
        Dónde está, cómo se le ubica y quién responde con él: domicilio fiscal y procesal
        **vigentes a la fecha**, el historial completo de domicilios, los contactos y los
        responsables solidarios (#488).

        Existe por dos motivos. Las escrituras de la ficha necesitan identificadores que
        ninguna lectura publicaba —dar de baja un contacto exige decir cuál—; y las cuatro
        consultas van en **una sola** transacción (#486): cuatro por separado dejarían sitio
        entre medias a una mudanza, y la ficha saldría diciendo que el contribuyente vive en
        dos sitios y en ninguno.

        `domicilioFiscal` puede ser nulo: un contribuyente recién dado de alta todavía no
        tiene ninguno, y decirlo es más honesto que devolver el último que hubo (regla 9).
      tags: ["Rentas · Registro"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Fecha de corte. Ausente, hoy. Lo vigente se resuelve A ESA FECHA, no «lo ultimo»."
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/vehiculos":
    get:
      operationId: vehiculos_del_contribuyente
      summary: "Vehiculos de un contribuyente"
      description: >-
        Los vehículos de un contribuyente, con su deuda a la fecha (#524).

        **La consulta ya existía y la sirve este mismo contexto** —ConsultaVehiculosController,
        GET /consultas/vehiculos—, pero bajo la opción del módulo **Consultas** y su permiso. El
        expediente del contribuyente de Rentas (#503 F2) no puede tomarla prestada de ahí: las
        conexiones de la interfaz llegan con el trozo de su módulo (#433), y quien tenga Rentas y
        no Consultas vería un aviso de permiso ajeno dentro de su propio expediente.

        Va detrás del permiso de la opción «Ficha de vehículo» —la de Rentas que ya existe— y
        **exige el contribuyente**: sin él, quien sólo tiene ese permiso pasaría de necesitar la
        placa para ver una ficha a poder listar el padrón vehicular entero.

        La fila es la misma que publica /consultas/vehiculos: dos formas distintas de la misma
        lectura dirían dos cosas del mismo vehículo, y la que se leyera en el expediente sería la
        que nadie compara.

        **Un código que no está en el padrón es 404, no una página vacía** (#595). Un 200 con cero
        filas se lee como «esta persona no tiene ningún vehículo», y sobre un código mal tecleado
        —o de otra municipalidad— esa frase es falsa; se leía además junto a la de predios, que
        desde #541 sí dice «ese código no está en el padrón», una debajo de la otra. Lo único que
        sigue siendo 200 con cero filas es un contribuyente del padrón sin vehículos.
      tags: ["Rentas · Registro"]
      parameters:
        - name: contribuyente
          in: query
          required: false
          description: "Codigo del contribuyente. Uno de los dos —este o «codContribuyente»— es OBLIGATORIO: sin ninguno, 422, porque sin criterio esto seria una segunda puerta al padron vehicular entero detras de un permiso mas estrecho que el de Consultas. Y un codigo que no esta en el padron es 404, no una pagina vacia (#595)."
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "El mismo filtro con el nombre que usa su hermana GET /rentas/predios, con la que esta lectura llena la misma seccion del expediente. Uno de los dos —este o «contribuyente»— es OBLIGATORIO: sin ninguno, 422 (#595)."
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Fecha de corte de la deuda (regla 9). Ausente, hoy."
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/predial/corridas/ultima":
    get:
      operationId: ultima_corrida_predial
      summary: "Estado de la ultima emision del ejercicio"
      description: >-
        Lo que hizo la última corrida de emisión anual del predial (#523): sus etapas, cuántos
        contribuyentes se leyeron, cuántos se determinaron, cuánto se emitió y **cuántos quedaron
        observados**.

        Hasta esto la corrida viajaba **sólo en la respuesta del POST que la ejecuta**: cerrar la
        pestaña perdía el resultado de un proceso que toca decenas de miles de cuentas, y volver a
        verlo exigía volver a correrlo. Los observados eran lo único que no se podía recomponer
        leyendo el padrón — un observado es, por definición, el que **no** tiene determinación.

        Devuelve también las **simulaciones**, y lo dice: el campo «simulacion» distingue las dos.
        Esconderlas haría que «ver los observados antes de emitir» —que es lo que hay que hacer
        antes de una emisión— no dejara nada que mirar después.

        Sin corridas del ejercicio contesta **204**, no una cabecera de ceros: «todavía no se ha
        corrido» y «se corrió y no emitió nada» son dos cosas distintas.
      tags: ["Rentas · Registro"]
      parameters:
        - name: ejercicio
          in: query
          required: false
          description: "El ejercicio cuya ultima corrida se pide. Ausente, el del reloj."
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/predial/corridas/{corridaId}/observados":
    get:
      operationId: observados_de_la_corrida
      summary: "Los observados de una corrida"
      description: >-
        Los contribuyentes que quedaron fuera de la emisión, cada uno **con su motivo** (#523). Es
        lo único que convierte «emitió menos de lo esperado» en una lista de cosas que arreglar.

        Van aparte de la cabecera y paginados, no dentro de ella: son cientos, y una portada que
        los trajera siempre sería la petición más pesada del sistema para una cifra que casi nadie
        abre.
      tags: ["Rentas · Registro"]
      parameters:
        - name: corridaId
          in: path
          required: true
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/contribuyentes/{id}/domicilios":
    post:
      operationId: mudar_contribuyente
      summary: "Mudanza: cierra el domicilio anterior y abre el nuevo"
      description: >-
        Muda al contribuyente (#488, RF-014). **Es un `POST` y no un `PUT` porque no
        reemplaza nada**: agrega un tramo de vigencia y cierra el anterior *el día antes* de que
        empiece el nuevo, en la misma transacción. Si fuera una edición, la dirección vieja
        desaparecería y con ella la única prueba de por qué se notificó donde se notificó.

        Que los dos no rijan el mismo día es deliberado: si lo hicieran, preguntar «dónde vivía
        ese día» tendría dos respuestas.

        El índice parcial `domicilio_fiscal_vigente_uq` impide que queden dos vigentes aunque
        el código se equivoque; lo que no puede exigir es que quede uno, y de eso se encarga la
        transacción. `vigenciaDesde` ausente es hoy.
      tags: ["Rentas · Registro"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/contribuyentes/{id}/contactos":
    post:
      operationId: registrar_contacto
      summary: "Alta de contacto del contribuyente"
      description: >-
        Un teléfono, un celular, un correo o un gestor (#488, RF-015). `nota` es la observación
        *del contacto* —«llamar después de las 6»— y `observacion` es la del usuario que guarda
        (RNF-052): se llaman distinto a propósito, porque la tabla las tiene con el mismo nombre
        y un cuerpo con dos `observacion` acabaría escribiendo una en el sitio de la otra.
      tags: ["Rentas · Registro"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/contribuyentes/{id}/contactos/{contactoId}":
    put:
      operationId: modificar_contacto
      summary: "Corrección o baja de un contacto"
      description: >-
        Corrige un contacto o lo da de baja (#488). **Lo que no viene, no cambia.**
        `vigente = false` es la baja y **no borra** (regla 4): un gestor que ya no lo es aparece
        en notificaciones anteriores, y explicar por qué se le notificó exige que su ficha siga
        ahí. La baja exige además el privilegio `ELIMINACION`.
      tags: ["Rentas · Registro"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: contactoId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/contribuyentes/{id}/responsables":
    post:
      operationId: registrar_responsable_solidario
      summary: "Alta de responsable solidario"
      description: >-
        Quién responde con el contribuyente y desde cuándo (#488, RF-016). `responsableId` es
        **otro contribuyente del mismo padrón**, no un nombre suelto: para notificarle hace falta
        su domicilio, y el domicilio cuelga del padrón.

        `porcentaje` viaja como **texto** y sólo lo admiten los vínculos que reparten; en los
        demás, mandarlo es 422 y no un campo ignorado en silencio. Como texto porque un número
        del JSON perdería escala antes de que nadie lo mire (regla 1).
      tags: ["Rentas · Registro"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/contribuyentes/{id}/responsables/{responsableId}":
    put:
      operationId: cerrar_responsable_solidario
      summary: "Cierre del vínculo de responsabilidad solidaria"
      description: >-
        Cierra el vínculo en una fecha (#488). **No lo borra** (regla 4): la deuda anterior sigue
        siendo suya, y una notificación de entonces se defiende enseñando que el vínculo regía.

        Sólo se cierra uno vigente hoy; uno ya cerrado sale como 404, que es lo que es —no hay
        tal vínculo abierto que cerrar—. `vigenciaHasta` ausente es hoy. Exige además el
        privilegio `ELIMINACION`.
      tags: ["Rentas · Registro"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: responsableId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/predios":
    get:
      operationId: predios_rentas
      summary: "Predios del contribuyente"
      description: "Padrón predial de rentas. Cada predio guarda su autovalúo, condición de propiedad y la fecha desde la que genera obligación."
      tags: ["Rentas · Registro"]
      parameters:
        - name: contribuyente
          in: query
          required: false
          description: "Codigo del contribuyente. Uno de los dos —este o «codContribuyente»— es OBLIGATORIO: sin ninguno, 422. Y un codigo que no esta en el padron es 404, no una pagina vacia (#541)."
          schema: { type: string }
          example: "codigo"
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla. Uno de los dos —este o «contribuyente»— es OBLIGATORIO: sin ninguno, 422."
          schema: { type: string }
        - name: codigoPredial
          in: query
          required: false
          description: "Filtro «Código predial» de la pantalla"
          schema: { type: string }
        - name: sector
          in: query
          required: false
          description: "Filtro «Sector» de la pantalla"
          schema: { type: string }
        - name: condicion
          in: query
          required: false
          description: "Filtro «Condición» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/predial/calculo-individual":
    post:
      operationId: predial_individual
      summary: "Cálculo individual del impuesto predial"
      description: "Determina el impuesto de un contribuyente sobre el autovalúo acumulado de todos sus predios en el distrito, con la escala progresiva acumulativa y el mínimo imponible de 0.6 % de la UIT."
      tags: ["Rentas · Registro"]
      parameters:
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Filtro «Año» de la pantalla"
          schema: { type: string }
        - name: ejercicio
          in: query
          required: false
          description: >-
            El ejercicio que se determina, con el nombre que lleva en el cuerpo y en el
            dominio. «ano» es el mismo dato con el rotulo del prototipo; si vienen los dos,
            gana el del cuerpo (FiltroDeLaConsulta) y despues este.
          schema: { type: string }
          example: "2026"
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/predial/calculo-masivo":
    post:
      operationId: predial_masivo
      summary: "Cálculo masivo del impuesto predial"
      description: >-
        Recalcula el padrón declarado del ejercicio y deja constancia de los contribuyentes
        **observados** que quedan fuera de la emisión (#523).

        El `alcance` admite las **cuatro** palabras que el manual dibuja, letra por letra (#577):

        - `TODOS` — todo el padrón declarado del ejercicio.
        - `SECTOR` — los que tienen al menos un predio en el sector; exige `sector`. Se determina
        igual sobre **todos** sus predios: la base es del contribuyente (NEG-05 §1) y recortarla al
        sector produciría un error a la baja. El sector elige a quién se emite, no qué se le cobra.
        - `RANGO_DE_CODIGO` — los que caen en un tramo de código de contribuyente; exige
        `codigoDesde` y `codigoHasta`, extremos incluidos. Se compara como texto, que es el orden
        con que la pantalla lista el padrón.
        - `OBSERVADOS` — los que la **última corrida del ejercicio** dejó observados. Sin corrida
        previa no recorre a nadie, y eso es lo correcto: «ninguno quedó observado» y «todavía no se
        ha corrido» son dos cosas distintas.

        Los dos interruptores que la pantalla dibuja y esta corrida no hace —arbitrios y cuponera— se
        **rechazan** con 422 en vez de ignorarse: una corrida «correcta» a quien pidió además los
        arbitrios sólo se notaría al buscar los recibos que nadie generó.
      tags: ["Rentas · Registro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/declaraciones/{djNro}":
    get:
      operationId: declaracion_jurada
      summary: "Declaración jurada — HR, PU y PR"
      description: "Formularios de la declaración: hoja resumen (HR), predio urbano (PU) y predio rústico (PR). Se imprimen para la firma del contribuyente y quedan como sustento del cálculo."
      tags: ["Rentas · Registro"]
      parameters:
        - name: djNro
          in: path
          required: true
          schema: { type: string }
        - name: djN
          in: query
          required: false
          description: "Filtro «DJ N°» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Filtro «Año» de la pantalla"
          schema: { type: string }
        - name: tipo
          in: query
          required: false
          description: "Filtro «Tipo» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/declaraciones/{djNro}/hoja":
    get:
      operationId: hoja_de_declaracion_jurada
      summary: "Hoja resumen de la declaración jurada"
      description: >-
        Lo que la hoja resumen consigna: el **declarante** —código, nombre, documento y domicilio
        fiscal **vigente a la fecha**—, sus **predios** con su % de propiedad, y las **cifras** de
        la última determinación predial del ejercicio (#563).

        Es el único documento del módulo pensado para **imprimirse y firmarse** —termina en
        «Declaro bajo juramento…» y dos líneas de firma— y todo lo que consignaba venía del juego
        de datos de la maqueta: el nombre y el DNI de otra persona, dos predios que no son suyos y
        un «total a pagar». Una vez impresa y firmada, una hoja así **no se distingue de una
        correcta**, y a diferencia de una pantalla nadie la vuelve a mirar contra la base.

        **Un campo nulo es un campo que no hay.** Sin determinación del ejercicio no hay autovalúo,
        ni valúo afecto, ni impuesto: publicar cero sería escribir «no debe nada» en un papel que
        alguien firma. Y ni siquiera con determinación viajan el **derecho de emisión** y el
        **total a pagar**: la determinación guarda la base y el impuesto, no el derecho, que es
        `DERECHO_EMISION_PREDIAL` del conjunto sellado —cifra de ordenanza local, D-02b—.

        `faltan` es una **lista de motivos**, no un booleano: «no se puede imprimir» sin decir por
        qué es lo que hace que alguien lo imprima igual desde otro sitio.

        Una declaración que no existe es **404**, no una hoja vacía.
      tags: ["Rentas · Registro"]
      parameters:
        - name: djNro
          in: path
          required: true
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Ejercicio de la declaración"
          schema: { type: string }
          example: "2026"
        - name: fecha
          in: query
          required: false
          description: "Fecha de corte a la que se resuelven el domicilio y la titularidad (regla 9); si falta, hoy"
          schema: { type: string }
          example: "2026-03-01"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/declaraciones":
    post:
      operationId: presentar_declaracion_jurada
      summary: "Presentación de la declaración jurada"
      description: >-
        Presenta una declaración jurada nueva —HR, PU, PR o VEHICULAR— y es **el acto que
        concilia** (ADR-0015 §3): a partir de él el predio pertenece al padrón afecto del
        ejercicio, y la columna «Conciliada» de
        `/catastro/fichas/conciliacion` lo dice. El cuerpo lleva el ejercicio, el código de
        contribuyente, el tipo, el predio o el vehículo según el tipo, la fecha de presentación
        y la observación del usuario, obligatoria (RNF-052).

        Lo que **no** viaja en el cuerpo, porque lo resuelve el servidor: el **número** —lo pone
        el sistema, con correlativo propio y plantilla parametrizada mientras D-09 siga
        abierta—, la **versión de ficha catastral** vigente a la fecha de presentación, y
        `fueraDePlazo`, que sale de comparar esa fecha con el plazo del conjunto sellado. Un
        ejercicio sellado sin ese parámetro responde 422 **nombrando la llave**
        `PLAZO:DECLARACION_JURADA`: inventar un plazo clasificaría mal cada declaración que se
        registre (regla 5).

        Ningún importe. Presentar fuera de plazo genera multa tributaria según el manual, pero
        esa multa es D-02c: aquí queda el hecho y nada que multiplique dinero.
      tags: ["Rentas · Registro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/declaraciones/{djNro}/rectificacion":
    post:
      operationId: rectificar_declaracion_jurada
      summary: "Rectificatoria de la declaración jurada"
      description: >-
        Crea la versión nueva de una declaración ya presentada y deja la anterior SUSTITUIDA sin
        tocarle una columna (regla 4): las dos filas quedan en la base y la nueva referencia a la
        que sustituye. **Puede cambiar de predio**, y la conciliación lo contempla — el predio
        que se declaró por error deja de conciliar por esa cadena y el que la rectificatoria
        declara pasa a hacerlo, sin que ninguno cuente dos veces.

        Solo se rectifica una declaración en pie: sobre una ANULADA o una ya SUSTITUIDA responde
        409. El número de la rectificatoria lo pone el sistema, como el de cualquier otra.
      tags: ["Rentas · Registro"]
      parameters:
        - name: djNro
          in: path
          required: true
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Ejercicio de la declaración que se rectifica, como en el GET de la ruta"
          schema: { type: string }
          example: "2026"
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/declaraciones/{djNro}/observacion":
    post:
      operationId: observar_declaracion_jurada
      summary: "Observación de la declaración jurada"
      description: >-
        La administración objeta el contenido de una declaración presentada. El cuerpo lleva
        **solo** la observación del usuario (RNF-052): el efecto lo decide el verbo.

        **Observarla no la retira**: el predio sigue conciliando (ADR-0015 §1), porque la
        administración objetó el contenido de una declaración que existe y fue presentada, y
        negarle la conciliación diría «este predio no genera deuda predial» de uno que sí la
        genera. Lo que la observación abre es el camino de la rectificatoria.
      tags: ["Rentas · Registro"]
      parameters:
        - name: djNro
          in: path
          required: true
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Ejercicio de la declaración que se observa, como en el GET de la ruta"
          schema: { type: string }
          example: "2026"
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/declaraciones/{djNro}/anulacion":
    post:
      operationId: anular_declaracion_jurada
      summary: "Anulación de la declaración jurada"
      description: >-
        La administración anula una declaración. El cuerpo lleva **solo** la observación del
        usuario (RNF-052).

        Al revés que observarla, anularla **sí** la retira: deja de sustentar nada y el predio
        deja de conciliar por ella. Y es terminal — una anulada no revive: si el contribuyente
        declara otra vez, se presenta otra declaración, con su número. Un acto sobre una
        declaración anulada o ya sustituida responde 409.
      tags: ["Rentas · Registro"]
      parameters:
        - name: djNro
          in: path
          required: true
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Ejercicio de la declaración que se anula, como en el GET de la ruta"
          schema: { type: string }
          example: "2026"
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/arbitrios":
    get:
      operationId: arbitrios
      summary: "Arbitrios municipales"
      description: >-
        Las cuotas de arbitrio ya determinadas de un ejercicio: una fila por servicio y mes (#31,
        RF-022). **Solo lectura.** La determinación existe —`DeterminarArbitrios`— y **no la publica
        ningún controlador ni ningún proceso**, así que hoy esta consulta lee una tabla que ninguna
        instalación llena; publicarla está bloqueada por D-02b, porque sus tasas son de ordenanza
        local.

        El ejercicio se pide con `ejercicio`, que es como se llama el dato en el resto del sistema;
        `anio` es su alias y hace lo mismo. Sin ninguno de los dos, el ejercicio del reloj del
        servidor.

        **«Zona» y «Uso» no se sirven, y se rechazan con 422 en vez de ignorarse.** No es falta de
        consulta: los valores que la pantalla ofrece —«Zona 1»…«Zona 4», y cinco usos en
        mayúsculas— **no existen en el sistema**. La zona es la del sector del predio y el uso el
        de su ficha catastral, los dos texto libre por municipalidad («Urbana», «Casa habitación»),
        así que ninguna de esas nueve opciones casaría con ningún dato y la respuesta sería la tabla
        vacía, que se lee como «no hay cuotas». Se acota por código predial. La pantalla los dibuja
        bloqueados con su motivo, como los de #322 y #398.
      tags: ["Rentas · Registro"]
      parameters:
        - name: anio
          in: query
          required: false
          description: "Alias de «ejercicio», el que trae el endpoint del prototipo. Si vienen los dos, manda «ejercicio» (ArbitriosController)."
          schema: { type: string }
          example: "2026"
        - name: ejercicio
          in: query
          required: false
          description: "Filtro «Ejercicio» de la pantalla, y el nombre canonico del dato. Ausente, el ejercicio del reloj del servidor."
          schema: { type: string }
        - name: codigoPredial
          in: query
          required: false
          description: "Filtro «Código predial» de la pantalla"
          schema: { type: string }
        - name: zona
          in: query
          required: false
          description: "Filtro «Zona» de la pantalla. NO SE SIRVE: se rechaza con 422 con cualquier valor. La zona de un predio la pone su sector (sector.zona, V1) y es texto libre por municipalidad —la carga real escribe «Urbana»/«Rustica»—, asi que ninguna de las cuatro opciones que el prototipo ofrece casa con ningun dato: filtrar por ellas devolveria la tabla vacia, que se lee como «no hay cuotas». La pantalla lo dibuja bloqueado con su motivo (#322, #398, #541)."
          schema: { type: string }
        - name: uso
          in: query
          required: false
          description: "Filtro «Uso» de la pantalla. NO SE SIRVE, y por lo mismo que «zona»: el uso vive en ficha_catastral.uso, tambien texto libre —«Casa habitacion», «Tienda de artesania»—, y ninguno de los cinco usos en mayusculas del desplegable casa con el (#541)."
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/transferencias/predio":
    post:
      operationId: transferencia_predio
      summary: "Transferencia de predio"
      description: "Da de baja al transferente y de alta al adquirente desde la fecha del acto. La obligación del vendedor corre hasta el 31 de diciembre del año de la transferencia."
      tags: ["Rentas · Registro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/alcabala":
    post:
      operationId: alcabala
      summary: "Impuesto de alcabala"
      description: "Grava la transferencia de propiedad con el 3 % sobre el exceso de las primeras 10 UIT, tomando como base el mayor valor entre el de transferencia y el autovalúo ajustado por el IPM."
      tags: ["Rentas · Registro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/vehiculos/{placa}":
    get:
      operationId: vehiculos
      summary: "Ficha de vehículo"
      description: "Registro del vehículo. La afectación corre tres ejercicios desde el año siguiente a la primera inscripción registral."
      tags: ["Rentas · Registro"]
      parameters:
        - name: placa
          in: path
          required: true
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: nombre
          in: query
          required: false
          description: "Filtro «Nombre» de la pantalla"
          schema: { type: string }
        - name: nroDocumento
          in: query
          required: false
          description: "Filtro «Nro. Documento» de la pantalla"
          schema: { type: string }
        - name: nroMotor
          in: query
          required: false
          description: "Filtro «Nro. Motor» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/vehicular/calculo":
    post:
      operationId: vehicular_calculo
      summary: "Cálculo del impuesto vehicular"
      description: >-
        Determina —o simula— el impuesto al patrimonio vehicular de **un ejercicio**, sobre los
        vehículos que el criterio elija: una placa, o todos los del contribuyente.

        **Un ejercicio, no tres.** La descripción de esta operación prometía «por los tres ejercicios
        en que el vehículo permanece afecto» y devolvía una determinación por vehículo del ejercicio
        pedido (#577). Los tres ejercicios son la **afectación** —estructural, y `GET
        /rentas/vehiculos` ya la publica como `afectoDesde`/`afectoHasta`—, no el alcance de este
        cálculo: determinar tres a la vez asentaría tres deudas por un botón que dice «Calcular». Cada
        fila de la respuesta dice de qué vehículo es, con su `placa`.

        **`baseImponible` se llamaba `valorReferencial` y no lo era**: es el mayor entre el valor
        de adquisición y el referencial del MEF (art. 32 LTM). Los dos operandos que la memoria del
        cálculo compara no viajan, porque la determinación guarda la base y no de qué salió.

        La marca `simulacion` del cuerpo es **obligatoria** y distingue calcular de asentar: no hay
        omisión segura en ninguna de las dos direcciones.
      tags: ["Rentas · Registro"]
      parameters:
        - name: placa
          in: query
          required: false
          description: "Filtro «Placa» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: ejercicio
          in: query
          required: false
          description: "Filtro «Ejercicio» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/transferencias/vehiculo":
    post:
      operationId: transferencia_vehiculo
      summary: "Transferencia de vehículo"
      description: "Registra el cambio de titular. El transferente responde por el impuesto hasta el 31 de diciembre del año en que se produce la venta."
      tags: ["Rentas · Registro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/espectaculos":
    post:
      operationId: espectaculos
      summary: "Espectáculos públicos no deportivos"
      description: "Grava el monto que se abona por presenciar el espectáculo. La tasa depende del tipo de evento y el organizador actúa como agente perceptor."
      tags: ["Rentas · Registro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/beneficios":
    get:
      operationId: beneficios
      summary: "Beneficios y exoneraciones"
      description: "Deducciones, inafectaciones y amnistías. La deducción de 50 UIT para pensionistas y adultos mayores exige predio único destinado a vivienda."
      tags: ["Rentas · Registro"]
      parameters:
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: tipo
          in: query
          required: false
          description: "Filtro «Tipo» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/deuda/altas":
    post:
      operationId: alta_deuda
      summary: "Alta de deuda"
      description: "Incorpora manualmente una obligación a la cuenta corriente cuando no proviene de la emisión masiva: determinaciones de fiscalización, multas o deuda migrada."
      tags: ["Rentas · Registro"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/rentas/deuda/bajas":
    post:
      operationId: baja_deuda
      summary: "Baja de deuda"
      description: "Extingue deuda de la cuenta corriente por prescripción, resolución que la deja sin efecto, error material o compensación. Requiere resolución y queda en la bitácora de auditoría."
      tags: ["Rentas · Registro"]
      parameters:
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Filtro «Año» de la pantalla"
          schema: { type: string }
        - name: tributo
          in: query
          required: false
          description: "Filtro «Tributo» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/programas":
    get:
      operationId: fisc_programas_listado
      summary: "Programas de fiscalización"
      description: "La grilla de programas de fiscalización de la pantalla `fisc_programa`, que declara el POST —programar— como su endpoint y necesita un verbo aparte para listar. Agregada por #431: sin ella un programa se registraba y no se podía volver a encontrar, y las actas predial y vehicular —que exigen el `programaId` de un programa ya generado— no tenían ninguna fila real de la que resolverlo. «Ejercicio» filtra por VIGENCIA y no por el año de inicio: un programa que arranca en diciembre y cierra en marzo es un programa del ejercicio siguiente para quien lo busca. Los otros dos desplegables de la pantalla —«Tipo» y «Estado»— no viajan: nombran clases y situaciones que el sistema no registra."
      tags: ["Fiscalización"]
      parameters:
        - name: nDePrograma
          in: query
          required: false
          description: "Filtro «Nº de programa» de la pantalla; es el codigo del programa, exacto"
          schema: { type: string }
        - name: ejercicio
          in: query
          required: false
          description: "Filtro «Ejercicio» de la pantalla: los programas VIGENTES en ese ejercicio"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: fisc_programa
      summary: "Programación de fiscalización"
      description: "Selección de la muestra a inspeccionar por sector y criterio de riesgo, con el fiscalizador asignado y el plazo del programa."
      tags: ["Fiscalización"]
      parameters:
        - name: nDePrograma
          in: query
          required: false
          description: "Filtro «Nº de programa» de la pantalla"
          schema: { type: string }
        - name: ejercicio
          in: query
          required: false
          description: "Filtro «Ejercicio» de la pantalla"
          schema: { type: string }
        - name: tipo
          in: query
          required: false
          description: "Filtro «Tipo» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/programas/{id}/muestra":
    get:
      operationId: fisc_programa_muestra
      summary: "Muestra sorteada del programa"
      description: "Los predios que el programa sorteó para inspeccionar, con la condición que la detección concluyó el día del sorteo y las dos superficies que comparó. Es la grilla «Predios seleccionados» de la pantalla y también de donde el acta de inspección resuelve sus tres identificadores —programa, contribuyente y predio—, que su catálogo dibuja de solo lectura. La columna «Estado» se DERIVA de si el predio ya tiene acta en el programa: no es una columna de la fila, porque guardarla dejaría dos verdades sobre lo mismo. Los tres campos del titular —contribuyenteId, codContribuyente y titular— salen NULOS en el predio sin titularidad vigente: es el predio que nadie reclama, el candidato de primer orden, y desde #586 la muestra lo admite en vez de apartarlo. Quien lo visite averigua quién lo ocupa y nombra al contribuyente al levantar el acta."
      tags: ["Fiscalización"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: predio
          in: query
          required: false
          description: "Acota la muestra a un predio: es como el acta de inspeccion pide su propia fila"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: fisc_programa_generar_muestra
      summary: "Generar la muestra del programa"
      description: "Sortea los predios que el programa va a inspeccionar, aplicando sobre el padrón el ejercicio, el sector y el criterio de riesgo que el propio programa declara. No sortea un predio que otro programa abierto ya se llevó ni uno ya fiscalizado en el ejercicio, así que la muestra depende del orden: el primer programa que se genere se lleva los predios. Responde 409 si el programa ya la sorteó — una muestra es un acto y no se regenera, porque hay actas levantadas sobre ella. La respuesta dice sobre qué padrón se sorteó y no sólo cuántos entraron: detectados, predios, sinTitular —cuántos de los sorteados no tienen titular vigente— y los excluidos POR MOTIVO, de modo que detectados = predios + excluidosPorOtroPrograma + excluidosPorActaDelEjercicio. Un número suelto no distinguiría «otro programa se lo llevó» de «ya se fiscalizó», que se arreglan de maneras distintas (#586)."
      tags: ["Fiscalización"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/predial/actas":
    post:
      operationId: fisc_predial
      summary: "Fiscalización predial — acta de inspección"
      description: "Formulario de campo optimizado para tablet. Contrasta lo verificado con lo declarado y determina si corresponde emitir resolución de determinación."
      tags: ["Fiscalización"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/vehicular":
    post:
      operationId: fisc_vehicular
      summary: "Fiscalización vehicular"
      description: "Cruce del padrón vehicular con la información registral y de SUNAT para detectar vehículos afectos no declarados o con valor subvaluado."
      tags: ["Fiscalización"]
      parameters:
        - name: hallazgo
          in: query
          required: false
          description: "Lo que el fiscalizador encontro en campo. El vocabulario es el del enumerado «Hallazgo», letra por letra —cuatro valores, y no los cinco de «CondicionFiscalizada»: un acta no consigna el uso observado—. Tambien se admite en el cuerpo, y ahi gana (#425)."
          schema: { type: string, enum: [CONFORME, OMISO, SUBVALUADOR, NO_UBICADO] }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/resultados":
    get:
      operationId: fisc_resultados
      summary: "Resultados y determinaciones"
      description: "Diferencias detectadas, deuda omitida por ejercicio y estado del valor emitido a partir de cada acta."
      tags: ["Fiscalización"]
      parameters:
        - name: programa
          in: query
          required: false
          description: "Filtro «Programa» de la pantalla"
          schema: { type: string }
        - name: hallazgo
          in: query
          required: false
          description: "Filtro «Hallazgo» de la pantalla: la condicion del contraste. El vocabulario es el del enumerado «CondicionFiscalizada», letra por letra. Sin el parametro, todas."
          schema: { type: string, enum: [CONFORME, OMISO, SUBVALUADOR, USO_DISTINTO, NO_UBICADO] }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla. El vocabulario es el del enumerado «EstadoDeLiquidacion», que se DERIVA del historial de movimientos y no es una columna. No existe «Reclamado». Se admite tambien la etiqueta con espacio, «EN PROCESO»."
          schema: { type: string, enum: [ABIERTA, EN_PROCESO, LIQUIDADA, NOTIFICADA, ANULADA] }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/omisos":
    get:
      operationId: fisc_omisos
      summary: "Omisos y subvaluadores"
      description: "Contribuyentes con predio en catastro pero sin declaración en rentas, y declaraciones cuyo autovalúo está por debajo del valor catastral verificado."
      tags: ["Fiscalización"]
      parameters:
        - name: ejercicio
          in: query
          required: false
          description: "Filtro «Ejercicio» de la pantalla"
          schema: { type: string }
        - name: sector
          in: query
          required: false
          description: "Filtro «Sector» de la pantalla"
          schema: { type: string }
        - name: condicion
          in: query
          required: false
          description: "Filtro «Condición» de la pantalla. El vocabulario es el del enumerado «CondicionFiscalizada», letra por letra; cualquier otra palabra se rechaza con 422 en vez de devolver una pagina vacia. Sin el parametro, todas."
          schema: { type: string, enum: [CONFORME, OMISO, SUBVALUADOR, USO_DISTINTO, NO_UBICADO] }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/estado-cuenta":
    get:
      operationId: fisc_estado_cuenta
      summary: "Estado de cuenta de fiscalización"
      description: "Consulta las deudas originadas en un proceso fiscalizador: diferencias de impuesto predial, arbitrios y patrimonio vehicular con sus multas tributarias."
      tags: ["Fiscalización"]
      parameters:
        - name: contribuyente
          in: query
          required: false
          schema: { type: string }
          example: "codigo"
        - name: fechaDeConsulta
          in: query
          required: false
          description: "A que dia se actualizan las cifras (regla 9, RNF-075). Sin el, hoy. En formato ISO (2026-09-01)"
          schema: { type: string }
          example: "2026-09-01"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/predial/historico":
    get:
      operationId: fisc_historico
      summary: "Histórico de fiscalización predial"
      description: "Versiones de un proceso fiscalizador: qué característica cambió, quién la modificó y en qué momento. Cada liquidación conserva su estado y su versión."
      tags: ["Fiscalización"]
      parameters:
        - name: nLiquidacion
          in: query
          required: false
          description: "Filtro «Nº Liquidación» de la pantalla"
          schema: { type: string }
        - name: codCont
          in: query
          required: false
          description: "Filtro «Cód. Cont.» de la pantalla"
          schema: { type: string }
        - name: nNotificacion
          in: query
          required: false
          description: "Filtro «Nº Notificación» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/liquidaciones":
    post:
      operationId: liquidar_fiscalizacion
      summary: "Liquidación de un acta de fiscalización"
      description: "Emite la liquidación de un acta: el contraste hallado/declarado, una línea por unidad y ejercicio del periodo fiscalizado. Cada línea fija el conjunto de parámetros SELLADO de su ejercicio, de modo que cambiar los parámetros de hoy no altera una liquidación ya emitida. Sin importes: los liquidados y las multas esperan a D-02a (#198). El cuerpo lleva la observación del usuario, obligatoria (RNF-052)."
      tags: ["Fiscalización"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/liquidaciones/{numero}/reliquidaciones":
    post:
      operationId: reliquidar_fiscalizacion
      summary: "Reliquidación"
      description: "Corrige una liquidación emitiendo OTRA versión que la referencia. La anterior no cambia ni una columna, las dos quedan, y la respuesta explica qué cambió entre ellas. Las líneas heredan el conjunto sellado de la versión anterior: una reliquidación corrige el contraste, no el marco normativo."
      tags: ["Fiscalización"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/transferencias":
    post:
      operationId: transferir_a_rentas
      summary: "Transferencia a rentas del resultado fiscalizado"
      description: "Inscribe lo hallado en el padrón como versión NUEVA de la ficha catastral —con origen FISCALIZACION, el documento que la sustenta y la observación del usuario—, asienta los cargos de la diferencia en la cuenta corriente y emite la resolución de determinación. Los tres pasos van en una transacción: ficha nueva, asientos y resolución, o nada. La versión anterior queda intacta, así que el padrón anterior se reconstruye pidiendo la ficha vigente a una fecha anterior. Sin sustento documental no se transfiere, y transferir dos veces la misma liquidación se rechaza."
      tags: ["Fiscalización"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/liquidaciones/{numero}/estados":
    patch:
      operationId: estado_de_liquidacion
      summary: "Estado de una liquidación de fiscalización"
      description: "Mueve la liquidación de estado conservando el historial. No actualiza ninguna fila: agrega un movimiento, y el estado se DERIVA de él. Una liquidación anulada no vuelve: corregirla es reliquidar."
      tags: ["Fiscalización"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/fiscalizacion/resoluciones/{numero}":
    get:
      operationId: resolucion_determinacion_fisc
      summary: "Resolución de determinación de fiscalización"
      description: "Valor emitido al cierre de un procedimiento de fiscalización: determina la diferencia de tributo por ejercicio y la multa tributaria que corresponde."
      tags: ["Fiscalización"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/papeletas":
    get:
      operationId: papeletas
      summary: "Papeletas de infracción de tránsito"
      description: "Papeletas levantadas por el inspector municipal, con el código del Reglamento Nacional de Tránsito, la sanción en porcentaje de UIT y la medida preventiva aplicada."
      tags: ["Tránsito"]
      parameters:
        - name: nroPapeleta
          in: query
          required: false
          description: "Filtro «Nro. Papeleta» de la pantalla"
          schema: { type: string }
        - name: placa
          in: query
          required: false
          description: "Filtro «Placa» de la pantalla"
          schema: { type: string }
        - name: documentoDelInfractor
          in: query
          required: false
          description: "Filtro «Documento del infractor» de la pantalla"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/papeletas/busqueda":
    get:
      operationId: transito_busqueda
      summary: "Búsqueda de infracciones"
      description: "Búsqueda avanzada de papeletas por número, placa, infractor, propietario, rango de fechas y estado de deuda. Muestra el estado de coactiva, el último pago y el usuario que registró la papeleta."
      tags: ["Tránsito"]
      parameters:
        - name: papeleta
          in: query
          required: false
          description: "Filtro «Papeleta» de la pantalla"
          schema: { type: string }
        - name: nPlaca
          in: query
          required: false
          description: "Filtro «Nº Placa» de la pantalla"
          schema: { type: string }
        - name: estadoDeDeuda
          in: query
          required: false
          description: "Filtro «Estado de deuda» de la pantalla"
          schema: { type: string }
        - name: ingresadoPor
          in: query
          required: false
          description: "Filtro «Ingresado por» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/codigos":
    get:
      operationId: codigos_transito
      summary: "Tabla de códigos de infracción de tránsito"
      description: "Catálogo del Reglamento Nacional de Tránsito con la sanción, los puntos y la medida preventiva que el sistema aplica al registrar cada papeleta."
      tags: ["Tránsito"]
      parameters:
        - name: codigo
          in: query
          required: false
          description: "Filtro «Código» de la pantalla"
          schema: { type: string }
        - name: gravedad
          in: query
          required: false
          description: "Filtro «Gravedad» de la pantalla"
          schema: { type: string }
        - name: textoDeLaInfraccion
          in: query
          required: false
          description: "Filtro «Texto de la infracción» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/descargos":
    post:
      operationId: transito_descargos
      summary: "Descargos y reclamos de papeletas"
      description: "Escrito de descargo presentado dentro del plazo, su evaluación y la resolución que declara fundada o infundada la impugnación."
      tags: ["Tránsito"]
      parameters:
        - name: nDeExpediente
          in: query
          required: false
          description: "Filtro «Nº de expediente» de la pantalla"
          schema: { type: string }
        - name: papeleta
          in: query
          required: false
          description: "Filtro «Papeleta» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/internamientos":
    get:
      operationId: internamiento
      summary: "Internamiento vehicular"
      description: "Control de vehículos en el depósito municipal, con el cómputo diario de la tasa de custodia y los requisitos para la liberación."
      tags: ["Tránsito"]
      parameters:
        - name: placa
          in: query
          required: false
          description: "Filtro «Placa» de la pantalla"
          schema: { type: string }
        - name: deposito
          in: query
          required: false
          description: "Filtro «Depósito» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: registrar_internamiento
      summary: "Registro de ingreso al depósito"
      description: >-
        Interna un vehículo en el depósito municipal y emite su acta (#50, RF-064). La
        pantalla «Internamiento vehicular» declara «GET /transito/internamientos» como su
        endpoint —la grilla— y su acción «Registrar ingreso» necesita un verbo aparte. El
        cuerpo lleva la placa, el depósito, el concepto del TUPA con que se cobrará la
        custodia y la observación del usuario, obligatoria (RNF-052).
      tags: ["Tránsito"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/internamientos/{placa}/liberacion":
    post:
      operationId: liberar_internamiento
      summary: "Liberación del vehículo internado"
      description: >-
        Entrega el vehículo a quien lo retira y emite el acta de liberación (#50, RF-064).
        Exige el recibo con que se pagó la custodia: el backend lo acredita contra
        `tesoreria` por su API pública, y sin esa acreditación el vehículo no sale. La
        casilla «Custodia cancelada» de la pantalla no basta —la marca quien entrega el
        vehículo—.
      tags: ["Tránsito"]
      parameters:
        - name: placa
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/papeletas/{numero}/actos":
    get:
      operationId: transito_documentos
      summary: "Emisión de resoluciones y otros documentos"
      description: "Registra los documentos emitidos por papeleta y conserva la secuencia del trámite, incluido el archivo digital de cada acto administrativo."
      tags: ["Tránsito"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: papeletaN
          in: query
          required: false
          description: "Filtro «Papeleta Nº» de la pantalla"
          schema: { type: string }
        - name: placaN
          in: query
          required: false
          description: "Filtro «Placa Nº» de la pantalla"
          schema: { type: string }
        - name: expediente
          in: query
          required: false
          description: "Filtro «Expediente» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/valores/generacion-masiva":
    post:
      operationId: transito_valores
      summary: "Generación de valores de tránsito"
      description: >-
        Registra el criterio de una generación masiva de valores por papeletas de tránsito, por
        selección de números o por rango de fechas (#53, RF-066, RF-073). Devuelve la corrida y
        sus candidatos; **no emite ningún valor**: la generación corre después, en el perfil
        batch. El número de cada resolución de multa lo pone `valor_correlativo` (#37) y **no
        entra por el cuerpo**.
      tags: ["Tránsito"]
      parameters:
        - name: codigoDescripcionDelCriterio
          in: query
          required: false
          description: "Filtro «Código / descripción del criterio» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/papeletas/{numero}/codigo":
    patch:
      operationId: transito_cambio_numero
      summary: "Cambio de número de papeleta de tránsito"
      description: "Corrige el número de papeleta o el número de placa registrados, cuando hubo error del operador al momento del registro."
      tags: ["Tránsito"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/reportes":
    post:
      operationId: transito_reportes
      summary: "Reportes de infracción de tránsito"
      description: >-
        Emisor de los reportes del módulo de tránsito y entrada de su centro de reportes
        (ADR-0014 §5, #396). El campo `reporte` elige entre `PADRON`, `PADRON_COACTIVA`,
        `PADRON_CONSTANCIAS`, `RECORD_CONDUCTOR`, `RECORD_VEHICULAR`, `RESUMEN_RECAUDACION`,
        `RESUMEN_PAPELETAS`, `RESUMEN_CODIGO` y `RESUMEN_PLACA`; con `formato` la respuesta es
        el documento en PDF, hoja de cálculo o texto enriquecido (RF-132), y sin él, el JSON. No
        hay ninguna consulta nueva detrás: llama a las mismas que los GET. **Un criterio que el
        reporte elegido no usa se rechaza con 422 nombrándolo**, en vez de ignorarse: pedir el
        resumen de recaudación «de una placa» devolvería el de todas y nada lo diría.
      tags: ["Tránsito"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/reportes/record-conductor":
    get:
      operationId: transito_record_conductor
      summary: "Record de conductor"
      description: >-
        Historial de infracciones de un conductor, por licencia de conducir o por documento del
        infractor (#53, RF-068). Uno de los dos es obligatorio: sin filtro esto sería el padrón
        entero con otro título. Con `?formato=PDF|XLS|RTF` sale como documento (RF-132).
      tags: ["Tránsito"]
      parameters:
        - name: licencia
          in: query
          required: false
          description: >-
            Licencia de conducir del infractor. Uno de los dos —esta o «documento»— es
            obligatorio: sin ninguno, 422 (RecordsDeTransitoController).
          schema: { type: string }
        - name: documento
          in: query
          required: false
          description: "Documento del infractor. Alternativo a «licencia»"
          schema: { type: string }
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/reportes/record-vehicular":
    get:
      operationId: transito_record_vehicular
      summary: "Record vehicular"
      description: >-
        Historial de papeletas de un vehículo (#53, RF-068). La placa es obligatoria, por el mismo
        motivo que la licencia en el record de conductor. Con `?formato=PDF|XLS|RTF` sale como
        documento (RF-132).
      tags: ["Tránsito"]
      parameters:
        - name: placa
          in: query
          required: false
          description: "Placa del vehiculo. Obligatoria: sin ella, 422 (RecordsDeTransitoController)"
          schema: { type: string }
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/constancias-libres":
    post:
      operationId: transito_constancia_libre
      summary: "Constancia libre de infracciones"
      description: >-
        Emite el documento con el que la municipalidad acredita que un vehículo no registra
        papeletas de tránsito pendientes (#53, RF-068). `verificadaAl` es el día al que se
        acredita —no el de emisión— y viaja impreso en el papel (regla 9, RNF-075). Si a esa fecha
        hay una sola papeleta pendiente responde **409** con los números que lo impiden. La
        respuesta es el archivo, en el formato pedido.
      tags: ["Tránsito"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/reportes/padron":
    get:
      operationId: transito_padron
      summary: "Padrón de papeletas de tránsito"
      description: >-
        Padrón de papeletas de tránsito por intervalo de fechas de infracción y estado (#53,
        RF-073). El importe de cada fila es el **del acta**, congelado al registrar la papeleta, y
        viaja con su fecha. Con `?formato=PDF|XLS|RTF` sale como documento (RF-132).
      tags: ["Tránsito"]
      parameters:
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: ordenadoPor
          in: query
          required: false
          description: "Filtro «Ordenado por» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/estado-cuenta":
    get:
      operationId: transito_estado_cuenta
      summary: "Estado de cuenta de infracciones"
      description: "Papeletas pendientes de pago de un conductor o de un vehículo, con importe, beneficio aplicable y situación de coactiva."
      tags: ["Tránsito"]
      parameters:
        - name: conductor
          in: query
          required: false
          description: "Filtro «Conductor» de la pantalla"
          schema: { type: string }
        - name: placa
          in: query
          required: false
          description: "Filtro «Placa» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: fechaDeCalculo
          in: query
          required: false
          description: "Filtro «Fecha de cálculo» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/papeletas/{numero}/hoja-informativa":
    get:
      operationId: transito_papeleta_reporte
      summary: "Reporte papeleta de infracción"
      description: >-
        Hoja informativa de una papeleta de tránsito: el acta con su desglose, el código del
        catálogo que la sustenta y a quién se le cobra (#396, RF-068). Los seis importes son los
        **del acta**, congelados al registrarla, y por eso `actualizadoA` es la fecha de la
        infracción y no la de hoy; `emitidaEl` es el día en que sale la hoja, con el que se
        resolvió el domicilio del obligado. **No dice lo que se debe hoy**: esa cifra es del libro
        y la publica `transito_estado_cuenta`. Una papeleta que no existe responde **404**, no
        una hoja vacía. Con `?formato=PDF|XLS|RTF` sale como documento (RF-132).
      tags: ["Tránsito"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/resoluciones/ordinaria":
    post:
      operationId: transito_rg_ordinaria
      summary: "Resolución de gerencia ordinaria"
      description: "Resolución que emite la municipalidad para la cobranza de la papeleta. De no cancelarse, el documento pasa al área de cobranza coactiva."
      tags: ["Tránsito"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/resoluciones/sancionadora":
    post:
      operationId: transito_rg_sancionadora
      summary: "Resolución de gerencia sancionadora"
      description: "Segunda resolución, emitida luego de la ordinaria. Tiene carácter sancionador y se deriva a la Dirección General de Transportes."
      tags: ["Tránsito"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/resoluciones/{numero}/notificacion":
    post:
      operationId: notificar_resolucion_transito
      summary: "Notificación de resolución de gerencia de tránsito"
      description: >-
        Cédula de notificación de la resolución ordinaria o sancionadora de tránsito, con
        su acuse (#50, RF-074). **Es de donde sale el derecho a la sancionadora**: la
        diligencia que surte efecto sobre la ordinaria fija, con el plazo parametrizado del
        conjunto sellado, el día desde el que se puede sancionar. Infracciones
        administrativas tiene la suya
        (`/infracciones/administrativas/resoluciones/{id}/notificacion`); tránsito no la
        tenía, y sin ella la sancionadora no se puede dictar nunca.
      tags: ["Tránsito"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/reportes/padron-coactiva":
    get:
      operationId: transito_padron_coactiva
      summary: "Padrón de papeletas enviadas a coactiva"
      description: >-
        Padrón de las papeletas que ya tienen su resolución de multa emitida (#53, RF-073). El
        ejecutor y el estado del expediente **no** son columnas de la papeleta: viven en el
        expediente coactivo y ese corte lo sirve `GET /coactiva/expedientes`; mandarlos aquí
        responde 422 diciéndolo, en vez de devolver el padrón sin filtrar.
      tags: ["Tránsito"]
      parameters:
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: ejecutor
          in: query
          required: false
          description: "Filtro «Ejecutor» de la pantalla"
          schema: { type: string }
        - name: estadoDelExpediente
          in: query
          required: false
          description: "Filtro «Estado del expediente» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/reportes/padron-constancias":
    get:
      operationId: transito_padron_constancias
      summary: "Padrón de constancias libres de infracciones"
      description: >-
        Padrón de constancias libres de infracciones emitidas (#53, RF-068). Cada fila lleva su
        `verificadaAl` junto a la fecha de emisión: son cosas distintas y la que acredita es la
        primera.
      tags: ["Tránsito"]
      parameters:
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: nDeConstancia
          in: query
          required: false
          description: "Filtro «Nº de constancia» de la pantalla"
          schema: { type: string }
        - name: usuarioQueEmitio
          in: query
          required: false
          description: "Filtro «Usuario que emitió» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/reportes/resumen-recaudacion":
    get:
      operationId: transito_resumen_recaudacion
      summary: "Resumen de recaudación de tránsito"
      description: >-
        Lo recaudado por papeletas de tránsito, según el **libro de cuenta corriente**: la suma
        exacta de los abonos vivos, desglosada por ejercicio, mes y tipo de cobranza (#53,
        RF-073). No se recompone sumando papeletas pagadas —esa cifra no cuenta los intereses
        cobrados, cuenta entero un pago parcial y sigue contando un recibo anulado—. Además de
        `lineas`, la respuesta trae `porMes`: una entrada por mes con las fases desglosadas y
        **su total ya sumado en el servidor** (#398), porque recomponerlo en el cliente es lo que
        RNF-083 prohíbe. El filtro por caja no se sirve aquí: la caja es de tesorería (`GET
        /tesoreria/recaudacion/por-area`).
      tags: ["Tránsito"]
      parameters:
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Filtro «Año» de la pantalla"
          schema: { type: string }
        - name: tipoDeCobranza
          in: query
          required: false
          description: "Filtro «Tipo de cobranza» de la pantalla"
          schema: { type: string }
        - name: agrupadoPor
          in: query
          required: false
          description: "Filtro «Agrupado por» de la pantalla"
          schema: { type: string }
        - name: caja
          in: query
          required: false
          description: "Filtro «Caja» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/reportes/resumen-papeletas":
    get:
      operationId: transito_resumen_papeletas
      summary: "Resumen de papeletas pendientes y pagadas"
      description: >-
        Cuántas papeletas hay y por cuánto, agrupadas por **año** —lo que toma por omisión—, mes,
        estado, código o iniciales de placa (#53, RF-073, #398). Cada línea publica `ano` cuando
        el agrupador lo determina —`ANO` y `MES`— y nulo cuando no, porque agrupar por estado o
        por código mezcla años dentro de un grupo. Todos los importes son los **de las actas**, no
        lo cobrado: lo cobrado está en `transito_resumen_recaudacion`. Cada línea trae las
        pendientes y las que están en coactiva en columnas separadas, así que no hace falta pedir
        el resumen dos veces.
      tags: ["Tránsito"]
      parameters:
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: agrupadoPor
          in: query
          required: false
          description: "Filtro «Agrupado por» de la pantalla"
          schema: { type: string }
        - name: cobranza
          in: query
          required: false
          description: "Filtro «Cobranza» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/reportes/resumen-por-codigo":
    get:
      operationId: transito_resumen_codigo
      summary: "Resumen de papeletas por código de infracción"
      description: >-
        El mismo resumen agrupado por código de infracción, con su descripción (#53, RF-073).
      tags: ["Tránsito"]
      parameters:
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
        - name: codigoDeInfraccion
          in: query
          required: false
          description: "Filtro «Código de infracción» de la pantalla"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/transito/reportes/resumen-por-placa":
    get:
      operationId: transito_resumen_placa
      summary: "Resumen de papeletas por iniciales de placa"
      description: >-
        El mismo resumen agrupado por las dos letras iniciales de la placa (#53, RF-073). El
        filtro por iniciales se resuelve como rango y no como `LIKE`: bajo RLS un `LIKE 'AB%'` no
        llega nunca al índice.
      tags: ["Tránsito"]
      parameters:
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
        - name: iniciales2Letras
          in: query
          required: false
          description: "Filtro «Iniciales (2 letras)» de la pantalla"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/notificaciones":
    post:
      operationId: adm_notificacion
      summary: "Notificación administrativa"
      description: "Registro previo de la notificación emitida en la vivienda o el negocio inspeccionado. Es el paso anterior a la generación de la multa administrativa."
      tags: ["Infracciones administrativas"]
      parameters:
        - name: serie
          in: query
          required: false
          description: "Filtro «Serie» de la pantalla"
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Filtro «Año» de la pantalla"
          schema: { type: string }
        - name: numero
          in: query
          required: false
          description: "Filtro «Número» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/actas":
    get:
      operationId: infracciones_adm
      summary: "Infracción administrativa"
      description: "Procedimiento sancionador municipal: notificación preventiva, acta de constatación y resolución de infracción y sanción con multa y medida complementaria."
      tags: ["Infracciones administrativas"]
      parameters:
        - name: nroDeActa
          in: query
          required: false
          description: "Filtro «Nro. de acta» de la pantalla"
          schema: { type: string }
        - name: administrado
          in: query
          required: false
          description: "Filtro «Administrado» de la pantalla"
          schema: { type: string }
        - name: codigoCuis
          in: query
          required: false
          description: "Filtro «Código CUIS» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/cuis":
    get:
      operationId: codigos_cuis
      summary: "Cuadro único de infracciones y sanciones (CUIS)"
      description: "Catálogo aprobado por ordenanza con el porcentaje de UIT y la medida complementaria de cada infracción administrativa."
      tags: ["Infracciones administrativas"]
      parameters:
        - name: codigo
          in: query
          required: false
          description: "Filtro «Código» de la pantalla"
          schema: { type: string }
        - name: materia
          in: query
          required: false
          description: "Filtro «Materia» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/codigos/reporte":
    get:
      operationId: adm_codigos_reporte
      summary: "Reporte de códigos de infracción administrativa"
      description: "Relación impresa del cuadro único de infracciones y sanciones vigente, con la base de cálculo y la sanción no pecuniaria de cada código."
      tags: ["Infracciones administrativas"]
      parameters:
        - name: codigo
          in: query
          required: false
          description: "Filtro «Código» de la pantalla"
          schema: { type: string }
        - name: descripcionContiene
          in: query
          required: false
          description: "Filtro «Descripción contiene» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: ordenadoPor
          in: query
          required: false
          description: "Filtro «Ordenado por» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/valores/generacion-masiva":
    post:
      operationId: adm_valores
      summary: "Generación de valores administrativa"
      description: >-
        Lo mismo que `transito_valores` para la familia administrativa, con su propio permiso
        (#53, RF-066). Detrás es el mismo caso de uso con otra familia: quien puede emitir los
        valores administrativos no tiene por qué poder emitir los de tránsito.
      tags: ["Infracciones administrativas"]
      parameters:
        - name: codigoDescripcionDelCriterio
          in: query
          required: false
          description: "Filtro «Código / descripción del criterio» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/estado-cuenta":
    get:
      operationId: adm_estado_cuenta
      summary: "Estado de cuenta de papeleta administrativa"
      description: "Deuda de una papeleta administrativa con su insoluto, reajuste, interés y gastos, y el importe con beneficio vigente."
      tags: ["Infracciones administrativas"]
      parameters:
        - name: papeleta
          in: query
          required: false
          description: "Filtro «Papeleta» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: fechaDeCalculo
          in: query
          required: false
          description: "Filtro «Fecha de cálculo» de la pantalla"
          schema: { type: string }
        - name: incluirGastos
          in: query
          required: false
          description: "Filtro «Incluir gastos» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/resoluciones":
    post:
      operationId: adm_resolucion_gerencia
      summary: "Resolución de gerencia"
      description: "Resolución que resuelve el procedimiento sancionador y determina la multa administrativa exigible."
      tags: ["Infracciones administrativas"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/resoluciones/{id}/notificacion":
    post:
      operationId: adm_notificacion_resolucion
      summary: "Notificación de resolución de gerencia"
      description: "Cédula de notificación de la resolución de gerencia, con el acuse de recibo y los datos del notificador y testigos."
      tags: ["Infracciones administrativas"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/reportes":
    post:
      operationId: adm_reportes
      summary: "Reportes de infracción administrativa"
      description: >-
        Emisor de los reportes del módulo de infracciones administrativas (#53, RF-074). El campo
        `reporte` elige entre `PADRON_NOTIFICACIONES`, `RESUMEN_PAPELETAS` y
        `RESUMEN_RECAUDACION`; con `formato` la respuesta es el documento en PDF, hoja de cálculo
        o texto enriquecido (RF-132), y sin él, el JSON. No hay ninguna consulta nueva detrás:
        llama a las mismas que los GET.
      tags: ["Infracciones administrativas"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/reportes/padron-notificaciones":
    get:
      operationId: adm_padron_notificaciones
      summary: "Padrón de notificaciones"
      description: >-
        Relación de notificaciones administrativas emitidas en un intervalo, con la papeleta que
        las siguió cuando la hay (#53, RF-074). Las tres columnas de la papeleta —número, estado e
        importe del acta— solo tienen valor cuando existe.
      tags: ["Infracciones administrativas"]
      parameters:
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: agrupadoPor
          in: query
          required: false
          description: "Filtro «Agrupado por» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/reportes/vencidas":
    get:
      operationId: adm_notificaciones_vencidas
      summary: "Notificaciones vencidas"
      description: "Notificaciones cuyo plazo de subsanación venció sin acreditarse el cumplimiento; habilitan la generación de la papeleta administrativa."
      tags: ["Infracciones administrativas"]
      parameters:
        - name: vencidasAl
          in: query
          required: false
          description: "Filtro «Vencidas al» de la pantalla"
          schema: { type: string }
        - name: fiscalizador
          in: query
          required: false
          description: "Filtro «Fiscalizador» de la pantalla"
          schema: { type: string }
        - name: infraccion
          in: query
          required: false
          description: "Filtro «Infracción» de la pantalla"
          schema: { type: string }
        - name: conPapeleta
          in: query
          required: false
          description: "Filtro «Con papeleta» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/reportes/por-contribuyente":
    get:
      operationId: adm_notificaciones_contribuyente
      summary: "Notificaciones por contribuyente"
      description: "Papeletas administrativas agrupadas por año y mes de cometida la infracción, con el estado de la multa y los datos de su pago."
      tags: ["Infracciones administrativas"]
      parameters:
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Filtro «Año» de la pantalla"
          schema: { type: string }
        - name: estadoDeDeuda
          in: query
          required: false
          description: "Filtro «Estado de deuda» de la pantalla"
          schema: { type: string }
        - name: agrupadoPor
          in: query
          required: false
          description: "Filtro «Agrupado por» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/infracciones/administrativas/reportes/resumen-recaudacion":
    get:
      operationId: adm_resumen_recaudacion
      summary: "Resumen de recaudación de papeletas"
      description: >-
        Lo recaudado por multas administrativas, según el libro (#53, RF-074). Mismo criterio que
        el de tránsito: la suma exacta de los abonos vivos.
      tags: ["Infracciones administrativas"]
      parameters:
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Filtro «Año» de la pantalla"
          schema: { type: string }
        - name: agrupadoPor
          in: query
          required: false
          description: "Filtro «Agrupado por» de la pantalla"
          schema: { type: string }
        - name: tipoDeCobranza
          in: query
          required: false
          description: "Filtro «Tipo de cobranza» de la pantalla"
          schema: { type: string }
        - name: caja
          in: query
          required: false
          description: "Filtro «Caja» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/caja/cobranza":
    post:
      operationId: caja_tributaria
      summary: "Caja tributaria"
      description: "Cobranza en ventanilla. Se elige la forma de pago, se filtra la deuda del contribuyente, se aplica el beneficio vigente y se emite el recibo."
      tags: ["Tesorería"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/caja/tasas":
    post:
      operationId: caja_tasas
      summary: "Caja de tasas y derechos administrativos"
      description: "Cobro de conceptos del TUPA que no forman parte de la cuenta corriente: constancias, copias, certificados y derechos de trámite."
      tags: ["Tesorería"]
      parameters:
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/fraccionamientos":
    post:
      operationId: fraccionamiento
      summary: "Fraccionamiento tributario"
      description: >-
        Acoge la deuda marcada a pago fraccionado, o solo **simula** su cronograma: lo decide
        `simular` en el cuerpo. Lo que sale de aquí es siempre un **preconvenio**; no acoge deuda ni
        toca el libro hasta que su cuota inicial se cobre en caja (`POST /tesoreria/caja/cobranza`
        con `tipoDePago = PRECONVENIO`).

        **La cabecera `Idempotency-Key` se lee** (#606). Reenviar el mismo intento —el doble clic, el
        reintento tras un tiempo de espera agotado— devuelve **201 con el convenio de la primera vez**,
        con su mismo número, en vez de abrir un segundo preconvenio sobre la misma deuda. Con una clave
        ya usada para **otro contribuyente** responde 409: devolver el convenio de la primera vez
        imprimiría en ventanilla el acuerdo de otra persona. Simular no consume la clave, porque no
        escribe nada. Sin la cabecera, dos envíos siguen siendo dos convenios.

        El interés, el máximo de cuotas y la política con que se redondea cada cuota salen del conjunto
        sellado del ejercicio: ninguno viaja en el cuerpo, y si falta alguno la respuesta es 422
        nombrando la llave o el ejercicio (#547).
      tags: ["Tesorería"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/convenios":
    get:
      operationId: consulta_convenios
      summary: "Consulta de convenios"
      description: "Seguimiento de los convenios suscritos, con las cuotas pagadas, las vencidas y los que están por quebrarse."
      tags: ["Tesorería"]
      parameters:
        - name: nroDeConvenio
          in: query
          required: false
          description: "Filtro «Nro. de convenio» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/recibos":
    get:
      operationId: recibos_listado
      summary: "Recibos emitidos"
      description: "La grilla «Recibos localizados» de la pantalla de duplicado de recibo: los recibos que cuadran con los filtros, con su número impreso, el instante de emisión, a quién se le cobró, el importe **con la fecha a la que estaba actualizado** (regla 9, RNF-075), el medio de pago, cuántos duplicados se han sacado y si sigue en pie. Agregada por #548: hasta entonces un recibo sólo se podía pedir por su número impreso, así que quien perdía el papel no tenía forma de encontrarlo. No trae el desglose —una página de veinte filas no puede costar veinte lecturas del detalle: para eso está la otra ruta, que ya recibe el número—. Un contribuyente sin recibos devuelve una página vacía con `totalElementos: 0`, no un 404."
      tags: ["Tesorería"]
      parameters:
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla; el codigo exacto del padron"
          schema: { type: string }
        - name: caja
          in: query
          required: false
          description: "Filtro «Caja» de la pantalla: el codigo de la ventanilla que emitio"
          schema: { type: string }
          example: "C-01"
        - name: cajero
          in: query
          required: false
          description: "La cuenta de quien cobro. Del backend: la pantalla no lo dibuja, y sin el no se puede reconstruir lo que emitio un turno"
          schema: { type: string }
          example: "jperez"
        - name: desde
          in: query
          required: false
          description: "Primer dia del rango de emision, inclusive. La pantalla dibuja una sola «Fecha»; el rango es del backend, porque quien perdio el recibo recuerda la semana y no el dia"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Ultimo dia del rango, inclusive"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Columna «Estado» de la grilla, usada como filtro. Se DERIVA del movimiento de anulacion (V30): el recibo no guarda ninguna columna de estado, porque no se actualiza. Un valor fuera de las dos palabras se rechaza con 422 en vez de leerse como «todos»"
          schema: { type: string, enum: [EMITIDO, ANULADO] }
          example: "EMITIDO"
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/recibos/{nro}/duplicado":
    get:
      operationId: duplicado_recibo
      summary: "Duplicado de recibo"
      description: "Reimpresión de un recibo ya emitido. El duplicado sale marcado como tal y queda registrado en la bitácora con el usuario que lo generó."
      tags: ["Tesorería"]
      parameters:
        - name: nro
          in: path
          required: true
          schema: { type: string }
        - name: nroDeRecibo
          in: query
          required: false
          description: "Filtro «Nro. de recibo» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Filtro «Fecha» de la pantalla"
          schema: { type: string }
        - name: caja
          in: query
          required: false
          description: "Filtro «Caja» de la pantalla"
          schema: { type: string }
        - name: formato
          in: query
          required: false
          description: >-
            Sin el, la respuesta es el contenido del recibo en JSON: la vista previa, que
            no emite nada. Con el, la respuesta es el duplicado como documento (#34,
            RF-132).
          schema: { type: string, enum: [PDF, XLS, RTF] }
        - name: observacion
          in: query
          required: false
          description: >-
            Por que se pide el duplicado. Obligatoria cuando se pide con «formato»: cada
            reimpresion queda registrada con quien la genero, y eso es una escritura
            (regla 10, RNF-052). Va en la consulta y no en el cuerpo porque el verbo de
            esta operacion lo fija el prototipo, y un GET no lleva cuerpo.
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/recibos/{nro}/anulacion":
    post:
      operationId: anulacion_recibo
      summary: "Anulación de recibo"
      description: "Deja sin efecto un recibo y devuelve la deuda a la cuenta corriente. Requiere autorización del responsable de tesorería y solo procede mientras la caja del turno siga abierta."
      tags: ["Tesorería"]
      parameters:
        - name: nro
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/convenios/{numero}/anulacion":
    post:
      operationId: anulacion_convenio
      summary: "Anulación de convenio"
      description: >-
        Anula, quiebra o reformula un convenio: las tres por la misma ruta, porque en el libro son el
        mismo acto —lo pendiente vuelve a la fase de la que salió, con asientos y no con un `UPDATE`
        de fase—. El motivo es obligatorio y queda en el acta. Anular exige además que el recibo de la
        cuota inicial ya esté anulado; quebrar no, porque ese dinero sí entró.

        **La cabecera `Idempotency-Key` se lee** (#606). Sin ella, reenviar el mismo intento chocaba
        con `convenio_movimiento_cierre_uq` y respondía **409**, que se lee como un fallo nuevo y no
        como «ya estaba hecho»; con ella devuelve **201 con el convenio ya cerrado** y su acta. En una
        reformulación la clave la reclama el acta de cierre, así que el reenvío no abre un segundo
        preconvenio. Con una clave que cerró **otro** convenio responde 409.
      tags: ["Tesorería"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
        - name: numConv
          in: query
          required: false
          description: "Filtro «Num. Conv.» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: estadoDelConvenio
          in: query
          required: false
          description: "Filtro «Estado del convenio» de la pantalla"
          schema: { type: string }
        - name: fechaDeAnulacion
          in: query
          required: false
          description: "Filtro «Fecha de anulación» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/caja/cierre":
    post:
      operationId: cierre_caja
      summary: "Cierre y arqueo de caja"
      description: "Arqueo del turno: recaudación por medio de pago, recibos emitidos y anulados, y diferencia entre lo declarado y lo registrado por el sistema."
      tags: ["Tesorería"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/recaudacion/avance":
    get:
      operationId: avance_recaudacion
      summary: "Avance de recaudación"
      description: "Comparación de lo emitido contra lo recaudado por tributo y periodo, base del seguimiento de la meta anual."
      tags: ["Tesorería"]
      parameters:
        - name: caja
          in: query
          required: false
          description: >-
            Codigo de la ventanilla. Junto con «cajero», la respuesta trae ademas el arqueo
            EN VIVO de su turno: lo que la pantalla de cierre llama «Cuadrar», mirado antes
            de firmar el acta (#36, RF-087). Del backend, no de la pantalla.
          schema: { type: string }
          example: "C-01"
        - name: cajero
          in: query
          required: false
          description: >-
            Cajero del turno. Solo tiene efecto junto con «caja»; si ese cajero no abrio
            turno ese dia, 404 —un arqueo en ceros haria pensar que abrio y no cobro—.
          schema: { type: string }
          example: "jperez"
        - name: ejercicio
          in: query
          required: false
          description: "Filtro «Ejercicio» de la pantalla"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: tributo
          in: query
          required: false
          description: "Filtro «Tributo» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/tesoreria/recaudacion/por-area":
    get:
      operationId: recaudacion_area
      summary: "Recaudación por área"
      description: "Recaudación desagregada por partida presupuestal y unidad orgánica generadora, para el reporte mensual a la gerencia de administración."
      tags: ["Tesorería"]
      parameters:
        - name: area
          in: query
          required: false
          description: "Filtro «Área» de la pantalla"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: agruparPorArea
          in: query
          required: false
          description: "Filtro «Agrupar por Área» de la pantalla"
          schema: { type: string }
        - name: agruparPorTributo
          in: query
          required: false
          description: "Filtro «Agrupar por Tributo» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/cuenta-corriente/{codigo}":
    get:
      operationId: cuenta_corriente
      summary: "Estado de cuenta corriente"
      description: "Deuda y pagos del contribuyente por ejercicio y tributo, con la fase en la que se encuentra cada obligación."
      tags: ["Consultas"]
      parameters:
        - name: codigo
          in: path
          required: true
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: ejercicio
          in: query
          required: false
          description: "Filtro «Ejercicio» de la pantalla"
          schema: { type: string }
        - name: tributo
          in: query
          required: false
          description: "Filtro «Tributo» de la pantalla"
          schema: { type: string }
        - name: situacion
          in: query
          required: false
          description: "Filtro «Situación» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/deuda":
    get:
      operationId: consulta_deuda
      summary: "Consulta de deuda"
      description: "Deuda exigible a una fecha de corte, con el interés moratorio calculado al día y el desglose por fase de cobranza."
      tags: ["Consultas"]
      parameters:
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: fechaDeCorte
          in: query
          required: false
          description: "Filtro «Fecha de corte» de la pantalla"
          schema: { type: string }
        - name: fase
          in: query
          required: false
          description: "Filtro «Fase» de la pantalla"
          schema: { type: string }
        - name: incluyeConvenios
          in: query
          required: false
          description: "Filtro «Incluye convenios» de la pantalla"
          schema: { type: string }
        - name: porPeriodo
          in: query
          required: false
          description: >-
            Una fila por cuota («true») o una por obligacion con sus periodos agregados
            (ausente o «false»). Cualquier otra palabra es 422: un «si» tecleado que se leyera
            como «false» devolveria filas agregadas a quien pidio cuotas, y esa respuesta es
            indistinguible de la correcta hasta que alguien intenta dar una de baja.
          schema: { type: string, enum: [true, false] }
          example: "true"
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/unificada":
    get:
      operationId: consulta_unificada
      summary: "Consulta unificada predial-arbitrios"
      description: "Vista única del contribuyente: impuesto anual por ejercicio, impuesto por predio y, en pestañas, deudas, pagos, altas y bajas, movimientos del predio, fraccionamientos y valores emitidos."
      tags: ["Consultas"]
      parameters:
        - name: contribuyente
          in: query
          required: false
          schema: { type: string }
          example: "codigo"
        - name: impresion
          in: query
          required: false
          description: "Filtro «Impresión» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/resumen-predial":
    get:
      operationId: consulta_resumen_predial
      summary: "Consulta resumen predial-arbitrios"
      description: "Resumen por predio: impuesto predial de cada ejercicio con su valúo afecto y el saldo de deuda, más el valúo de arbitrios y los movimientos del predio."
      tags: ["Consultas"]
      parameters:
        - name: codCatastral
          in: query
          required: false
          description: "Filtro «Cod. Catastral» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: uso
          in: query
          required: false
          description: "Filtro «Uso» de la pantalla"
          schema: { type: string }
        - name: palabra
          in: query
          required: false
          description: "Filtro «Palabra» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/altas-bajas":
    get:
      operationId: consulta_altas_bajas
      summary: "Consulta de altas y bajas"
      description: >-
        Los **actos** de alta y de baja de deuda de un contribuyente (RF-043, RF-044), con el documento
        que los aprueba y el motivo con que se registraron.

        **No es todo el movimiento del libro.** Un cobro de ventanilla no sale aquí —es un pago, y lo
        lista `GET /consultas/pagos`— ni el cargo de la emisión, aunque los tres se escriban con los
        mismos conceptos del desglose: el abono de una baja y el de una cobranza son, columna a
        columna, el mismo asiento. Lo que los separa es de qué acto nace la fila.

        **Los movimientos anteriores a la migración que estampa ese dato no aparecen**, y no se pueden
        recuperar: el libro no admite corrección —se anula, se da de baja o se reversa— y las filas ya
        escritas nacieron sin él. Para una instalación con historia previa, esta relación empieza el día
        en que se aplicó esa migración; lo anterior se consulta como movimientos de la cuenta corriente.

        **También salen las bajas que dicta una resolución de gerencia** —la que deja una multa sin
        efecto, y la que declara fundado un descargo—: son una baja de deuda como la de la pantalla que
        la registra a mano, con las mismas causales, y el documento que las sustenta es el número de esa
        resolución.

        **«Auto / Manual» no se sirve** y se rechaza con 422 en vez de ignorarse: el origen de un acto
        —tecleado en ventanilla o dictado por un procedimiento— no es un dato que esta relación pueda
        consultar. Lo que el libro guarda es de qué acto nace la fila, no quién la produjo; quien lo
        dice es el documento que la sustenta, y eso es texto.
      tags: ["Consultas"]
      parameters:
        - name: tipoDeConsulta
          in: query
          required: false
          description: "Filtro «Tipo de consulta» de la pantalla"
          schema: { type: string }
        - name: codigoCont
          in: query
          required: false
          description: "Filtro «Código Cont.» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Codigo del contribuyente, con el nombre unificado. «codigoCont» es el mismo dato"
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Filtro «Año» de la pantalla, dentro de «Filtros del detalle»"
          schema: { type: string }
        - name: tributo
          in: query
          required: false
          description: "Filtro «Tributo» de la pantalla, dentro de «Filtros del detalle»"
          schema: { type: string }
        - name: altaBaja
          in: query
          required: false
          description: "Filtro «Alta / Baja» de la pantalla"
          schema: { type: string }
        - name: autoManual
          in: query
          required: false
          description: "Filtro «Auto / Manual» de la pantalla. El backend todavia no distingue un movimiento automatico de uno manual y lo ignora"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/deudas-con-beneficio":
    get:
      operationId: consulta_deudas_beneficio
      summary: "Consulta de deudas con beneficio"
      description: "Simula el acogimiento de la deuda a un beneficio vigente: muestra la deuda total, la deuda acogida y la deuda con beneficio, con la tasa aplicada y el ahorro resultante."
      tags: ["Consultas"]
      parameters:
        - name: tipoDePapeleta
          in: query
          required: false
          description: "Filtro «Tipo de papeleta» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: formaDePago
          in: query
          required: false
          description: "Filtro «Forma de pago» de la pantalla"
          schema: { type: string }
        - name: benefAplicable
          in: query
          required: false
          description: "Filtro «Benef. aplicable» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/pagos":
    get:
      operationId: consulta_pagos
      summary: "Consulta de pagos"
      description: "Historial de pagos con el recibo, la caja y el concepto imputado."
      tags: ["Consultas"]
      parameters:
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: medioDePago
          in: query
          required: false
          description: "Filtro «Medio de pago» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/predios":
    get:
      operationId: consulta_predios
      summary: "Consulta de predios"
      description: "Búsqueda de predios por titular, ubicación o código, con el autovalúo vigente y la deuda asociada a cada unidad."
      tags: ["Consultas"]
      parameters:
        - name: codigoPredial
          in: query
          required: false
          description: "Filtro «Código predial» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Codigo del contribuyente, con el nombre unificado. «contribuyente» es el mismo dato"
          schema: { type: string }
        - name: calle
          in: query
          required: false
          description: "Filtro «Calle» de la pantalla"
          schema: { type: string }
        - name: manzana
          in: query
          required: false
          description: "Filtro «Manzana» de la pantalla"
          schema: { type: string }
        - name: lote
          in: query
          required: false
          description: "Filtro «Lote» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/vehiculos":
    get:
      operationId: consulta_vehiculos
      summary: "Consulta de vehículos"
      description: "Padrón vehicular consultable por placa, motor o titular, con los ejercicios afectos y la deuda vigente."
      tags: ["Consultas"]
      parameters:
        - name: placa
          in: query
          required: false
          description: "Filtro «Placa» de la pantalla"
          schema: { type: string }
        - name: nroMotor
          in: query
          required: false
          description: "Filtro «Nro. Motor» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Codigo del contribuyente, con el nombre unificado. «contribuyente» es el mismo dato"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla. Solo «BAJA» filtra contra el padron; el resto —AFECTO, INAFECTO, EXONERADO— es afectacion calculada y no se traduce todavia"
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Fecha de corte de la deuda de cada fila. Sin ella, hoy"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/valores":
    get:
      operationId: consulta_valores
      summary: "Consulta de valores emitidos"
      description: "Órdenes de pago, resoluciones de determinación y de multa emitidas a un contribuyente, con su estado de notificación y firmeza."
      tags: ["Consultas"]
      parameters:
        - name: nroDeValor
          in: query
          required: false
          description: "Filtro «Nro. de valor» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: tipo
          in: query
          required: false
          description: "Filtro «Tipo» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/consultas/constancias/no-adeudo":
    get:
      operationId: constancia
      summary: "Constancia de no adeudo"
      description: >-
        Vista previa del documento que se entrega al contribuyente. Se imprime con el mismo
        formato en papel membretado. Con `?formato=PDF|XLS|RTF` sale como documento (RF-132,
        RNF-081).
      tags: ["Consultas"]
      parameters:
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: fecha
          in: query
          required: false
          description: "Fecha de corte; sin ella, la de hoy"
          schema: { type: string }
        - name: formato
          in: query
          required: false
          description: "PDF, XLS o RTF; sin el, la respuesta es JSON (RF-132)"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/valores":
    post:
      operationId: valores_individual
      summary: "Generación individual de valores"
      description: "Emisión de una orden de pago, resolución de determinación o resolución de multa para un contribuyente, con la base legal que la sustenta."
      tags: ["Valores"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    get:
      operationId: valores_busqueda
      summary: "Búsqueda y mantenimiento de valores"
      description: "Localiza un valor por número, contribuyente o periodo para consultarlo, anularlo o derivarlo a cobranza coactiva."
      tags: ["Valores"]
      parameters:
        - name: nroDeValor
          in: query
          required: false
          description: "Filtro «Nro. de valor» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: tipo
          in: query
          required: false
          description: "Filtro «Tipo» de la pantalla"
          schema: { type: string }
        - name: ejercicio
          in: query
          required: false
          description: "Filtro «Ejercicio» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/valores/masivo":
    post:
      operationId: valores_masivo
      summary: "Generación masiva de valores"
      description: "Emite órdenes de pago en bloque para toda la deuda vencida que cumpla el filtro, respetando el monto mínimo de emisión fijado por ordenanza."
      tags: ["Valores"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/valores/{nro}/notificacion":
    post:
      operationId: notificacion_valores
      summary: "Notificación de valores"
      description: "Registro del acto de notificación. La fecha de notificación determina el inicio del plazo de reclamación y, vencido este, la firmeza del valor."
      tags: ["Valores"]
      parameters:
        - name: nro
          in: path
          required: true
          schema: { type: string }
        - name: nroDeValor
          in: query
          required: false
          description: "Filtro «Nro. de valor» de la pantalla"
          schema: { type: string }
        - name: notificador
          in: query
          required: false
          description: "Filtro «Notificador» de la pantalla"
          schema: { type: string }
        - name: resultado
          in: query
          required: false
          description: "Filtro «Resultado» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/prescripcion":
    post:
      operationId: prescripcion
      summary: "Prescripción de la deuda"
      description: "Solicitud de prescripción de la acción de cobro, con el cómputo del plazo y los actos que lo interrumpen o suspenden."
      tags: ["Valores"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/valores/{numero}/movimientos":
    post:
      operationId: pase_coactiva
      summary: "Pase de valores a coactiva"
      description: "Registra el movimiento del valor hacia el área de cobranza coactiva: PCO — pase a coactivas, ACO — aceptado en coactivas o RCO — rechazado en coactivas."
      tags: ["Valores"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
        - name: contrib
          in: query
          required: false
          description: "Filtro «Contrib.» de la pantalla"
          schema: { type: string }
        - name: tipoDeValor
          in: query
          required: false
          description: "Filtro «Tipo de valor» de la pantalla"
          schema: { type: string }
        - name: tipoMov
          in: query
          required: false
          description: "Filtro «Tipo Mov.» de la pantalla"
          schema: { type: string }
        - name: nroValor
          in: query
          required: false
          description: "Filtro «Nro. Valor» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/expedientes":
    get:
      operationId: coactiva_expedientes
      summary: "Expedientes coactivos"
      description: "Cobranza coactiva de valores firmes: resolución de ejecución, medidas cautelares, costas y gastos, y causales de suspensión."
      tags: ["Coactiva"]
      parameters:
        - name: nroDeExpediente
          in: query
          required: false
          description: "Filtro «Nro. de expediente» de la pantalla"
          schema: { type: string }
        - name: codContribuyente
          in: query
          required: false
          description: "Filtro «Cod. Contribuyente» de la pantalla"
          schema: { type: string }
        - name: ejecutor
          in: query
          required: false
          description: "Filtro «Ejecutor» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/expedientes/importacion":
    post:
      operationId: importacion_valores
      summary: "Importación de valores a coactiva"
      description: "Ingresa a coactiva un valor ya generado en el módulo de valores y le asigna número de expediente coactivo, auxiliar y ejecutor para su tratamiento posterior."
      tags: ["Coactiva"]
      parameters:
        - name: tipoDeDeuda
          in: query
          required: false
          description: "Filtro «Tipo de deuda» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: codUnidad
          in: query
          required: false
          description: "Filtro «Cod. Unidad» de la pantalla"
          schema: { type: string }
        - name: filtro
          in: query
          required: false
          description: "Filtro «Filtro» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/expedientes/{numero}/proceso":
    get:
      operationId: proceso_coactivo
      summary: "Proceso coactivo"
      description: "Seguimiento del expediente coactivo: datos generales, actuaciones del proceso y detalle de los valores que lo integran, con la deuda proyectada a la fecha."
      tags: ["Coactiva"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: expedienteAno
          in: query
          required: false
          description: "Filtro «Expediente — año» de la pantalla"
          schema: { type: string }
        - name: expedienteNumero
          in: query
          required: false
          description: "Filtro «Expediente — número» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/rec/impresion":
    post:
      operationId: rec_impresion
      summary: "Impresión de resolución de ejecución coactiva"
      description: "Genera e imprime la REC de los expedientes pendientes de pago, con la deuda proyectada al día elegido. Permite imprimir la carátula y la REC 2."
      tags: ["Coactiva"]
      parameters:
        - name: tipoDeDeuda
          in: query
          required: false
          description: "Filtro «Tipo de deuda» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Filtro «Año» de la pantalla"
          schema: { type: string }
        - name: proyectarInteresAl
          in: query
          required: false
          description: "Filtro «Proyectar interés al» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/expedientes/{numero}/estados":
    patch:
      operationId: expediente_historial
      summary: "Gestionar historial del expediente"
      description: "Cambia el estado del expediente coactivo y conserva el historial de estados con su documento de respaldo, motivo y observaciones."
      tags: ["Coactiva"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: nExpediente
          in: query
          required: false
          description: "Filtro «Nº Expediente» de la pantalla"
          schema: { type: string }
        - name: estadoActual
          in: query
          required: false
          description: "Filtro «Estado actual» de la pantalla"
          schema: { type: string }
        - name: ano
          in: query
          required: false
          description: "Filtro «Año» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/expedientes/{numero}/direccion-referencial":
    patch:
      operationId: cambiar_direccion_ref
      summary: "Cambiar dirección referencial"
      description: "Reemplaza la dirección referencial del expediente coactivo, que es la que se usa para notificar al obligado cuando difiere del domicilio fiscal."
      tags: ["Coactiva"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/liquidaciones-costas":
    get:
      operationId: costas_procesales_listado
      summary: "Liquidaciones de costas procesales encontradas"
      description: "Grilla «Liquidaciones encontradas» de la pantalla de costas procesales, con el pendiente y el estado de cada liquidacion a la fecha de consulta. Agregada por #42: la opcion declaraba solo su accion principal y la pantalla tiene grilla propia con estos mismos filtros."
      tags: ["Coactiva"]
      parameters:
        - name: nroLiquidacion
          in: query
          required: false
          description: "Filtro «Nro. Liquidación» de la pantalla"
          schema: { type: string }
        - name: nroExpedCoact
          in: query
          required: false
          description: "Filtro «Nro. Exped. Coact.» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla. Se derivan del libro: ACTIVA o CANCELADA"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: costas_procesales
      summary: "Liquidación de costas procesales"
      description: "Liquida las costas y gastos del procedimiento coactivo por expediente, según el arancel de costas aprobado."
      tags: ["Coactiva"]
      parameters:
        - name: nroLiquidacion
          in: query
          required: false
          description: "Filtro «Nro. Liquidación» de la pantalla"
          schema: { type: string }
        - name: nroExpedCoact
          in: query
          required: false
          description: "Filtro «Nro. Exped. Coact.» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/expedientes/{numero}/deuda":
    get:
      operationId: coactiva_deuda_del_expediente
      summary: "Deuda del expediente coactivo, obligación por obligación"
      description: "La deuda de un expediente **desglosada por obligación**: una fila por tributo, ejercicio y unidad, con su insoluto, reajuste, interés y gastos. Es la lectura de la que «Fraccionamiento coactivo» saca las filas que se acogen —su cuerpo las pide una a una, y una suma no las tiene—. Sale de la MISMA composición y a la misma fecha que la deuda del expediente que imprime la REC-2, así que la grilla y el papel no pueden discrepar. Las costas del procedimiento viajan marcadas (`esCosta`) y no escondidas: se cobran igual, pero no se acogen como una cuota más. Los tres totales vienen calculados del servidor (RNF-083)."
      tags: ["Coactiva"]
      parameters:
        - name: numero
          in: path
          required: true
          description: "El numero del expediente coactivo, tal como esta impreso"
          schema: { type: string }
        - name: fechaDeCalculo
          in: query
          required: false
          description: "A que dia se actualizan TODAS las cifras. Sin el, hoy; y viaja de vuelta en la respuesta (regla 9)"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/convenios":
    post:
      operationId: fraccionamiento_coactivo
      summary: "Fraccionamiento coactivo"
      description: "Convenio tributario coactivo. Se inicia con un pago inicial y sobre el saldo se elabora el cronograma de cuotas, con el beneficio aplicable a la deuda acogida."
      tags: ["Coactiva"]
      parameters:
        - name: formaDePago
          in: query
          required: false
          description: "Filtro «Forma de pago» de la pantalla"
          schema: { type: string }
        - name: benefAplicable
          in: query
          required: false
          description: "Filtro «Benef. aplicable» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: coact
          in: query
          required: false
          description: "Filtro «Coact.» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/expedientes/{numero}/actos":
    post:
      operationId: actos_coactivos
      summary: "Registro de actos coactivos"
      description: "Registra y emite los documentos de las medidas coactivas adoptadas: embargos, retenciones y demás actos, con su archivo digital adjunto."
      tags: ["Coactiva"]
      parameters:
        - name: numero
          in: path
          required: true
          schema: { type: string }
        - name: expAno
          in: query
          required: false
          description: "Filtro «Exp. — año» de la pantalla"
          schema: { type: string }
        - name: expNumero
          in: query
          required: false
          description: "Filtro «Exp. — número» de la pantalla"
          schema: { type: string }
        - name: contrib
          in: query
          required: false
          description: "Filtro «Contrib.» de la pantalla"
          schema: { type: string }
        - name: tributo
          in: query
          required: false
          description: "Filtro «Tributo» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/notificaciones":
    post:
      operationId: notificaciones_coactivas
      summary: "Emisión de notificaciones coactivas"
      description: "Registra y emite las notificaciones de las resoluciones de ejecución coactiva. Admite una o varias notificaciones por expediente según el tratamiento del caso."
      tags: ["Coactiva"]
      parameters:
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: tipoDeValor
          in: query
          required: false
          description: "Filtro «Tipo de valor» de la pantalla"
          schema: { type: string }
        - name: valorN
          in: query
          required: false
          description: "Filtro «Valor Nº» de la pantalla"
          schema: { type: string }
        - name: expCoac
          in: query
          required: false
          description: "Filtro «Exp. Coac.» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/deudas":
    get:
      operationId: coactiva_consulta_deudas
      summary: "Consulta de deudas en coactiva"
      description: "Deuda en cobranza coactiva por contribuyente y expediente, con su estado procesal y la última actuación registrada."
      tags: ["Coactiva"]
      parameters:
        - name: tipoDeDeuda
          in: query
          required: false
          description: "Filtro «Tipo de deuda» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: nExpediente
          in: query
          required: false
          description: "Filtro «Nº Expediente» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/coactiva/deudas-en-beneficio":
    get:
      operationId: coactiva_deudas_beneficio
      summary: "Consulta de deudas en beneficio (coactiva)"
      description: "Deuda en cobranza coactiva acogible a un beneficio vigente, con las costas procesales incorporadas al cálculo."
      tags: ["Coactiva"]
      parameters:
        - name: tipoDeDeuda
          in: query
          required: false
          description: "Filtro «Tipo de deuda» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: benefAplicable
          in: query
          required: false
          description: "Filtro «Benef. aplicable» de la pantalla"
          schema: { type: string }
        - name: fechaDeCalculo
          in: query
          required: false
          description: "Filtro «Fecha de cálculo» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/autorizaciones/anuncios":
    get:
      operationId: anuncios
      summary: "Anuncio y propaganda"
      description: "Autorización para instalar elementos publicitarios. La tasa resulta del área del anuncio, el número de lados y su clase."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: nroAutorizacion
          in: query
          required: false
          description: "Filtro «Nro. Autorización» de la pantalla"
          schema: { type: string }
        - name: contribuyente
          in: query
          required: false
          description: "Filtro «Contribuyente» de la pantalla"
          schema: { type: string }
        - name: rUC
          in: query
          required: false
          description: "Filtro «R.U.C.» de la pantalla"
          schema: { type: string }
        - name: nExpediente
          in: query
          required: false
          description: "Filtro «Nº Expediente» de la pantalla"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "Filtro «Dirección» de la pantalla"
          schema: { type: string }
        - name: dNI
          in: query
          required: false
          description: "Filtro «D.N.I.» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: registrar_anuncio
      summary: "Registro de autorización de anuncio"
      description: "Registra una autorización de anuncio y GENERA SU DEUDA por la tasa (RF-114). El cuerpo lleva el titular, el establecimiento asociado —opcional—, la clase y el tipo del elemento, sus medidas, la ubicación, la vigencia y la observación del usuario, obligatoria (RNF-052). El número de la autorización lo pone el sistema desde su correlativo y la tasa sale del conjunto sellado: ninguno de los dos viene en el cuerpo. La cabecera `Idempotency-Key` se lee: reenviar el mismo registro devuelve 200 con la autorización de la primera vez y no genera un segundo cargo."
      tags: ["Autorizaciones y licencias"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/autorizaciones/anuncios/{id}/renovacion":
    post:
      operationId: renovar_anuncio
      summary: "Renovación de autorización de anuncio"
      description: "Prorroga la autorización por otro ejercicio y devenga otra vez la tasa (RF-114). Un anuncio cesado o retirado no se renueva, y una misma autorización no devenga dos veces el mismo ejercicio. El `id` de la ruta es el número impreso de la autorización."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/autorizaciones/anuncios/{id}/cese":
    post:
      operationId: cesar_anuncio
      summary: "Cese de autorización de anuncio"
      description: "Deja sin efecto la autorización, con su motivo (RF-114). Detiene la deuda futura —un anuncio cesado no se renueva— y NO toca la ya devengada: no borra ni reversa ningún cargo (regla 4, RNF-051). El cuerpo lleva la fecha, el motivo y la observación del usuario, obligatoria (RNF-052)."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/autorizaciones/anuncios/{id}/retiro":
    post:
      operationId: retirar_anuncio
      summary: "Retiro del elemento publicitario"
      description: "Registra que el elemento se retiró de la calle, comprobado en campo (RF-114). Va después del cese: primero la autorización deja de regir y después el soporte desaparece. Al revés, el padrón diría que se desmontó un anuncio que sigue autorizado."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/autorizaciones/anuncios/reportes":
    post:
      operationId: anuncios_reportes
      summary: "Reportes de anuncio y propaganda"
      description: "Emite el padrón de autorizaciones de anuncio y propaganda por contribuyente, dirección, estado o intervalo de fechas."
      tags: ["Autorizaciones y licencias"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/funcionamiento":
    get:
      operationId: licencia_funcionamiento
      summary: "Licencia de funcionamiento"
      description: "Registro y seguimiento de licencias comerciales, con giros CIIU, zonificación, aforo, inspección técnica de seguridad y arbitrios del establecimiento."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: nroLicencia
          in: query
          required: false
          description: "Filtro «Nro. Licencia» de la pantalla"
          schema: { type: string }
        - name: nExpediente
          in: query
          required: false
          description: "Filtro «Nº Expediente» de la pantalla"
          schema: { type: string }
        - name: nombreDelContribuyente
          in: query
          required: false
          description: "Filtro «Nombre del Contribuyente» de la pantalla"
          schema: { type: string }
        - name: denominacionComercial
          in: query
          required: false
          description: "Filtro «Denominación Comercial» de la pantalla"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "Filtro «Dirección» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: emitir_licencia
      summary: "Emisión de licencia de funcionamiento"
      description: "Emite una licencia de funcionamiento con sus giros CIIU y su papel (RF-110). El cuerpo lleva el titular, el establecimiento, los giros con su actividad principal, el número del recibo de caja de tasas del derecho de trámite y la observación del usuario, obligatoria (RNF-052). Sin un recibo válido —de caja de tasas, no anulado, del titular y por el concepto del TUPA que corresponde— no se emite. El número de la licencia lo pone el sistema desde su correlativo: no viene en el cuerpo."
      tags: ["Autorizaciones y licencias"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/funcionamiento/reportes/padron":
    post:
      operationId: licencia_padron
      summary: "Padrón de licencias de funcionamiento"
      description: "Padrón de licencias municipales con agrupación por año y subagrupación por giro, dirección o contribuyente. El orden y los filtros se definen antes de emitir."
      tags: ["Autorizaciones y licencias"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/funcionamiento/reportes/resumen-anual":
    get:
      operationId: licencia_resumen_anual
      summary: "Resumen de licencias por año"
      description: "Cantidades de licencias emitidas, canceladas y duplicadas por año, con la recaudación por derecho de trámite."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: desdeElAno
          in: query
          required: false
          description: "Filtro «Desde el año» de la pantalla"
          schema: { type: string }
        - name: hastaElAno
          in: query
          required: false
          description: "Filtro «Hasta el año» de la pantalla"
          schema: { type: string }
        - name: tipoDeLicencia
          in: query
          required: false
          description: "Filtro «Tipo de licencia» de la pantalla"
          schema: { type: string }
        - name: agrupadoPor
          in: query
          required: false
          description: "Filtro «Agrupado por» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/funcionamiento/{id}/cancelacion":
    post:
      operationId: licencia_resolucion_cancelacion
      summary: "Resolución de cancelación de licencia"
      description: "Resolución que deja sin efecto la licencia de funcionamiento, por solicitud del titular o por cierre del establecimiento."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/funcionamiento/{id}/duplicado":
    post:
      operationId: licencia_resolucion_duplicado
      summary: "Resolución de duplicado de licencia"
      description: "Resolución que autoriza la emisión de un duplicado de la licencia de funcionamiento, con el número de duplicado que corresponde."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/edificacion":
    get:
      operationId: fue_edificacion
      summary: "Formulario único de edificación (FUE)"
      description: "Licencia de obra bajo la Ley 29090. La modalidad de aprobación determina si basta la verificación administrativa o se requiere comisión técnica."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: nroExpediente
          in: query
          required: false
          description: "Filtro «NRO EXPEDIENTE» de la pantalla"
          schema: { type: string }
        - name: nroLicencia
          in: query
          required: false
          description: "Filtro «NRO LICENCIA» de la pantalla"
          schema: { type: string }
        - name: nombreContribuyente
          in: query
          required: false
          description: "Filtro «NOMBRE CONTRIBUYENTE» de la pantalla"
          schema: { type: string }
        - name: lugarMz
          in: query
          required: false
          description: "Filtro «LUGAR — Mz.» de la pantalla"
          schema: { type: string }
        - name: lugarLt
          in: query
          required: false
          description: "Filtro «LUGAR — Lt.» de la pantalla"
          schema: { type: string }
        - name: tipoTramite
          in: query
          required: false
          description: "Filtro «TIPO TRAMITE» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: presentar_fue
      summary: "Presentación del FUE"
      description: "Da de alta el expediente del Formulario Único de Edificaciones (RF-113). El cuerpo lleva el expediente, el solicitante, el tipo de trámite, la obra, la modalidad de aprobación, el representante legal —opcional— y la observación del usuario, obligatoria (RNF-052). Presentar NO otorga nada: no numera ninguna licencia ni comprueba ningún derecho de trámite. Una ampliación o una revalidación nombran aquí la licencia original, y la referencian sin sustituirla."
      tags: ["Autorizaciones y licencias"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/edificacion/{expediente}/secciones":
    post:
      operationId: completar_seccion_fue
      summary: "Sección del FUE completada"
      description: "Completa una sección del FUE: TERRENO, PROYECTO, VALORIZACION, PROFESIONALES o DOCUMENTOS. Se pueden completar en cualquier orden y en visitas distintas; completar una que ya estaba guarda la versión siguiente y deja la anterior entera. La valorización va por pisos y estructuras y NO admite importes: el valor por metro cuadrado sale del cuadro de valores unitarios de edificación."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: expediente
          in: path
          required: true
          description: "Numero de expediente del FUE"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/edificacion/{expediente}/licencia":
    post:
      operationId: emitir_licencia_edificacion
      summary: "Emisión de licencia de edificación"
      description: "Otorga la licencia de edificación del expediente (RF-113). Sólo se emite cuando están las cinco secciones obligatorias, y el error dice cuáles faltan. Sin un recibo válido de caja de tasas —del titular, no anulado y por el concepto del TUPA que corresponde— no se emite. El número de la licencia lo pone el sistema desde su correlativo; la vigencia entra como dato del acto, porque el plazo es una cifra normativa y no se compila."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: expediente
          in: path
          required: true
          description: "Numero de expediente del FUE"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/edificacion/{expediente}/revalidacion":
    post:
      operationId: revalidar_licencia_edificacion
      summary: "Revalidación de licencia de edificación"
      description: "Prorroga el plazo de la licencia que el expediente de revalidación nombra. NO sustituye la vigencia original: agrega el tramo siguiente, y la respuesta devuelve los dos con el acto que concedió cada uno. Se cobra en caja de tasas antes, con su propio concepto del TUPA."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: expediente
          in: path
          required: true
          description: "Numero de expediente de la revalidacion"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/edificacion/reportes/general":
    get:
      operationId: edificacion_reporte
      summary: "Reporte general de licencias de edificación"
      description: "Relación de licencias de edificación por modalidad, con el área a construir, el valor de obra declarado y el estado del expediente."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: modalidad
          in: query
          required: false
          description: "Filtro «Modalidad» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/ciiu":
    get:
      operationId: ciiu
      summary: "Catálogo CIIU de giros"
      description: "Clasificación industrial internacional uniforme. Determina la compatibilidad del giro con la zonificación y el nivel de riesgo de la ITSE."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: codigoCiiu
          in: query
          required: false
          description: "Filtro «Código CIIU» de la pantalla"
          schema: { type: string }
        - name: descripcion
          in: query
          required: false
          description: "Filtro «Descripción» de la pantalla"
          schema: { type: string }
        - name: seccion
          in: query
          required: false
          description: "Filtro «Sección» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: registrar_ciiu
      summary: "Alta de giro CIIU"
      description: "Agrega un giro al catálogo CIIU de la municipalidad (RF-112). El cuerpo lleva el código, la descripción, la sección, el nivel de riesgo de la ITSE si ya está clasificado, la zonificación compatible y la observación del usuario, obligatoria (RNF-052). El giro nace activo y marcado como extensión local: la clasificación oficial se carga por otro camino."
      tags: ["Autorizaciones y licencias"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/certificados":
    get:
      operationId: certificados_listado
      summary: "Certificados emitidos"
      description: "La grilla «Certificados emitidos» de la pantalla `certificados`, que declara el POST —la emisión— como su endpoint y necesita un verbo aparte para listar. Hacer que el POST devolviera también la grilla convertiría una consulta en una escritura, y una pantalla que lista al abrirse consumiría un correlativo cada vez. El estado de cada fila —VIGENTE o CADUCADO— se deriva a la fecha de hoy y viaja con ella (RNF-075)."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: nDeCertificado
          in: query
          required: false
          description: "Filtro «Nº de certificado» de la pantalla"
          schema: { type: string }
        - name: tipo
          in: query
          required: false
          description: "Filtro «Tipo» de la pantalla"
          schema: { type: string }
        - name: predio
          in: query
          required: false
          description: "Filtro «Predio» de la pantalla"
          schema: { type: string }
        - name: solicitante
          in: query
          required: false
          description: "Filtro por nombre del solicitante; se resuelve contra el padrón por aproximación"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    post:
      operationId: certificados
      summary: "Certificados de numeración y zonificación"
      description: "Emisión de los certificados que acreditan el número municipal asignado y los parámetros urbanísticos del predio."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: nDeCertificado
          in: query
          required: false
          description: "Filtro «Nº de certificado» de la pantalla"
          schema: { type: string }
        - name: tipo
          in: query
          required: false
          description: "Filtro «Tipo» de la pantalla"
          schema: { type: string }
        - name: predio
          in: query
          required: false
          description: "Filtro «Predio» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/licencias/certificados/{numero}/impresion":
    post:
      operationId: imprimir_certificado
      summary: "Impresión de un certificado emitido"
      description: "Vuelve a sacar un certificado ya emitido, con su número original y en el formato que se pida —PDF, hoja de cálculo o texto enriquecido (RF-132)—. El contenido sale de los datos guardados el día de la emisión, no de lo que hoy digan el padrón o el TUPA, y el backend comprueba el SHA-256 antes de entregarlo: si dibujar esos datos ya no da los mismos bytes, la reimpresión falla en lugar de entregar un papel distinto al original con el mismo número. Escribe —cuenta la reimpresión y deja su traza—, así que el cuerpo lleva la observación del usuario, obligatoria (RNF-052). Es la acción «Imprimir certificado» de la pantalla."
      tags: ["Autorizaciones y licencias"]
      parameters:
        - name: numero
          in: path
          required: true
          description: "El número del certificado, tal como está impreso"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/octet-stream:
              schema: { type: string, format: binary }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/modulos":
    get:
      operationId: modulos
      summary: "Módulos del sistema"
      description: "Sistemas controlados por el módulo de seguridad integrada. Cada módulo agrupa sus grupos, accesos y permisos."
      tags: ["Seguridad"]
      parameters:
        - name: modulo
          in: query
          required: false
          description: "Filtro «Módulo» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/usuarios":
    get:
      operationId: usuarios
      summary: "Usuarios del sistema"
      description: "Alta de usuarios con su unidad orgánica, la caja asignada y el grupo de acceso que define qué opciones del menú puede ejecutar."
      tags: ["Seguridad"]
      parameters:
        - name: usuario
          in: query
          required: false
          description: "Filtro «Usuario» de la pantalla"
          schema: { type: string }
        - name: unidadOrganica
          in: query
          required: false
          description: "Filtro «Unidad orgánica» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/usuarios/{id}/grupos":
    get:
      operationId: grupos_del_usuario
      summary: "Grupos a los que pertenece un usuario"
      description: |
        A qué grupos pertenece un usuario (#543, RF-120). Sin esta lectura no hay
        «heredado» que calcular en la matriz de permisos: se sabía qué da cada grupo y
        no a cuáles pertenece cada persona —`/seguridad/grupos/{grupo}/miembros` es
        sólo `POST`—.

        **Sólo las pertenencias activas.** Una baja no se borra —la fila sigue ahí con
        `activo` en falso (RNF-051)—, pero quien salió de un grupo ya no pertenece a
        él. Lo que **sí** devuelve son los grupos inhabilitados o fuera de vigencia a
        los que se sigue perteneciendo: pertenecer y surtir efecto son cosas distintas,
        y cada grupo publica su estado y su vigencia para separarlas.

        Un `id` que no existe en esta municipalidad es **404**; no pertenecer a ningún
        grupo es una página vacía con **200**.
      tags: ["Seguridad"]
      parameters:
        - name: id
          in: path
          required: true
          description: "El usuario, por el `id` que publica cada fila de `GET /seguridad/usuarios`"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/grupos":
    get:
      operationId: grupos
      summary: "Grupos de usuarios"
      description: "Agrupación jerárquica de cuentas. El grupo concentra los accesos y todo usuario hereda los permisos del grupo al que pertenece."
      tags: ["Seguridad"]
      parameters:
        - name: grupo
          in: query
          required: false
          description: "Filtro «Grupo» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/accesos":
    get:
      operationId: accesos
      summary: "Accesos y políticas"
      description: "Opciones de menú y políticas del sistema controlado. La búsqueda admite filtrar por tipo y por parte del nombre del acceso."
      tags: ["Seguridad"]
      parameters:
        - name: tipo
          in: query
          required: false
          description: "Filtro «Tipo» de la pantalla"
          schema: { type: string }
        - name: nombreDelAcceso
          in: query
          required: false
          description: "Filtro «Nombre del acceso» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/grupos/{grupo}/miembros":
    post:
      operationId: miembros
      summary: "Gestión de miembros"
      description: "Afiliación de usuarios a uno o varios grupos, base de la posterior asignación de permisos a nivel de grupo. El árbol de la izquierda lista los grupos del módulo y sus usuarios."
      tags: ["Seguridad"]
      parameters:
        - name: grupo
          in: path
          required: true
          schema: { type: string }
        - name: usuario
          in: query
          required: false
          description: "Filtro «Usuario» de la pantalla"
          schema: { type: string }
        - name: modulo
          in: query
          required: false
          description: "Filtro «Modulo» de la pantalla"
          schema: { type: string }
        - name: estado
          in: query
          required: false
          description: "Filtro «Estado» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    get:
      operationId: miembros_del_grupo
      summary: "Usuarios que pertenecen a un grupo"
      description: |
        Quiénes están en un grupo (#582, RF-120): la pregunta inversa de
        `/seguridad/usuarios/{id}/grupos`. Hasta ahora no se podía hacer —de esta
        ruta sólo existía el `POST` que afilia—, así que responder «quién está
        dentro» obligaba a recorrer el padrón de cuentas preguntando por cada una.

        **Sólo las pertenencias activas.** Una baja no se borra —la fila sigue ahí con
        `activo` en falso (RNF-051)—, pero quien salió del grupo ya no está en él.
        Lo que **sí** devuelve son los usuarios **deshabilitados** que siguen
        afiliados: estar en el grupo y poder entrar son cosas distintas, y cada fila
        publica `habilitado` para separarlas — es lo que permite contestar qué cuenta
        deshabilitada conserva permisos sin una segunda lectura.

        Un `grupo` que no existe en esta municipalidad es **404**; un grupo sin
        miembros es una página vacía con **200**. No tener a nadie y no existir son dos
        respuestas distintas.
      tags: ["Seguridad"]
      parameters:
        - name: grupo
          in: path
          required: true
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/grupos/{id}/permisos":
    put:
      operationId: permisos
      summary: "Permisos y niveles de accesibilidad"
      description: >-
        Fija los niveles de accesibilidad de un grupo (RF-121). Recibe la lista **completa** de
        accesos con sus privilegios y no un cambio incremental: la pantalla es una tabla de
        casillas que se marcan y se desmarcan, y aceptar un delta obligaría a la interfaz a
        calcular qué cambió — y a acertar. Un acceso ausente del cuerpo se queda como estaba;
        para retirar, se manda con la lista de privilegios vacía.

        **Puede contestar 409, y hay que saber contarlo**: un cambio que dejaría a la
        municipalidad sin ningún usuario capaz de administrar permisos se rechaza. No es una
        precaución teórica — el error más caro de esta pantalla es también el más fácil de
        cometer, quitarse a uno mismo (o al grupo del que uno es el único miembro) el privilegio
        que hacía falta para devolvérselo—, y de ahí no se sale por el sistema: hace falta entrar
        por la base de datos. La comprobación corre **después** de escribir el cambio y dentro de
        la misma transacción, porque lo que hay que verificar no es el estado actual sino el que
        quedaría.

        Exige la observación del usuario, obligatoria (RNF-052).
      tags: ["Seguridad"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: buscarPor
          in: query
          required: false
          description: "Filtro «Buscar por» de la pantalla"
          schema: { type: string }
        - name: grupoUsuario
          in: query
          required: false
          description: "Filtro «Grupo / Usuario» de la pantalla"
          schema: { type: string }
        - name: acceso
          in: query
          required: false
          description: "Filtro «Acceso» de la pantalla"
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
    get:
      operationId: permisos_de_grupo
      summary: "Permisos ya otorgados de un grupo"
      description: "Los permisos que el grupo ya tiene configurados, para cargar la matriz antes de guardarla (PUT de la misma ruta). No trae las 134 opciones del catalogo: solo las que el grupo ya tiene."
      tags: ["Seguridad"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/sesion/permisos":
    get:
      operationId: permisos_de_la_sesion
      summary: "Permisos efectivos de la sesión"
      description: |
        La matriz de permisos del usuario en curso: por cada opción del catálogo
        sobre la que tiene algún privilegio, la lista de privilegios. La interfaz
        la usa para dibujar el menú y la paleta de comandos.

        Autenticada, pero **no es una opción del catálogo**: leer los permisos
        propios no revela nada que no se pueda enumerar probando cada endpoint
        (REQ-03 §5). Un usuario sin ningún permiso recibe `{}`, no un 403.
      tags: ["Seguridad"]
      responses:
        200:
          description: "La matriz de permisos efectivos del usuario en curso"
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: array
                  items:
                    type: string
                    enum: [ejecucion, lectura, registro, modificacion, eliminacion, impresion, especial]
              example:
                modulos: [lectura, registro, modificacion, eliminacion, impresion, ejecucion, especial]
                contribuyentes: [lectura]
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
  "/seguridad/sesion":
    get:
      operationId: identidad_de_la_sesion
      summary: "Quién es la sesión"
      description: |
        La persona autenticada, ya resuelta a la fila de `usuario` de **esta**
        municipalidad: su `usuarioId`, su `cuenta`, su `nombre` y el ejercicio de
        trabajo que tenga registrado.

        **El `usuarioId` es lo que ninguna otra lectura publicaba.**
        `PUT /seguridad/usuarios/{id}/clave` sólo admite la clave propia —el servidor
        compara la cuenta del token con la del usuario que ese `id` nombra—, y hasta
        aquí la interfaz no sabía cuál era el suyo: las dos únicas operaciones que
        publican un `usuario.id` son el listado de usuarios y la matriz de otro, las
        dos detrás de un permiso de administración mucho mayor que «cambiar mi propia
        contraseña».

        **Sin ningún parámetro, y eso es la decisión.** El sujeto sale de la cuenta del
        token y se resuelve dentro del contexto de tenant. Con un identificador esto
        sería el padrón de usuarios sin su permiso, y devolvería el `id` de otro —que
        es justo lo que la guarda del cambio de clave existe para rechazar—. Un
        parámetro de más se rechaza con 422 nombrándolo.

        Autenticada, pero **no es una opción del catálogo**: leer quién es uno mismo no
        revela nada que no revele el token que ya se presentó. Cualquier sesión válida
        la lee, tenga los permisos que tenga — el mismo trato que
        `permisos_de_la_sesion` y `municipalidad_de_la_sesion` (ADR-0013).

        `ejercicioDeTrabajo` es **nulo** mientras nadie lo haya fijado con
        `PUT /seguridad/sesion/ejercicio`, y eso no es una falta de dato: es la
        respuesta. El año del reloj del servidor ahí afirmaría que alguien lo eligió, y
        lo que hay que poder separar es exactamente eso — el filtro de vista, que es
        local y no necesita permiso, del acto registrado con su observación y su
        privilegio `ESPECIAL` sobre `cambiar_anio`.
      tags: ["Seguridad"]
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/sesion/municipalidad":
    get:
      operationId: municipalidad_de_la_sesion
      summary: "Municipalidad de la sesión"
      description: |
        A quién pertenecen las cifras de la pantalla: la municipalidad a la que
        pertenece la sesión, con su nombre tal como sale impreso.

        **Sin ningún parámetro, y eso es la decisión.** El identificador sale del claim
        `municipalidad_id` del token, se fija una vez con `SET LOCAL` y lo compara el
        `WHERE` de la consulta (ARQ-03 §3.1). Admitir un identificador convertiría esta
        lectura en un **directorio de municipalidades**: con el token de A no hay forma de
        obtener el nombre de B, y un parámetro de más se rechaza con 422 nombrándolo.

        Autenticada, pero **no es una opción del catálogo**: el rótulo de la entidad no es
        de un módulo, es del sistema entero, y sin él las doce pantallas quedan sin decir
        de quién son sus cifras. Cualquier sesión válida la lee, tenga los permisos que
        tenga — el mismo trato que `permisos_de_la_sesion` (ADR-0013).

        `nombre` es el nombre **completo**, con su tipo delante —«Municipalidad Distrital
        de Catacaos»—, porque de ahí sale el membrete de las hojas que se imprimen; `tipo`
        va aparte para quien necesite distinguir una distrital de una provincial y **no se
        antepone**. `ubigeo` son los seis dígitos del distrito.
      tags: ["Seguridad"]
      responses:
        200:
          description: "La municipalidad a la que pertenece la sesión"
          content:
            application/json:
              schema:
                type: object
                required: [id, ubigeo, nombre, tipo]
                properties:
                  id: { type: integer, format: int64 }
                  ubigeo: { type: string, minLength: 6, maxLength: 6 }
                  nombre: { type: string }
                  tipo: { type: string, enum: [DISTRITAL, PROVINCIAL] }
              example:
                id: 1
                ubigeo: "200105"
                nombre: "Municipalidad Distrital de Catacaos"
                tipo: "DISTRITAL"
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
  "/seguridad/usuarios/{id}/permisos":
    get:
      operationId: permisos_efectivos_de_usuario
      summary: "Permisos efectivos de un usuario"
      description: |
        Lo que un usuario **puede hacer**, opción por opción, con la precedencia ya
        resuelta por el servidor (#543, RF-121).

        **Cada fila dice de dónde viene**: `origen: "EXCEPCION"` cuando manda su
        excepción de usuario, y `origen: "GRUPO"` cuando la hereda —con `grupoId`
        cuando hay un solo grupo vigente que la otorga—. No es un adorno: una fila de
        excepción **sustituye** al grupo entero para ese acceso, otorgue o niegue, y
        publicar las dos listas por separado obligaría a quien pregunta a
        reimplementar esa regla. Es exactamente la que no se puede equivocar: la
        interfaz la tenía invertida y calculaba la unión, que convierte una excepción
        que **restringe** en una que amplía.

        **Una fila con `privilegios: []` no sobra**: sólo la produce una excepción que
        niega, y es lo único que distingue «se le negó expresamente» de «nunca lo
        tuvo». Las opciones sobre las que no hay nada configurado no aparecen.

        Misma regla que el guardia en todo lo demás: vigencia y habilitación se
        comprueban en el usuario, en el grupo y en la pertenencia (RF-123), así que un
        usuario deshabilitado o fuera de vigencia recibe la lista **vacía**. Un `id`
        que no existe en esta municipalidad es **404**, no una lista vacía: no tener
        permisos y no existir son dos respuestas distintas.
      tags: ["Seguridad"]
      parameters:
        - name: id
          in: path
          required: true
          description: "El usuario, por el `id` que publica cada fila de `GET /seguridad/usuarios`"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/sesion/ejercicio":
    put:
      operationId: cambiar_anio
      summary: "Cambiar el año de trabajo"
      description: "Fija el ejercicio sobre el que operan todas las opciones del sistema. Los registros se graban contra el año seleccionado."
      tags: ["Seguridad"]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/usuarios/{id}/clave":
    put:
      operationId: cambiar_clave
      summary: "Cambiar contraseña"
      description: "Cambio de la clave del usuario en sesión. La contraseña caduca cada 90 días y no puede repetir las tres últimas."
      tags: ["Seguridad"]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/auditoria":
    get:
      operationId: auditoria
      summary: "Auditoría del sistema"
      description: "Bitácora de operaciones sensibles: anulaciones, extornos, bajas de deuda, cambios de valor y accesos fallidos."
      tags: ["Seguridad"]
      parameters:
        - name: ejercicio
          in: query
          required: false
          description: "Ejercicio de trabajo. Obligatorio: es la clave de particion de la bitacora"
          schema: { type: string }
          example: "2026"
        - name: usuario
          in: query
          required: false
          description: "Filtro «Usuario» de la pantalla"
          schema: { type: string }
        - name: operacion
          in: query
          required: false
          description: >-
            Que clase de acto se registro. El vocabulario es el del enumerado «Operacion» y el
            del CHECK de «auditoria.operacion» (V5), letra por letra; cualquier otra palabra se
            rechaza con 422 en vez de devolver una pagina vacia. No hay ELIMINACION: la
            aplicacion no borra (RNF-051, regla 4), y lo que parece un borrado es una BAJA, una
            ANULACION o una REVERSION.
          schema: { type: string, enum: [ALTA, MODIFICACION, BAJA, ANULACION, REVERSION, PERMISO, ACCESO] }
          example: "ANULACION"
        - name: tabla
          in: query
          required: false
          description: >-
            Sobre que tabla se actuo, tal como la bitacora la nombra —«recibo», «predio»,
            «sesion»—. Es lo que la grilla dibuja en la columna «Opción».
          schema: { type: string }
          example: "recibo"
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/parametros":
    get:
      operationId: parametros
      summary: "Parámetros del sistema"
      description: "Valores que gobiernan el cálculo tributario del ejercicio. Cambiarlos afecta a todas las liquidaciones posteriores."
      tags: ["Seguridad"]
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/parametros/ejercicios/{ejercicio}":
    get:
      operationId: ejercicio_parametrizado
      summary: "Si el ejercicio tiene parámetros sellados"
      description: |
        Si un ejercicio tiene conjunto de parámetros **sellado**, y cuál — para poder
        decirlo **antes** de calcular (#605, ADR-0007).

        Hasta aquí ninguna ruta lo publicaba, así que toda pantalla que calcula tenía que
        fallar para enterarse: se rellenaba el formulario entero —y en «Registrar
        preconvenio» también la observación que exige la regla 10— para recibir al final
        el 422 «El ejercicio 2026 no tiene un conjunto de parámetros sellado», que con
        D-02a abierta es lo que contestan hoy **todas** las municipalidades. El mismo
        agujero tienen la determinación predial, la valorización del FUE, la liquidación de
        fiscalización y el resumen anual de licencias.

        **Ninguna cifra.** Sale `sellado` y la identidad del conjunto —`conjuntoId` y
        `version`—, que es lo que `ConvenioResource.conjuntoDeParametros` ya publica
        cuando el convenio existe. Los valores siguen detrás del permiso de `parametros`
        (REQ-03: quien opera el sistema no publica ni consulta las cifras con las que se
        calcula).

        **«No hay conjunto sellado» es 200 diciendo que no**, no un 404: la pregunta es si
        **hay conjunto sellado** y la respuesta puede ser que no. Conviene no leerla como
        «se puede calcular»: sin conjunto sellado seguro que **no** se puede, pero con él
        el cálculo puede fallar igual si falta dentro alguna llave que la regla pida —el
        422 que nombra la llave (#547, #562)—. Esta lectura adelanta la primera mitad, que
        es la que hoy falla en todas las municipalidades. Un ejercicio **fuera del rango
        1990 a 2100** sí es distinto, y sale 422 nombrando el rango.

        **No deja fila en la bitácora**, a diferencia de las demás lecturas que registran
        `ACCESO`: es la única fuera del catálogo, y auditarla pondría una escritura sin
        cota, sobre una tabla que no se poda, al alcance de cualquier token válido.

        Autenticada, pero **no es una opción del catálogo**: exigir `parametros` —del
        módulo Seguridad— dejaría esta lectura fuera del alcance de quien la necesita, y
        otorgárselo en cada implantación invertiría la separación de funciones de REQ-03
        para poder leer un booleano. El criterio es el de `permisos_de_la_sesion`
        (ADR-0013, REQ-03 §5) y aquí es literal: el 422 de cualquier operación que calcule
        ya dice lo mismo. Lo que sí deja, y las otras lecturas de sesión no, es su fila de
        `ACCESO`: ésta admite un parámetro, así que quien recorra 1990 a 2100 deja su
        nombre en cada intento.
      tags: ["Seguridad"]
      parameters:
        - name: ejercicio
          in: path
          required: true
          description: "El año que se pregunta, de 1990 a 2100"
          schema: { type: string }
      responses:
        200:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
  "/seguridad/respaldos":
    post:
      operationId: respaldo
      summary: "Copias de seguridad"
      description: "Respaldo de la base de datos. El manual exige una copia diaria al cierre de caja y una copia mensual fuera del servidor."
      tags: ["Seguridad"]
      parameters:
        - name: desde
          in: query
          required: false
          description: "Filtro «Desde» de la pantalla"
          schema: { type: string }
        - name: hasta
          in: query
          required: false
          description: "Filtro «Hasta» de la pantalla"
          schema: { type: string }
        - name: tipo
          in: query
          required: false
          description: "Filtro «Tipo» de la pantalla"
          schema: { type: string }
        - name: pagina
          in: query
          required: false
          description: "Pagina que se pide, contada desde 0"
          schema: { type: string }
          example: "0"
        - name: tamano
          in: query
          required: false
          description: "Filas por pagina"
          schema: { type: string }
          example: "20"
        - name: ordenarPor
          in: query
          required: false
          description: "Campo por el que se ordena, en camelCase"
          schema: { type: string }
        - name: direccion
          in: query
          required: false
          description: "ASCENDENTE | DESCENDENTE"
          schema: { type: string }
          example: "ASCENDENTE"
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        201:
          description: Operacion realizada
          content:
            application/json:
              schema: { type: object }
        "403":
          $ref: "#/components/responses/SinMunicipalidad"
        "422":
          $ref: "#/components/responses/ErrorDeValidacion"
components:
  securitySchemes:
    tokenDeAcceso:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        Token OIDC validado, de **uno de los dos emisores** (ADR-0005, ADR-0020).

        - Realm de **funcionarios**: debe traer el claim `municipalidad_id`; sin
          el, la peticion recibe 403 y no llega al controlador (RNF-032). Vale
          para toda la API **salvo** `/portal/**`.
        - Realm del **ciudadano**: debe traer `numero_documento` y no lleva
          municipalidad. Vale **solo** para `/portal/**`.

        Son dos cadenas de seguridad con dos decodificadores, cada uno apuntando
        a un solo emisor: un token de una poblacion no autentica en la otra.
  schemas:
    Importe:
      type: string
      pattern: "^-?[0-9]+\\.[0-9]{2}$"
      description: |
        Decimal exacto como cadena. Nunca numero JSON: el `number` de
        JavaScript es binario de doble precision y pierde centimos (RNF-055).
      example: "1234.50"
    Error:
      type: object
      required: [codigo, mensaje]
      properties:
        codigo: { type: string, example: "DEUDA_YA_CANCELADA" }
        mensaje: { type: string, description: "En castellano; se muestra al usuario" }
        detalles: { type: array, items: { type: string } }
  responses:
    SinMunicipalidad:
      description: |
        El token no identifica una municipalidad. No hay valor por omision ni
        modo sin municipalidad.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    SinDocumentoAcreditado:
      description: |
        El token del ciudadano no identifica un documento. No hay valor por
        omision ni modo sin documento (ADR-0020).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    ErrorDeValidacion:
      description: La peticion no cumple una regla de negocio
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
